(self["webpackChunkmy_webpack"] = self["webpackChunkmy_webpack"] || []).push([["vendors-node_modules_reduxjs_toolkit_dist_redux-toolkit_esm_js-node_modules_query-string_inde-923475"],{

/***/ "./node_modules/@reduxjs/toolkit/dist/redux-toolkit.esm.js":
/*!*****************************************************************!*\
  !*** ./node_modules/@reduxjs/toolkit/dist/redux-toolkit.esm.js ***!
  \*****************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "__DO_NOT_USE__ActionTypes": () => (/* reexport safe */ redux__WEBPACK_IMPORTED_MODULE_0__.__DO_NOT_USE__ActionTypes),
/* harmony export */   "applyMiddleware": () => (/* reexport safe */ redux__WEBPACK_IMPORTED_MODULE_0__.applyMiddleware),
/* harmony export */   "bindActionCreators": () => (/* reexport safe */ redux__WEBPACK_IMPORTED_MODULE_0__.bindActionCreators),
/* harmony export */   "combineReducers": () => (/* reexport safe */ redux__WEBPACK_IMPORTED_MODULE_0__.combineReducers),
/* harmony export */   "compose": () => (/* reexport safe */ redux__WEBPACK_IMPORTED_MODULE_0__.compose),
/* harmony export */   "createStore": () => (/* reexport safe */ redux__WEBPACK_IMPORTED_MODULE_0__.createStore),
/* harmony export */   "MiddlewareArray": () => (/* binding */ MiddlewareArray),
/* harmony export */   "configureStore": () => (/* binding */ configureStore),
/* harmony export */   "createAction": () => (/* binding */ createAction),
/* harmony export */   "createAsyncThunk": () => (/* binding */ createAsyncThunk),
/* harmony export */   "createDraftSafeSelector": () => (/* binding */ createDraftSafeSelector),
/* harmony export */   "createEntityAdapter": () => (/* binding */ createEntityAdapter),
/* harmony export */   "createImmutableStateInvariantMiddleware": () => (/* binding */ createImmutableStateInvariantMiddleware),
/* harmony export */   "createNextState": () => (/* reexport safe */ immer__WEBPACK_IMPORTED_MODULE_2__.default),
/* harmony export */   "createReducer": () => (/* binding */ createReducer),
/* harmony export */   "createSelector": () => (/* reexport safe */ reselect__WEBPACK_IMPORTED_MODULE_1__.createSelector),
/* harmony export */   "createSerializableStateInvariantMiddleware": () => (/* binding */ createSerializableStateInvariantMiddleware),
/* harmony export */   "createSlice": () => (/* binding */ createSlice),
/* harmony export */   "current": () => (/* reexport safe */ immer__WEBPACK_IMPORTED_MODULE_2__.current),
/* harmony export */   "findNonSerializableValue": () => (/* binding */ findNonSerializableValue),
/* harmony export */   "freeze": () => (/* reexport safe */ immer__WEBPACK_IMPORTED_MODULE_2__.freeze),
/* harmony export */   "getDefaultMiddleware": () => (/* binding */ getDefaultMiddleware),
/* harmony export */   "getType": () => (/* binding */ getType),
/* harmony export */   "isAllOf": () => (/* binding */ isAllOf),
/* harmony export */   "isAnyOf": () => (/* binding */ isAnyOf),
/* harmony export */   "isAsyncThunkAction": () => (/* binding */ isAsyncThunkAction),
/* harmony export */   "isDraft": () => (/* reexport safe */ immer__WEBPACK_IMPORTED_MODULE_2__.isDraft),
/* harmony export */   "isFulfilled": () => (/* binding */ isFulfilled),
/* harmony export */   "isImmutableDefault": () => (/* binding */ isImmutableDefault),
/* harmony export */   "isPending": () => (/* binding */ isPending),
/* harmony export */   "isPlain": () => (/* binding */ isPlain),
/* harmony export */   "isPlainObject": () => (/* binding */ isPlainObject),
/* harmony export */   "isRejected": () => (/* binding */ isRejected),
/* harmony export */   "isRejectedWithValue": () => (/* binding */ isRejectedWithValue),
/* harmony export */   "miniSerializeError": () => (/* binding */ miniSerializeError),
/* harmony export */   "nanoid": () => (/* binding */ nanoid),
/* harmony export */   "original": () => (/* reexport safe */ immer__WEBPACK_IMPORTED_MODULE_2__.original),
/* harmony export */   "unwrapResult": () => (/* binding */ unwrapResult)
/* harmony export */ });
/* harmony import */ var immer__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! immer */ "./node_modules/immer/dist/immer.esm.js");
/* harmony import */ var redux__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! redux */ "./node_modules/redux/es/redux.js");
/* harmony import */ var reselect__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! reselect */ "./node_modules/reselect/es/index.js");
/* harmony import */ var redux_thunk__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! redux-thunk */ "./node_modules/redux-thunk/es/index.js");
var __extends = (undefined && undefined.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __generator = (undefined && undefined.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (_) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
var __spreadArray = (undefined && undefined.__spreadArray) || function (to, from) {
    for (var i = 0, il = from.length, j = to.length; i < il; i++, j++)
        to[j] = from[i];
    return to;
};
var __defProp = Object.defineProperty;
var __defProps = Object.defineProperties;
var __getOwnPropDescs = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols = Object.getOwnPropertySymbols;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __propIsEnum = Object.prototype.propertyIsEnumerable;
var __defNormalProp = function (obj, key, value) { return key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value: value }) : obj[key] = value; };
var __spreadValues = function (a, b) {
    for (var prop in b || (b = {}))
        if (__hasOwnProp.call(b, prop))
            __defNormalProp(a, prop, b[prop]);
    if (__getOwnPropSymbols)
        for (var _i = 0, _b = __getOwnPropSymbols(b); _i < _b.length; _i++) {
            var prop = _b[_i];
            if (__propIsEnum.call(b, prop))
                __defNormalProp(a, prop, b[prop]);
        }
    return a;
};
var __spreadProps = function (a, b) { return __defProps(a, __getOwnPropDescs(b)); };
var __async = function (__this, __arguments, generator) {
    return new Promise(function (resolve, reject) {
        var fulfilled = function (value) {
            try {
                step(generator.next(value));
            }
            catch (e) {
                reject(e);
            }
        };
        var rejected = function (value) {
            try {
                step(generator.throw(value));
            }
            catch (e) {
                reject(e);
            }
        };
        var step = function (x) { return x.done ? resolve(x.value) : Promise.resolve(x.value).then(fulfilled, rejected); };
        step((generator = generator.apply(__this, __arguments)).next());
    });
};
// src/index.ts




// src/createDraftSafeSelector.ts


var createDraftSafeSelector = function () {
    var args = [];
    for (var _i = 0; _i < arguments.length; _i++) {
        args[_i] = arguments[_i];
    }
    var selector = reselect__WEBPACK_IMPORTED_MODULE_1__.createSelector.apply(void 0, args);
    var wrappedSelector = function (value) {
        var rest = [];
        for (var _i = 1; _i < arguments.length; _i++) {
            rest[_i - 1] = arguments[_i];
        }
        return selector.apply(void 0, __spreadArray([(0,immer__WEBPACK_IMPORTED_MODULE_2__.isDraft)(value) ? (0,immer__WEBPACK_IMPORTED_MODULE_2__.current)(value) : value], rest));
    };
    return wrappedSelector;
};
// src/configureStore.ts

// src/devtoolsExtension.ts

var composeWithDevTools = typeof window !== "undefined" && window.__REDUX_DEVTOOLS_EXTENSION_COMPOSE__ ? window.__REDUX_DEVTOOLS_EXTENSION_COMPOSE__ : function () {
    if (arguments.length === 0)
        return void 0;
    if (typeof arguments[0] === "object")
        return redux__WEBPACK_IMPORTED_MODULE_0__.compose;
    return redux__WEBPACK_IMPORTED_MODULE_0__.compose.apply(null, arguments);
};
var devToolsEnhancer = typeof window !== "undefined" && window.__REDUX_DEVTOOLS_EXTENSION__ ? window.__REDUX_DEVTOOLS_EXTENSION__ : function () {
    return function (noop) {
        return noop;
    };
};
// src/isPlainObject.ts
function isPlainObject(value) {
    if (typeof value !== "object" || value === null)
        return false;
    var proto = value;
    while (Object.getPrototypeOf(proto) !== null) {
        proto = Object.getPrototypeOf(proto);
    }
    return Object.getPrototypeOf(value) === proto;
}
// src/getDefaultMiddleware.ts

// src/utils.ts
function getTimeMeasureUtils(maxDelay, fnName) {
    var elapsed = 0;
    return {
        measureTime: function (fn) {
            var started = Date.now();
            try {
                return fn();
            }
            finally {
                var finished = Date.now();
                elapsed += finished - started;
            }
        },
        warnIfExceeded: function () {
            if (elapsed > maxDelay) {
                console.warn(fnName + " took " + elapsed + "ms, which is more than the warning threshold of " + maxDelay + "ms. \nIf your state or actions are very large, you may want to disable the middleware as it might cause too much of a slowdown in development mode. See https://redux-toolkit.js.org/api/getDefaultMiddleware for instructions.\nIt is disabled in production builds, so you don't need to worry about that.");
            }
        }
    };
}
var MiddlewareArray = /** @class */ (function (_super) {
    __extends(MiddlewareArray, _super);
    function MiddlewareArray() {
        var args = [];
        for (var _i = 0; _i < arguments.length; _i++) {
            args[_i] = arguments[_i];
        }
        var _this = _super.apply(this, args) || this;
        Object.setPrototypeOf(_this, MiddlewareArray.prototype);
        return _this;
    }
    Object.defineProperty(MiddlewareArray, Symbol.species, {
        get: function () {
            return MiddlewareArray;
        },
        enumerable: false,
        configurable: true
    });
    MiddlewareArray.prototype.concat = function () {
        var arr = [];
        for (var _i = 0; _i < arguments.length; _i++) {
            arr[_i] = arguments[_i];
        }
        return _super.prototype.concat.apply(this, arr);
    };
    MiddlewareArray.prototype.prepend = function () {
        var arr = [];
        for (var _i = 0; _i < arguments.length; _i++) {
            arr[_i] = arguments[_i];
        }
        if (arr.length === 1 && Array.isArray(arr[0])) {
            return new (MiddlewareArray.bind.apply(MiddlewareArray, __spreadArray([void 0], arr[0].concat(this))))();
        }
        return new (MiddlewareArray.bind.apply(MiddlewareArray, __spreadArray([void 0], arr.concat(this))))();
    };
    return MiddlewareArray;
}(Array));
// src/immutableStateInvariantMiddleware.ts
var isProduction = "development" === "production";
var prefix = "Invariant failed";
function invariant(condition, message) {
    if (condition) {
        return;
    }
    if (isProduction) {
        throw new Error(prefix);
    }
    throw new Error(prefix + ": " + (message || ""));
}
function stringify(obj, serializer, indent, decycler) {
    return JSON.stringify(obj, getSerialize(serializer, decycler), indent);
}
function getSerialize(serializer, decycler) {
    var stack = [], keys = [];
    if (!decycler)
        decycler = function (_, value) {
            if (stack[0] === value)
                return "[Circular ~]";
            return "[Circular ~." + keys.slice(0, stack.indexOf(value)).join(".") + "]";
        };
    return function (key, value) {
        if (stack.length > 0) {
            var thisPos = stack.indexOf(this);
            ~thisPos ? stack.splice(thisPos + 1) : stack.push(this);
            ~thisPos ? keys.splice(thisPos, Infinity, key) : keys.push(key);
            if (~stack.indexOf(value))
                value = decycler.call(this, key, value);
        }
        else
            stack.push(value);
        return serializer == null ? value : serializer.call(this, key, value);
    };
}
function isImmutableDefault(value) {
    return typeof value !== "object" || value === null || typeof value === "undefined" || Object.isFrozen(value);
}
function trackForMutations(isImmutable, ignorePaths, obj) {
    var trackedProperties = trackProperties(isImmutable, ignorePaths, obj);
    return {
        detectMutations: function () {
            return detectMutations(isImmutable, ignorePaths, trackedProperties, obj);
        }
    };
}
function trackProperties(isImmutable, ignorePaths, obj, path) {
    if (ignorePaths === void 0) { ignorePaths = []; }
    if (path === void 0) { path = ""; }
    var tracked = { value: obj };
    if (!isImmutable(obj)) {
        tracked.children = {};
        for (var key in obj) {
            var childPath = path ? path + "." + key : key;
            if (ignorePaths.length && ignorePaths.indexOf(childPath) !== -1) {
                continue;
            }
            tracked.children[key] = trackProperties(isImmutable, ignorePaths, obj[key], childPath);
        }
    }
    return tracked;
}
function detectMutations(isImmutable, ignorePaths, trackedProperty, obj, sameParentRef, path) {
    if (ignorePaths === void 0) { ignorePaths = []; }
    if (sameParentRef === void 0) { sameParentRef = false; }
    if (path === void 0) { path = ""; }
    var prevObj = trackedProperty ? trackedProperty.value : void 0;
    var sameRef = prevObj === obj;
    if (sameParentRef && !sameRef && !Number.isNaN(obj)) {
        return { wasMutated: true, path: path };
    }
    if (isImmutable(prevObj) || isImmutable(obj)) {
        return { wasMutated: false };
    }
    var keysToDetect = {};
    for (var key in trackedProperty.children) {
        keysToDetect[key] = true;
    }
    for (var key in obj) {
        keysToDetect[key] = true;
    }
    for (var key in keysToDetect) {
        var childPath = path ? path + "." + key : key;
        if (ignorePaths.length && ignorePaths.indexOf(childPath) !== -1) {
            continue;
        }
        var result = detectMutations(isImmutable, ignorePaths, trackedProperty.children[key], obj[key], sameRef, childPath);
        if (result.wasMutated) {
            return result;
        }
    }
    return { wasMutated: false };
}
function createImmutableStateInvariantMiddleware(options) {
    if (options === void 0) { options = {}; }
    if (false) {}
    var _b = options.isImmutable, isImmutable = _b === void 0 ? isImmutableDefault : _b, ignoredPaths = options.ignoredPaths, _c = options.warnAfter, warnAfter = _c === void 0 ? 32 : _c, ignore = options.ignore;
    ignoredPaths = ignoredPaths || ignore;
    var track = trackForMutations.bind(null, isImmutable, ignoredPaths);
    return function (_b) {
        var getState = _b.getState;
        var state = getState();
        var tracker = track(state);
        var result;
        return function (next) { return function (action) {
            var measureUtils = getTimeMeasureUtils(warnAfter, "ImmutableStateInvariantMiddleware");
            measureUtils.measureTime(function () {
                state = getState();
                result = tracker.detectMutations();
                tracker = track(state);
                invariant(!result.wasMutated, "A state mutation was detected between dispatches, in the path '" + (result.path || "") + "'.  This may cause incorrect behavior. (https://redux.js.org/style-guide/style-guide#do-not-mutate-state)");
            });
            var dispatchedAction = next(action);
            measureUtils.measureTime(function () {
                state = getState();
                result = tracker.detectMutations();
                tracker = track(state);
                result.wasMutated && invariant(!result.wasMutated, "A state mutation was detected inside a dispatch, in the path: " + (result.path || "") + ". Take a look at the reducer(s) handling the action " + stringify(action) + ". (https://redux.js.org/style-guide/style-guide#do-not-mutate-state)");
            });
            measureUtils.warnIfExceeded();
            return dispatchedAction;
        }; };
    };
}
// src/serializableStateInvariantMiddleware.ts
function isPlain(val) {
    var type = typeof val;
    return type === "undefined" || val === null || type === "string" || type === "boolean" || type === "number" || Array.isArray(val) || isPlainObject(val);
}
function findNonSerializableValue(value, path, isSerializable, getEntries, ignoredPaths) {
    if (path === void 0) { path = ""; }
    if (isSerializable === void 0) { isSerializable = isPlain; }
    if (ignoredPaths === void 0) { ignoredPaths = []; }
    var foundNestedSerializable;
    if (!isSerializable(value)) {
        return {
            keyPath: path || "<root>",
            value: value
        };
    }
    if (typeof value !== "object" || value === null) {
        return false;
    }
    var entries = getEntries != null ? getEntries(value) : Object.entries(value);
    var hasIgnoredPaths = ignoredPaths.length > 0;
    for (var _i = 0, entries_1 = entries; _i < entries_1.length; _i++) {
        var _b = entries_1[_i], key = _b[0], nestedValue = _b[1];
        var nestedPath = path ? path + "." + key : key;
        if (hasIgnoredPaths && ignoredPaths.indexOf(nestedPath) >= 0) {
            continue;
        }
        if (!isSerializable(nestedValue)) {
            return {
                keyPath: nestedPath,
                value: nestedValue
            };
        }
        if (typeof nestedValue === "object") {
            foundNestedSerializable = findNonSerializableValue(nestedValue, nestedPath, isSerializable, getEntries, ignoredPaths);
            if (foundNestedSerializable) {
                return foundNestedSerializable;
            }
        }
    }
    return false;
}
function createSerializableStateInvariantMiddleware(options) {
    if (options === void 0) { options = {}; }
    if (false) {}
    var _b = options.isSerializable, isSerializable = _b === void 0 ? isPlain : _b, getEntries = options.getEntries, _c = options.ignoredActions, ignoredActions = _c === void 0 ? [] : _c, _d = options.ignoredActionPaths, ignoredActionPaths = _d === void 0 ? ["meta.arg", "meta.baseQueryMeta"] : _d, _e = options.ignoredPaths, ignoredPaths = _e === void 0 ? [] : _e, _f = options.warnAfter, warnAfter = _f === void 0 ? 32 : _f, _g = options.ignoreState, ignoreState = _g === void 0 ? false : _g;
    return function (storeAPI) { return function (next) { return function (action) {
        if (ignoredActions.length && ignoredActions.indexOf(action.type) !== -1) {
            return next(action);
        }
        var measureUtils = getTimeMeasureUtils(warnAfter, "SerializableStateInvariantMiddleware");
        measureUtils.measureTime(function () {
            var foundActionNonSerializableValue = findNonSerializableValue(action, "", isSerializable, getEntries, ignoredActionPaths);
            if (foundActionNonSerializableValue) {
                var keyPath = foundActionNonSerializableValue.keyPath, value = foundActionNonSerializableValue.value;
                console.error("A non-serializable value was detected in an action, in the path: `" + keyPath + "`. Value:", value, "\nTake a look at the logic that dispatched this action: ", action, "\n(See https://redux.js.org/faq/actions#why-should-type-be-a-string-or-at-least-serializable-why-should-my-action-types-be-constants)", "\n(To allow non-serializable values see: https://redux-toolkit.js.org/usage/usage-guide#working-with-non-serializable-data)");
            }
        });
        var result = next(action);
        if (!ignoreState) {
            measureUtils.measureTime(function () {
                var state = storeAPI.getState();
                var foundStateNonSerializableValue = findNonSerializableValue(state, "", isSerializable, getEntries, ignoredPaths);
                if (foundStateNonSerializableValue) {
                    var keyPath = foundStateNonSerializableValue.keyPath, value = foundStateNonSerializableValue.value;
                    console.error("A non-serializable value was detected in the state, in the path: `" + keyPath + "`. Value:", value, "\nTake a look at the reducer(s) handling this action type: " + action.type + ".\n(See https://redux.js.org/faq/organizing-state#can-i-put-functions-promises-or-other-non-serializable-items-in-my-store-state)");
                }
            });
            measureUtils.warnIfExceeded();
        }
        return result;
    }; }; };
}
// src/getDefaultMiddleware.ts
function isBoolean(x) {
    return typeof x === "boolean";
}
function curryGetDefaultMiddleware() {
    return function curriedGetDefaultMiddleware(options) {
        return getDefaultMiddleware(options);
    };
}
function getDefaultMiddleware(options) {
    if (options === void 0) { options = {}; }
    var _b = options.thunk, thunk = _b === void 0 ? true : _b, _c = options.immutableCheck, immutableCheck = _c === void 0 ? true : _c, _d = options.serializableCheck, serializableCheck = _d === void 0 ? true : _d;
    var middlewareArray = new MiddlewareArray();
    if (thunk) {
        if (isBoolean(thunk)) {
            middlewareArray.push(redux_thunk__WEBPACK_IMPORTED_MODULE_3__.default);
        }
        else {
            middlewareArray.push(redux_thunk__WEBPACK_IMPORTED_MODULE_3__.default.withExtraArgument(thunk.extraArgument));
        }
    }
    if (true) {
        if (immutableCheck) {
            var immutableOptions = {};
            if (!isBoolean(immutableCheck)) {
                immutableOptions = immutableCheck;
            }
            middlewareArray.unshift(createImmutableStateInvariantMiddleware(immutableOptions));
        }
        if (serializableCheck) {
            var serializableOptions = {};
            if (!isBoolean(serializableCheck)) {
                serializableOptions = serializableCheck;
            }
            middlewareArray.push(createSerializableStateInvariantMiddleware(serializableOptions));
        }
    }
    return middlewareArray;
}
// src/configureStore.ts
var IS_PRODUCTION = "development" === "production";
function configureStore(options) {
    var curriedGetDefaultMiddleware = curryGetDefaultMiddleware();
    var _b = options || {}, _c = _b.reducer, reducer = _c === void 0 ? void 0 : _c, _d = _b.middleware, middleware = _d === void 0 ? curriedGetDefaultMiddleware() : _d, _e = _b.devTools, devTools = _e === void 0 ? true : _e, _f = _b.preloadedState, preloadedState = _f === void 0 ? void 0 : _f, _g = _b.enhancers, enhancers = _g === void 0 ? void 0 : _g;
    var rootReducer;
    if (typeof reducer === "function") {
        rootReducer = reducer;
    }
    else if (isPlainObject(reducer)) {
        rootReducer = (0,redux__WEBPACK_IMPORTED_MODULE_0__.combineReducers)(reducer);
    }
    else {
        throw new Error('"reducer" is a required argument, and must be a function or an object of functions that can be passed to combineReducers');
    }
    var finalMiddleware = middleware;
    if (typeof finalMiddleware === "function") {
        finalMiddleware = finalMiddleware(curriedGetDefaultMiddleware);
        if (!IS_PRODUCTION && !Array.isArray(finalMiddleware)) {
            throw new Error("when using a middleware builder function, an array of middleware must be returned");
        }
    }
    if (!IS_PRODUCTION && finalMiddleware.some(function (item) { return typeof item !== "function"; })) {
        throw new Error("each middleware provided to configureStore must be a function");
    }
    var middlewareEnhancer = redux__WEBPACK_IMPORTED_MODULE_0__.applyMiddleware.apply(void 0, finalMiddleware);
    var finalCompose = redux__WEBPACK_IMPORTED_MODULE_0__.compose;
    if (devTools) {
        finalCompose = composeWithDevTools(__spreadValues({
            trace: !IS_PRODUCTION
        }, typeof devTools === "object" && devTools));
    }
    var storeEnhancers = [middlewareEnhancer];
    if (Array.isArray(enhancers)) {
        storeEnhancers = __spreadArray([middlewareEnhancer], enhancers);
    }
    else if (typeof enhancers === "function") {
        storeEnhancers = enhancers(storeEnhancers);
    }
    var composedEnhancer = finalCompose.apply(void 0, storeEnhancers);
    return (0,redux__WEBPACK_IMPORTED_MODULE_0__.createStore)(rootReducer, preloadedState, composedEnhancer);
}
// src/createAction.ts
function createAction(type, prepareAction) {
    function actionCreator() {
        var args = [];
        for (var _i = 0; _i < arguments.length; _i++) {
            args[_i] = arguments[_i];
        }
        if (prepareAction) {
            var prepared = prepareAction.apply(void 0, args);
            if (!prepared) {
                throw new Error("prepareAction did not return an object");
            }
            return __spreadValues(__spreadValues({
                type: type,
                payload: prepared.payload
            }, "meta" in prepared && { meta: prepared.meta }), "error" in prepared && { error: prepared.error });
        }
        return { type: type, payload: args[0] };
    }
    actionCreator.toString = function () { return "" + type; };
    actionCreator.type = type;
    actionCreator.match = function (action) { return action.type === type; };
    return actionCreator;
}
function isFSA(action) {
    return isPlainObject(action) && typeof action.type === "string" && Object.keys(action).every(isValidKey);
}
function isValidKey(key) {
    return ["type", "payload", "error", "meta"].indexOf(key) > -1;
}
function getType(actionCreator) {
    return "" + actionCreator;
}
// src/createReducer.ts

// src/mapBuilders.ts
function executeReducerBuilderCallback(builderCallback) {
    var actionsMap = {};
    var actionMatchers = [];
    var defaultCaseReducer;
    var builder = {
        addCase: function (typeOrActionCreator, reducer) {
            if (true) {
                if (actionMatchers.length > 0) {
                    throw new Error("`builder.addCase` should only be called before calling `builder.addMatcher`");
                }
                if (defaultCaseReducer) {
                    throw new Error("`builder.addCase` should only be called before calling `builder.addDefaultCase`");
                }
            }
            var type = typeof typeOrActionCreator === "string" ? typeOrActionCreator : typeOrActionCreator.type;
            if (type in actionsMap) {
                throw new Error("addCase cannot be called with two reducers for the same action type");
            }
            actionsMap[type] = reducer;
            return builder;
        },
        addMatcher: function (matcher, reducer) {
            if (true) {
                if (defaultCaseReducer) {
                    throw new Error("`builder.addMatcher` should only be called before calling `builder.addDefaultCase`");
                }
            }
            actionMatchers.push({ matcher: matcher, reducer: reducer });
            return builder;
        },
        addDefaultCase: function (reducer) {
            if (true) {
                if (defaultCaseReducer) {
                    throw new Error("`builder.addDefaultCase` can only be called once");
                }
            }
            defaultCaseReducer = reducer;
            return builder;
        }
    };
    builderCallback(builder);
    return [actionsMap, actionMatchers, defaultCaseReducer];
}
// src/createReducer.ts
function createReducer(initialState, mapOrBuilderCallback, actionMatchers, defaultCaseReducer) {
    if (actionMatchers === void 0) { actionMatchers = []; }
    var _b = typeof mapOrBuilderCallback === "function" ? executeReducerBuilderCallback(mapOrBuilderCallback) : [mapOrBuilderCallback, actionMatchers, defaultCaseReducer], actionsMap = _b[0], finalActionMatchers = _b[1], finalDefaultCaseReducer = _b[2];
    var frozenInitialState = (0,immer__WEBPACK_IMPORTED_MODULE_2__.default)(initialState, function () {
    });
    return function (state, action) {
        if (state === void 0) { state = frozenInitialState; }
        var caseReducers = __spreadArray([
            actionsMap[action.type]
        ], finalActionMatchers.filter(function (_b) {
            var matcher = _b.matcher;
            return matcher(action);
        }).map(function (_b) {
            var reducer = _b.reducer;
            return reducer;
        }));
        if (caseReducers.filter(function (cr) { return !!cr; }).length === 0) {
            caseReducers = [finalDefaultCaseReducer];
        }
        return caseReducers.reduce(function (previousState, caseReducer) {
            if (caseReducer) {
                if ((0,immer__WEBPACK_IMPORTED_MODULE_2__.isDraft)(previousState)) {
                    var draft = previousState;
                    var result = caseReducer(draft, action);
                    if (typeof result === "undefined") {
                        return previousState;
                    }
                    return result;
                }
                else if (!(0,immer__WEBPACK_IMPORTED_MODULE_2__.isDraftable)(previousState)) {
                    var result = caseReducer(previousState, action);
                    if (typeof result === "undefined") {
                        if (previousState === null) {
                            return previousState;
                        }
                        throw Error("A case reducer on a non-draftable value must not return undefined");
                    }
                    return result;
                }
                else {
                    return (0,immer__WEBPACK_IMPORTED_MODULE_2__.default)(previousState, function (draft) {
                        return caseReducer(draft, action);
                    });
                }
            }
            return previousState;
        }, state);
    };
}
// src/createSlice.ts
function getType2(slice, actionKey) {
    return slice + "/" + actionKey;
}
function createSlice(options) {
    var name = options.name, initialState = options.initialState;
    if (!name) {
        throw new Error("`name` is a required option for createSlice");
    }
    var reducers = options.reducers || {};
    var _b = typeof options.extraReducers === "function" ? executeReducerBuilderCallback(options.extraReducers) : [options.extraReducers], _c = _b[0], extraReducers = _c === void 0 ? {} : _c, _d = _b[1], actionMatchers = _d === void 0 ? [] : _d, _e = _b[2], defaultCaseReducer = _e === void 0 ? void 0 : _e;
    var reducerNames = Object.keys(reducers);
    var sliceCaseReducersByName = {};
    var sliceCaseReducersByType = {};
    var actionCreators = {};
    reducerNames.forEach(function (reducerName) {
        var maybeReducerWithPrepare = reducers[reducerName];
        var type = getType2(name, reducerName);
        var caseReducer;
        var prepareCallback;
        if ("reducer" in maybeReducerWithPrepare) {
            caseReducer = maybeReducerWithPrepare.reducer;
            prepareCallback = maybeReducerWithPrepare.prepare;
        }
        else {
            caseReducer = maybeReducerWithPrepare;
        }
        sliceCaseReducersByName[reducerName] = caseReducer;
        sliceCaseReducersByType[type] = caseReducer;
        actionCreators[reducerName] = prepareCallback ? createAction(type, prepareCallback) : createAction(type);
    });
    var finalCaseReducers = __spreadValues(__spreadValues({}, extraReducers), sliceCaseReducersByType);
    var reducer = createReducer(initialState, finalCaseReducers, actionMatchers, defaultCaseReducer);
    return {
        name: name,
        reducer: reducer,
        actions: actionCreators,
        caseReducers: sliceCaseReducersByName
    };
}
// src/entities/entity_state.ts
function getInitialEntityState() {
    return {
        ids: [],
        entities: {}
    };
}
function createInitialStateFactory() {
    function getInitialState(additionalState) {
        if (additionalState === void 0) { additionalState = {}; }
        return Object.assign(getInitialEntityState(), additionalState);
    }
    return { getInitialState: getInitialState };
}
// src/entities/state_selectors.ts
function createSelectorsFactory() {
    function getSelectors(selectState) {
        var selectIds = function (state) { return state.ids; };
        var selectEntities = function (state) { return state.entities; };
        var selectAll = createDraftSafeSelector(selectIds, selectEntities, function (ids, entities) { return ids.map(function (id) { return entities[id]; }); });
        var selectId = function (_, id) { return id; };
        var selectById = function (entities, id) { return entities[id]; };
        var selectTotal = createDraftSafeSelector(selectIds, function (ids) { return ids.length; });
        if (!selectState) {
            return {
                selectIds: selectIds,
                selectEntities: selectEntities,
                selectAll: selectAll,
                selectTotal: selectTotal,
                selectById: createDraftSafeSelector(selectEntities, selectId, selectById)
            };
        }
        var selectGlobalizedEntities = createDraftSafeSelector(selectState, selectEntities);
        return {
            selectIds: createDraftSafeSelector(selectState, selectIds),
            selectEntities: selectGlobalizedEntities,
            selectAll: createDraftSafeSelector(selectState, selectAll),
            selectTotal: createDraftSafeSelector(selectState, selectTotal),
            selectById: createDraftSafeSelector(selectGlobalizedEntities, selectId, selectById)
        };
    }
    return { getSelectors: getSelectors };
}
// src/entities/state_adapter.ts

function createSingleArgumentStateOperator(mutator) {
    var operator = createStateOperator(function (_, state) { return mutator(state); });
    return function operation(state) {
        return operator(state, void 0);
    };
}
function createStateOperator(mutator) {
    return function operation(state, arg) {
        function isPayloadActionArgument(arg2) {
            return isFSA(arg2);
        }
        var runMutator = function (draft) {
            if (isPayloadActionArgument(arg)) {
                mutator(arg.payload, draft);
            }
            else {
                mutator(arg, draft);
            }
        };
        if ((0,immer__WEBPACK_IMPORTED_MODULE_2__.isDraft)(state)) {
            runMutator(state);
            return state;
        }
        else {
            return (0,immer__WEBPACK_IMPORTED_MODULE_2__.default)(state, runMutator);
        }
    };
}
// src/entities/utils.ts
function selectIdValue(entity, selectId) {
    var key = selectId(entity);
    if ( true && key === void 0) {
        console.warn("The entity passed to the `selectId` implementation returned undefined.", "You should probably provide your own `selectId` implementation.", "The entity that was passed:", entity, "The `selectId` implementation:", selectId.toString());
    }
    return key;
}
function ensureEntitiesArray(entities) {
    if (!Array.isArray(entities)) {
        entities = Object.values(entities);
    }
    return entities;
}
function splitAddedUpdatedEntities(newEntities, selectId, state) {
    newEntities = ensureEntitiesArray(newEntities);
    var added = [];
    var updated = [];
    for (var _i = 0, newEntities_1 = newEntities; _i < newEntities_1.length; _i++) {
        var entity = newEntities_1[_i];
        var id = selectIdValue(entity, selectId);
        if (id in state.entities) {
            updated.push({ id: id, changes: entity });
        }
        else {
            added.push(entity);
        }
    }
    return [added, updated];
}
// src/entities/unsorted_state_adapter.ts
function createUnsortedStateAdapter(selectId) {
    function addOneMutably(entity, state) {
        var key = selectIdValue(entity, selectId);
        if (key in state.entities) {
            return;
        }
        state.ids.push(key);
        state.entities[key] = entity;
    }
    function addManyMutably(newEntities, state) {
        newEntities = ensureEntitiesArray(newEntities);
        for (var _i = 0, newEntities_2 = newEntities; _i < newEntities_2.length; _i++) {
            var entity = newEntities_2[_i];
            addOneMutably(entity, state);
        }
    }
    function setOneMutably(entity, state) {
        var key = selectIdValue(entity, selectId);
        if (!(key in state.entities)) {
            state.ids.push(key);
        }
        state.entities[key] = entity;
    }
    function setManyMutably(newEntities, state) {
        newEntities = ensureEntitiesArray(newEntities);
        for (var _i = 0, newEntities_3 = newEntities; _i < newEntities_3.length; _i++) {
            var entity = newEntities_3[_i];
            setOneMutably(entity, state);
        }
    }
    function setAllMutably(newEntities, state) {
        newEntities = ensureEntitiesArray(newEntities);
        state.ids = [];
        state.entities = {};
        addManyMutably(newEntities, state);
    }
    function removeOneMutably(key, state) {
        return removeManyMutably([key], state);
    }
    function removeManyMutably(keys, state) {
        var didMutate = false;
        keys.forEach(function (key) {
            if (key in state.entities) {
                delete state.entities[key];
                didMutate = true;
            }
        });
        if (didMutate) {
            state.ids = state.ids.filter(function (id) { return id in state.entities; });
        }
    }
    function removeAllMutably(state) {
        Object.assign(state, {
            ids: [],
            entities: {}
        });
    }
    function takeNewKey(keys, update, state) {
        var original2 = state.entities[update.id];
        var updated = Object.assign({}, original2, update.changes);
        var newKey = selectIdValue(updated, selectId);
        var hasNewKey = newKey !== update.id;
        if (hasNewKey) {
            keys[update.id] = newKey;
            delete state.entities[update.id];
        }
        state.entities[newKey] = updated;
        return hasNewKey;
    }
    function updateOneMutably(update, state) {
        return updateManyMutably([update], state);
    }
    function updateManyMutably(updates, state) {
        var newKeys = {};
        var updatesPerEntity = {};
        updates.forEach(function (update) {
            if (update.id in state.entities) {
                updatesPerEntity[update.id] = {
                    id: update.id,
                    changes: __spreadValues(__spreadValues({}, updatesPerEntity[update.id] ? updatesPerEntity[update.id].changes : null), update.changes)
                };
            }
        });
        updates = Object.values(updatesPerEntity);
        var didMutateEntities = updates.length > 0;
        if (didMutateEntities) {
            var didMutateIds = updates.filter(function (update) { return takeNewKey(newKeys, update, state); }).length > 0;
            if (didMutateIds) {
                state.ids = state.ids.map(function (id) { return newKeys[id] || id; });
            }
        }
    }
    function upsertOneMutably(entity, state) {
        return upsertManyMutably([entity], state);
    }
    function upsertManyMutably(newEntities, state) {
        var _b = splitAddedUpdatedEntities(newEntities, selectId, state), added = _b[0], updated = _b[1];
        updateManyMutably(updated, state);
        addManyMutably(added, state);
    }
    return {
        removeAll: createSingleArgumentStateOperator(removeAllMutably),
        addOne: createStateOperator(addOneMutably),
        addMany: createStateOperator(addManyMutably),
        setOne: createStateOperator(setOneMutably),
        setMany: createStateOperator(setManyMutably),
        setAll: createStateOperator(setAllMutably),
        updateOne: createStateOperator(updateOneMutably),
        updateMany: createStateOperator(updateManyMutably),
        upsertOne: createStateOperator(upsertOneMutably),
        upsertMany: createStateOperator(upsertManyMutably),
        removeOne: createStateOperator(removeOneMutably),
        removeMany: createStateOperator(removeManyMutably)
    };
}
// src/entities/sorted_state_adapter.ts
function createSortedStateAdapter(selectId, sort) {
    var _b = createUnsortedStateAdapter(selectId), removeOne = _b.removeOne, removeMany = _b.removeMany, removeAll = _b.removeAll;
    function addOneMutably(entity, state) {
        return addManyMutably([entity], state);
    }
    function addManyMutably(newEntities, state) {
        newEntities = ensureEntitiesArray(newEntities);
        var models = newEntities.filter(function (model) { return !(selectIdValue(model, selectId) in state.entities); });
        if (models.length !== 0) {
            merge(models, state);
        }
    }
    function setOneMutably(entity, state) {
        return setManyMutably([entity], state);
    }
    function setManyMutably(newEntities, state) {
        newEntities = ensureEntitiesArray(newEntities);
        if (newEntities.length !== 0) {
            merge(newEntities, state);
        }
    }
    function setAllMutably(newEntities, state) {
        newEntities = ensureEntitiesArray(newEntities);
        state.entities = {};
        state.ids = [];
        addManyMutably(newEntities, state);
    }
    function updateOneMutably(update, state) {
        return updateManyMutably([update], state);
    }
    function takeUpdatedModel(models, update, state) {
        if (!(update.id in state.entities)) {
            return false;
        }
        var original2 = state.entities[update.id];
        var updated = Object.assign({}, original2, update.changes);
        var newKey = selectIdValue(updated, selectId);
        delete state.entities[update.id];
        models.push(updated);
        return newKey !== update.id;
    }
    function updateManyMutably(updates, state) {
        var models = [];
        updates.forEach(function (update) { return takeUpdatedModel(models, update, state); });
        if (models.length !== 0) {
            merge(models, state);
        }
    }
    function upsertOneMutably(entity, state) {
        return upsertManyMutably([entity], state);
    }
    function upsertManyMutably(newEntities, state) {
        var _b = splitAddedUpdatedEntities(newEntities, selectId, state), added = _b[0], updated = _b[1];
        updateManyMutably(updated, state);
        addManyMutably(added, state);
    }
    function areArraysEqual(a, b) {
        if (a.length !== b.length) {
            return false;
        }
        for (var i = 0; i < a.length && i < b.length; i++) {
            if (a[i] === b[i]) {
                continue;
            }
            return false;
        }
        return true;
    }
    function merge(models, state) {
        models.forEach(function (model) {
            state.entities[selectId(model)] = model;
        });
        var allEntities = Object.values(state.entities);
        allEntities.sort(sort);
        var newSortedIds = allEntities.map(selectId);
        var ids = state.ids;
        if (!areArraysEqual(ids, newSortedIds)) {
            state.ids = newSortedIds;
        }
    }
    return {
        removeOne: removeOne,
        removeMany: removeMany,
        removeAll: removeAll,
        addOne: createStateOperator(addOneMutably),
        updateOne: createStateOperator(updateOneMutably),
        upsertOne: createStateOperator(upsertOneMutably),
        setOne: createStateOperator(setOneMutably),
        setMany: createStateOperator(setManyMutably),
        setAll: createStateOperator(setAllMutably),
        addMany: createStateOperator(addManyMutably),
        updateMany: createStateOperator(updateManyMutably),
        upsertMany: createStateOperator(upsertManyMutably)
    };
}
// src/entities/create_adapter.ts
function createEntityAdapter(options) {
    if (options === void 0) { options = {}; }
    var _b = __spreadValues({
        sortComparer: false,
        selectId: function (instance) { return instance.id; }
    }, options), selectId = _b.selectId, sortComparer = _b.sortComparer;
    var stateFactory = createInitialStateFactory();
    var selectorsFactory = createSelectorsFactory();
    var stateAdapter = sortComparer ? createSortedStateAdapter(selectId, sortComparer) : createUnsortedStateAdapter(selectId);
    return __spreadValues(__spreadValues(__spreadValues({
        selectId: selectId,
        sortComparer: sortComparer
    }, stateFactory), selectorsFactory), stateAdapter);
}
// src/nanoid.ts
var urlAlphabet = "ModuleSymbhasOwnPr-0123456789ABCDEFGHNRVfgctiUvz_KqYTJkLxpZXIjQW";
var nanoid = function (size) {
    if (size === void 0) { size = 21; }
    var id = "";
    var i = size;
    while (i--) {
        id += urlAlphabet[Math.random() * 64 | 0];
    }
    return id;
};
// src/createAsyncThunk.ts
var commonProperties = [
    "name",
    "message",
    "stack",
    "code"
];
var RejectWithValue = /** @class */ (function () {
    function RejectWithValue(payload, meta) {
        this.payload = payload;
        this.meta = meta;
    }
    return RejectWithValue;
}());
var FulfillWithMeta = /** @class */ (function () {
    function FulfillWithMeta(payload, meta) {
        this.payload = payload;
        this.meta = meta;
    }
    return FulfillWithMeta;
}());
var miniSerializeError = function (value) {
    if (typeof value === "object" && value !== null) {
        var simpleError = {};
        for (var _i = 0, commonProperties_1 = commonProperties; _i < commonProperties_1.length; _i++) {
            var property = commonProperties_1[_i];
            if (typeof value[property] === "string") {
                simpleError[property] = value[property];
            }
        }
        return simpleError;
    }
    return { message: String(value) };
};
function createAsyncThunk(typePrefix, payloadCreator, options) {
    var fulfilled = createAction(typePrefix + "/fulfilled", function (payload, requestId, arg, meta) { return ({
        payload: payload,
        meta: __spreadProps(__spreadValues({}, meta || {}), {
            arg: arg,
            requestId: requestId,
            requestStatus: "fulfilled"
        })
    }); });
    var pending = createAction(typePrefix + "/pending", function (requestId, arg, meta) { return ({
        payload: void 0,
        meta: __spreadProps(__spreadValues({}, meta || {}), {
            arg: arg,
            requestId: requestId,
            requestStatus: "pending"
        })
    }); });
    var rejected = createAction(typePrefix + "/rejected", function (error, requestId, arg, payload, meta) { return ({
        payload: payload,
        error: (options && options.serializeError || miniSerializeError)(error || "Rejected"),
        meta: __spreadProps(__spreadValues({}, meta || {}), {
            arg: arg,
            requestId: requestId,
            rejectedWithValue: !!payload,
            requestStatus: "rejected",
            aborted: (error == null ? void 0 : error.name) === "AbortError",
            condition: (error == null ? void 0 : error.name) === "ConditionError"
        })
    }); });
    var displayedWarning = false;
    var AC = typeof AbortController !== "undefined" ? AbortController : /** @class */ (function () {
        function class_1() {
            this.signal = {
                aborted: false,
                addEventListener: function () {
                },
                dispatchEvent: function () {
                    return false;
                },
                onabort: function () {
                },
                removeEventListener: function () {
                }
            };
        }
        class_1.prototype.abort = function () {
            if (true) {
                if (!displayedWarning) {
                    displayedWarning = true;
                    console.info("This platform does not implement AbortController. \nIf you want to use the AbortController to react to `abort` events, please consider importing a polyfill like 'abortcontroller-polyfill/dist/abortcontroller-polyfill-only'.");
                }
            }
        };
        return class_1;
    }());
    function actionCreator(arg) {
        return function (dispatch, getState, extra) {
            var _a;
            var requestId = ((_a = options == null ? void 0 : options.idGenerator) != null ? _a : nanoid)();
            var abortController = new AC();
            var abortReason;
            var abortedPromise = new Promise(function (_, reject) { return abortController.signal.addEventListener("abort", function () { return reject({ name: "AbortError", message: abortReason || "Aborted" }); }); });
            var started = false;
            function abort(reason) {
                if (started) {
                    abortReason = reason;
                    abortController.abort();
                }
            }
            var promise = function () {
                return __async(this, null, function () {
                    var _a2, finalAction, err_1, skipDispatch;
                    return __generator(this, function (_b) {
                        switch (_b.label) {
                            case 0:
                                _b.trys.push([0, 2, , 3]);
                                if (options && options.condition && options.condition(arg, { getState: getState, extra: extra }) === false) {
                                    throw {
                                        name: "ConditionError",
                                        message: "Aborted due to condition callback returning false."
                                    };
                                }
                                started = true;
                                dispatch(pending(requestId, arg, (_a2 = options == null ? void 0 : options.getPendingMeta) == null ? void 0 : _a2.call(options, { requestId: requestId, arg: arg }, { getState: getState, extra: extra })));
                                return [4 /*yield*/, Promise.race([
                                        abortedPromise,
                                        Promise.resolve(payloadCreator(arg, {
                                            dispatch: dispatch,
                                            getState: getState,
                                            extra: extra,
                                            requestId: requestId,
                                            signal: abortController.signal,
                                            rejectWithValue: function (value, meta) {
                                                return new RejectWithValue(value, meta);
                                            },
                                            fulfillWithValue: function (value, meta) {
                                                return new FulfillWithMeta(value, meta);
                                            }
                                        })).then(function (result) {
                                            if (result instanceof RejectWithValue) {
                                                throw result;
                                            }
                                            if (result instanceof FulfillWithMeta) {
                                                return fulfilled(result.payload, requestId, arg, result.meta);
                                            }
                                            return fulfilled(result, requestId, arg);
                                        })
                                    ])];
                            case 1:
                                finalAction = _b.sent();
                                return [3 /*break*/, 3];
                            case 2:
                                err_1 = _b.sent();
                                finalAction = err_1 instanceof RejectWithValue ? rejected(null, requestId, arg, err_1.payload, err_1.meta) : rejected(err_1, requestId, arg);
                                return [3 /*break*/, 3];
                            case 3:
                                skipDispatch = options && !options.dispatchConditionRejection && rejected.match(finalAction) && finalAction.meta.condition;
                                if (!skipDispatch) {
                                    dispatch(finalAction);
                                }
                                return [2 /*return*/, finalAction];
                        }
                    });
                });
            }();
            return Object.assign(promise, {
                abort: abort,
                requestId: requestId,
                arg: arg,
                unwrap: function () {
                    return promise.then(unwrapResult);
                }
            });
        };
    }
    return Object.assign(actionCreator, {
        pending: pending,
        rejected: rejected,
        fulfilled: fulfilled,
        typePrefix: typePrefix
    });
}
function unwrapResult(action) {
    if (action.meta && action.meta.rejectedWithValue) {
        throw action.payload;
    }
    if (action.error) {
        throw action.error;
    }
    return action.payload;
}
// src/tsHelpers.ts
var hasMatchFunction = function (v) {
    return v && typeof v.match === "function";
};
// src/matchers.ts
var matches = function (matcher, action) {
    if (hasMatchFunction(matcher)) {
        return matcher.match(action);
    }
    else {
        return matcher(action);
    }
};
function isAnyOf() {
    var matchers = [];
    for (var _i = 0; _i < arguments.length; _i++) {
        matchers[_i] = arguments[_i];
    }
    return function (action) {
        return matchers.some(function (matcher) { return matches(matcher, action); });
    };
}
function isAllOf() {
    var matchers = [];
    for (var _i = 0; _i < arguments.length; _i++) {
        matchers[_i] = arguments[_i];
    }
    return function (action) {
        return matchers.every(function (matcher) { return matches(matcher, action); });
    };
}
function hasExpectedRequestMetadata(action, validStatus) {
    if (!action || !action.meta)
        return false;
    var hasValidRequestId = typeof action.meta.requestId === "string";
    var hasValidRequestStatus = validStatus.indexOf(action.meta.requestStatus) > -1;
    return hasValidRequestId && hasValidRequestStatus;
}
function isAsyncThunkArray(a) {
    return typeof a[0] === "function" && "pending" in a[0] && "fulfilled" in a[0] && "rejected" in a[0];
}
function isPending() {
    var asyncThunks = [];
    for (var _i = 0; _i < arguments.length; _i++) {
        asyncThunks[_i] = arguments[_i];
    }
    if (asyncThunks.length === 0) {
        return function (action) { return hasExpectedRequestMetadata(action, ["pending"]); };
    }
    if (!isAsyncThunkArray(asyncThunks)) {
        return isPending()(asyncThunks[0]);
    }
    return function (action) {
        var matchers = asyncThunks.map(function (asyncThunk) { return asyncThunk.pending; });
        var combinedMatcher = isAnyOf.apply(void 0, matchers);
        return combinedMatcher(action);
    };
}
function isRejected() {
    var asyncThunks = [];
    for (var _i = 0; _i < arguments.length; _i++) {
        asyncThunks[_i] = arguments[_i];
    }
    if (asyncThunks.length === 0) {
        return function (action) { return hasExpectedRequestMetadata(action, ["rejected"]); };
    }
    if (!isAsyncThunkArray(asyncThunks)) {
        return isRejected()(asyncThunks[0]);
    }
    return function (action) {
        var matchers = asyncThunks.map(function (asyncThunk) { return asyncThunk.rejected; });
        var combinedMatcher = isAnyOf.apply(void 0, matchers);
        return combinedMatcher(action);
    };
}
function isRejectedWithValue() {
    var asyncThunks = [];
    for (var _i = 0; _i < arguments.length; _i++) {
        asyncThunks[_i] = arguments[_i];
    }
    var hasFlag = function (action) {
        return action && action.meta && action.meta.rejectedWithValue;
    };
    if (asyncThunks.length === 0) {
        return function (action) {
            var combinedMatcher = isAllOf(isRejected.apply(void 0, asyncThunks), hasFlag);
            return combinedMatcher(action);
        };
    }
    if (!isAsyncThunkArray(asyncThunks)) {
        return isRejectedWithValue()(asyncThunks[0]);
    }
    return function (action) {
        var combinedMatcher = isAllOf(isRejected.apply(void 0, asyncThunks), hasFlag);
        return combinedMatcher(action);
    };
}
function isFulfilled() {
    var asyncThunks = [];
    for (var _i = 0; _i < arguments.length; _i++) {
        asyncThunks[_i] = arguments[_i];
    }
    if (asyncThunks.length === 0) {
        return function (action) { return hasExpectedRequestMetadata(action, ["fulfilled"]); };
    }
    if (!isAsyncThunkArray(asyncThunks)) {
        return isFulfilled()(asyncThunks[0]);
    }
    return function (action) {
        var matchers = asyncThunks.map(function (asyncThunk) { return asyncThunk.fulfilled; });
        var combinedMatcher = isAnyOf.apply(void 0, matchers);
        return combinedMatcher(action);
    };
}
function isAsyncThunkAction() {
    var asyncThunks = [];
    for (var _i = 0; _i < arguments.length; _i++) {
        asyncThunks[_i] = arguments[_i];
    }
    if (asyncThunks.length === 0) {
        return function (action) { return hasExpectedRequestMetadata(action, ["pending", "fulfilled", "rejected"]); };
    }
    if (!isAsyncThunkArray(asyncThunks)) {
        return isAsyncThunkAction()(asyncThunks[0]);
    }
    return function (action) {
        var matchers = [];
        for (var _i = 0, asyncThunks_1 = asyncThunks; _i < asyncThunks_1.length; _i++) {
            var asyncThunk = asyncThunks_1[_i];
            matchers.push(asyncThunk.pending, asyncThunk.rejected, asyncThunk.fulfilled);
        }
        var combinedMatcher = isAnyOf.apply(void 0, matchers);
        return combinedMatcher(action);
    };
}
// src/index.ts
(0,immer__WEBPACK_IMPORTED_MODULE_2__.enableES5)();

//# sourceMappingURL=module.js.map

/***/ }),

/***/ "./node_modules/css-loader/dist/cjs.js??ruleSet[1].rules[1].use[1]!./node_modules/sass-loader/dist/cjs.js!./node_modules/react-range-slider-ts/dist/index.css":
/*!********************************************************************************************************************************************************************!*\
  !*** ./node_modules/css-loader/dist/cjs.js??ruleSet[1].rules[1].use[1]!./node_modules/sass-loader/dist/cjs.js!./node_modules/react-range-slider-ts/dist/index.css ***!
  \********************************************************************************************************************************************************************/
/***/ ((module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _css_loader_dist_runtime_cssWithMappingToString_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../../css-loader/dist/runtime/cssWithMappingToString.js */ "./node_modules/css-loader/dist/runtime/cssWithMappingToString.js");
/* harmony import */ var _css_loader_dist_runtime_cssWithMappingToString_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_css_loader_dist_runtime_cssWithMappingToString_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../css-loader/dist/runtime/api.js */ "./node_modules/css-loader/dist/runtime/api.js");
/* harmony import */ var _css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1__);
// Imports


var ___CSS_LOADER_EXPORT___ = _css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1___default()((_css_loader_dist_runtime_cssWithMappingToString_js__WEBPACK_IMPORTED_MODULE_0___default()));
// Module
___CSS_LOADER_EXPORT___.push([module.id, "._FHxWW, ._29f73 {\n  width: 100%;\n  height: 100%;\n}\n\n._29f73, ._38pIP {\n  position: absolute;\n  left: 0;\n  top: 50%;\n  transform: translateY(-50%);\n}\n\n._FHxWW {\n  position: relative;\n  pointer-events: all;\n  -webkit-user-select: none;\n  -moz-user-select: none;\n  -ms-user-select: none;\n  user-select: none;\n}\n\n._29f73 {\n  cursor: pointer;\n}\n\n._38pIP {\n  height: 100%;\n  pointer-events: none;\n}\n\n._1nNAP {\n  position: absolute;\n  top: 50%;\n  transform: translate(-50%, -50%);\n  z-index: 1;\n  cursor: pointer;\n}\n\n._1bNv5 ._1fq1o, ._1bNv5 ._1DbHP {\n  transition: all 200ms ease-in;\n}\n\n._tGccK {\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: all;\n  -webkit-user-select: none;\n  -moz-user-select: none;\n  -ms-user-select: none;\n  user-select: none;\n}\n\n._2nVMZ {\n  position: absolute;\n  left: 0;\n  top: 50%;\n  transform: translateY(-50%);\n  width: 100%;\n  height: 100%;\n  cursor: pointer;\n}\n\n._12Rcd {\n  position: absolute;\n  left: 0;\n  top: 50%;\n  transform: translateY(-50%);\n  height: 100%;\n  pointer-events: none;\n}\n\n._1DbHP {\n  position: absolute;\n  top: 50%;\n  transform: translate(-50%, -50%);\n  z-index: 1;\n}", "",{"version":3,"sources":["webpack://./node_modules/react-range-slider-ts/dist/index.css"],"names":[],"mappings":"AAAA;EACE,WAAA;EACA,YAAA;AACF;;AACA;EACE,kBAAA;EACA,OAAA;EACA,QAAA;EACA,2BAAA;AAEF;;AAAA;EACE,kBAAA;EACA,mBAAA;EACA,yBAAA;EACG,sBAAA;EACC,qBAAA;EACI,iBAAA;AAGV;;AADA;EACE,eAAA;AAIF;;AAFA;EACE,YAAA;EACA,oBAAA;AAKF;;AAHA;EACE,kBAAA;EACA,QAAA;EACA,gCAAA;EACA,UAAA;EACA,eAAA;AAMF;;AAJA;EACE,6BAAA;AAOF;;AALA;EACE,kBAAA;EACA,WAAA;EACA,YAAA;EACA,mBAAA;EACA,yBAAA;EACG,sBAAA;EACC,qBAAA;EACI,iBAAA;AAQV;;AANA;EACE,kBAAA;EACA,OAAA;EACA,QAAA;EACA,2BAAA;EACA,WAAA;EACA,YAAA;EACA,eAAA;AASF;;AAPA;EACE,kBAAA;EACA,OAAA;EACA,QAAA;EACA,2BAAA;EACA,YAAA;EACA,oBAAA;AAUF;;AARA;EACE,kBAAA;EACA,QAAA;EACA,gCAAA;EACA,UAAA;AAWF","sourcesContent":["._FHxWW, ._29f73 {\n  width: 100%;\n  height: 100%; }\n\n._29f73, ._38pIP {\n  position: absolute;\n  left: 0;\n  top: 50%;\n  transform: translateY(-50%); }\n\n._FHxWW {\n  position: relative;\n  pointer-events: all;\n  -webkit-user-select: none;\n     -moz-user-select: none;\n      -ms-user-select: none;\n          user-select: none; }\n\n._29f73 {\n  cursor: pointer; }\n\n._38pIP {\n  height: 100%;\n  pointer-events: none; }\n\n._1nNAP {\n  position: absolute;\n  top: 50%;\n  transform: translate(-50%, -50%);\n  z-index: 1;\n  cursor: pointer; }\n\n._1bNv5 ._1fq1o, ._1bNv5 ._1DbHP {\n  transition: all 200ms ease-in; }\n\n._tGccK {\n  position: relative;\n  width: 100%;\n  height: 100%;\n  pointer-events: all;\n  -webkit-user-select: none;\n     -moz-user-select: none;\n      -ms-user-select: none;\n          user-select: none; }\n\n._2nVMZ {\n  position: absolute;\n  left: 0;\n  top: 50%;\n  transform: translateY(-50%);\n  width: 100%;\n  height: 100%;\n  cursor: pointer; }\n\n._12Rcd {\n  position: absolute;\n  left: 0;\n  top: 50%;\n  transform: translateY(-50%);\n  height: 100%;\n  pointer-events: none; }\n\n._1DbHP {\n  position: absolute;\n  top: 50%;\n  transform: translate(-50%, -50%);\n  z-index: 1; }\n"],"sourceRoot":""}]);
// Exports
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (___CSS_LOADER_EXPORT___);


/***/ }),

/***/ "./node_modules/css-loader/dist/cjs.js??ruleSet[1].rules[1].use[1]!./node_modules/sass-loader/dist/cjs.js!./node_modules/react-video-ts/dist/index.css":
/*!*************************************************************************************************************************************************************!*\
  !*** ./node_modules/css-loader/dist/cjs.js??ruleSet[1].rules[1].use[1]!./node_modules/sass-loader/dist/cjs.js!./node_modules/react-video-ts/dist/index.css ***!
  \*************************************************************************************************************************************************************/
/***/ ((module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _css_loader_dist_runtime_cssWithMappingToString_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../../css-loader/dist/runtime/cssWithMappingToString.js */ "./node_modules/css-loader/dist/runtime/cssWithMappingToString.js");
/* harmony import */ var _css_loader_dist_runtime_cssWithMappingToString_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_css_loader_dist_runtime_cssWithMappingToString_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../css-loader/dist/runtime/api.js */ "./node_modules/css-loader/dist/runtime/api.js");
/* harmony import */ var _css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1__);
// Imports


var ___CSS_LOADER_EXPORT___ = _css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1___default()((_css_loader_dist_runtime_cssWithMappingToString_js__WEBPACK_IMPORTED_MODULE_0___default()));
// Module
___CSS_LOADER_EXPORT___.push([module.id, "._3_w-2 {\n  display: flex;\n  align-items: center;\n  cursor: pointer;\n}\n\n._Q3vXB {\n  font-size: 12px;\n}\n\n._3NXmW {\n  height: 9px;\n  padding: 0 6px;\n}\n\n._3NXmW:hover ._EDj68 {\n  opacity: 1;\n}\n\n._2MMPr ._EDj68 {\n  opacity: 1;\n}\n\n._HmPZp {\n  position: absolute;\n  left: 0;\n  top: 0;\n  height: 100%;\n}\n\n._2ylVe {\n  position: relative;\n  z-index: 1;\n  width: 100%;\n  height: 100%;\n  padding: 3px 0;\n  border-radius: 2px;\n}\n\n._Y_T8c {\n  width: 100%;\n  height: 100%;\n  background-color: #ffffff66;\n}\n\n._172X9 {\n  background-color: white;\n}\n\n._HmPZp {\n  padding: 3px 0;\n}\n\n._29Vqs {\n  height: 100%;\n  border-radius: 2px;\n  background-color: #ffffff80;\n}\n\n._EDj68 {\n  width: 12px;\n  height: 12px;\n  border-radius: 50%;\n  background-color: white;\n  cursor: pointer;\n  opacity: 0;\n  transition: opacity 250ms ease-in-out;\n}\n\n._2M3Uj {\n  cursor: pointer;\n}\n\n._3Q6k7 {\n  transition: transform 150ms ease-in;\n}\n\n._3vkak {\n  transform: rotate(45deg);\n}\n\n._2g7Ug ._39UP- {\n  transform: translate(-50%, 50%);\n}\n\n._2g7Ug ._1eoEJ {\n  transform: translate(50%, -50%);\n}\n\n._2_cmi {\n  display: flex;\n  cursor: pointer;\n}\n\nhtml[data-fullscreen-hide-cursor] {\n  cursor: none;\n}\n\n._3auwb {\n  display: none;\n}\n\n._7mHcq {\n  position: relative;\n  padding-left: 4px;\n}\n\n._1Cegy ._Sk0aX,\n._7mHcq:hover ._Sk0aX {\n  opacity: 1;\n  visibility: visible;\n}\n\n._1Cegy ._1inAH,\n._7mHcq:hover ._1inAH {\n  box-shadow: 0 0 2px rgba(0, 0, 0, 0.1);\n}\n\n._4QmIq {\n  display: flex;\n  position: relative;\n  z-index: 1;\n  cursor: pointer;\n}\n\n._Sk0aX {\n  position: absolute;\n  left: 50%;\n  top: 50%;\n  height: 18px;\n  width: 100px;\n  padding-left: 15px;\n  padding-bottom: 5px;\n  padding-top: 5px;\n  opacity: 0;\n  visibility: hidden;\n  transform: translate(-50%, -50%) rotate(-90deg) translateX(50%);\n  transition: all 250ms ease-in-out;\n}\n\n._38Plw {\n  width: 100%;\n  height: 100%;\n  cursor: pointer;\n}\n\n._BpprM {\n  width: 100%;\n  height: 100%;\n  border-radius: 15px;\n  background-color: currentColor;\n}\n\n._3HzbK {\n  background-color: #007bff;\n}\n\n._1inAH {\n  width: 12px;\n  height: 12px;\n  border-radius: 50%;\n  background-color: white;\n}\n\n._3M8_9 {\n  padding: 5px;\n  background-image: linear-gradient(rgba(0, 0, 0, 0), rgba(0, 0, 0, 0.6));\n  color: white;\n}\n\n._1kCDJ {\n  display: flex;\n  align-items: center;\n}\n\n._EqT8M {\n  margin-left: 5px;\n  margin-right: 5px;\n}\n\n._1WcoX {\n  flex-grow: 1;\n}\n\n._21MzP {\n  padding: 5px 0;\n}\n\n._2t6HJ {\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  width: 100%;\n  height: 100%;\n}\n\n._3rDTI {\n  max-width: 100%;\n  max-height: 100%;\n  min-width: 50%;\n}\n\n._mlaMQ {\n  position: absolute;\n  left: 0;\n  bottom: 0;\n  width: 100%;\n  opacity: 1;\n  visibility: visible;\n  transition: all 200ms ease-in-out;\n}\n\n._13DoI {\n  opacity: 0;\n  visibility: hidden;\n}\n\n._UGXh9 {\n  position: relative;\n  width: 100%;\n  height: 100%;\n  -webkit-user-select: none;\n  -moz-user-select: none;\n  -ms-user-select: none;\n  user-select: none;\n}\n\n._153Mx {\n  position: absolute;\n  top: 0;\n  left: 0;\n  width: 100%;\n  height: 100%;\n}\n\n._Aavz4 {\n  position: absolute;\n  bottom: 48px;\n  right: 12px;\n  width: 250px;\n  max-height: calc(100% - 96px);\n  border-radius: 10px;\n  background-color: rgba(28, 28, 28, 0.9);\n  overflow-x: hidden;\n  overflow-y: auto;\n  line-height: 16px;\n  font-size: 14px;\n  color: white;\n}\n\n._Aavz4::-webkit-scrollbar {\n  display: none;\n}\n\n._31h-n {\n  display: block;\n}\n\n._2-tit {\n  display: flex;\n  align-items: center;\n}\n\n._qsMuA {\n  margin-left: 8px;\n}\n\n._1MyH9 {\n  display: flex;\n  align-items: center;\n}\n\n._3QR78 {\n  margin-right: 8px;\n}\n\n._2FQaW {\n  display: flex;\n  justify-content: space-between;\n  align-items: center;\n  padding: 10px;\n  cursor: pointer;\n}\n\n._2FQaW:hover {\n  background-color: rgba(255, 255, 255, 0.2);\n}\n\n._2vFYr {\n  display: flex;\n  align-items: center;\n  cursor: pointer;\n}\n\n._3TtMv {\n  margin-right: 8px;\n}\n\n._3YEJD {\n  display: flex;\n  justify-content: space-between;\n  align-items: center;\n  padding: 12px 10px;\n  border-bottom: 1px solid rgba(255, 255, 255, 0.2);\n}\n\n._1cQZi {\n  display: flex;\n  align-items: center;\n  padding: 10px;\n  cursor: pointer;\n}\n\n._1cQZi:hover {\n  background-color: rgba(255, 255, 255, 0.2);\n}\n\n._3Ux6m {\n  margin-right: 5px;\n}\n\n._2IBfB {\n  opacity: 0;\n}\n\n._37L6V {\n  -webkit-text-decoration: 1px underline currentColor;\n  text-decoration: 1px underline currentColor;\n  cursor: pointer;\n}\n\n._tMpvC {\n  padding: 30px 12px;\n}\n\n._1zeKD {\n  height: 6px;\n}\n\n._aDhmN {\n  margin-top: 12px;\n  text-align: center;\n  font-size: 16px;\n}\n\n._3s4Gu {\n  width: 100%;\n  height: 100%;\n  border-radius: 5px;\n  background-color: white;\n}\n\n._2Dra7 {\n  width: 16px;\n  height: 16px;\n  border-radius: 50px;\n  background-color: white;\n}\n\n._3NIih {\n  font-size: 12px;\n}\n\n._1-KVX {\n  padding: 5px;\n  background-image: linear-gradient(rgba(0, 0, 0, 0), rgba(0, 0, 0, 0.6));\n  color: white;\n}\n\n._3BMDB ._29fpJ {\n  background-color: #dc3545;\n}\n\n._3BMDB ._34akd {\n  background-color: #dc3545;\n}\n\n._1oxBS {\n  display: flex;\n  align-items: center;\n}\n\n._a7jbE {\n  margin-left: 5px;\n  margin-right: 5px;\n}\n\n._29fpJ {\n  display: flex;\n  padding-left: 5px;\n  padding-right: 5px;\n  border-radius: 4px;\n  background-color: white;\n  color: #050505;\n  line-height: 20px;\n  font-size: 12px;\n  font-weight: 500;\n  cursor: pointer;\n}\n\n._280C3 {\n  flex-grow: 1;\n}\n\n._1SaML ._1-KVX {\n  padding: 5px 0;\n}\n\n._1SaML ._29fpJ {\n  line-height: 16px;\n}\n\n._1_n5C {\n  display: flex;\n  align-items: center;\n  line-height: 12px;\n  font-size: 13px;\n  color: white;\n  font-weight: 500;\n}\n\n._ko_Qh {\n  padding: 6px;\n  border-radius: 4px;\n  background-color: #ccc;\n}\n\n._39gHO {\n  background-color: red;\n}\n\n._2Mtwc {\n  display: flex;\n  justify-content: center;\n  align-items: center;\n  margin-left: 8px;\n  padding: 6px;\n  border-radius: 4px;\n  background-color: rgba(0, 0, 0, 0.5);\n}\n\n._3mB0i {\n  margin-left: 5px;\n}\n\n._1A6mG {\n  position: relative;\n  width: 100%;\n  height: 100%;\n}\n\n._1A6mG ._3d0TT {\n  stroke: currentColor;\n}\n\n._1A6mG ._31AXa {\n  fill: currentColor;\n}\n\n._Vtb9_ {\n  position: absolute;\n  top: 0;\n  left: 0;\n  width: 100%;\n  height: 100%;\n}\n\n._11mcL {\n  position: absolute;\n  left: 0;\n  top: 0;\n  padding: 12px;\n}\n\n._7xCaU._3Edu5 ._11mcL,\n._7xCaU._3Edu5 ._1sTiA {\n  opacity: 0;\n  visibility: hidden;\n  transition: all 250ms ease-in-out;\n}\n\n._7xCaU._1A6mG:hover ._11mcL,\n._7xCaU._1A6mG:hover ._1sTiA {\n  opacity: 1;\n  visibility: visible;\n}\n\n._7xCaU ._11mcL {\n  padding: 8px;\n}", "",{"version":3,"sources":["webpack://./node_modules/react-video-ts/dist/index.css"],"names":[],"mappings":"AAAA;EACE,aAAA;EACA,mBAAA;EACA,eAAA;AACF;;AACA;EACE,eAAA;AAEF;;AAAA;EACE,WAAA;EACA,cAAA;AAGF;;AAFE;EACE,UAAA;AAKJ;;AAHA;EACE,UAAA;AAMF;;AAJA;EACE,kBAAA;EACA,OAAA;EACA,MAAA;EACA,YAAA;AAOF;;AALA;EACE,kBAAA;EACA,UAAA;EACA,WAAA;EACA,YAAA;EACA,cAAA;EACA,kBAAA;AAQF;;AANA;EACE,WAAA;EACA,YAAA;EACA,2BAAA;AASF;;AAPA;EACE,uBAAA;AAUF;;AARA;EACE,cAAA;AAWF;;AATA;EACE,YAAA;EACA,kBAAA;EACA,2BAAA;AAYF;;AAVA;EACE,WAAA;EACA,YAAA;EACA,kBAAA;EACA,uBAAA;EACA,eAAA;EACA,UAAA;EACA,qCAAA;AAaF;;AAXA;EACE,eAAA;AAcF;;AAZA;EACE,mCAAA;AAeF;;AAbA;EACE,wBAAA;AAgBF;;AAdA;EACE,+BAAA;AAiBF;;AAfA;EACE,+BAAA;AAkBF;;AAhBA;EACE,aAAA;EACA,eAAA;AAmBF;;AAjBA;EACE,YAAA;AAoBF;;AAjBA;EACE,aAAA;AAoBF;;AAjBA;EACE,kBAAA;EACA,iBAAA;AAoBF;;AAlBA;;EAEE,UAAA;EACA,mBAAA;AAqBF;;AAnBA;;EAEE,sCAAA;AAsBF;;AApBA;EACE,aAAA;EACA,kBAAA;EACA,UAAA;EACA,eAAA;AAuBF;;AArBA;EACE,kBAAA;EACA,SAAA;EACA,QAAA;EACA,YAAA;EACA,YAAA;EACA,kBAAA;EACA,mBAAA;EACA,gBAAA;EACA,UAAA;EACA,kBAAA;EACA,+DAAA;EACA,iCAAA;AAwBF;;AAtBA;EACE,WAAA;EACA,YAAA;EACA,eAAA;AAyBF;;AAvBA;EACE,WAAA;EACA,YAAA;EACA,mBAAA;EACA,8BAAA;AA0BF;;AAxBA;EACE,yBAAA;AA2BF;;AAzBA;EACE,WAAA;EACA,YAAA;EACA,kBAAA;EACA,uBAAA;AA4BF;;AA1BA;EACE,YAAA;EACA,uEAAA;EACA,YAAA;AA6BF;;AA3BA;EACE,aAAA;EACA,mBAAA;AA8BF;;AA5BA;EACE,gBAAA;EACA,iBAAA;AA+BF;;AA7BA;EACE,YAAA;AAgCF;;AA9BA;EACE,cAAA;AAiCF;;AA/BA;EACE,aAAA;EACA,mBAAA;EACA,uBAAA;EACA,WAAA;EACA,YAAA;AAkCF;;AAhCA;EACE,eAAA;EACA,gBAAA;EACA,cAAA;AAmCF;;AAjCA;EACE,kBAAA;EACA,OAAA;EACA,SAAA;EACA,WAAA;EACA,UAAA;EACA,mBAAA;EACA,iCAAA;AAoCF;;AAlCA;EACE,UAAA;EACA,kBAAA;AAqCF;;AAnCA;EACE,kBAAA;EACA,WAAA;EACA,YAAA;EACA,yBAAA;EACG,sBAAA;EACC,qBAAA;EACI,iBAAA;AAsCV;;AApCA;EACE,kBAAA;EACA,MAAA;EACA,OAAA;EACA,WAAA;EACA,YAAA;AAuCF;;AArCA;EACE,kBAAA;EACA,YAAA;EACA,WAAA;EACA,YAAA;EACA,6BAAA;EACA,mBAAA;EACA,uCAAA;EACA,kBAAA;EACA,gBAAA;EACA,iBAAA;EACA,eAAA;EACA,YAAA;AAwCF;;AAvCE;EACE,aAAA;AA0CJ;;AAxCA;EACE,cAAA;AA2CF;;AAzCA;EACE,aAAA;EACA,mBAAA;AA4CF;;AA1CA;EACE,gBAAA;AA6CF;;AA3CA;EACE,aAAA;EACA,mBAAA;AA8CF;;AA5CA;EACE,iBAAA;AA+CF;;AA7CA;EACE,aAAA;EACA,8BAAA;EACA,mBAAA;EACA,aAAA;EACA,eAAA;AAgDF;;AA/CE;EACE,0CAAA;AAkDJ;;AA/CA;EACE,aAAA;EACA,mBAAA;EACA,eAAA;AAkDF;;AAhDA;EACE,iBAAA;AAmDF;;AAjDA;EACE,aAAA;EACA,8BAAA;EACA,mBAAA;EACA,kBAAA;EACA,iDAAA;AAoDF;;AAlDA;EACE,aAAA;EACA,mBAAA;EACA,aAAA;EACA,eAAA;AAqDF;;AApDE;EACE,0CAAA;AAuDJ;;AArDA;EACE,iBAAA;AAwDF;;AAtDA;EACE,UAAA;AAyDF;;AAvDA;EACE,mDAAA;EACQ,2CAAA;EACR,eAAA;AA0DF;;AAxDA;EACE,kBAAA;AA2DF;;AAzDA;EACE,WAAA;AA4DF;;AA1DA;EACE,gBAAA;EACA,kBAAA;EACA,eAAA;AA6DF;;AA3DA;EACE,WAAA;EACA,YAAA;EACA,kBAAA;EACA,uBAAA;AA8DF;;AA5DA;EACE,WAAA;EACA,YAAA;EACA,mBAAA;EACA,uBAAA;AA+DF;;AA7DA;EACE,eAAA;AAgEF;;AA9DA;EACE,YAAA;EACA,uEAAA;EACA,YAAA;AAiEF;;AA/DA;EACE,yBAAA;AAkEF;;AAhEA;EACE,yBAAA;AAmEF;;AAjEA;EACE,aAAA;EACA,mBAAA;AAoEF;;AAlEA;EACE,gBAAA;EACA,iBAAA;AAqEF;;AAnEA;EACE,aAAA;EACA,iBAAA;EACA,kBAAA;EACA,kBAAA;EACA,uBAAA;EACA,cAAA;EACA,iBAAA;EACA,eAAA;EACA,gBAAA;EACA,eAAA;AAsEF;;AApEA;EACE,YAAA;AAuEF;;AArEA;EACE,cAAA;AAwEF;;AAtEA;EACE,iBAAA;AAyEF;;AAvEA;EACE,aAAA;EACA,mBAAA;EACA,iBAAA;EACA,eAAA;EACA,YAAA;EACA,gBAAA;AA0EF;;AAxEA;EACE,YAAA;EACA,kBAAA;EACA,sBAAA;AA2EF;;AAzEA;EACE,qBAAA;AA4EF;;AA1EA;EACE,aAAA;EACA,uBAAA;EACA,mBAAA;EACA,gBAAA;EACA,YAAA;EACA,kBAAA;EACA,oCAAA;AA6EF;;AA3EA;EACE,gBAAA;AA8EF;;AA5EA;EACE,kBAAA;EACA,WAAA;EACA,YAAA;AA+EF;;AA9EE;EACE,oBAAA;AAiFJ;;AAhFE;EACE,kBAAA;AAmFJ;;AAjFA;EACE,kBAAA;EACA,MAAA;EACA,OAAA;EACA,WAAA;EACA,YAAA;AAoFF;;AAlFA;EACE,kBAAA;EACA,OAAA;EACA,MAAA;EACA,aAAA;AAqFF;;AAnFA;;EAEE,UAAA;EACA,kBAAA;EACA,iCAAA;AAsFF;;AApFA;;EAEE,UAAA;EACA,mBAAA;AAuFF;;AArFA;EACE,YAAA;AAwFF","sourcesContent":["._3_w-2 {\n  display: flex;\n  align-items: center;\n  cursor: pointer; }\n\n._Q3vXB {\n  font-size: 12px; }\n\n._3NXmW {\n  height: 9px;\n  padding: 0 6px; }\n  ._3NXmW:hover ._EDj68 {\n    opacity: 1; }\n\n._2MMPr ._EDj68 {\n  opacity: 1; }\n\n._HmPZp {\n  position: absolute;\n  left: 0;\n  top: 0;\n  height: 100%; }\n\n._2ylVe {\n  position: relative;\n  z-index: 1;\n  width: 100%;\n  height: 100%;\n  padding: 3px 0;\n  border-radius: 2px; }\n\n._Y_T8c {\n  width: 100%;\n  height: 100%;\n  background-color: #ffffff66; }\n\n._172X9 {\n  background-color: white; }\n\n._HmPZp {\n  padding: 3px 0; }\n\n._29Vqs {\n  height: 100%;\n  border-radius: 2px;\n  background-color: #ffffff80; }\n\n._EDj68 {\n  width: 12px;\n  height: 12px;\n  border-radius: 50%;\n  background-color: white;\n  cursor: pointer;\n  opacity: 0;\n  transition: opacity 250ms ease-in-out; }\n\n._2M3Uj {\n  cursor: pointer; }\n\n._3Q6k7 {\n  transition: transform 150ms ease-in; }\n\n._3vkak {\n  transform: rotate(45deg); }\n\n._2g7Ug ._39UP- {\n  transform: translate(-50%, 50%); }\n\n._2g7Ug ._1eoEJ {\n  transform: translate(50%, -50%); }\n\n._2_cmi {\n  display: flex;\n  cursor: pointer; }\n\nhtml[data-fullscreen-hide-cursor] {\r\n  cursor: none;\r\n}\r\n\r\n._3auwb {\r\n  display: none;\r\n}\r\n\n._7mHcq {\n  position: relative;\n  padding-left: 4px; }\n\n._1Cegy ._Sk0aX,\n._7mHcq:hover ._Sk0aX {\n  opacity: 1;\n  visibility: visible; }\n\n._1Cegy ._1inAH,\n._7mHcq:hover ._1inAH {\n  box-shadow: 0 0 2px rgba(0, 0, 0, 0.1); }\n\n._4QmIq {\n  display: flex;\n  position: relative;\n  z-index: 1;\n  cursor: pointer; }\n\n._Sk0aX {\n  position: absolute;\n  left: 50%;\n  top: 50%;\n  height: 18px;\n  width: 100px;\n  padding-left: 15px;\n  padding-bottom: 5px;\n  padding-top: 5px;\n  opacity: 0;\n  visibility: hidden;\n  transform: translate(-50%, -50%) rotate(-90deg) translateX(50%);\n  transition: all 250ms ease-in-out; }\n\n._38Plw {\n  width: 100%;\n  height: 100%;\n  cursor: pointer; }\n\n._BpprM {\n  width: 100%;\n  height: 100%;\n  border-radius: 15px;\n  background-color: currentColor; }\n\n._3HzbK {\n  background-color: #007bff; }\n\n._1inAH {\n  width: 12px;\n  height: 12px;\n  border-radius: 50%;\n  background-color: white; }\n\n._3M8_9 {\n  padding: 5px;\n  background-image: linear-gradient(rgba(0, 0, 0, 0), rgba(0, 0, 0, 0.6));\n  color: white; }\n\n._1kCDJ {\n  display: flex;\n  align-items: center; }\n\n._EqT8M {\n  margin-left: 5px;\n  margin-right: 5px; }\n\n._1WcoX {\n  flex-grow: 1; }\n\n._21MzP {\n  padding: 5px 0; }\n\n._2t6HJ {\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  width: 100%;\n  height: 100%; }\n\n._3rDTI {\n  max-width: 100%;\n  max-height: 100%;\n  min-width: 50%; }\n\n._mlaMQ {\n  position: absolute;\n  left: 0;\n  bottom: 0;\n  width: 100%;\n  opacity: 1;\n  visibility: visible;\n  transition: all 200ms ease-in-out; }\n\n._13DoI {\n  opacity: 0;\n  visibility: hidden; }\n\n._UGXh9 {\n  position: relative;\n  width: 100%;\n  height: 100%;\n  -webkit-user-select: none;\n     -moz-user-select: none;\n      -ms-user-select: none;\n          user-select: none; }\n\n._153Mx {\n  position: absolute;\n  top: 0;\n  left: 0;\n  width: 100%;\n  height: 100%; }\n\n._Aavz4 {\n  position: absolute;\n  bottom: 48px;\n  right: 12px;\n  width: 250px;\n  max-height: calc(100% - 96px);\n  border-radius: 10px;\n  background-color: rgba(28, 28, 28, 0.9);\n  overflow-x: hidden;\n  overflow-y: auto;\n  line-height: 16px;\n  font-size: 14px;\n  color: white; }\n  ._Aavz4::-webkit-scrollbar {\n    display: none; }\n\n._31h-n {\n  display: block; }\n\n._2-tit {\n  display: flex;\n  align-items: center; }\n\n._qsMuA {\n  margin-left: 8px; }\n\n._1MyH9 {\n  display: flex;\n  align-items: center; }\n\n._3QR78 {\n  margin-right: 8px; }\n\n._2FQaW {\n  display: flex;\n  justify-content: space-between;\n  align-items: center;\n  padding: 10px;\n  cursor: pointer; }\n  ._2FQaW:hover {\n    background-color: rgba(255, 255, 255, 0.2); }\n\n\n._2vFYr {\n  display: flex;\n  align-items: center;\n  cursor: pointer; }\n\n._3TtMv {\n  margin-right: 8px; }\n\n._3YEJD {\n  display: flex;\n  justify-content: space-between;\n  align-items: center;\n  padding: 12px 10px;\n  border-bottom: 1px solid rgba(255, 255, 255, 0.2); }\n\n._1cQZi {\n  display: flex;\n  align-items: center;\n  padding: 10px;\n  cursor: pointer; }\n  ._1cQZi:hover {\n    background-color: rgba(255, 255, 255, 0.2); }\n\n._3Ux6m {\n  margin-right: 5px; }\n\n._2IBfB {\n  opacity: 0; }\n\n._37L6V {\n  -webkit-text-decoration: 1px underline currentColor;\n          text-decoration: 1px underline currentColor;\n  cursor: pointer; }\n\n._tMpvC {\n  padding: 30px 12px; }\n\n._1zeKD {\n  height: 6px; }\n\n._aDhmN {\n  margin-top: 12px;\n  text-align: center;\n  font-size: 16px; }\n\n._3s4Gu {\n  width: 100%;\n  height: 100%;\n  border-radius: 5px;\n  background-color: white; }\n\n._2Dra7 {\n  width: 16px;\n  height: 16px;\n  border-radius: 50px;\n  background-color: white; }\n\n._3NIih {\n  font-size: 12px; }\n\n._1-KVX {\n  padding: 5px;\n  background-image: linear-gradient(rgba(0, 0, 0, 0), rgba(0, 0, 0, 0.6));\n  color: white; }\n\n._3BMDB ._29fpJ {\n  background-color: #dc3545; }\n\n._3BMDB ._34akd {\n  background-color: #dc3545; }\n\n._1oxBS {\n  display: flex;\n  align-items: center; }\n\n._a7jbE {\n  margin-left: 5px;\n  margin-right: 5px; }\n\n._29fpJ {\n  display: flex;\n  padding-left: 5px;\n  padding-right: 5px;\n  border-radius: 4px;\n  background-color: white;\n  color: #050505;\n  line-height: 20px;\n  font-size: 12px;\n  font-weight: 500;\n  cursor: pointer; }\n\n._280C3 {\n  flex-grow: 1; }\n\n._1SaML ._1-KVX {\n  padding: 5px 0; }\n\n._1SaML ._29fpJ {\n  line-height: 16px; }\n\n._1_n5C {\n  display: flex;\n  align-items: center;\n  line-height: 12px;\n  font-size: 13px;\n  color: white;\n  font-weight: 500; }\n\n._ko_Qh {\n  padding: 6px;\n  border-radius: 4px;\n  background-color: #ccc; }\n\n._39gHO {\n  background-color: red; }\n\n._2Mtwc {\n  display: flex;\n  justify-content: center;\n  align-items: center;\n  margin-left: 8px;\n  padding: 6px;\n  border-radius: 4px;\n  background-color: rgba(0, 0, 0, 0.5); }\n\n._3mB0i {\n  margin-left: 5px; }\n\n._1A6mG {\n  position: relative;\n  width: 100%;\n  height: 100%; }\n  ._1A6mG ._3d0TT {\n    stroke: currentColor; }\n  ._1A6mG ._31AXa {\n    fill: currentColor; }\n\n._Vtb9_ {\n  position: absolute;\n  top: 0;\n  left: 0;\n  width: 100%;\n  height: 100%; }\n\n._11mcL {\n  position: absolute;\n  left: 0;\n  top: 0;\n  padding: 12px; }\n\n._7xCaU._3Edu5 ._11mcL,\n._7xCaU._3Edu5 ._1sTiA {\n  opacity: 0;\n  visibility: hidden;\n  transition: all 250ms ease-in-out; }\n\n._7xCaU._1A6mG:hover ._11mcL,\n._7xCaU._1A6mG:hover ._1sTiA {\n  opacity: 1;\n  visibility: visible; }\n\n._7xCaU ._11mcL {\n  padding: 8px; }\n"],"sourceRoot":""}]);
// Exports
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (___CSS_LOADER_EXPORT___);


/***/ }),

/***/ "./node_modules/css-loader/dist/runtime/api.js":
/*!*****************************************************!*\
  !*** ./node_modules/css-loader/dist/runtime/api.js ***!
  \*****************************************************/
/***/ ((module) => {

"use strict";


/*
  MIT License http://www.opensource.org/licenses/mit-license.php
  Author Tobias Koppers @sokra
*/
// css base code, injected by the css-loader
// eslint-disable-next-line func-names
module.exports = function (cssWithMappingToString) {
  var list = []; // return the list of modules as css string

  list.toString = function toString() {
    return this.map(function (item) {
      var content = cssWithMappingToString(item);

      if (item[2]) {
        return "@media ".concat(item[2], " {").concat(content, "}");
      }

      return content;
    }).join("");
  }; // import a list of modules into the list
  // eslint-disable-next-line func-names


  list.i = function (modules, mediaQuery, dedupe) {
    if (typeof modules === "string") {
      // eslint-disable-next-line no-param-reassign
      modules = [[null, modules, ""]];
    }

    var alreadyImportedModules = {};

    if (dedupe) {
      for (var i = 0; i < this.length; i++) {
        // eslint-disable-next-line prefer-destructuring
        var id = this[i][0];

        if (id != null) {
          alreadyImportedModules[id] = true;
        }
      }
    }

    for (var _i = 0; _i < modules.length; _i++) {
      var item = [].concat(modules[_i]);

      if (dedupe && alreadyImportedModules[item[0]]) {
        // eslint-disable-next-line no-continue
        continue;
      }

      if (mediaQuery) {
        if (!item[2]) {
          item[2] = mediaQuery;
        } else {
          item[2] = "".concat(mediaQuery, " and ").concat(item[2]);
        }
      }

      list.push(item);
    }
  };

  return list;
};

/***/ }),

/***/ "./node_modules/css-loader/dist/runtime/cssWithMappingToString.js":
/*!************************************************************************!*\
  !*** ./node_modules/css-loader/dist/runtime/cssWithMappingToString.js ***!
  \************************************************************************/
/***/ ((module) => {

"use strict";


function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _unsupportedIterableToArray(arr, i) || _nonIterableRest(); }

function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }

function _unsupportedIterableToArray(o, minLen) { if (!o) return; if (typeof o === "string") return _arrayLikeToArray(o, minLen); var n = Object.prototype.toString.call(o).slice(8, -1); if (n === "Object" && o.constructor) n = o.constructor.name; if (n === "Map" || n === "Set") return Array.from(o); if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen); }

function _arrayLikeToArray(arr, len) { if (len == null || len > arr.length) len = arr.length; for (var i = 0, arr2 = new Array(len); i < len; i++) { arr2[i] = arr[i]; } return arr2; }

function _iterableToArrayLimit(arr, i) { if (typeof Symbol === "undefined" || !(Symbol.iterator in Object(arr))) return; var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"] != null) _i["return"](); } finally { if (_d) throw _e; } } return _arr; }

function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }

module.exports = function cssWithMappingToString(item) {
  var _item = _slicedToArray(item, 4),
      content = _item[1],
      cssMapping = _item[3];

  if (typeof btoa === "function") {
    // eslint-disable-next-line no-undef
    var base64 = btoa(unescape(encodeURIComponent(JSON.stringify(cssMapping))));
    var data = "sourceMappingURL=data:application/json;charset=utf-8;base64,".concat(base64);
    var sourceMapping = "/*# ".concat(data, " */");
    var sourceURLs = cssMapping.sources.map(function (source) {
      return "/*# sourceURL=".concat(cssMapping.sourceRoot || "").concat(source, " */");
    });
    return [content].concat(sourceURLs).concat([sourceMapping]).join("\n");
  }

  return [content].join("\n");
};

/***/ }),

/***/ "./node_modules/filter-obj/index.js":
/*!******************************************!*\
  !*** ./node_modules/filter-obj/index.js ***!
  \******************************************/
/***/ ((module) => {

"use strict";

module.exports = function (obj, predicate) {
	var ret = {};
	var keys = Object.keys(obj);
	var isArr = Array.isArray(predicate);

	for (var i = 0; i < keys.length; i++) {
		var key = keys[i];
		var val = obj[key];

		if (isArr ? predicate.indexOf(key) !== -1 : predicate(key, val, obj)) {
			ret[key] = val;
		}
	}

	return ret;
};


/***/ }),

/***/ "./node_modules/immer/dist/immer.esm.js":
/*!**********************************************!*\
  !*** ./node_modules/immer/dist/immer.esm.js ***!
  \**********************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__),
/* harmony export */   "Immer": () => (/* binding */ un),
/* harmony export */   "applyPatches": () => (/* binding */ pn),
/* harmony export */   "castDraft": () => (/* binding */ K),
/* harmony export */   "castImmutable": () => (/* binding */ $),
/* harmony export */   "createDraft": () => (/* binding */ ln),
/* harmony export */   "current": () => (/* binding */ D),
/* harmony export */   "enableAllPlugins": () => (/* binding */ J),
/* harmony export */   "enableES5": () => (/* binding */ N),
/* harmony export */   "enableMapSet": () => (/* binding */ C),
/* harmony export */   "enablePatches": () => (/* binding */ T),
/* harmony export */   "finishDraft": () => (/* binding */ dn),
/* harmony export */   "freeze": () => (/* binding */ d),
/* harmony export */   "immerable": () => (/* binding */ L),
/* harmony export */   "isDraft": () => (/* binding */ t),
/* harmony export */   "isDraftable": () => (/* binding */ r),
/* harmony export */   "nothing": () => (/* binding */ H),
/* harmony export */   "original": () => (/* binding */ e),
/* harmony export */   "produce": () => (/* binding */ fn),
/* harmony export */   "produceWithPatches": () => (/* binding */ cn),
/* harmony export */   "setAutoFreeze": () => (/* binding */ sn),
/* harmony export */   "setUseProxies": () => (/* binding */ vn)
/* harmony export */ });
function n(n){for(var t=arguments.length,r=Array(t>1?t-1:0),e=1;e<t;e++)r[e-1]=arguments[e];if(true){var i=Y[n],o=i?"function"==typeof i?i.apply(null,r):i:"unknown error nr: "+n;throw Error("[Immer] "+o)}throw Error("[Immer] minified error nr: "+n+(r.length?" "+r.map((function(n){return"'"+n+"'"})).join(","):"")+". Find the full error at: https://bit.ly/3cXEKWf")}function t(n){return!!n&&!!n[Q]}function r(n){return!!n&&(function(n){if(!n||"object"!=typeof n)return!1;var t=Object.getPrototypeOf(n);if(null===t)return!0;var r=Object.hasOwnProperty.call(t,"constructor")&&t.constructor;return r===Object||"function"==typeof r&&Function.toString.call(r)===Z}(n)||Array.isArray(n)||!!n[L]||!!n.constructor[L]||s(n)||v(n))}function e(r){return t(r)||n(23,r),r[Q].t}function i(n,t,r){void 0===r&&(r=!1),0===o(n)?(r?Object.keys:nn)(n).forEach((function(e){r&&"symbol"==typeof e||t(e,n[e],n)})):n.forEach((function(r,e){return t(e,r,n)}))}function o(n){var t=n[Q];return t?t.i>3?t.i-4:t.i:Array.isArray(n)?1:s(n)?2:v(n)?3:0}function u(n,t){return 2===o(n)?n.has(t):Object.prototype.hasOwnProperty.call(n,t)}function a(n,t){return 2===o(n)?n.get(t):n[t]}function f(n,t,r){var e=o(n);2===e?n.set(t,r):3===e?(n.delete(t),n.add(r)):n[t]=r}function c(n,t){return n===t?0!==n||1/n==1/t:n!=n&&t!=t}function s(n){return X&&n instanceof Map}function v(n){return q&&n instanceof Set}function p(n){return n.o||n.t}function l(n){if(Array.isArray(n))return Array.prototype.slice.call(n);var t=tn(n);delete t[Q];for(var r=nn(t),e=0;e<r.length;e++){var i=r[e],o=t[i];!1===o.writable&&(o.writable=!0,o.configurable=!0),(o.get||o.set)&&(t[i]={configurable:!0,writable:!0,enumerable:o.enumerable,value:n[i]})}return Object.create(Object.getPrototypeOf(n),t)}function d(n,e){return void 0===e&&(e=!1),y(n)||t(n)||!r(n)?n:(o(n)>1&&(n.set=n.add=n.clear=n.delete=h),Object.freeze(n),e&&i(n,(function(n,t){return d(t,!0)}),!0),n)}function h(){n(2)}function y(n){return null==n||"object"!=typeof n||Object.isFrozen(n)}function b(t){var r=rn[t];return r||n(18,t),r}function m(n,t){rn[n]||(rn[n]=t)}function _(){return false||U||n(0),U}function j(n,t){t&&(b("Patches"),n.u=[],n.s=[],n.v=t)}function O(n){g(n),n.p.forEach(S),n.p=null}function g(n){n===U&&(U=n.l)}function w(n){return U={p:[],l:U,h:n,m:!0,_:0}}function S(n){var t=n[Q];0===t.i||1===t.i?t.j():t.O=!0}function P(t,e){e._=e.p.length;var i=e.p[0],o=void 0!==t&&t!==i;return e.h.g||b("ES5").S(e,t,o),o?(i[Q].P&&(O(e),n(4)),r(t)&&(t=M(e,t),e.l||x(e,t)),e.u&&b("Patches").M(i[Q],t,e.u,e.s)):t=M(e,i,[]),O(e),e.u&&e.v(e.u,e.s),t!==H?t:void 0}function M(n,t,r){if(y(t))return t;var e=t[Q];if(!e)return i(t,(function(i,o){return A(n,e,t,i,o,r)}),!0),t;if(e.A!==n)return t;if(!e.P)return x(n,e.t,!0),e.t;if(!e.I){e.I=!0,e.A._--;var o=4===e.i||5===e.i?e.o=l(e.k):e.o;i(3===e.i?new Set(o):o,(function(t,i){return A(n,e,o,t,i,r)})),x(n,o,!1),r&&n.u&&b("Patches").R(e,r,n.u,n.s)}return e.o}function A(e,i,o,a,c,s){if( true&&c===o&&n(5),t(c)){var v=M(e,c,s&&i&&3!==i.i&&!u(i.D,a)?s.concat(a):void 0);if(f(o,a,v),!t(v))return;e.m=!1}if(r(c)&&!y(c)){if(!e.h.F&&e._<1)return;M(e,c),i&&i.A.l||x(e,c)}}function x(n,t,r){void 0===r&&(r=!1),n.h.F&&n.m&&d(t,r)}function z(n,t){var r=n[Q];return(r?p(r):n)[t]}function I(n,t){if(t in n)for(var r=Object.getPrototypeOf(n);r;){var e=Object.getOwnPropertyDescriptor(r,t);if(e)return e;r=Object.getPrototypeOf(r)}}function k(n){n.P||(n.P=!0,n.l&&k(n.l))}function E(n){n.o||(n.o=l(n.t))}function R(n,t,r){var e=s(t)?b("MapSet").N(t,r):v(t)?b("MapSet").T(t,r):n.g?function(n,t){var r=Array.isArray(n),e={i:r?1:0,A:t?t.A:_(),P:!1,I:!1,D:{},l:t,t:n,k:null,o:null,j:null,C:!1},i=e,o=en;r&&(i=[e],o=on);var u=Proxy.revocable(i,o),a=u.revoke,f=u.proxy;return e.k=f,e.j=a,f}(t,r):b("ES5").J(t,r);return(r?r.A:_()).p.push(e),e}function D(e){return t(e)||n(22,e),function n(t){if(!r(t))return t;var e,u=t[Q],c=o(t);if(u){if(!u.P&&(u.i<4||!b("ES5").K(u)))return u.t;u.I=!0,e=F(t,c),u.I=!1}else e=F(t,c);return i(e,(function(t,r){u&&a(u.t,t)===r||f(e,t,n(r))})),3===c?new Set(e):e}(e)}function F(n,t){switch(t){case 2:return new Map(n);case 3:return Array.from(n)}return l(n)}function N(){function r(n,t){var r=s[n];return r?r.enumerable=t:s[n]=r={configurable:!0,enumerable:t,get:function(){var t=this[Q];return true&&f(t),en.get(t,n)},set:function(t){var r=this[Q]; true&&f(r),en.set(r,n,t)}},r}function e(n){for(var t=n.length-1;t>=0;t--){var r=n[t][Q];if(!r.P)switch(r.i){case 5:a(r)&&k(r);break;case 4:o(r)&&k(r)}}}function o(n){for(var t=n.t,r=n.k,e=nn(r),i=e.length-1;i>=0;i--){var o=e[i];if(o!==Q){var a=t[o];if(void 0===a&&!u(t,o))return!0;var f=r[o],s=f&&f[Q];if(s?s.t!==a:!c(f,a))return!0}}var v=!!t[Q];return e.length!==nn(t).length+(v?0:1)}function a(n){var t=n.k;if(t.length!==n.t.length)return!0;var r=Object.getOwnPropertyDescriptor(t,t.length-1);return!(!r||r.get)}function f(t){t.O&&n(3,JSON.stringify(p(t)))}var s={};m("ES5",{J:function(n,t){var e=Array.isArray(n),i=function(n,t){if(n){for(var e=Array(t.length),i=0;i<t.length;i++)Object.defineProperty(e,""+i,r(i,!0));return e}var o=tn(t);delete o[Q];for(var u=nn(o),a=0;a<u.length;a++){var f=u[a];o[f]=r(f,n||!!o[f].enumerable)}return Object.create(Object.getPrototypeOf(t),o)}(e,n),o={i:e?5:4,A:t?t.A:_(),P:!1,I:!1,D:{},l:t,t:n,k:i,o:null,O:!1,C:!1};return Object.defineProperty(i,Q,{value:o,writable:!0}),i},S:function(n,r,o){o?t(r)&&r[Q].A===n&&e(n.p):(n.u&&function n(t){if(t&&"object"==typeof t){var r=t[Q];if(r){var e=r.t,o=r.k,f=r.D,c=r.i;if(4===c)i(o,(function(t){t!==Q&&(void 0!==e[t]||u(e,t)?f[t]||n(o[t]):(f[t]=!0,k(r)))})),i(e,(function(n){void 0!==o[n]||u(o,n)||(f[n]=!1,k(r))}));else if(5===c){if(a(r)&&(k(r),f.length=!0),o.length<e.length)for(var s=o.length;s<e.length;s++)f[s]=!1;else for(var v=e.length;v<o.length;v++)f[v]=!0;for(var p=Math.min(o.length,e.length),l=0;l<p;l++)void 0===f[l]&&n(o[l])}}}}(n.p[0]),e(n.p))},K:function(n){return 4===n.i?o(n):a(n)}})}function T(){function e(n){if(!r(n))return n;if(Array.isArray(n))return n.map(e);if(s(n))return new Map(Array.from(n.entries()).map((function(n){return[n[0],e(n[1])]})));if(v(n))return new Set(Array.from(n).map(e));var t=Object.create(Object.getPrototypeOf(n));for(var i in n)t[i]=e(n[i]);return u(n,L)&&(t[L]=n[L]),t}function f(n){return t(n)?e(n):n}var c="add";m("Patches",{$:function(t,r){return r.forEach((function(r){for(var i=r.path,u=r.op,f=t,s=0;s<i.length-1;s++){var v=o(f),p=""+i[s];0!==v&&1!==v||"__proto__"!==p&&"constructor"!==p||n(24),"function"==typeof f&&"prototype"===p&&n(24),"object"!=typeof(f=a(f,p))&&n(15,i.join("/"))}var l=o(f),d=e(r.value),h=i[i.length-1];switch(u){case"replace":switch(l){case 2:return f.set(h,d);case 3:n(16);default:return f[h]=d}case c:switch(l){case 1:return f.splice(h,0,d);case 2:return f.set(h,d);case 3:return f.add(d);default:return f[h]=d}case"remove":switch(l){case 1:return f.splice(h,1);case 2:return f.delete(h);case 3:return f.delete(r.value);default:return delete f[h]}default:n(17,u)}})),t},R:function(n,t,r,e){switch(n.i){case 0:case 4:case 2:return function(n,t,r,e){var o=n.t,s=n.o;i(n.D,(function(n,i){var v=a(o,n),p=a(s,n),l=i?u(o,n)?"replace":c:"remove";if(v!==p||"replace"!==l){var d=t.concat(n);r.push("remove"===l?{op:l,path:d}:{op:l,path:d,value:p}),e.push(l===c?{op:"remove",path:d}:"remove"===l?{op:c,path:d,value:f(v)}:{op:"replace",path:d,value:f(v)})}}))}(n,t,r,e);case 5:case 1:return function(n,t,r,e){var i=n.t,o=n.D,u=n.o;if(u.length<i.length){var a=[u,i];i=a[0],u=a[1];var s=[e,r];r=s[0],e=s[1]}for(var v=0;v<i.length;v++)if(o[v]&&u[v]!==i[v]){var p=t.concat([v]);r.push({op:"replace",path:p,value:f(u[v])}),e.push({op:"replace",path:p,value:f(i[v])})}for(var l=i.length;l<u.length;l++){var d=t.concat([l]);r.push({op:c,path:d,value:f(u[l])})}i.length<u.length&&e.push({op:"replace",path:t.concat(["length"]),value:i.length})}(n,t,r,e);case 3:return function(n,t,r,e){var i=n.t,o=n.o,u=0;i.forEach((function(n){if(!o.has(n)){var i=t.concat([u]);r.push({op:"remove",path:i,value:n}),e.unshift({op:c,path:i,value:n})}u++})),u=0,o.forEach((function(n){if(!i.has(n)){var o=t.concat([u]);r.push({op:c,path:o,value:n}),e.unshift({op:"remove",path:o,value:n})}u++}))}(n,t,r,e)}},M:function(n,t,r,e){r.push({op:"replace",path:[],value:t===H?void 0:t}),e.push({op:"replace",path:[],value:n.t})}})}function C(){function t(n,t){function r(){this.constructor=n}a(n,t),n.prototype=(r.prototype=t.prototype,new r)}function e(n){n.o||(n.D=new Map,n.o=new Map(n.t))}function o(n){n.o||(n.o=new Set,n.t.forEach((function(t){if(r(t)){var e=R(n.A.h,t,n);n.p.set(t,e),n.o.add(e)}else n.o.add(t)})))}function u(t){t.O&&n(3,JSON.stringify(p(t)))}var a=function(n,t){return(a=Object.setPrototypeOf||{__proto__:[]}instanceof Array&&function(n,t){n.__proto__=t}||function(n,t){for(var r in t)t.hasOwnProperty(r)&&(n[r]=t[r])})(n,t)},f=function(){function n(n,t){return this[Q]={i:2,l:t,A:t?t.A:_(),P:!1,I:!1,o:void 0,D:void 0,t:n,k:this,C:!1,O:!1},this}t(n,Map);var o=n.prototype;return Object.defineProperty(o,"size",{get:function(){return p(this[Q]).size}}),o.has=function(n){return p(this[Q]).has(n)},o.set=function(n,t){var r=this[Q];return u(r),p(r).has(n)&&p(r).get(n)===t||(e(r),k(r),r.D.set(n,!0),r.o.set(n,t),r.D.set(n,!0)),this},o.delete=function(n){if(!this.has(n))return!1;var t=this[Q];return u(t),e(t),k(t),t.D.set(n,!1),t.o.delete(n),!0},o.clear=function(){var n=this[Q];u(n),p(n).size&&(e(n),k(n),n.D=new Map,i(n.t,(function(t){n.D.set(t,!1)})),n.o.clear())},o.forEach=function(n,t){var r=this;p(this[Q]).forEach((function(e,i){n.call(t,r.get(i),i,r)}))},o.get=function(n){var t=this[Q];u(t);var i=p(t).get(n);if(t.I||!r(i))return i;if(i!==t.t.get(n))return i;var o=R(t.A.h,i,t);return e(t),t.o.set(n,o),o},o.keys=function(){return p(this[Q]).keys()},o.values=function(){var n,t=this,r=this.keys();return(n={})[V]=function(){return t.values()},n.next=function(){var n=r.next();return n.done?n:{done:!1,value:t.get(n.value)}},n},o.entries=function(){var n,t=this,r=this.keys();return(n={})[V]=function(){return t.entries()},n.next=function(){var n=r.next();if(n.done)return n;var e=t.get(n.value);return{done:!1,value:[n.value,e]}},n},o[V]=function(){return this.entries()},n}(),c=function(){function n(n,t){return this[Q]={i:3,l:t,A:t?t.A:_(),P:!1,I:!1,o:void 0,t:n,k:this,p:new Map,O:!1,C:!1},this}t(n,Set);var r=n.prototype;return Object.defineProperty(r,"size",{get:function(){return p(this[Q]).size}}),r.has=function(n){var t=this[Q];return u(t),t.o?!!t.o.has(n)||!(!t.p.has(n)||!t.o.has(t.p.get(n))):t.t.has(n)},r.add=function(n){var t=this[Q];return u(t),this.has(n)||(o(t),k(t),t.o.add(n)),this},r.delete=function(n){if(!this.has(n))return!1;var t=this[Q];return u(t),o(t),k(t),t.o.delete(n)||!!t.p.has(n)&&t.o.delete(t.p.get(n))},r.clear=function(){var n=this[Q];u(n),p(n).size&&(o(n),k(n),n.o.clear())},r.values=function(){var n=this[Q];return u(n),o(n),n.o.values()},r.entries=function(){var n=this[Q];return u(n),o(n),n.o.entries()},r.keys=function(){return this.values()},r[V]=function(){return this.values()},r.forEach=function(n,t){for(var r=this.values(),e=r.next();!e.done;)n.call(t,e.value,e.value,this),e=r.next()},n}();m("MapSet",{N:function(n,t){return new f(n,t)},T:function(n,t){return new c(n,t)}})}function J(){N(),C(),T()}function K(n){return n}function $(n){return n}var G,U,W="undefined"!=typeof Symbol&&"symbol"==typeof Symbol("x"),X="undefined"!=typeof Map,q="undefined"!=typeof Set,B="undefined"!=typeof Proxy&&void 0!==Proxy.revocable&&"undefined"!=typeof Reflect,H=W?Symbol.for("immer-nothing"):((G={})["immer-nothing"]=!0,G),L=W?Symbol.for("immer-draftable"):"__$immer_draftable",Q=W?Symbol.for("immer-state"):"__$immer_state",V="undefined"!=typeof Symbol&&Symbol.iterator||"@@iterator",Y={0:"Illegal state",1:"Immer drafts cannot have computed properties",2:"This object has been frozen and should not be mutated",3:function(n){return"Cannot use a proxy that has been revoked. Did you pass an object from inside an immer function to an async process? "+n},4:"An immer producer returned a new value *and* modified its draft. Either return a new value *or* modify the draft.",5:"Immer forbids circular references",6:"The first or second argument to `produce` must be a function",7:"The third argument to `produce` must be a function or undefined",8:"First argument to `createDraft` must be a plain object, an array, or an immerable object",9:"First argument to `finishDraft` must be a draft returned by `createDraft`",10:"The given draft is already finalized",11:"Object.defineProperty() cannot be used on an Immer draft",12:"Object.setPrototypeOf() cannot be used on an Immer draft",13:"Immer only supports deleting array indices",14:"Immer only supports setting array indices and the 'length' property",15:function(n){return"Cannot apply patch, path doesn't resolve: "+n},16:'Sets cannot have "replace" patches.',17:function(n){return"Unsupported patch operation: "+n},18:function(n){return"The plugin for '"+n+"' has not been loaded into Immer. To enable the plugin, import and call `enable"+n+"()` when initializing your application."},20:"Cannot use proxies if Proxy, Proxy.revocable or Reflect are not available",21:function(n){return"produce can only be called on things that are draftable: plain objects, arrays, Map, Set or classes that are marked with '[immerable]: true'. Got '"+n+"'"},22:function(n){return"'current' expects a draft, got: "+n},23:function(n){return"'original' expects a draft, got: "+n},24:"Patching reserved attributes like __proto__, prototype and constructor is not allowed"},Z=""+Object.prototype.constructor,nn="undefined"!=typeof Reflect&&Reflect.ownKeys?Reflect.ownKeys:void 0!==Object.getOwnPropertySymbols?function(n){return Object.getOwnPropertyNames(n).concat(Object.getOwnPropertySymbols(n))}:Object.getOwnPropertyNames,tn=Object.getOwnPropertyDescriptors||function(n){var t={};return nn(n).forEach((function(r){t[r]=Object.getOwnPropertyDescriptor(n,r)})),t},rn={},en={get:function(n,t){if(t===Q)return n;var e=p(n);if(!u(e,t))return function(n,t,r){var e,i=I(t,r);return i?"value"in i?i.value:null===(e=i.get)||void 0===e?void 0:e.call(n.k):void 0}(n,e,t);var i=e[t];return n.I||!r(i)?i:i===z(n.t,t)?(E(n),n.o[t]=R(n.A.h,i,n)):i},has:function(n,t){return t in p(n)},ownKeys:function(n){return Reflect.ownKeys(p(n))},set:function(n,t,r){var e=I(p(n),t);if(null==e?void 0:e.set)return e.set.call(n.k,r),!0;if(!n.P){var i=z(p(n),t),o=null==i?void 0:i[Q];if(o&&o.t===r)return n.o[t]=r,n.D[t]=!1,!0;if(c(r,i)&&(void 0!==r||u(n.t,t)))return!0;E(n),k(n)}return n.o[t]===r&&"number"!=typeof r&&(void 0!==r||t in n.o)||(n.o[t]=r,n.D[t]=!0,!0)},deleteProperty:function(n,t){return void 0!==z(n.t,t)||t in n.t?(n.D[t]=!1,E(n),k(n)):delete n.D[t],n.o&&delete n.o[t],!0},getOwnPropertyDescriptor:function(n,t){var r=p(n),e=Reflect.getOwnPropertyDescriptor(r,t);return e?{writable:!0,configurable:1!==n.i||"length"!==t,enumerable:e.enumerable,value:r[t]}:e},defineProperty:function(){n(11)},getPrototypeOf:function(n){return Object.getPrototypeOf(n.t)},setPrototypeOf:function(){n(12)}},on={};i(en,(function(n,t){on[n]=function(){return arguments[0]=arguments[0][0],t.apply(this,arguments)}})),on.deleteProperty=function(t,r){return true&&isNaN(parseInt(r))&&n(13),en.deleteProperty.call(this,t[0],r)},on.set=function(t,r,e){return true&&"length"!==r&&isNaN(parseInt(r))&&n(14),en.set.call(this,t[0],r,e,t[0])};var un=function(){function e(t){var e=this;this.g=B,this.F=!0,this.produce=function(t,i,o){if("function"==typeof t&&"function"!=typeof i){var u=i;i=t;var a=e;return function(n){var t=this;void 0===n&&(n=u);for(var r=arguments.length,e=Array(r>1?r-1:0),o=1;o<r;o++)e[o-1]=arguments[o];return a.produce(n,(function(n){var r;return(r=i).call.apply(r,[t,n].concat(e))}))}}var f;if("function"!=typeof i&&n(6),void 0!==o&&"function"!=typeof o&&n(7),r(t)){var c=w(e),s=R(e,t,void 0),v=!0;try{f=i(s),v=!1}finally{v?O(c):g(c)}return"undefined"!=typeof Promise&&f instanceof Promise?f.then((function(n){return j(c,o),P(n,c)}),(function(n){throw O(c),n})):(j(c,o),P(f,c))}if(!t||"object"!=typeof t){if((f=i(t))===H)return;return void 0===f&&(f=t),e.F&&d(f,!0),f}n(21,t)},this.produceWithPatches=function(n,t){return"function"==typeof n?function(t){for(var r=arguments.length,i=Array(r>1?r-1:0),o=1;o<r;o++)i[o-1]=arguments[o];return e.produceWithPatches(t,(function(t){return n.apply(void 0,[t].concat(i))}))}:[e.produce(n,t,(function(n,t){r=n,i=t})),r,i];var r,i},"boolean"==typeof(null==t?void 0:t.useProxies)&&this.setUseProxies(t.useProxies),"boolean"==typeof(null==t?void 0:t.autoFreeze)&&this.setAutoFreeze(t.autoFreeze)}var i=e.prototype;return i.createDraft=function(e){r(e)||n(8),t(e)&&(e=D(e));var i=w(this),o=R(this,e,void 0);return o[Q].C=!0,g(i),o},i.finishDraft=function(t,r){var e=t&&t[Q]; true&&(e&&e.C||n(9),e.I&&n(10));var i=e.A;return j(i,r),P(void 0,i)},i.setAutoFreeze=function(n){this.F=n},i.setUseProxies=function(t){t&&!B&&n(20),this.g=t},i.applyPatches=function(n,r){var e;for(e=r.length-1;e>=0;e--){var i=r[e];if(0===i.path.length&&"replace"===i.op){n=i.value;break}}var o=b("Patches").$;return t(n)?o(n,r):this.produce(n,(function(n){return o(n,r.slice(e+1))}))},e}(),an=new un,fn=an.produce,cn=an.produceWithPatches.bind(an),sn=an.setAutoFreeze.bind(an),vn=an.setUseProxies.bind(an),pn=an.applyPatches.bind(an),ln=an.createDraft.bind(an),dn=an.finishDraft.bind(an);/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (fn);
//# sourceMappingURL=immer.esm.js.map


/***/ }),

/***/ "./node_modules/query-string/index.js":
/*!********************************************!*\
  !*** ./node_modules/query-string/index.js ***!
  \********************************************/
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {

"use strict";

const strictUriEncode = __webpack_require__(/*! strict-uri-encode */ "./node_modules/query-string/node_modules/strict-uri-encode/index.js");
const decodeComponent = __webpack_require__(/*! decode-uri-component */ "./node_modules/query-string/node_modules/decode-uri-component/index.js");
const splitOnFirst = __webpack_require__(/*! split-on-first */ "./node_modules/query-string/node_modules/split-on-first/index.js");
const filterObject = __webpack_require__(/*! filter-obj */ "./node_modules/filter-obj/index.js");

const isNullOrUndefined = value => value === null || value === undefined;

function encoderForArrayFormat(options) {
	switch (options.arrayFormat) {
		case 'index':
			return key => (result, value) => {
				const index = result.length;

				if (
					value === undefined ||
					(options.skipNull && value === null) ||
					(options.skipEmptyString && value === '')
				) {
					return result;
				}

				if (value === null) {
					return [...result, [encode(key, options), '[', index, ']'].join('')];
				}

				return [
					...result,
					[encode(key, options), '[', encode(index, options), ']=', encode(value, options)].join('')
				];
			};

		case 'bracket':
			return key => (result, value) => {
				if (
					value === undefined ||
					(options.skipNull && value === null) ||
					(options.skipEmptyString && value === '')
				) {
					return result;
				}

				if (value === null) {
					return [...result, [encode(key, options), '[]'].join('')];
				}

				return [...result, [encode(key, options), '[]=', encode(value, options)].join('')];
			};

		case 'comma':
		case 'separator':
			return key => (result, value) => {
				if (value === null || value === undefined || value.length === 0) {
					return result;
				}

				if (result.length === 0) {
					return [[encode(key, options), '=', encode(value, options)].join('')];
				}

				return [[result, encode(value, options)].join(options.arrayFormatSeparator)];
			};

		default:
			return key => (result, value) => {
				if (
					value === undefined ||
					(options.skipNull && value === null) ||
					(options.skipEmptyString && value === '')
				) {
					return result;
				}

				if (value === null) {
					return [...result, encode(key, options)];
				}

				return [...result, [encode(key, options), '=', encode(value, options)].join('')];
			};
	}
}

function parserForArrayFormat(options) {
	let result;

	switch (options.arrayFormat) {
		case 'index':
			return (key, value, accumulator) => {
				result = /\[(\d*)\]$/.exec(key);

				key = key.replace(/\[\d*\]$/, '');

				if (!result) {
					accumulator[key] = value;
					return;
				}

				if (accumulator[key] === undefined) {
					accumulator[key] = {};
				}

				accumulator[key][result[1]] = value;
			};

		case 'bracket':
			return (key, value, accumulator) => {
				result = /(\[\])$/.exec(key);
				key = key.replace(/\[\]$/, '');

				if (!result) {
					accumulator[key] = value;
					return;
				}

				if (accumulator[key] === undefined) {
					accumulator[key] = [value];
					return;
				}

				accumulator[key] = [].concat(accumulator[key], value);
			};

		case 'comma':
		case 'separator':
			return (key, value, accumulator) => {
				const isArray = typeof value === 'string' && value.includes(options.arrayFormatSeparator);
				const isEncodedArray = (typeof value === 'string' && !isArray && decode(value, options).includes(options.arrayFormatSeparator));
				value = isEncodedArray ? decode(value, options) : value;
				const newValue = isArray || isEncodedArray ? value.split(options.arrayFormatSeparator).map(item => decode(item, options)) : value === null ? value : decode(value, options);
				accumulator[key] = newValue;
			};

		default:
			return (key, value, accumulator) => {
				if (accumulator[key] === undefined) {
					accumulator[key] = value;
					return;
				}

				accumulator[key] = [].concat(accumulator[key], value);
			};
	}
}

function validateArrayFormatSeparator(value) {
	if (typeof value !== 'string' || value.length !== 1) {
		throw new TypeError('arrayFormatSeparator must be single character string');
	}
}

function encode(value, options) {
	if (options.encode) {
		return options.strict ? strictUriEncode(value) : encodeURIComponent(value);
	}

	return value;
}

function decode(value, options) {
	if (options.decode) {
		return decodeComponent(value);
	}

	return value;
}

function keysSorter(input) {
	if (Array.isArray(input)) {
		return input.sort();
	}

	if (typeof input === 'object') {
		return keysSorter(Object.keys(input))
			.sort((a, b) => Number(a) - Number(b))
			.map(key => input[key]);
	}

	return input;
}

function removeHash(input) {
	const hashStart = input.indexOf('#');
	if (hashStart !== -1) {
		input = input.slice(0, hashStart);
	}

	return input;
}

function getHash(url) {
	let hash = '';
	const hashStart = url.indexOf('#');
	if (hashStart !== -1) {
		hash = url.slice(hashStart);
	}

	return hash;
}

function extract(input) {
	input = removeHash(input);
	const queryStart = input.indexOf('?');
	if (queryStart === -1) {
		return '';
	}

	return input.slice(queryStart + 1);
}

function parseValue(value, options) {
	if (options.parseNumbers && !Number.isNaN(Number(value)) && (typeof value === 'string' && value.trim() !== '')) {
		value = Number(value);
	} else if (options.parseBooleans && value !== null && (value.toLowerCase() === 'true' || value.toLowerCase() === 'false')) {
		value = value.toLowerCase() === 'true';
	}

	return value;
}

function parse(query, options) {
	options = Object.assign({
		decode: true,
		sort: true,
		arrayFormat: 'none',
		arrayFormatSeparator: ',',
		parseNumbers: false,
		parseBooleans: false
	}, options);

	validateArrayFormatSeparator(options.arrayFormatSeparator);

	const formatter = parserForArrayFormat(options);

	// Create an object with no prototype
	const ret = Object.create(null);

	if (typeof query !== 'string') {
		return ret;
	}

	query = query.trim().replace(/^[?#&]/, '');

	if (!query) {
		return ret;
	}

	for (const param of query.split('&')) {
		if (param === '') {
			continue;
		}

		let [key, value] = splitOnFirst(options.decode ? param.replace(/\+/g, ' ') : param, '=');

		// Missing `=` should be `null`:
		// http://w3.org/TR/2012/WD-url-20120524/#collect-url-parameters
		value = value === undefined ? null : ['comma', 'separator'].includes(options.arrayFormat) ? value : decode(value, options);
		formatter(decode(key, options), value, ret);
	}

	for (const key of Object.keys(ret)) {
		const value = ret[key];
		if (typeof value === 'object' && value !== null) {
			for (const k of Object.keys(value)) {
				value[k] = parseValue(value[k], options);
			}
		} else {
			ret[key] = parseValue(value, options);
		}
	}

	if (options.sort === false) {
		return ret;
	}

	return (options.sort === true ? Object.keys(ret).sort() : Object.keys(ret).sort(options.sort)).reduce((result, key) => {
		const value = ret[key];
		if (Boolean(value) && typeof value === 'object' && !Array.isArray(value)) {
			// Sort object keys, not values
			result[key] = keysSorter(value);
		} else {
			result[key] = value;
		}

		return result;
	}, Object.create(null));
}

exports.extract = extract;
exports.parse = parse;

exports.stringify = (object, options) => {
	if (!object) {
		return '';
	}

	options = Object.assign({
		encode: true,
		strict: true,
		arrayFormat: 'none',
		arrayFormatSeparator: ','
	}, options);

	validateArrayFormatSeparator(options.arrayFormatSeparator);

	const shouldFilter = key => (
		(options.skipNull && isNullOrUndefined(object[key])) ||
		(options.skipEmptyString && object[key] === '')
	);

	const formatter = encoderForArrayFormat(options);

	const objectCopy = {};

	for (const key of Object.keys(object)) {
		if (!shouldFilter(key)) {
			objectCopy[key] = object[key];
		}
	}

	const keys = Object.keys(objectCopy);

	if (options.sort !== false) {
		keys.sort(options.sort);
	}

	return keys.map(key => {
		const value = object[key];

		if (value === undefined) {
			return '';
		}

		if (value === null) {
			return encode(key, options);
		}

		if (Array.isArray(value)) {
			return value
				.reduce(formatter(key), [])
				.join('&');
		}

		return encode(key, options) + '=' + encode(value, options);
	}).filter(x => x.length > 0).join('&');
};

exports.parseUrl = (url, options) => {
	options = Object.assign({
		decode: true
	}, options);

	const [url_, hash] = splitOnFirst(url, '#');

	return Object.assign(
		{
			url: url_.split('?')[0] || '',
			query: parse(extract(url), options)
		},
		options && options.parseFragmentIdentifier && hash ? {fragmentIdentifier: decode(hash, options)} : {}
	);
};

exports.stringifyUrl = (object, options) => {
	options = Object.assign({
		encode: true,
		strict: true
	}, options);

	const url = removeHash(object.url).split('?')[0] || '';
	const queryFromUrl = exports.extract(object.url);
	const parsedQueryFromUrl = exports.parse(queryFromUrl, {sort: false});

	const query = Object.assign(parsedQueryFromUrl, object.query);
	let queryString = exports.stringify(query, options);
	if (queryString) {
		queryString = `?${queryString}`;
	}

	let hash = getHash(object.url);
	if (object.fragmentIdentifier) {
		hash = `#${encode(object.fragmentIdentifier, options)}`;
	}

	return `${url}${queryString}${hash}`;
};

exports.pick = (input, filter, options) => {
	options = Object.assign({
		parseFragmentIdentifier: true
	}, options);

	const {url, query, fragmentIdentifier} = exports.parseUrl(input, options);
	return exports.stringifyUrl({
		url,
		query: filterObject(query, filter),
		fragmentIdentifier
	}, options);
};

exports.exclude = (input, filter, options) => {
	const exclusionFilter = Array.isArray(filter) ? key => !filter.includes(key) : (key, value) => !filter(key, value);

	return exports.pick(input, exclusionFilter, options);
};


/***/ }),

/***/ "./node_modules/query-string/node_modules/decode-uri-component/index.js":
/*!******************************************************************************!*\
  !*** ./node_modules/query-string/node_modules/decode-uri-component/index.js ***!
  \******************************************************************************/
/***/ ((module) => {

"use strict";

var token = '%[a-f0-9]{2}';
var singleMatcher = new RegExp(token, 'gi');
var multiMatcher = new RegExp('(' + token + ')+', 'gi');

function decodeComponents(components, split) {
	try {
		// Try to decode the entire string first
		return decodeURIComponent(components.join(''));
	} catch (err) {
		// Do nothing
	}

	if (components.length === 1) {
		return components;
	}

	split = split || 1;

	// Split the array in 2 parts
	var left = components.slice(0, split);
	var right = components.slice(split);

	return Array.prototype.concat.call([], decodeComponents(left), decodeComponents(right));
}

function decode(input) {
	try {
		return decodeURIComponent(input);
	} catch (err) {
		var tokens = input.match(singleMatcher);

		for (var i = 1; i < tokens.length; i++) {
			input = decodeComponents(tokens, i).join('');

			tokens = input.match(singleMatcher);
		}

		return input;
	}
}

function customDecodeURIComponent(input) {
	// Keep track of all the replacements and prefill the map with the `BOM`
	var replaceMap = {
		'%FE%FF': '\uFFFD\uFFFD',
		'%FF%FE': '\uFFFD\uFFFD'
	};

	var match = multiMatcher.exec(input);
	while (match) {
		try {
			// Decode as big chunks as possible
			replaceMap[match[0]] = decodeURIComponent(match[0]);
		} catch (err) {
			var result = decode(match[0]);

			if (result !== match[0]) {
				replaceMap[match[0]] = result;
			}
		}

		match = multiMatcher.exec(input);
	}

	// Add `%C2` at the end of the map to make sure it does not replace the combinator before everything else
	replaceMap['%C2'] = '\uFFFD';

	var entries = Object.keys(replaceMap);

	for (var i = 0; i < entries.length; i++) {
		// Replace all decoded components
		var key = entries[i];
		input = input.replace(new RegExp(key, 'g'), replaceMap[key]);
	}

	return input;
}

module.exports = function (encodedURI) {
	if (typeof encodedURI !== 'string') {
		throw new TypeError('Expected `encodedURI` to be of type `string`, got `' + typeof encodedURI + '`');
	}

	try {
		encodedURI = encodedURI.replace(/\+/g, ' ');

		// Try the built in decoder first
		return decodeURIComponent(encodedURI);
	} catch (err) {
		// Fallback to a more advanced decoder
		return customDecodeURIComponent(encodedURI);
	}
};


/***/ }),

/***/ "./node_modules/query-string/node_modules/split-on-first/index.js":
/*!************************************************************************!*\
  !*** ./node_modules/query-string/node_modules/split-on-first/index.js ***!
  \************************************************************************/
/***/ ((module) => {

"use strict";


module.exports = (string, separator) => {
	if (!(typeof string === 'string' && typeof separator === 'string')) {
		throw new TypeError('Expected the arguments to be of type `string`');
	}

	if (separator === '') {
		return [string];
	}

	const separatorIndex = string.indexOf(separator);

	if (separatorIndex === -1) {
		return [string];
	}

	return [
		string.slice(0, separatorIndex),
		string.slice(separatorIndex + separator.length)
	];
};


/***/ }),

/***/ "./node_modules/query-string/node_modules/strict-uri-encode/index.js":
/*!***************************************************************************!*\
  !*** ./node_modules/query-string/node_modules/strict-uri-encode/index.js ***!
  \***************************************************************************/
/***/ ((module) => {

"use strict";

module.exports = str => encodeURIComponent(str).replace(/[!'()*]/g, x => `%${x.charCodeAt(0).toString(16).toUpperCase()}`);


/***/ }),

/***/ "./node_modules/react-click-outside-ts/dist/index.modern.js":
/*!******************************************************************!*\
  !*** ./node_modules/react-click-outside-ts/dist/index.modern.js ***!
  \******************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "ClickingOutSide": () => (/* binding */ ClickingOutSide),
/* harmony export */   "ClickingOutSideFull": () => (/* binding */ ClickingOutSideFull)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "./node_modules/react/index.js");


function ClickingOutSide(_ref) {
  var ref_click_outside = _ref.ref_click_outside,
      is_show = _ref.is_show,
      children = _ref.children,
      _ref$beforeDetectOuts = _ref.beforeDetectOutside,
      beforeDetectOutside = _ref$beforeDetectOuts === void 0 ? function () {} : _ref$beforeDetectOuts,
      handleClickOutSide = _ref.handleClickOutSide;
  var ref_handle_click = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(function () {
    if (!ref_handle_click.current) {
      ref_handle_click.current = handleClick;
    }

    if (is_show) {
      window.addEventListener('click', ref_handle_click.current);
    } else {
      window.removeEventListener('click', ref_handle_click.current);
    }

    return function () {
      ref_handle_click.current && window.removeEventListener('click', ref_handle_click.current);
    };
  }, [is_show]);

  function handleClick(event) {
    beforeDetectOutside(event);

    if (!ref_click_outside.current) {
      ref_click_outside.current = true;
      return;
    }

    handleClickOutSide();
  }

  return children;
}

function ClickingOutSideFull(_ref) {
  var ref_child = _ref.ref_child,
      refs_target = _ref.refs_target,
      is_show = _ref.is_show,
      children = _ref.children,
      handleClickOutSide = _ref.handleClickOutSide;
  var ref_handle_click = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  var ref_handle_click_inside = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  var ref_click_outside = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(true);
  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(function () {
    handleAddFunc();

    if (is_show) {
      handleAddEvent();
    } else {
      handleRemoveEvent();
    }

    return function () {
      handleRemoveEvent();
    };
  }, [is_show]);

  function handleAddFunc() {
    if (!ref_handle_click.current) {
      ref_handle_click.current = handleClick;
    }

    if (!ref_handle_click_inside.current) {
      ref_handle_click_inside.current = handleClickInside;
    }
  }

  function handleAddEvent() {
    ref_handle_click.current && window.addEventListener('click', ref_handle_click.current);
    [ref_child].concat(refs_target).forEach(function (item) {
      var _item$current;

      ref_handle_click_inside.current && ((_item$current = item.current) === null || _item$current === void 0 ? void 0 : _item$current.addEventListener('click', ref_handle_click_inside.current));
    });
  }

  function handleRemoveEvent() {
    ref_handle_click.current && window.removeEventListener('click', ref_handle_click.current);
    [ref_child].concat(refs_target).forEach(function (item) {
      var _item$current2;

      ref_handle_click_inside.current && ((_item$current2 = item.current) === null || _item$current2 === void 0 ? void 0 : _item$current2.removeEventListener('click', ref_handle_click_inside.current));
    });
  }

  function handleClickInside() {
    ref_click_outside.current = false;
  }

  function handleClick() {
    if (!ref_click_outside.current) {
      ref_click_outside.current = true;
      return;
    }

    handleClickOutSide();
  }

  return children;
}


//# sourceMappingURL=index.modern.js.map


/***/ }),

/***/ "./node_modules/react-commons-ts/dist/index.modern.js":
/*!************************************************************!*\
  !*** ./node_modules/react-commons-ts/dist/index.modern.js ***!
  \************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "common_constant": () => (/* binding */ constant),
/* harmony export */   "common_types": () => (/* binding */ _common),
/* harmony export */   "formatLocalDateString": () => (/* binding */ formatLocalDateString),
/* harmony export */   "formatLocalDateTimeString": () => (/* binding */ formatLocalDateTimeString),
/* harmony export */   "formatNum": () => (/* binding */ formatNum),
/* harmony export */   "getTimeAndUnit": () => (/* binding */ getTimeAndUnit),
/* harmony export */   "unitNumber": () => (/* binding */ unitNumber),
/* harmony export */   "unitTime": () => (/* binding */ unitTime),
/* harmony export */   "unitTimeJustNow": () => (/* binding */ unitTimeJustNow),
/* harmony export */   "useBool": () => (/* binding */ useBool),
/* harmony export */   "useDisplayBlockToNone": () => (/* binding */ useDisplayBlockToNone),
/* harmony export */   "useForceUpdate": () => (/* binding */ useForceUpdate),
/* harmony export */   "useHold": () => (/* binding */ useHold),
/* harmony export */   "useMounted": () => (/* binding */ useMounted),
/* harmony export */   "useMouseDownToWindowUp": () => (/* binding */ useMouseDownToWindowUp),
/* harmony export */   "waitingToDoLast": () => (/* binding */ waitingToDoLast)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "./node_modules/react/index.js");


var IS_MOBILE = navigator.userAgent.toUpperCase().search('MOBILE') >= 0;

var constant = {
    __proto__: null,
    IS_MOBILE: IS_MOBILE
};



var _common = {
    __proto__: null
};

var useBool = function useBool(initial_bool) {
  if (initial_bool === void 0) {
    initial_bool = false;
  }

  var _useState = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(initial_bool),
      is_true = _useState[0],
      setIsTrue = _useState[1];

  function toggleBool() {
    setIsTrue(function (is_true) {
      return !is_true;
    });
  }

  return {
    is_true: is_true,
    setIsTrue: setIsTrue,
    toggleBool: toggleBool
  };
};

function useMounted() {
  var _useState = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(true),
      mounted = _useState[0],
      setMounted = _useState[1];

  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(function () {
    return function () {
      setMounted(false);
    };
  }, []);
  return mounted;
}

function useHold(_ref) {
  var _ref$time = _ref.time,
      time = _ref$time === void 0 ? 500 : _ref$time,
      _ref$use_holding = _ref.use_holding,
      use_holding = _ref$use_holding === void 0 ? false : _ref$use_holding,
      _ref$time_holding_sta = _ref.time_holding_start,
      time_holding_start = _ref$time_holding_sta === void 0 ? 100 : _ref$time_holding_sta;

  var _useState = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(false),
      holding = _useState[0],
      setHolding = _useState[1];

  var timeout = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  var timeout_holding = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  var mounted = useMounted();

  function StartHold(callback) {
    if (callback === void 0) {
      callback = function callback() {};
    }

    timeout_holding.current = setTimeout(function () {
      if (!mounted) {
        return;
      }

      use_holding && setHolding(true);
      handleHolding(callback);
    }, time_holding_start);
  }

  function handleHolding(callback) {
    if (callback === void 0) {
      callback = function callback() {};
    }

    timeout.current = setTimeout(function () {
      if (!mounted) {
        return;
      }

      use_holding && setHolding(false);
      timeout_holding.current && clearTimeout(timeout_holding.current);
      callback();
    }, time);
  }

  function StopHold() {
    use_holding && setHolding(false);
    timeout_holding.current && clearTimeout(timeout_holding.current);
    timeout.current && clearTimeout(timeout.current);
  }

  return {
    holding: holding,
    StartHold: StartHold,
    StopHold: StopHold
  };
}

function useDisplayBlockToNone(_ref) {
  var _ref$trans_time = _ref.trans_time,
      trans_time = _ref$trans_time === void 0 ? 250 : _ref$trans_time,
      _ref$initial_display_ = _ref.initial_display_none,
      initial_display_none = _ref$initial_display_ === void 0 ? true : _ref$initial_display_;

  var _useState = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(initial_display_none),
      opacity_0 = _useState[0],
      setOpacity0 = _useState[1];

  var _useState2 = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(initial_display_none),
      display_none = _useState2[0],
      setDisplayNone = _useState2[1];

  var ref_interval_to_none = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  var ref_interval_to_block = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);

  function changeBlockToNone() {
    setOpacity0(true);
    ref_interval_to_none.current = setTimeout(function () {
      setDisplayNone(true);
    }, trans_time);
  }

  function changeNoneToBlock() {
    setDisplayNone(false);
    ref_interval_to_block.current = setTimeout(function () {
      setOpacity0(false);
    }, 0);
  }

  function toggleBlockNone() {
    if (ref_interval_to_none.current) {
      clearTimeout(ref_interval_to_none.current);
      ref_interval_to_none.current = null;
      changeNoneToBlock();
      return;
    }

    if (ref_interval_to_block.current) {
      clearTimeout(ref_interval_to_block.current);
      ref_interval_to_block.current = null;
      changeBlockToNone();
      return;
    }

    if (opacity_0) {
      changeNoneToBlock();
    } else {
      changeBlockToNone();
    }
  }

  return {
    opacity_0: opacity_0,
    display_none: display_none,
    changeNoneToBlock: changeNoneToBlock,
    changeBlockToNone: changeBlockToNone,
    toggleBlockNone: toggleBlockNone
  };
}

function useForceUpdate() {
  var _useState = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(false),
      setForceUpdate = _useState[1];

  function forceUpdate() {
    setForceUpdate(function (force_update) {
      return !force_update;
    });
  }

  return forceUpdate;
}

function detectNoTouch(e) {
  return e.touches.length == 0;
}

function detectHasTouch(e) {
  return e.touches.length > 0;
}

function useMouseDownToWindowUp(_ref) {
  var _ref$handleDown = _ref.handleDown,
      handleDown = _ref$handleDown === void 0 ? function () {} : _ref$handleDown,
      _ref$handleMove = _ref.handleMove,
      handleMove = _ref$handleMove === void 0 ? function () {} : _ref$handleMove,
      _ref$handleEnd = _ref.handleEnd,
      handleEnd = _ref$handleEnd === void 0 ? function () {} : _ref$handleEnd,
      _ref$detectEndTouch = _ref.detectEndTouch,
      detectEndTouch = _ref$detectEndTouch === void 0 ? detectNoTouch : _ref$detectEndTouch,
      _ref$detectStartTouch = _ref.detectStartTouch,
      detectStartTouch = _ref$detectStartTouch === void 0 ? detectHasTouch : _ref$detectStartTouch;
  var ref_func_move = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  var ref_func_up = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);

  function onDown(e) {
    handleDown(e);

    if (!ref_func_move.current) {
      ref_func_move.current = onMove;
    }

    if (!ref_func_up.current) {
      ref_func_up.current = onUp;
    }

    if (e instanceof TouchEvent) {
      if (detectStartTouch(e)) {
        window.addEventListener('touchmove', ref_func_move.current);
        window.addEventListener('touchend', ref_func_up.current);
      }
    } else {
      window.addEventListener('mousemove', ref_func_move.current);
      window.addEventListener('mouseup', ref_func_up.current);
    }
  }

  function onMove(e) {
    handleMove(e);
  }

  function onUp(e) {
    handleEnd(e);

    if (e instanceof TouchEvent) {
      if (detectEndTouch(e)) {
        ref_func_move.current && window.removeEventListener('touchmove', ref_func_move.current);
        ref_func_up.current && window.removeEventListener('touchend', ref_func_up.current);
      }
    } else {
      ref_func_move.current && window.removeEventListener('mousemove', ref_func_move.current);
      ref_func_up.current && window.removeEventListener('mouseup', ref_func_up.current);
    }

    ref_func_move.current = null;
    ref_func_up.current = null;
  }

  return {
    onDown: onDown
  };
}

function formatLocalDateString(_ref) {
  var _ref$date = _ref.date,
      date = _ref$date === void 0 ? new Date() : _ref$date,
      _ref$locales = _ref.locales,
      locales = _ref$locales === void 0 ? 'vi-VN' : _ref$locales;
  return new Intl.DateTimeFormat(locales, {
    day: '2-digit',
    month: '2-digit',
    year: 'numeric'
  }).format(new Date(date));
}
function formatLocalDateTimeString(_ref2) {
  var _ref2$date = _ref2.date,
      date = _ref2$date === void 0 ? new Date() : _ref2$date,
      _ref2$locales = _ref2.locales,
      locales = _ref2$locales === void 0 ? 'vi-VN' : _ref2$locales;
  return new Intl.DateTimeFormat(locales, {
    hour: '2-digit',
    minute: '2-digit',
    second: '2-digit',
    day: '2-digit',
    month: '2-digit',
    year: 'numeric'
  }).format(new Date(date));
}

var DATA_UNIT_LONG_ARR = [' year', ' month', ' day', ' hour', ' minute', ' second'];
function getTimeAndUnit(_ref) {
  var _ref$time_change = _ref.time_change,
      time_change = _ref$time_change === void 0 ? 0 : _ref$time_change,
      _ref$unit_arr = _ref.unit_arr,
      unit_arr = _ref$unit_arr === void 0 ? DATA_UNIT_LONG_ARR : _ref$unit_arr;

  if (time_change >= 31536000000) {
    return {
      time: Math.floor(time_change / 31536000000),
      unit: unit_arr[0]
    };
  }

  if (time_change >= 2592000000) {
    return {
      time: Math.floor(time_change / 2592000000),
      unit: unit_arr[1]
    };
  }

  if (time_change >= 86400000) {
    return {
      time: Math.floor(time_change / 86400000),
      unit: unit_arr[2]
    };
  }

  if (time_change >= 3600000) {
    return {
      time: Math.floor(time_change / 3600000),
      unit: unit_arr[3]
    };
  }

  if (time_change >= 60000) {
    return {
      time: Math.floor(time_change / 3600000),
      unit: unit_arr[4]
    };
  }

  return {
    time: Math.floor(time_change / 1000),
    unit: unit_arr[5]
  };
}
function unitTime(_ref2) {
  var _ref2$time_change = _ref2.time_change,
      time_change = _ref2$time_change === void 0 ? 0 : _ref2$time_change,
      _ref2$unit_arr = _ref2.unit_arr,
      unit_arr = _ref2$unit_arr === void 0 ? DATA_UNIT_LONG_ARR : _ref2$unit_arr;
  var unit_and_time = getTimeAndUnit({
    time_change: time_change,
    unit_arr: unit_arr
  });
  return "" + unit_and_time.time + unit_and_time.unit;
}
function unitTimeHasSpecial(_ref3) {
  var _ref3$time_change = _ref3.time_change,
      time_change = _ref3$time_change === void 0 ? 0 : _ref3$time_change,
      _ref3$unit_arr = _ref3.unit_arr,
      unit_arr = _ref3$unit_arr === void 0 ? DATA_UNIT_LONG_ARR : _ref3$unit_arr,
      _ref3$detectIsSpecial = _ref3.detectIsSpecial,
      detectIsSpecial = _ref3$detectIsSpecial === void 0 ? function () {
    return false;
  } : _ref3$detectIsSpecial,
      _ref3$getUnitTimeSpec = _ref3.getUnitTimeSpecial,
      getUnitTimeSpecial = _ref3$getUnitTimeSpec === void 0 ? function () {
    return '';
  } : _ref3$getUnitTimeSpec;

  if (detectIsSpecial()) {
    return getUnitTimeSpecial();
  }

  return unitTime({
    time_change: time_change,
    unit_arr: unit_arr
  });
}
function unitTimeJustNow(_ref4) {
  var _ref4$time_change = _ref4.time_change,
      time_change = _ref4$time_change === void 0 ? 0 : _ref4$time_change,
      _ref4$unit_arr = _ref4.unit_arr,
      unit_arr = _ref4$unit_arr === void 0 ? DATA_UNIT_LONG_ARR : _ref4$unit_arr;
  unitTimeHasSpecial({
    time_change: time_change,
    unit_arr: unit_arr,
    detectIsSpecial: function detectIsSpecial() {
      return time_change <= 60;
    },
    getUnitTimeSpecial: function getUnitTimeSpecial() {
      return 'Just now';
    }
  });
}

function formatNum(num) {
  if (num === void 0) {
    num = 0;
  }

  return new Intl.NumberFormat('en').format(num);
}

function unitNumber(_ref) {
  var _ref$num = _ref.num,
      num = _ref$num === void 0 ? 0 : _ref$num,
      _ref$unit_arr = _ref.unit_arr,
      unit_arr = _ref$unit_arr === void 0 ? ['K', 'M', 'B'] : _ref$unit_arr;

  if (num >= 1000000000) {
    return "" + Math.floor(num / 100000000) / 10 + unit_arr[2];
  }

  if (num >= 1000000) {
    return "" + Math.floor(num / 100000) / 10 + unit_arr[1];
  }

  if (num >= 1000) {
    return "" + Math.floor(num / 100) / 10 + unit_arr[0];
  }

  return num;
}

function waitingToDoLast(_ref) {
  var ref_interval = _ref.ref_interval,
      _ref$time = _ref.time,
      time = _ref$time === void 0 ? 200 : _ref$time,
      _ref$callback = _ref.callback,
      callback = _ref$callback === void 0 ? function () {} : _ref$callback;
  ref_interval.current && clearTimeout(ref_interval.current);
  ref_interval.current = setTimeout(function () {
    ref_interval.current && clearTimeout(ref_interval.current);
    callback();
  }, time);
}


//# sourceMappingURL=index.modern.js.map


/***/ }),

/***/ "./node_modules/react-observer-ts/dist/index.modern.js":
/*!*************************************************************!*\
  !*** ./node_modules/react-observer-ts/dist/index.modern.js ***!
  \*************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "observeToDo": () => (/* binding */ observeToDo),
/* harmony export */   "observeVirtualScroll": () => (/* binding */ observeVirtualScroll),
/* harmony export */   "observerAppearance": () => (/* binding */ observerAppearance),
/* harmony export */   "observerDisplay": () => (/* binding */ observerDisplay),
/* harmony export */   "observerOverflow": () => (/* binding */ observerOverflow),
/* harmony export */   "observerSticky": () => (/* binding */ observerSticky),
/* harmony export */   "observerVidPic": () => (/* binding */ observerVidPic),
/* harmony export */   "useDataShowMore": () => (/* binding */ useDataShowMore),
/* harmony export */   "useObserveVidPic": () => (/* binding */ useObserveVidPic),
/* harmony export */   "useObserverGetData": () => (/* binding */ useObserverGetData),
/* harmony export */   "useObserverShowMore": () => (/* binding */ useObserverShowMore),
/* harmony export */   "useObserverVirtualScroll": () => (/* binding */ useObserverVirtualScroll)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "./node_modules/react/index.js");
/* harmony import */ var react_commons_ts__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! react-commons-ts */ "./node_modules/react-observer-ts/node_modules/react-commons-ts/dist/index.modern.js");



function _extends() {
  _extends = Object.assign || function (target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];

      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }

    return target;
  };

  return _extends.apply(this, arguments);
}

// A type of promise-like that resolves synchronously and supports only one observer

const _iteratorSymbol = /*#__PURE__*/ typeof Symbol !== "undefined" ? (Symbol.iterator || (Symbol.iterator = Symbol("Symbol.iterator"))) : "@@iterator";

const _asyncIteratorSymbol = /*#__PURE__*/ typeof Symbol !== "undefined" ? (Symbol.asyncIterator || (Symbol.asyncIterator = Symbol("Symbol.asyncIterator"))) : "@@asyncIterator";

// Asynchronously call a function and send errors to recovery continuation
function _catch(body, recover) {
	try {
		var result = body();
	} catch(e) {
		return recover(e);
	}
	if (result && result.then) {
		return result.then(void 0, recover);
	}
	return result;
}

function useDataShowMore(_ref) {
  var refreshData_API = function refreshData_API(start_obj_state) {
    if (start_obj_state === void 0) {
      start_obj_state = {};
    }

    try {
      data_count.current = 0;
      return Promise.resolve(getData_API({
        start_obj_state: _extends({
          data_arr: [],
          count: 0,
          has_fetched: false
        }, start_obj_state)
      })).then(function () {});
    } catch (e) {
      return Promise.reject(e);
    }
  };

  var getData_API = function getData_API(data_get_api) {
    if (data_get_api === void 0) {
      data_get_api = {
        start_obj_state: {},
        handleWhenFinally: function handleWhenFinally() {},
        getOtherDataStateWhenSetState: function getOtherDataStateWhenSetState() {
          return {};
        }
      };
    }

    try {
      var _temp3 = function _temp3() {
        handleWhenFinally();
        ref_fetching.current = false;
      };

      if (ref_fetching.current) {
        return Promise.resolve();
      }

      ref_fetching.current = true;
      var _data_get_api = data_get_api,
          _data_get_api$start_o = _data_get_api.start_obj_state,
          start_obj_state = _data_get_api$start_o === void 0 ? {} : _data_get_api$start_o,
          _data_get_api$handleW = _data_get_api.handleWhenFinally,
          handleWhenFinally = _data_get_api$handleW === void 0 ? function () {} : _data_get_api$handleW,
          _data_get_api$getOthe = _data_get_api.getOtherDataStateWhenSetState,
          getOtherDataStateWhenSetState = _data_get_api$getOthe === void 0 ? function () {
        return {};
      } : _data_get_api$getOthe;

      var _temp4 = _catch(function () {
        setDataState(function (data_state) {
          return _extends({}, data_state, {
            is_fetching: true
          }, start_obj_state);
        });
        return Promise.resolve(handle_API_L(data_count.current)).then(function (_ref2) {
          var data = _ref2.data,
              new_count = _ref2.count;
          mounted && setDataState(function (data_state) {
            var has_fetched = data_state.has_fetched,
                data_arr = data_state.data_arr,
                count = data_state.count;
            var total_count = has_fetched && count ? count : new_count;
            data_count.current += data.length;
            is_max.current = data_count.current >= total_count;
            var other_data_state = getOtherDataStateWhenSetState(data_state);
            return _extends({}, data_state, {
              data_arr: has_fetched ? [].concat(data_arr, data) : data,
              count: total_count,
              is_fetching: false,
              has_fetched: true
            }, other_data_state);
          });
        });
      }, function (e) {
        console.log(e);
      });

      return Promise.resolve(_temp4 && _temp4.then ? _temp4.then(_temp3) : _temp3(_temp4));
    } catch (e) {
      return Promise.reject(e);
    }
  };

  var _ref$initial_arr = _ref.initial_arr,
      initial_arr = _ref$initial_arr === void 0 ? [] : _ref$initial_arr,
      _ref$other_state = _ref.other_state,
      other_state = _ref$other_state === void 0 ? {} : _ref$other_state,
      handle_API_L = _ref.handle_API_L;

  var _useState = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(_extends({
    data_arr: initial_arr,
    count: 0,
    has_fetched: initial_arr.length > 0,
    is_fetching: false
  }, other_state)),
      data_state = _useState[0],
      setDataState = _useState[1];

  var is_max = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(false);
  var ref_fetching = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(false);
  var data_count = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(initial_arr.length);
  var mounted = (0,react_commons_ts__WEBPACK_IMPORTED_MODULE_1__.useMounted)();
  return {
    data_state: data_state,
    setDataState: setDataState,
    is_max: is_max,
    ref_fetching: ref_fetching,
    data_count: data_count,
    getData_API: getData_API,
    refreshData_API: refreshData_API
  };
}

function useObserverGetData(_ref) {
  var getData_API = _ref.getData_API,
      getIsMax = _ref.getIsMax,
      getIsFetching = _ref.getIsFetching,
      _ref$unobserve_when_m = _ref.unobserve_when_max,
      unobserve_when_max = _ref$unobserve_when_m === void 0 ? true : _ref$unobserve_when_m;
  var ref_fake_elm_end = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  var ref_margin = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(0);
  var ref_handle_scroll = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(function () {
    return function () {
      ref_handle_scroll.current && window.removeEventListener('scroll', ref_handle_scroll.current);
    };
  }, []);
  var observerShowMore = IntersectionObserver ? intersectShowMore : scrollShowMore;

  function intersectShowMore(_ref2) {
    var fake_elm_end = _ref2.fake_elm_end,
        _ref2$root = _ref2.root,
        root = _ref2$root === void 0 ? null : _ref2$root,
        _ref2$rootMargin = _ref2.rootMargin,
        rootMargin = _ref2$rootMargin === void 0 ? '0px 0px' : _ref2$rootMargin;
    var observe = new IntersectionObserver(function (entries) {
      entries.forEach(function (entry) {
        if (!entry.isIntersecting) {
          return;
        }

        if (getIsMax()) {
          unobserve_when_max && observe.unobserve(entry.target);
          return;
        }

        if (!getIsFetching()) {
          getData_API();
        }
      });
    }, {
      root: root,
      rootMargin: rootMargin
    });
    observe.observe(fake_elm_end);
  }

  function scrollShowMore(_ref3) {
    var fake_elm_end = _ref3.fake_elm_end,
        _ref3$root = _ref3.root,
        root = _ref3$root === void 0 ? null : _ref3$root,
        way_scroll = _ref3.way_scroll,
        _ref3$margin = _ref3.margin,
        margin = _ref3$margin === void 0 ? 0 : _ref3$margin;

    if (!root) {
      ref_fake_elm_end.current = fake_elm_end;
      ref_margin.current = margin;
      ref_handle_scroll.current = [handleWindowScrollDown, handleWindowScrollRight, handleWindowScrollUp, handleWindowScrollLeft][['to_bottom', 'to_right', 'to_top', 'to_left'].indexOf(way_scroll)];
      window.addEventListener('scroll', ref_handle_scroll.current);
    } else {
      root.onscroll = function () {
        return [handleScrollDown, handleScrollRight, handleScrollUp, handleScrollLeft][['to_bottom', 'to_right', 'to_top', 'to_left'].indexOf(way_scroll)](root, fake_elm_end, margin);
      };
    }
  }

  function handleScrollGetAPI(root) {
    if (root === void 0) {
      root = null;
    }

    if (getIsMax()) {
      !root ? ref_handle_scroll.current && window.removeEventListener('scroll', ref_handle_scroll.current) : root.onscroll = null;
      return;
    }

    getData_API();
  }

  function handleScrollDown(root, fake_elm_end, margin) {
    if (root.getBoundingClientRect().bottom + margin >= fake_elm_end.getBoundingClientRect().top) {
      handleScrollGetAPI(root);
    }
  }

  function handleScrollRight(root, fake_elm_end, margin) {
    if (root.getBoundingClientRect().right + margin >= fake_elm_end.getBoundingClientRect().left) {
      handleScrollGetAPI(root);
    }
  }

  function handleScrollUp(root, fake_elm_end, margin) {
    if (fake_elm_end.getBoundingClientRect().bottom + margin >= root.getBoundingClientRect().top) {
      handleScrollGetAPI(root);
    }
  }

  function handleScrollLeft(root, fake_elm_end, margin) {
    if (fake_elm_end.getBoundingClientRect().right + margin >= root.getBoundingClientRect().left) {
      handleScrollGetAPI(root);
    }
  }

  function handleWindowScrollDown() {
    if (!ref_fake_elm_end.current) {
      return;
    }

    if (ref_fake_elm_end.current.getBoundingClientRect().top && innerHeight + ref_margin.current >= ref_fake_elm_end.current.getBoundingClientRect().top) {
      handleScrollGetAPI();
    }
  }

  function handleWindowScrollRight() {
    if (!ref_fake_elm_end.current) {
      return;
    }

    if (ref_fake_elm_end.current.getBoundingClientRect().top && innerWidth + ref_margin.current >= ref_fake_elm_end.current.getBoundingClientRect().left) {
      handleScrollGetAPI();
    }
  }

  function handleWindowScrollUp() {
    if (!ref_fake_elm_end.current) {
      return;
    }

    if (ref_fake_elm_end.current.getBoundingClientRect().top && ref_fake_elm_end.current.getBoundingClientRect().bottom + ref_margin.current >= 0) {
      handleScrollGetAPI();
    }
  }

  function handleWindowScrollLeft() {
    if (!ref_fake_elm_end.current) {
      return;
    }

    if (ref_fake_elm_end.current.getBoundingClientRect().top && ref_fake_elm_end.current.getBoundingClientRect().right + ref_margin.current >= 0) {
      handleScrollGetAPI();
    }
  }

  return {
    observerShowMore: observerShowMore
  };
}

function useObserverShowMore(_ref) {
  var _ref$initial_arr = _ref.initial_arr,
      initial_arr = _ref$initial_arr === void 0 ? [] : _ref$initial_arr,
      handle_API_L = _ref.handle_API_L,
      _ref$other_state = _ref.other_state,
      other_state = _ref$other_state === void 0 ? {} : _ref$other_state;

  var _useDataShowMore = useDataShowMore({
    initial_arr: initial_arr,
    handle_API_L: handle_API_L,
    other_state: other_state
  }),
      data_state = _useDataShowMore.data_state,
      setDataState = _useDataShowMore.setDataState,
      is_max = _useDataShowMore.is_max,
      ref_fetching = _useDataShowMore.ref_fetching,
      data_count = _useDataShowMore.data_count,
      getData_API = _useDataShowMore.getData_API,
      refreshData_API = _useDataShowMore.refreshData_API;

  var _useObserverGetData = useObserverGetData({
    getData_API: getData_API,
    getIsMax: function getIsMax() {
      return is_max.current;
    },
    getIsFetching: function getIsFetching() {
      return ref_fetching.current;
    }
  }),
      observerShowMore = _useObserverGetData.observerShowMore;

  return {
    data_state: data_state,
    setDataState: setDataState,
    is_max: is_max,
    data_count: data_count,
    ref_fetching: ref_fetching,
    observerShowMore: observerShowMore,
    getData_API: getData_API,
    refreshData_API: refreshData_API
  };
}

function observeVirtualScroll(_ref) {
  var elm = _ref.elm,
      _ref$options = _ref.options,
      options = _ref$options === void 0 ? {
    rootMargin: '500px 0'
  } : _ref$options,
      handleIsIntersecting = _ref.handleIsIntersecting;
  var observer = new IntersectionObserver(function (entries) {
    entries.forEach(function (entry) {
      if (entry.boundingClientRect.height) {
        handleIsIntersecting(entry.isIntersecting);
      }
    });
  }, options);
  observer.observe(elm);
}

function useObserverVirtualScroll(_ref) {
  var _ref$ref_observer_elm = _ref.ref_observer_elm,
      ref_observer_elm = _ref$ref_observer_elm === void 0 ? {
    current: null
  } : _ref$ref_observer_elm,
      _ref$ref_contain_elm = _ref.ref_contain_elm,
      ref_contain_elm = _ref$ref_contain_elm === void 0 ? {
    current: null
  } : _ref$ref_contain_elm,
      _ref$ref_root = _ref.ref_root,
      ref_root = _ref$ref_root === void 0 ? {
    current: null
  } : _ref$ref_root,
      rootMargin_y = _ref.rootMargin_y,
      _ref$has_callback = _ref.has_callback,
      has_callback = _ref$has_callback === void 0 ? false : _ref$has_callback,
      _ref$callback = _ref.callback,
      callback = _ref$callback === void 0 ? function () {} : _ref$callback;
  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(function () {
    if (ref_observer_elm.current) {
      observeVirtualScroll({
        elm: ref_observer_elm.current,
        handleIsIntersecting: handleIsIntersecting,
        options: {
          root: ref_root.current,
          rootMargin: rootMargin_y + "px 0px"
        }
      });
    }
  }, []);

  function handleIsIntersecting(is_intersecting) {
    if (is_intersecting === void 0) {
      is_intersecting = false;
    }

    if (!ref_observer_elm.current || !ref_contain_elm.current) {
      return;
    }

    var height = is_intersecting ? 'auto' : ref_observer_elm.current.offsetHeight + 'px';
    var display = is_intersecting ? 'block' : 'none';

    if (has_callback) {
      callback(height, display);
    } else {
      ref_observer_elm.current.style.height = height;
      ref_contain_elm.current.style.display = display;
    }
  }
}

function observerVidPic(_ref) {
  var elements = _ref.elements,
      _ref$callback = _ref.callback,
      callback = _ref$callback === void 0 ? function () {} : _ref$callback,
      _ref$options = _ref.options,
      options = _ref$options === void 0 ? {
    threshold: 0
  } : _ref$options;
  var observer = new IntersectionObserver(function (entries, observer) {
    entries.forEach(function (entry) {
      if (entry.isIntersecting) {
        var target = entry.target;

        if (target.dataset.src) {
          target.src = target.dataset.src;
          target.removeAttribute('data-src');
          observer.unobserve(target);
          callback();
        }
      }
    });
  }, options);
  elements.forEach(function (element) {
    element && observer.observe(element);
  });
}

function useObserveVidPic(_ref) {
  var ref_elm = _ref.ref_elm,
      _ref$class_hide = _ref.class_hide,
      class_hide = _ref$class_hide === void 0 ? 'vid_pic-observe opacity-0' : _ref$class_hide,
      _ref$class_show = _ref.class_show,
      class_show = _ref$class_show === void 0 ? 'vid_pic-observe opacity-1' : _ref$class_show;

  var _useState = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(class_hide),
      class_vid_pic = _useState[0],
      setClassVidPic = _useState[1];

  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(function () {
    ref_elm.current && observerVidPic({
      elements: [ref_elm.current],
      callback: function callback() {
        setClassVidPic(class_show);
      }
    });
  }, [ref_elm]);
  return class_vid_pic;
}

var observerAppearance = function observerAppearance(_ref) {
  var elm = _ref.elm,
      options = _ref.options,
      _ref$data_appearance = _ref.data_appearance,
      data_appearance = _ref$data_appearance === void 0 ? 'data-appearance' : _ref$data_appearance;
  var observer = new IntersectionObserver(function (entries, observer) {
    entries.forEach(function (entry) {
      var target = entry.target;

      if (entry.isIntersecting) {
        target.setAttribute(data_appearance, '1');
        observer.unobserve(target);
      } else {
        if (target.getAttribute(data_appearance)) {
          target.removeAttribute(data_appearance);
          observer.unobserve(target);
        }
      }
    });
  }, options);
  observer.observe(elm);
};

function observerDisplay(_ref) {
  var elm = _ref.elm,
      options = _ref.options,
      _ref$callbackDisplay = _ref.callbackDisplay,
      callbackDisplay = _ref$callbackDisplay === void 0 ? function () {} : _ref$callbackDisplay,
      _ref$callbackNoDispla = _ref.callbackNoDisplay,
      callbackNoDisplay = _ref$callbackNoDispla === void 0 ? function () {} : _ref$callbackNoDispla;
  var observer = new IntersectionObserver(function (entries) {
    entries.forEach(function (entry) {
      if (entry.isIntersecting) {
        callbackDisplay();
      } else {
        callbackNoDisplay();
      }
    }, options);
  });
  observer.observe(elm);
}

function observerOverflow(_ref) {
  var elm = _ref.elm,
      options = _ref.options,
      detectOverflow = _ref.detectOverflow,
      callbackOverflow = _ref.callbackOverflow;
  var observer = new IntersectionObserver(function (entries) {
    entries.forEach(function (entry) {
      if (detectOverflow({
        entry: entry
      })) {
        callbackOverflow({
          entry: entry,
          observer: observer
        });
      }
    }, options);
  });
  observer.observe(elm);
}

function observerSticky(_ref) {
  var fake_elm = _ref.fake_elm,
      _ref$fake_bottom = _ref.fake_bottom,
      fake_bottom = _ref$fake_bottom === void 0 ? 0 : _ref$fake_bottom,
      options = _ref.options,
      callbackOver = _ref.callbackOver,
      callbackNotOver = _ref.callbackNotOver;
  var observer = new IntersectionObserver(function (entries) {
    entries.forEach(function (entry) {
      if (entry.boundingClientRect.bottom <= fake_bottom) {
        callbackOver();
      } else {
        callbackNotOver();
      }
    }, options);
  });
  observer.observe(fake_elm);
  return observer;
}

function observeToDo(_ref) {
  var elm = _ref.elm,
      _ref$when_over = _ref.when_over,
      when_over = _ref$when_over === void 0 ? false : _ref$when_over,
      callback = _ref.callback,
      _ref$options = _ref.options,
      options = _ref$options === void 0 ? {
    root: null,
    rootMargin: "500px",
    threshold: 0
  } : _ref$options;
  var observer = new IntersectionObserver(function (entries, observer) {
    entries.forEach(function (entry) {
      if (entry.isIntersecting || when_over && entry.boundingClientRect.bottom <= 0) {
        callback();
        observer.unobserve(entry.target);
      }
    });
  }, options);
  observer.observe(elm);
}


//# sourceMappingURL=index.modern.js.map


/***/ }),

/***/ "./node_modules/react-observer-ts/node_modules/react-commons-ts/dist/index.modern.js":
/*!*******************************************************************************************!*\
  !*** ./node_modules/react-observer-ts/node_modules/react-commons-ts/dist/index.modern.js ***!
  \*******************************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "common_constant": () => (/* binding */ constant),
/* harmony export */   "common_types": () => (/* binding */ _common),
/* harmony export */   "formatLocalDateString": () => (/* binding */ formatLocalDateString),
/* harmony export */   "formatLocalDateTimeString": () => (/* binding */ formatLocalDateTimeString),
/* harmony export */   "formatNum": () => (/* binding */ formatNum),
/* harmony export */   "getClassModuleCss": () => (/* binding */ getClassModuleCss),
/* harmony export */   "getTimeAndUnit": () => (/* binding */ getTimeAndUnit),
/* harmony export */   "toggleDataset": () => (/* binding */ toggleDataset),
/* harmony export */   "unitNumber": () => (/* binding */ unitNumber),
/* harmony export */   "unitTime": () => (/* binding */ unitTime),
/* harmony export */   "unitTimeJustNow": () => (/* binding */ unitTimeJustNow),
/* harmony export */   "useBool": () => (/* binding */ useBool),
/* harmony export */   "useDisplayBlockToNone": () => (/* binding */ useDisplayBlockToNone),
/* harmony export */   "useForceUpdate": () => (/* binding */ useForceUpdate),
/* harmony export */   "useHold": () => (/* binding */ useHold),
/* harmony export */   "useMounted": () => (/* binding */ useMounted),
/* harmony export */   "useMouseDownToWindowUp": () => (/* binding */ useMouseDownToWindowUp),
/* harmony export */   "useToggleDataset": () => (/* binding */ useToggleDataset),
/* harmony export */   "waitingToDoLast": () => (/* binding */ waitingToDoLast)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "./node_modules/react/index.js");


var IS_MOBILE = navigator.userAgent.toUpperCase().search('MOBILE') >= 0;

var constant = {
    __proto__: null,
    IS_MOBILE: IS_MOBILE
};



var _common = {
    __proto__: null
};

var useBool = function useBool(initial_bool) {
  if (initial_bool === void 0) {
    initial_bool = false;
  }

  var _useState = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(initial_bool),
      is_true = _useState[0],
      setIsTrue = _useState[1];

  function toggleBool() {
    setIsTrue(function (is_true) {
      return !is_true;
    });
  }

  return {
    is_true: is_true,
    setIsTrue: setIsTrue,
    toggleBool: toggleBool
  };
};

function useMounted() {
  var _useState = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(true),
      mounted = _useState[0],
      setMounted = _useState[1];

  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(function () {
    return function () {
      setMounted(false);
    };
  }, []);
  return mounted;
}

function useHold(_ref) {
  var _ref$time = _ref.time,
      time = _ref$time === void 0 ? 500 : _ref$time,
      _ref$use_holding = _ref.use_holding,
      use_holding = _ref$use_holding === void 0 ? false : _ref$use_holding,
      _ref$time_holding_sta = _ref.time_holding_start,
      time_holding_start = _ref$time_holding_sta === void 0 ? 100 : _ref$time_holding_sta;

  var _useState = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(false),
      holding = _useState[0],
      setHolding = _useState[1];

  var timeout = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  var timeout_holding = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  var mounted = useMounted();

  function StartHold(callback) {
    if (callback === void 0) {
      callback = function callback() {};
    }

    timeout_holding.current = setTimeout(function () {
      if (!mounted) {
        return;
      }

      use_holding && setHolding(true);
      handleHolding(callback);
    }, time_holding_start);
  }

  function handleHolding(callback) {
    if (callback === void 0) {
      callback = function callback() {};
    }

    timeout.current = setTimeout(function () {
      if (!mounted) {
        return;
      }

      use_holding && setHolding(false);
      timeout_holding.current && clearTimeout(timeout_holding.current);
      callback();
    }, time);
  }

  function StopHold() {
    use_holding && setHolding(false);
    timeout_holding.current && clearTimeout(timeout_holding.current);
    timeout.current && clearTimeout(timeout.current);
  }

  return {
    holding: holding,
    StartHold: StartHold,
    StopHold: StopHold
  };
}

function useForceUpdate() {
  var _useState = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(false),
      setForceUpdate = _useState[1];

  function forceUpdate() {
    setForceUpdate(function (force_update) {
      return !force_update;
    });
  }

  return forceUpdate;
}

function useDisplayBlockToNone(_ref) {
  var _ref$trans_time = _ref.trans_time,
      trans_time = _ref$trans_time === void 0 ? 250 : _ref$trans_time,
      _ref$initial_display_ = _ref.initial_display_none,
      initial_display_none = _ref$initial_display_ === void 0 ? true : _ref$initial_display_;

  var _useState = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(initial_display_none),
      opacity_0 = _useState[0],
      setOpacity0 = _useState[1];

  var _useState2 = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(initial_display_none),
      display_none = _useState2[0],
      setDisplayNone = _useState2[1];

  var ref_interval_to_none = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  var ref_interval_to_block = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);

  function changeBlockToNone() {
    setOpacity0(true);
    ref_interval_to_none.current = setTimeout(function () {
      setDisplayNone(true);
    }, trans_time);
  }

  function changeNoneToBlock() {
    setDisplayNone(false);
    ref_interval_to_block.current = setTimeout(function () {
      setOpacity0(false);
    }, 0);
  }

  function toggleBlockNone() {
    if (ref_interval_to_none.current) {
      clearTimeout(ref_interval_to_none.current);
      ref_interval_to_none.current = null;
      changeNoneToBlock();
      return;
    }

    if (ref_interval_to_block.current) {
      clearTimeout(ref_interval_to_block.current);
      ref_interval_to_block.current = null;
      changeBlockToNone();
      return;
    }

    if (opacity_0) {
      changeNoneToBlock();
    } else {
      changeBlockToNone();
    }
  }

  return {
    opacity_0: opacity_0,
    display_none: display_none,
    changeNoneToBlock: changeNoneToBlock,
    changeBlockToNone: changeBlockToNone,
    toggleBlockNone: toggleBlockNone
  };
}

function toggleDataset(_ref) {
  var elm = _ref.elm,
      attr_str = _ref.attr_str,
      is_remove = _ref.is_remove;
  var value = parseInt(elm.getAttribute(attr_str) || '0');

  if (!is_remove) {
    elm.setAttribute(attr_str, (value + 1).toString());
  } else {
    if (value <= 1) {
      elm.removeAttribute(attr_str);
    } else {
      elm.setAttribute(attr_str, (value - 1).toString());
    }
  }
}

function useToggleDataset(_ref) {
  var elm = _ref.elm,
      attr_str = _ref.attr_str,
      is_remove = _ref.is_remove;
  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(function () {
    toggleDataset({
      elm: elm,
      attr_str: attr_str,
      is_remove: is_remove
    });
    return function () {
      toggleDataset({
        elm: elm,
        attr_str: attr_str,
        is_remove: true
      });
    };
  }, [is_remove]);
}

function detectNoTouch(e) {
  return e.touches.length == 0;
}

function detectHasTouch(e) {
  return e.touches.length > 0;
}

function useMouseDownToWindowUp(_ref) {
  var _ref$handleDown = _ref.handleDown,
      handleDown = _ref$handleDown === void 0 ? function () {} : _ref$handleDown,
      _ref$handleMove = _ref.handleMove,
      handleMove = _ref$handleMove === void 0 ? function () {} : _ref$handleMove,
      _ref$handleEnd = _ref.handleEnd,
      handleEnd = _ref$handleEnd === void 0 ? function () {} : _ref$handleEnd,
      _ref$detectEndTouch = _ref.detectEndTouch,
      detectEndTouch = _ref$detectEndTouch === void 0 ? detectNoTouch : _ref$detectEndTouch,
      _ref$detectStartTouch = _ref.detectStartTouch,
      detectStartTouch = _ref$detectStartTouch === void 0 ? detectHasTouch : _ref$detectStartTouch;
  var ref_func_move = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  var ref_func_up = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);

  function onDown(e) {
    handleDown(e);

    if (!ref_func_move.current) {
      ref_func_move.current = onMove;
    }

    if (!ref_func_up.current) {
      ref_func_up.current = onUp;
    }

    if (e instanceof TouchEvent) {
      if (detectStartTouch(e)) {
        window.addEventListener('touchmove', ref_func_move.current);
        window.addEventListener('touchend', ref_func_up.current);
      }
    } else {
      window.addEventListener('mousemove', ref_func_move.current);
      window.addEventListener('mouseup', ref_func_up.current);
    }
  }

  function onMove(e) {
    handleMove(e);
  }

  function onUp(e) {
    handleEnd(e);

    if (e instanceof TouchEvent) {
      if (detectEndTouch(e)) {
        ref_func_move.current && window.removeEventListener('touchmove', ref_func_move.current);
        ref_func_up.current && window.removeEventListener('touchend', ref_func_up.current);
      }
    } else {
      ref_func_move.current && window.removeEventListener('mousemove', ref_func_move.current);
      ref_func_up.current && window.removeEventListener('mouseup', ref_func_up.current);
    }

    ref_func_move.current = null;
    ref_func_up.current = null;
  }

  return {
    onDown: onDown
  };
}

function formatLocalDateString(_ref) {
  var _ref$date = _ref.date,
      date = _ref$date === void 0 ? new Date() : _ref$date,
      _ref$locales = _ref.locales,
      locales = _ref$locales === void 0 ? 'vi-VN' : _ref$locales;
  return new Intl.DateTimeFormat(locales, {
    day: '2-digit',
    month: '2-digit',
    year: 'numeric'
  }).format(new Date(date));
}
function formatLocalDateTimeString(_ref2) {
  var _ref2$date = _ref2.date,
      date = _ref2$date === void 0 ? new Date() : _ref2$date,
      _ref2$locales = _ref2.locales,
      locales = _ref2$locales === void 0 ? 'vi-VN' : _ref2$locales;
  return new Intl.DateTimeFormat(locales, {
    hour: '2-digit',
    minute: '2-digit',
    second: '2-digit',
    day: '2-digit',
    month: '2-digit',
    year: 'numeric'
  }).format(new Date(date));
}

var DATA_UNIT_LONG_ARR = [' year', ' month', ' day', ' hour', ' minute', ' second'];
function getTimeAndUnit(_ref) {
  var _ref$time_change = _ref.time_change,
      time_change = _ref$time_change === void 0 ? 0 : _ref$time_change,
      _ref$unit_arr = _ref.unit_arr,
      unit_arr = _ref$unit_arr === void 0 ? DATA_UNIT_LONG_ARR : _ref$unit_arr;

  if (time_change >= 31536000000) {
    return {
      time: Math.floor(time_change / 31536000000),
      unit: unit_arr[0]
    };
  }

  if (time_change >= 2592000000) {
    return {
      time: Math.floor(time_change / 2592000000),
      unit: unit_arr[1]
    };
  }

  if (time_change >= 86400000) {
    return {
      time: Math.floor(time_change / 86400000),
      unit: unit_arr[2]
    };
  }

  if (time_change >= 3600000) {
    return {
      time: Math.floor(time_change / 3600000),
      unit: unit_arr[3]
    };
  }

  if (time_change >= 60000) {
    return {
      time: Math.floor(time_change / 3600000),
      unit: unit_arr[4]
    };
  }

  return {
    time: Math.floor(time_change / 1000),
    unit: unit_arr[5]
  };
}
function unitTime(_ref2) {
  var _ref2$time_change = _ref2.time_change,
      time_change = _ref2$time_change === void 0 ? 0 : _ref2$time_change,
      _ref2$unit_arr = _ref2.unit_arr,
      unit_arr = _ref2$unit_arr === void 0 ? DATA_UNIT_LONG_ARR : _ref2$unit_arr;
  var unit_and_time = getTimeAndUnit({
    time_change: time_change,
    unit_arr: unit_arr
  });
  return "" + unit_and_time.time + unit_and_time.unit;
}
function unitTimeHasSpecial(_ref3) {
  var _ref3$time_change = _ref3.time_change,
      time_change = _ref3$time_change === void 0 ? 0 : _ref3$time_change,
      _ref3$unit_arr = _ref3.unit_arr,
      unit_arr = _ref3$unit_arr === void 0 ? DATA_UNIT_LONG_ARR : _ref3$unit_arr,
      _ref3$detectIsSpecial = _ref3.detectIsSpecial,
      detectIsSpecial = _ref3$detectIsSpecial === void 0 ? function () {
    return false;
  } : _ref3$detectIsSpecial,
      _ref3$getUnitTimeSpec = _ref3.getUnitTimeSpecial,
      getUnitTimeSpecial = _ref3$getUnitTimeSpec === void 0 ? function () {
    return '';
  } : _ref3$getUnitTimeSpec;

  if (detectIsSpecial()) {
    return getUnitTimeSpecial();
  }

  return unitTime({
    time_change: time_change,
    unit_arr: unit_arr
  });
}
function unitTimeJustNow(_ref4) {
  var _ref4$time_change = _ref4.time_change,
      time_change = _ref4$time_change === void 0 ? 0 : _ref4$time_change,
      _ref4$unit_arr = _ref4.unit_arr,
      unit_arr = _ref4$unit_arr === void 0 ? DATA_UNIT_LONG_ARR : _ref4$unit_arr;
  unitTimeHasSpecial({
    time_change: time_change,
    unit_arr: unit_arr,
    detectIsSpecial: function detectIsSpecial() {
      return time_change <= 60;
    },
    getUnitTimeSpecial: function getUnitTimeSpecial() {
      return 'Just now';
    }
  });
}

function formatNum(num) {
  if (num === void 0) {
    num = 0;
  }

  return new Intl.NumberFormat('en').format(num);
}

function unitNumber(_ref) {
  var _ref$num = _ref.num,
      num = _ref$num === void 0 ? 0 : _ref$num,
      _ref$unit_arr = _ref.unit_arr,
      unit_arr = _ref$unit_arr === void 0 ? ['K', 'M', 'B'] : _ref$unit_arr;

  if (num >= 1000000000) {
    return "" + Math.floor(num / 100000000) / 10 + unit_arr[2];
  }

  if (num >= 1000000) {
    return "" + Math.floor(num / 100000) / 10 + unit_arr[1];
  }

  if (num >= 1000) {
    return "" + Math.floor(num / 100) / 10 + unit_arr[0];
  }

  return num;
}

function waitingToDoLast(_ref) {
  var ref_interval = _ref.ref_interval,
      _ref$time = _ref.time,
      time = _ref$time === void 0 ? 200 : _ref$time,
      _ref$callback = _ref.callback,
      callback = _ref$callback === void 0 ? function () {} : _ref$callback;
  ref_interval.current && clearTimeout(ref_interval.current);
  ref_interval.current = setTimeout(function () {
    ref_interval.current && clearTimeout(ref_interval.current);
    callback();
  }, time);
}

function getClassModuleCss(_ref) {
  var _ref$className = _ref.className,
      className = _ref$className === void 0 ? '' : _ref$className,
      styles = _ref.styles;
  return className + " " + (styles[className] || '');
}


//# sourceMappingURL=index.modern.js.map


/***/ }),

/***/ "./node_modules/react-range-slider-ts/dist/index.modern.js":
/*!*****************************************************************!*\
  !*** ./node_modules/react-range-slider-ts/dist/index.modern.js ***!
  \*****************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "OneSlider": () => (/* binding */ OneSlider),
/* harmony export */   "OneSliderElm": () => (/* binding */ OneSliderElm),
/* harmony export */   "TwoSliders": () => (/* binding */ TwoSliders),
/* harmony export */   "TwoSlidersElm": () => (/* binding */ TwoSlidersElm),
/* harmony export */   "useOneSlider": () => (/* binding */ useOneSlider),
/* harmony export */   "useTwoSlider": () => (/* binding */ useTwoSlider)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "./node_modules/react/index.js");
/* harmony import */ var react_commons_ts__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! react-commons-ts */ "./node_modules/react-commons-ts/dist/index.modern.js");



function getClassModuleCss(className, styles) {
  if (className === void 0) {
    className = '';
  }

  return className + " " + (styles[className] || '');
}

var OneSliderElmStyles = {"OneSliderElm":"_FHxWW","OneSliderElm_range":"_29f73","OneSliderElm_range_active":"_38pIP","OneSliderElm_slider":"_1nNAP"};

function _getClassModuleCss(className) {
  if (className === void 0) {
    className = '';
  }

  return getClassModuleCss(className, OneSliderElmStyles);
}

function OneSliderElm(_ref) {
  var ref_range_elm = _ref.ref_range_elm,
      is_run = _ref.is_run,
      _ref$only_drag_slider = _ref.only_drag_slider,
      only_drag_slider = _ref$only_drag_slider === void 0 ? false : _ref$only_drag_slider,
      range = _ref.range,
      active_range = _ref.active_range,
      slider = _ref.slider,
      value = _ref.value,
      handleClick = _ref.handleClick,
      handleStart = _ref.handleStart;
  return react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    ref: ref_range_elm,
    className: _getClassModuleCss('OneSliderElm') + " " + (is_run ? 'input-range-running' : ''),
    onMouseDown: !only_drag_slider && !react_commons_ts__WEBPACK_IMPORTED_MODULE_1__.common_constant.IS_MOBILE ? handleStart : undefined,
    onTouchStart: !only_drag_slider && react_commons_ts__WEBPACK_IMPORTED_MODULE_1__.common_constant.IS_MOBILE ? handleStart : undefined
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss('OneSliderElm_range'),
    onClick: handleClick
  }, range), react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss('OneSliderElm_range_active'),
    style: {
      width: value + "%"
    }
  }, active_range), react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss('OneSliderElm_slider'),
    style: {
      left: value + "%"
    },
    onMouseDown: only_drag_slider && !react_commons_ts__WEBPACK_IMPORTED_MODULE_1__.common_constant.IS_MOBILE ? handleStart : undefined,
    onTouchStart: only_drag_slider && react_commons_ts__WEBPACK_IMPORTED_MODULE_1__.common_constant.IS_MOBILE ? handleStart : undefined
  }, slider));
}

function _extends() {
  _extends = Object.assign || function (target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];

      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }

    return target;
  };

  return _extends.apply(this, arguments);
}

function getClientXY(e) {
  return e instanceof TouchEvent ? {
    client_x: e.touches[0].clientX,
    client_y: e.touches[0].clientY
  } : {
    client_x: e.clientX,
    client_y: e.clientY
  };
}

function getTruePercent(new_percent) {
  return new_percent >= 1 ? 1 : new_percent <= 0 ? 0 : new_percent;
}

function getSliderPercentX(_ref) {
  var _ref$x_range_start = _ref.x_range_start,
      x_range_start = _ref$x_range_start === void 0 ? 0 : _ref$x_range_start,
      _ref$x_range_end = _ref.x_range_end,
      x_range_end = _ref$x_range_end === void 0 ? 0 : _ref$x_range_end,
      _ref$clientX = _ref.clientX,
      clientX = _ref$clientX === void 0 ? 0 : _ref$clientX;
  var new_percent = (clientX - x_range_start) / (x_range_end - x_range_start);
  return getTruePercent(new_percent);
}

function getSliderPercentY(_ref2) {
  var _ref2$y_range_start = _ref2.y_range_start,
      y_range_start = _ref2$y_range_start === void 0 ? 0 : _ref2$y_range_start,
      _ref2$y_range_end = _ref2.y_range_end,
      y_range_end = _ref2$y_range_end === void 0 ? 0 : _ref2$y_range_end,
      _ref2$clientY = _ref2.clientY,
      clientY = _ref2$clientY === void 0 ? 0 : _ref2$clientY;
  var new_percent = (clientY - y_range_start) / (y_range_end - y_range_start);
  return getTruePercent(new_percent);
}

function getSliderPercent(_ref3) {
  var _ref3$range_angel = _ref3.range_angel,
      range_angel = _ref3$range_angel === void 0 ? 0 : _ref3$range_angel,
      _ref3$x_range_start = _ref3.x_range_start,
      x_range_start = _ref3$x_range_start === void 0 ? 0 : _ref3$x_range_start,
      _ref3$y_range_start = _ref3.y_range_start,
      y_range_start = _ref3$y_range_start === void 0 ? 0 : _ref3$y_range_start,
      _ref3$x_range_end = _ref3.x_range_end,
      x_range_end = _ref3$x_range_end === void 0 ? 0 : _ref3$x_range_end,
      _ref3$y_range_end = _ref3.y_range_end,
      y_range_end = _ref3$y_range_end === void 0 ? 0 : _ref3$y_range_end,
      _ref3$clientX = _ref3.clientX,
      clientX = _ref3$clientX === void 0 ? 0 : _ref3$clientX,
      _ref3$clientY = _ref3.clientY,
      clientY = _ref3$clientY === void 0 ? 0 : _ref3$clientY;

  if (range_angel == 0) {
    return getSliderPercentX({
      x_range_start: x_range_start,
      x_range_end: x_range_end,
      clientX: clientX
    });
  }

  if (range_angel == 90) {
    return getSliderPercentY({
      y_range_start: y_range_start,
      y_range_end: y_range_end,
      clientY: clientY
    });
  }

  return 0;
}

function refGetBoundingClientRect(ref_elm) {
  return (ref_elm.current ? ref_elm.current : document.getElementsByTagName('div')[0]).getBoundingClientRect();
}

function useOneSlider(_ref) {
  var ref_range_elm = _ref.ref_range_elm,
      _ref$ref_has_change_r = _ref.ref_has_change_range,
      ref_has_change_range = _ref$ref_has_change_r === void 0 ? {
    current: false
  } : _ref$ref_has_change_r,
      _ref$ref_is_run = _ref.ref_is_run,
      ref_is_run = _ref$ref_is_run === void 0 ? {
    current: false
  } : _ref$ref_is_run,
      _ref$getRangeAngel = _ref.getRangeAngel,
      getRangeAngel = _ref$getRangeAngel === void 0 ? function () {
    return 0;
  } : _ref$getRangeAngel,
      _ref$handleChange = _ref.handleChange,
      handleChange = _ref$handleChange === void 0 ? function () {} : _ref$handleChange,
      _ref$afterMouseDown = _ref.afterMouseDown,
      afterMouseDown = _ref$afterMouseDown === void 0 ? function () {} : _ref$afterMouseDown,
      _ref$afterMousemove = _ref.afterMousemove,
      afterMousemove = _ref$afterMousemove === void 0 ? function () {} : _ref$afterMousemove,
      _ref$afterMouseUp = _ref.afterMouseUp,
      afterMouseUp = _ref$afterMouseUp === void 0 ? function () {} : _ref$afterMouseUp;
  var ref_range_obj = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)({
    range_angel: 0,
    x_range_start: 0,
    y_range_start: 0,
    x_range_end: 0,
    y_range_end: 0
  });
  var forceUpdate = (0,react_commons_ts__WEBPACK_IMPORTED_MODULE_1__.useForceUpdate)();

  var _useMouseDownToWindow = (0,react_commons_ts__WEBPACK_IMPORTED_MODULE_1__.useMouseDownToWindowUp)({
    handleDown: onMouseDown,
    handleMove: onMouseMove,
    handleEnd: onMouseEnd
  }),
      onDown = _useMouseDownToWindow.onDown;

  function changeRangeObj() {
    if (ref_has_change_range.current) {
      return;
    }

    ref_has_change_range.current = true;

    var _refGetBoundingClient = refGetBoundingClientRect(ref_range_elm),
        top = _refGetBoundingClient.top,
        left = _refGetBoundingClient.left,
        right = _refGetBoundingClient.right,
        bottom = _refGetBoundingClient.bottom;

    var new_range_angel = getRangeAngel();

    if (new_range_angel == 0) {
      ref_range_obj.current = _extends({}, ref_range_obj.current, {
        range_angel: 0,
        x_range_start: left,
        x_range_end: right
      });
    } else if (new_range_angel == -90) {
      ref_range_obj.current = _extends({}, ref_range_obj.current, {
        range_angel: 90,
        y_range_start: bottom,
        y_range_end: top
      });
    }
  }

  function onChange(_ref2) {
    var clientX = _ref2.clientX,
        clientY = _ref2.clientY;
    var new_percent = getSliderPercent(_extends({}, ref_range_obj.current, {
      clientX: clientX,
      clientY: clientY
    }));
    handleChange(new_percent * 100);
  }

  function onMouseDown(e) {
    if (!ref_has_change_range.current) {
      changeRangeObj();
    }

    var _getClientXY = getClientXY(e),
        client_x = _getClientXY.client_x,
        client_y = _getClientXY.client_y;

    onChange({
      clientX: client_x,
      clientY: client_y
    });
    ref_is_run.current = true;
    forceUpdate();
    afterMouseDown();
  }

  function onMouseMove(e) {
    if (ref_is_run.current) {
      var _getClientXY2 = getClientXY(e),
          client_x = _getClientXY2.client_x,
          client_y = _getClientXY2.client_y;

      onChange({
        clientX: client_x,
        clientY: client_y
      });
      afterMousemove();
    }
  }

  function onMouseEnd() {
    ref_is_run.current = false;
    forceUpdate();
    afterMouseUp();
  }

  function handleClick(e) {
    if (!ref_has_change_range.current) {
      changeRangeObj();
    }

    onChange({
      clientX: e.clientX,
      clientY: e.clientY
    });
  }

  return {
    ref_range_obj: ref_range_obj,
    onDown: onDown,
    handleClick: handleClick
  };
}

function OneSlider(_ref) {
  var range = _ref.range,
      active_range = _ref.active_range,
      slider = _ref.slider,
      value = _ref.value,
      only_drag_slider = _ref.only_drag_slider,
      _ref$ref_has_change_r = _ref.ref_has_change_range,
      ref_has_change_range = _ref$ref_has_change_r === void 0 ? {
    current: false
  } : _ref$ref_has_change_r,
      _ref$ref_is_run = _ref.ref_is_run,
      ref_is_run = _ref$ref_is_run === void 0 ? {
    current: false
  } : _ref$ref_is_run,
      getRangeAngel = _ref.getRangeAngel,
      handleChange = _ref.handleChange,
      _ref$afterMouseDown = _ref.afterMouseDown,
      afterMouseDown = _ref$afterMouseDown === void 0 ? function () {} : _ref$afterMouseDown,
      _ref$afterMousemove = _ref.afterMousemove,
      afterMousemove = _ref$afterMousemove === void 0 ? function () {} : _ref$afterMousemove,
      _ref$afterMouseUp = _ref.afterMouseUp,
      afterMouseUp = _ref$afterMouseUp === void 0 ? function () {} : _ref$afterMouseUp;
  var ref_range_elm = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);

  var _useOneSlider = useOneSlider({
    ref_range_elm: ref_range_elm,
    ref_has_change_range: ref_has_change_range,
    ref_is_run: ref_is_run,
    getRangeAngel: getRangeAngel,
    handleChange: handleChange,
    afterMouseDown: afterMouseDown,
    afterMousemove: afterMousemove,
    afterMouseUp: afterMouseUp
  }),
      onDown = _useOneSlider.onDown,
      handleClick = _useOneSlider.handleClick;

  var _handleClick = function _handleClick(e) {
    handleClick(e.nativeEvent);
  };

  var _onDown = function _onDown(e) {
    onDown(e.nativeEvent);
  };

  return react__WEBPACK_IMPORTED_MODULE_0__.createElement(OneSliderElm, {
    ref_range_elm: ref_range_elm,
    is_run: ref_is_run.current,
    only_drag_slider: only_drag_slider,
    range: range,
    active_range: active_range,
    slider: slider,
    value: value,
    handleClick: _handleClick,
    handleStart: _onDown
  });
}

var TwoSlidersElmStyles = {"TwoSlidersElm-not-run":"_1bNv5","TwoSlidersElm_range-active":"_1fq1o","TwoSlidersElm_slider":"_1DbHP","TwoSlidersElm":"_tGccK","TwoSlidersElm_range":"_2nVMZ","TwoSlidersElm_range_active":"_12Rcd"};

function _getClassModuleCss$1(className) {
  if (className === void 0) {
    className = '';
  }

  return getClassModuleCss(className, TwoSlidersElmStyles);
}

function TwoSlidersElm(_ref) {
  var ref_range_elm = _ref.ref_range_elm,
      slider_num = _ref.slider_num,
      range = _ref.range,
      active_range = _ref.active_range,
      slider1 = _ref.slider1,
      slider2 = _ref.slider2,
      value1 = _ref.value1,
      value2 = _ref.value2,
      handleMouseDown = _ref.handleMouseDown;
  return react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    ref: ref_range_elm,
    className: _getClassModuleCss$1('TwoSlidersElm') + " " + _getClassModuleCss$1(slider_num == 1 ? 'TwoSlidersElm-1-run' : slider_num == 2 ? 'TwoSlidersElm-2-run' : 'TwoSlidersElm-not-run'),
    onMouseDown: react_commons_ts__WEBPACK_IMPORTED_MODULE_1__.common_constant.IS_MOBILE ? undefined : handleMouseDown,
    onTouchStart: react_commons_ts__WEBPACK_IMPORTED_MODULE_1__.common_constant.IS_MOBILE ? handleMouseDown : undefined
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$1('TwoSlidersElm_range')
  }, range), react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$1('TwoSlidersElm_range_active'),
    style: {
      left: value1 + "%",
      width: value2 - value1 + "%"
    }
  }, active_range), react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$1('TwoSlidersElm_slider'),
    style: {
      left: value1 + "%"
    }
  }, slider1), react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$1('TwoSlidersElm_slider'),
    style: {
      left: value2 + "%"
    }
  }, slider2));
}

function useTwoSlider(_ref) {
  var ref_range_elm = _ref.ref_range_elm,
      _ref$ref_has_change_r = _ref.ref_has_change_range,
      ref_has_change_range = _ref$ref_has_change_r === void 0 ? {
    current: false
  } : _ref$ref_has_change_r,
      _ref$ref_slider_num_r = _ref.ref_slider_num_run,
      ref_slider_num_run = _ref$ref_slider_num_r === void 0 ? {
    current: -1
  } : _ref$ref_slider_num_r,
      value1 = _ref.value1,
      value2 = _ref.value2,
      _ref$getRangeAngel = _ref.getRangeAngel,
      getRangeAngel = _ref$getRangeAngel === void 0 ? function () {
    return 0;
  } : _ref$getRangeAngel,
      _ref$handleChangeSlid = _ref.handleChangeSlider1,
      handleChangeSlider1 = _ref$handleChangeSlid === void 0 ? function () {} : _ref$handleChangeSlid,
      _ref$handleChangeSlid2 = _ref.handleChangeSlider2,
      handleChangeSlider2 = _ref$handleChangeSlid2 === void 0 ? function () {} : _ref$handleChangeSlid2,
      _ref$afterMouseDown = _ref.afterMouseDown,
      afterMouseDown = _ref$afterMouseDown === void 0 ? function () {} : _ref$afterMouseDown,
      _ref$afterMousemove = _ref.afterMousemove,
      afterMousemove = _ref$afterMousemove === void 0 ? function () {} : _ref$afterMousemove,
      _ref$afterMouseUp = _ref.afterMouseUp,
      afterMouseUp = _ref$afterMouseUp === void 0 ? function () {} : _ref$afterMouseUp;
  var ref_value1 = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(value1);
  var ref_value2 = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(value2);

  var _useOneSlider = useOneSlider({
    ref_range_elm: ref_range_elm,
    ref_has_change_range: ref_has_change_range,
    handleChange: handleChange,
    getRangeAngel: getRangeAngel,
    afterMouseDown: _afterMouseDown,
    afterMouseUp: _afterMouseUp,
    afterMousemove: afterMousemove
  }),
      onDown = _useOneSlider.onDown;

  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(function () {
    if (ref_slider_num_run.current == -1) {
      ref_value1.current = value1;
    }
  }, [value1]);
  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(function () {
    if (ref_slider_num_run.current == -1) {
      ref_value2.current = value2;
    }
  }, [value2]);

  function _afterMouseDown() {
    afterMouseDown();
  }

  function _afterMouseUp() {
    ref_slider_num_run.current = -1;
    afterMouseUp();
  }

  function handleChange(new_percent) {
    if (Math.abs(ref_value1.current - new_percent) <= Math.abs(ref_value2.current - new_percent)) {
      ref_value1.current = new_percent;
      ref_slider_num_run.current = 1;
      handleChangeSlider1(new_percent);
    } else {
      ref_value2.current = new_percent;
      ref_slider_num_run.current = 2;
      handleChangeSlider2(new_percent);
    }
  }

  return {
    onDown: onDown
  };
}

function TwoSliders(_ref) {
  var range = _ref.range,
      active_range = _ref.active_range,
      slider1 = _ref.slider1,
      slider2 = _ref.slider2,
      _ref$ref_slider_num_r = _ref.ref_slider_num_run,
      ref_slider_num_run = _ref$ref_slider_num_r === void 0 ? {
    current: -1
  } : _ref$ref_slider_num_r,
      _ref$ref_has_change_r = _ref.ref_has_change_range,
      ref_has_change_range = _ref$ref_has_change_r === void 0 ? {
    current: false
  } : _ref$ref_has_change_r,
      value1 = _ref.value1,
      value2 = _ref.value2,
      getRangeAngel = _ref.getRangeAngel,
      handleChangeSlider1 = _ref.handleChangeSlider1,
      handleChangeSlider2 = _ref.handleChangeSlider2,
      afterMouseDown = _ref.afterMouseDown,
      afterMousemove = _ref.afterMousemove,
      afterMouseUp = _ref.afterMouseUp;
  var ref_range_elm = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);

  var _useTwoSlider = useTwoSlider({
    ref_range_elm: ref_range_elm,
    ref_has_change_range: ref_has_change_range,
    ref_slider_num_run: ref_slider_num_run,
    value1: value1,
    value2: value2,
    getRangeAngel: getRangeAngel,
    handleChangeSlider1: handleChangeSlider1,
    handleChangeSlider2: handleChangeSlider2,
    afterMouseDown: afterMouseDown,
    afterMousemove: afterMousemove,
    afterMouseUp: afterMouseUp
  }),
      onDown = _useTwoSlider.onDown;

  function handleMouseDown(e) {
    onDown(e.nativeEvent);
  }

  return react__WEBPACK_IMPORTED_MODULE_0__.createElement(TwoSlidersElm, {
    ref_range_elm: ref_range_elm,
    slider_num: ref_slider_num_run.current,
    range: range,
    active_range: active_range,
    slider1: slider1,
    slider2: slider2,
    value1: value1,
    value2: value2,
    handleMouseDown: handleMouseDown
  });
}


//# sourceMappingURL=index.modern.js.map


/***/ }),

/***/ "./node_modules/react-video-ts/dist/index.modern.js":
/*!**********************************************************!*\
  !*** ./node_modules/react-video-ts/dist/index.modern.js ***!
  \**********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "Video": () => (/* binding */ Video),
/* harmony export */   "VideoElm": () => (/* binding */ VideoElm),
/* harmony export */   "VideoLive": () => (/* binding */ VideoLive),
/* harmony export */   "VideoLiveElm": () => (/* binding */ VideoLiveElm),
/* harmony export */   "useVideoUtils": () => (/* binding */ useVideoUtils)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "./node_modules/react/index.js");
/* harmony import */ var react_commons_ts__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! react-commons-ts */ "./node_modules/react-video-ts/node_modules/react-commons-ts/dist/index.modern.js");
/* harmony import */ var react_click_outside_ts__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! react-click-outside-ts */ "./node_modules/react-click-outside-ts/dist/index.modern.js");
/* harmony import */ var react_range_slider_ts__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! react-range-slider-ts */ "./node_modules/react-range-slider-ts/dist/index.modern.js");
/* harmony import */ var react_range_slider_ts_dist_index_css__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! react-range-slider-ts/dist/index.css */ "./node_modules/react-range-slider-ts/dist/index.css");






function changeVideoSubtitle(video_elm, track_ix) {
  if (track_ix === void 0) {
    track_ix = -1;
  }

  for (var i = 0; i < video_elm.textTracks.length; i++) {
    video_elm.textTracks[i].mode = 'hidden';
  }

  if (track_ix >= 0) {
    video_elm.textTracks[track_ix].mode = 'showing';
  }
}

function requestFullscreen(_ref) {
  var elm = _ref.elm;

  if (elm.requestFullscreen) {
    elm.requestFullscreen();
  }
}
function exitFullscreen() {
  if (document.fullscreenElement) {
    if (document.exitFullscreen) {
      document.exitFullscreen();
    }
  }
}

function observerDisplay(_ref) {
  var elm = _ref.elm,
      _ref$callbackDisplay = _ref.callbackDisplay,
      callbackDisplay = _ref$callbackDisplay === void 0 ? function () {} : _ref$callbackDisplay,
      _ref$callbackNoDispla = _ref.callbackNoDisplay,
      callbackNoDisplay = _ref$callbackNoDispla === void 0 ? function () {} : _ref$callbackNoDispla,
      _ref$options = _ref.options,
      options = _ref$options === void 0 ? {} : _ref$options;
  var observer = new IntersectionObserver(function (entries) {
    entries.forEach(function (entry) {
      if (entry.isIntersecting) {
        callbackDisplay();
      } else {
        callbackNoDisplay();
      }
    }, options);
  });
  observer.observe(elm);
}

function useHideCursorWhenFullscreen(_ref) {
  var _ref$fullscreen = _ref.fullscreen,
      fullscreen = _ref$fullscreen === void 0 ? false : _ref$fullscreen,
      _ref$use_fullscreen = _ref.use_fullscreen,
      use_fullscreen = _ref$use_fullscreen === void 0 ? false : _ref$use_fullscreen,
      _ref$time_to_hide = _ref.time_to_hide,
      time_to_hide = _ref$time_to_hide === void 0 ? 1500 : _ref$time_to_hide;
  var ref_interval = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  var ref_c_time = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(0);
  var ref_mouse_down = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(false);
  var ref_handle_down = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  var ref_handle_move = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  var ref_handle_up = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  var ref_is_hide_cursor = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(false);
  var forceUpdate = (0,react_commons_ts__WEBPACK_IMPORTED_MODULE_1__.useForceUpdate)();
  (0,react_commons_ts__WEBPACK_IMPORTED_MODULE_1__.useToggleDataset)({
    elm: document.getElementsByTagName('html')[0],
    attr_str: 'data-fullscreen-hide-cursor',
    is_remove: !ref_is_hide_cursor.current
  });
  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(function () {
    if (use_fullscreen) {
      if (fullscreen) {
        addMouseEvent();
      } else {
        removeMouseEvent();
      }
    }
  }, [use_fullscreen && fullscreen]);

  function addMouseEvent() {
    ref_handle_down.current = ref_handle_down.current || handleMouseDown;
    ref_handle_move.current = ref_handle_move.current || handleMouseMove;
    ref_handle_up.current = ref_handle_up.current || handleMouseUp;
    window.addEventListener('mousedown', ref_handle_down.current);
    window.addEventListener('mousemove', ref_handle_move.current);
    window.addEventListener('mouseup', ref_handle_up.current);
    startCountUp();
  }

  function removeMouseEvent() {
    ref_handle_down.current && window.removeEventListener('mousedown', ref_handle_down.current);
    ref_handle_move.current && window.removeEventListener('mousemove', ref_handle_move.current);
    ref_handle_up.current && window.removeEventListener('mouseup', ref_handle_up.current);
    ref_handle_down.current = null;
    ref_handle_move.current = null;
    ref_handle_up.current = null;

    if (ref_interval.current) {
      clearInterval(ref_interval.current);
      ref_interval.current = null;
    }
  }

  function startCountUp() {
    if (ref_interval.current) {
      return;
    }

    ref_interval.current = setInterval(function () {
      ref_c_time.current += 100;

      if (ref_c_time.current == time_to_hide) {
        ref_interval.current && clearInterval(ref_interval.current);
        ref_interval.current = null;
        ref_is_hide_cursor.current = true;
        ref_c_time.current = 0;
        forceUpdate();
      }
    }, 100);
  }

  function handleMouseMove() {
    if (ref_mouse_down.current) {
      return;
    }

    ref_c_time.current = 0;

    if (ref_is_hide_cursor.current) {
      ref_is_hide_cursor.current = false;
      forceUpdate();
    } else {
      startCountUp();
    }
  }

  function handleMouseDown() {
    ref_mouse_down.current = true;
    ref_c_time.current = 0;
    ref_interval.current && clearInterval(ref_interval.current);
    ref_interval.current = null;

    if (ref_is_hide_cursor.current) {
      ref_is_hide_cursor.current = false;
      forceUpdate();
    }
  }

  function handleMouseUp() {
    ref_mouse_down.current = false;
    startCountUp();
  }

  return {
    ref_is_hide_cursor: ref_is_hide_cursor
  };
}

var ref_main_video_default = {
  current: document.getElementsByTagName('div')[0]
};
var ref_video_elm_default = {
  current: document.getElementsByTagName('video')[0]
};
function useVideoUtils(_ref) {
  var _ref$ref_main_video = _ref.ref_main_video,
      ref_main_video = _ref$ref_main_video === void 0 ? ref_main_video_default : _ref$ref_main_video,
      _ref$ref_video_elm = _ref.ref_video_elm,
      ref_video_elm = _ref$ref_video_elm === void 0 ? ref_video_elm_default : _ref$ref_video_elm,
      _ref$is_live = _ref.is_live,
      is_live = _ref$is_live === void 0 ? false : _ref$is_live,
      _ref$stop_when_over = _ref.stop_when_over,
      stop_when_over = _ref$stop_when_over === void 0 ? true : _ref$stop_when_over,
      _ref$use_fullscreen = _ref.use_fullscreen,
      use_fullscreen = _ref$use_fullscreen === void 0 ? false : _ref$use_fullscreen,
      _ref$initial_is_play = _ref.initial_is_play,
      initial_is_play = _ref$initial_is_play === void 0 ? false : _ref$initial_is_play,
      _ref$initial_zoom_lv = _ref.initial_zoom_lv,
      initial_zoom_lv = _ref$initial_zoom_lv === void 0 ? 0 : _ref$initial_zoom_lv,
      _ref$max_zoom_lv = _ref.max_zoom_lv,
      max_zoom_lv = _ref$max_zoom_lv === void 0 ? 0 : _ref$max_zoom_lv,
      _ref$initial_volume = _ref.initial_volume,
      initial_volume = _ref$initial_volume === void 0 ? 0.5 : _ref$initial_volume,
      _ref$initial_is_mute = _ref.initial_is_mute,
      initial_is_mute = _ref$initial_is_mute === void 0 ? true : _ref$initial_is_mute,
      _ref$initial_c_time = _ref.initial_c_time,
      initial_c_time = _ref$initial_c_time === void 0 ? 0 : _ref$initial_c_time,
      _ref$initial_total_ti = _ref.initial_total_time,
      initial_total_time = _ref$initial_total_ti === void 0 ? 0 : _ref$initial_total_ti,
      _ref$beforeTogglePlay = _ref.beforeTogglePlay,
      beforeTogglePlay = _ref$beforeTogglePlay === void 0 ? function () {} : _ref$beforeTogglePlay,
      _ref$afterTogglePlay = _ref.afterTogglePlay,
      afterTogglePlay = _ref$afterTogglePlay === void 0 ? function () {} : _ref$afterTogglePlay,
      _ref$beforeChangeVolu = _ref.beforeChangeVolume,
      beforeChangeVolume = _ref$beforeChangeVolu === void 0 ? function () {} : _ref$beforeChangeVolu,
      _ref$afterChangeVolum = _ref.afterChangeVolume,
      afterChangeVolume = _ref$afterChangeVolum === void 0 ? function () {} : _ref$afterChangeVolum,
      _ref$beforeToggleMute = _ref.beforeToggleMute,
      beforeToggleMute = _ref$beforeToggleMute === void 0 ? function () {} : _ref$beforeToggleMute,
      _ref$afterToggleMute = _ref.afterToggleMute,
      afterToggleMute = _ref$afterToggleMute === void 0 ? function () {} : _ref$afterToggleMute,
      _ref$beforeChangeZoom = _ref.beforeChangeZoomLv,
      beforeChangeZoomLv = _ref$beforeChangeZoom === void 0 ? function () {} : _ref$beforeChangeZoom,
      _ref$afterChangeZoomL = _ref.afterChangeZoomLv,
      afterChangeZoomLv = _ref$afterChangeZoomL === void 0 ? function () {} : _ref$afterChangeZoomL,
      _ref$beforeChangeTime = _ref.beforeChangeTime,
      beforeChangeTime = _ref$beforeChangeTime === void 0 ? function () {} : _ref$beforeChangeTime,
      _ref$afterChangeTime = _ref.afterChangeTime,
      afterChangeTime = _ref$afterChangeTime === void 0 ? function () {} : _ref$afterChangeTime,
      _ref$beforeGotoLiveVi = _ref.beforeGotoLiveView,
      beforeGotoLiveView = _ref$beforeGotoLiveVi === void 0 ? function () {} : _ref$beforeGotoLiveVi,
      _ref$afterGotoLiveVie = _ref.afterGotoLiveView,
      afterGotoLiveView = _ref$afterGotoLiveVie === void 0 ? function () {} : _ref$afterGotoLiveVie;
  var ref_is_play = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(initial_is_play);
  var ref_zoom_lv = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(initial_zoom_lv);
  var ref_volume = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(initial_volume);
  var ref_is_mute = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(initial_is_mute);
  var ref_c_time = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(initial_c_time);
  var ref_total_time = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(initial_total_time);
  var ref_buffer_time = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(0);
  var ref_holding_slider = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(false);
  var ref_is_over = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(false);
  var ref_open_setting = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(false);
  var ref_setting_name = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)('home');
  var ref_speed = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(1);
  var ref_custom_speed = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(1);
  var ref_has_custom_speed = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(false);
  var ref_chosen_custom_speed = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(false);
  var is_zoom_max = ref_zoom_lv.current == max_zoom_lv;
  var forceUpdate = (0,react_commons_ts__WEBPACK_IMPORTED_MODULE_1__.useForceUpdate)();

  var _useHideCursorWhenFul = useHideCursorWhenFullscreen({
    use_fullscreen: use_fullscreen,
    fullscreen: is_zoom_max
  }),
      ref_is_hide_cursor = _useHideCursorWhenFul.ref_is_hide_cursor;

  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(function () {
    if (!ref_video_elm.current) {
      return;
    }

    ref_video_elm.current.addEventListener('loadedmetadata', handleLoadMetadata);
    ref_video_elm.current.addEventListener('timeupdate', handleTimeUpDate);
    ref_video_elm.current.addEventListener('progress', handleProgress);
    ref_video_elm.current.addEventListener('ended', handleEnded);
    ref_video_elm.current.addEventListener('durationchange', handleChangeDuration);
    ref_video_elm.current.addEventListener('pause', handleWhenPause);
    return function () {
      if (ref_video_elm.current) {
        ref_video_elm.current.removeEventListener('loadedmetadata', handleLoadMetadata);
        ref_video_elm.current.removeEventListener('timeupdate', handleTimeUpDate);
        ref_video_elm.current.removeEventListener('progress', handleProgress);
        ref_video_elm.current.removeEventListener('ended', handleEnded);
        ref_video_elm.current.removeEventListener('durationchange', handleChangeDuration);
        ref_video_elm.current.removeEventListener('pause', handleWhenPause);
      }
    };
  }, []);
  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(function () {
    stop_when_over && ref_video_elm.current && observerDisplay({
      elm: ref_video_elm.current,
      callbackDisplay: function callbackDisplay() {
        ref_is_over.current = false;
      },
      callbackNoDisplay: function callbackNoDisplay() {
        ref_is_over.current = true;

        if (ref_is_play.current) {
          togglePlay();
        }
      }
    });
  }, []);
  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(function () {
    if (!use_fullscreen) {
      return;
    }

    if (is_zoom_max && ref_main_video.current) {
      requestFullscreen({
        elm: ref_main_video.current
      });
    } else {
      exitFullscreen();
    }

    document.addEventListener('fullscreenchange', handleScreenChange);
    return function () {
      document.removeEventListener('fullscreenchange', handleScreenChange);
    };
  }, [is_zoom_max]);

  function handleLoadMetadata() {
    if (!ref_video_elm.current) {
      return;
    }

    if (is_live) {
      ref_c_time.current = ref_total_time.current;
    }

    ref_video_elm.current.currentTime = ref_c_time.current;
    ref_total_time.current = ref_video_elm.current.duration;
    ref_is_mute.current && (ref_video_elm.current.muted = true);
    ref_is_play.current && ref_video_elm.current.play();
    forceUpdate();
  }

  function handleTimeUpDate() {
    if (!ref_video_elm.current) {
      return;
    }

    ref_c_time.current = ref_video_elm.current.currentTime;
    forceUpdate();
  }

  function handleProgress() {
    if (!ref_video_elm.current) {
      return;
    }

    var range = 0;
    var bf = ref_video_elm.current.buffered;
    var c_time = ref_video_elm.current.currentTime;

    if (bf.length == 0) {
      return;
    }

    while (!(bf.start(range) <= c_time || bf.end(range) >= c_time)) {
      range += 1;
    }

    ref_buffer_time.current = bf.end(range);
  }

  function handleEnded() {
    ref_is_play.current = false;
    forceUpdate();
  }

  function handleChangeDuration() {
    if (!ref_video_elm.current) {
      return;
    }

    ref_total_time.current = ref_video_elm.current.duration;
    forceUpdate();
  }

  function handleWhenPause() {
    if (!ref_video_elm.current) {
      return;
    }

    if (ref_holding_slider.current) {
      return;
    }

    ref_is_play.current = false;
    forceUpdate();
  }

  function handleScreenChange() {
    if (document.fullscreenElement || ref_zoom_lv.current == 0) {
      return;
    }

    ref_zoom_lv.current = 0;
    forceUpdate();
  }

  function togglePlay() {
    if (!ref_video_elm.current) {
      return;
    }

    beforeTogglePlay();
    var new_is_play = !ref_is_play.current;
    ref_is_play.current = new_is_play;

    if (new_is_play) {
      ref_video_elm.current.play();
    } else {
      ref_video_elm.current.pause();
    }

    forceUpdate();
    afterTogglePlay();
  }

  function changeZoomLv() {
    if (!ref_video_elm.current) {
      return;
    }

    var new_zoom_lv = ref_zoom_lv.current + 1;

    if (new_zoom_lv > max_zoom_lv) {
      new_zoom_lv = 0;
    }

    beforeChangeZoomLv();
    ref_zoom_lv.current = new_zoom_lv;
    forceUpdate();
    afterChangeZoomLv();
  }

  function changeVolume(_ref2) {
    var _ref2$new_volume = _ref2.new_volume,
        new_volume = _ref2$new_volume === void 0 ? 0 : _ref2$new_volume;

    if (!ref_video_elm.current) {
      return;
    }

    beforeChangeVolume();
    ref_volume.current = new_volume;
    ref_is_mute.current = false;
    ref_video_elm.current.volume = new_volume;
    ref_video_elm.current.muted && (ref_video_elm.current.muted = false);
    forceUpdate();
    afterChangeVolume();
  }

  function toggleMute() {
    if (!ref_video_elm.current) {
      return;
    }

    beforeToggleMute();
    var new_is_mute = !ref_is_mute.current;
    ref_is_mute.current = new_is_mute;
    ref_video_elm.current.muted = new_is_mute;
    forceUpdate();
    afterToggleMute();
  }

  function changeTime(_ref3) {
    var _ref3$new_c_time = _ref3.new_c_time,
        new_c_time = _ref3$new_c_time === void 0 ? 0 : _ref3$new_c_time;

    if (!ref_video_elm.current) {
      return;
    }

    beforeChangeTime();
    ref_c_time.current = new_c_time;
    ref_video_elm.current.currentTime = new_c_time;
    forceUpdate();
    afterChangeTime();
  }

  function changeTotalTime(new_total_time) {
    if (new_total_time === void 0) {
      new_total_time = 0;
    }

    if (!ref_video_elm.current) {
      return;
    }

    ref_total_time.current = new_total_time;
    forceUpdate();
  }

  function startMoveTime() {
    if (!ref_video_elm.current) {
      return;
    }

    ref_holding_slider.current = true;

    if (ref_is_play.current) {
      ref_video_elm.current.pause();
    }
  }

  function endMoveTime() {
    if (!ref_video_elm.current) {
      return;
    }

    ref_holding_slider.current = false;

    if (ref_is_play.current) {
      ref_video_elm.current.play();
    }
  }

  function toggleOpenSetting() {
    ref_open_setting.current = !ref_open_setting.current;
    forceUpdate();
  }

  function handleSettingDetail(setting_name) {
    ref_setting_name.current = setting_name;
    forceUpdate();
  }

  function changeSubtitleIx(track_ix) {
    if (track_ix === void 0) {
      track_ix = -1;
    }

    ref_video_elm.current && changeVideoSubtitle(ref_video_elm.current, track_ix);
  }

  function handleChangeSpeed(speed) {
    if (ref_video_elm.current) {
      ref_chosen_custom_speed.current = false;
      ref_speed.current = speed;
      ref_video_elm.current.playbackRate = speed;
      forceUpdate();
    }
  }

  function chooseCustomSpeed() {
    if (ref_video_elm.current) {
      ref_chosen_custom_speed.current = true;
      ref_speed.current = ref_custom_speed.current;
      forceUpdate();
    }
  }

  function handleChangeCustomSpeed(speed) {
    if (ref_video_elm.current) {
      ref_chosen_custom_speed.current = true;
      ref_has_custom_speed.current = true;
      ref_custom_speed.current = speed;
      var new_speed = Math.round(speed / 0.05) * 0.05;

      if (new_speed != ref_speed.current) {
        ref_speed.current = new_speed;
        ref_video_elm.current.playbackRate = new_speed;
      }

      forceUpdate();
    }
  }

  function afterEndCustomSpeed() {
    if (ref_chosen_custom_speed.current) {
      ref_custom_speed.current = ref_speed.current;
      forceUpdate();
    }
  }

  function getTotalTime() {
    if (!ref_video_elm.current) {
      return;
    }

    return ref_video_elm.current.duration;
  }

  function gotoLiveView() {
    if (!ref_video_elm.current) {
      return;
    }

    if (ref_c_time.current == ref_total_time.current) {
      return;
    }

    beforeGotoLiveView();
    ref_c_time.current = ref_total_time.current;
    ref_video_elm.current.currentTime = ref_total_time.current;
    forceUpdate();
    afterGotoLiveView();
  }

  return {
    ref_is_play: ref_is_play,
    ref_zoom_lv: ref_zoom_lv,
    ref_volume: ref_volume,
    ref_is_mute: ref_is_mute,
    ref_c_time: ref_c_time,
    ref_total_time: ref_total_time,
    ref_buffer_time: ref_buffer_time,
    ref_holding_slider: ref_holding_slider,
    ref_is_hide_cursor: ref_is_hide_cursor,
    ref_setting_name: ref_setting_name,
    ref_open_setting: ref_open_setting,
    ref_speed: ref_speed,
    ref_custom_speed: ref_custom_speed,
    ref_has_custom_speed: ref_has_custom_speed,
    ref_chosen_custom_speed: ref_chosen_custom_speed,
    toggleOpenSetting: toggleOpenSetting,
    handleSettingDetail: handleSettingDetail,
    changeSubtitleIx: changeSubtitleIx,
    chooseCustomSpeed: chooseCustomSpeed,
    handleChangeSpeed: handleChangeSpeed,
    afterEndCustomSpeed: afterEndCustomSpeed,
    handleChangeCustomSpeed: handleChangeCustomSpeed,
    togglePlay: togglePlay,
    changeVolume: changeVolume,
    toggleMute: toggleMute,
    changeZoomLv: changeZoomLv,
    changeTime: changeTime,
    startMoveTime: startMoveTime,
    endMoveTime: endMoveTime,
    getTotalTime: getTotalTime,
    changeTotalTime: changeTotalTime,
    gotoLiveView: gotoLiveView
  };
}

var INITIAL_SPEED_ARR = function INITIAL_SPEED_ARR() {
  return [{
    speed: 0.25,
    title: '0.25'
  }, {
    speed: 0.5,
    title: '0.5'
  }, {
    speed: 0.75,
    title: '0.75'
  }, {
    speed: 1,
    title: 'Standard'
  }, {
    speed: 1.25,
    title: '1.25'
  }, {
    speed: 1.5,
    title: '1.5'
  }, {
    speed: 1.75,
    title: '1.75'
  }, {
    speed: 2,
    title: '2'
  }];
};

var IS_MOBILE = react_commons_ts__WEBPACK_IMPORTED_MODULE_1__.common_constant.IS_MOBILE;

function getClassModuleCss(_ref) {
  var _ref$className = _ref.className,
      className = _ref$className === void 0 ? '' : _ref$className,
      styles = _ref.styles;
  return className + " " + (styles[className] || '');
}

function IconsPlayPause(_ref) {
  var _ref$size_icon = _ref.size_icon,
      size_icon = _ref$size_icon === void 0 ? '1.5rem' : _ref$size_icon,
      _ref$x = _ref.x,
      x = _ref$x === void 0 ? 0 : _ref$x,
      _ref$y = _ref.y,
      y = _ref$y === void 0 ? 0 : _ref$y,
      _ref$color = _ref.color,
      color = _ref$color === void 0 ? 'white' : _ref$color;
  return react__WEBPACK_IMPORTED_MODULE_0__.createElement("svg", {
    className: 'IconsPlayPause',
    viewBox: x + " " + y + " 200 200",
    width: size_icon,
    height: size_icon
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement("g", {
    className: 'IconsPlayPause_play',
    fill: color
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement("path", {
    d: 'M45,25 155,100 45,175 Z'
  })), react__WEBPACK_IMPORTED_MODULE_0__.createElement("g", {
    className: 'IconsPlayPause_pause',
    stroke: color,
    strokeWidth: '25'
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement("line", {
    x1: '270',
    y1: '25',
    x2: '270',
    y2: '175'
  }), react__WEBPACK_IMPORTED_MODULE_0__.createElement("line", {
    x1: '330',
    y1: '25',
    x2: '330',
    y2: '175'
  })));
}

var stylesVideoPause = {"VideoPause":"_3_w-2"};

function _getClassModuleCss(className) {
  if (className === void 0) {
    className = '';
  }

  return getClassModuleCss({
    className: className,
    styles: stylesVideoPause
  });
}

function VideoPause(_ref) {
  var is_play = _ref.is_play,
      size_icon = _ref.size_icon,
      color = _ref.color,
      togglePlayPause = _ref.togglePlayPause;
  return react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss('VideoPause'),
    onClick: togglePlayPause
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement(IconsPlayPause, {
    x: is_play ? 200 : 0,
    size_icon: size_icon,
    color: color
  }));
}

var time_200 = new Date('2000/01/01').getTime();

function roundNum(num) {
  if (num === void 0) {
    num = 0;
  }

  return Math.round(num);
}

function getNumberBy2Digit(num) {
  if (num === void 0) {
    num = 0;
  }

  var _num = roundNum(num);

  if (_num <= 9) {
    return "0" + _num;
  }

  return _num;
}

function getVideoTimeFromSeconds(_ref) {
  var _ref$second = _ref.second,
      second = _ref$second === void 0 ? 0 : _ref$second;

  if (second < 60) {
    return "0:" + getNumberBy2Digit(second);
  }

  if (second < 3600) {
    var minute = Math.floor(second / 60);

    var _rest_second = second - minute * 60;

    return minute + ":" + getNumberBy2Digit(_rest_second);
  }

  if (second < 86400) {
    var hour = Math.floor(second / 86400);

    var _minute = Math.floor((second - 86400) / 60);

    var _rest_second2 = second - _minute * 60;

    return hour + ":" + getNumberBy2Digit(_minute) + ":" + getNumberBy2Digit(_rest_second2);
  }

  var day = Math.floor(second / (24 * 60 * 60));
  var rest_second = second - day * 24 * 60 * 60;
  var time_str = new Date(time_200 + rest_second * 1000).toTimeString().slice(0, 8);
  return day + ":" + time_str;
}

var stylesVideoTimeTotalTime = {"VideoTimeTotalTime":"_Q3vXB"};

function _getClassModuleCss$1(className) {
  if (className === void 0) {
    className = '';
  }

  return getClassModuleCss({
    className: className,
    styles: stylesVideoTimeTotalTime
  });
}

function VideoTimeTotalTime(_ref) {
  var c_time = _ref.c_time,
      total_time = _ref.total_time;
  return react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$1('VideoTimeTotalTime')
  }, IS_MOBILE ? getVideoTimeFromSeconds({
    second: total_time - c_time
  }) : getVideoTimeFromSeconds({
    second: c_time
  }) + " : " + getVideoTimeFromSeconds({
    second: total_time
  }));
}

function MyOneSlider(_ref) {
  var range = _ref.range,
      active_range = _ref.active_range,
      slider = _ref.slider,
      value = _ref.value,
      only_drag_slider = _ref.only_drag_slider,
      ref_has_change_range = _ref.ref_has_change_range,
      ref_is_run = _ref.ref_is_run,
      getRangeAngel = _ref.getRangeAngel,
      handleChange = _ref.handleChange,
      afterMouseDown = _ref.afterMouseDown,
      afterMousemove = _ref.afterMousemove,
      afterMouseUp = _ref.afterMouseUp;
  return react__WEBPACK_IMPORTED_MODULE_0__.createElement(react_range_slider_ts__WEBPACK_IMPORTED_MODULE_3__.OneSlider, {
    range: range,
    active_range: active_range,
    slider: slider,
    value: value,
    only_drag_slider: only_drag_slider,
    ref_has_change_range: ref_has_change_range,
    ref_is_run: ref_is_run,
    getRangeAngel: getRangeAngel,
    handleChange: handleChange,
    afterMouseDown: afterMouseDown,
    afterMousemove: afterMousemove,
    afterMouseUp: afterMouseUp
  });
}

var stylesVideoTimeLine = {"VideoTimeLine":"_3NXmW","VideoTimeLine_slider":"_EDj68","VideoTimeLine-run":"_2MMPr","VideoTimeLine_buffer":"_HmPZp","VideoTimeLine_range":"_2ylVe","VideoTimeLine_range_contain":"_Y_T8c","VideoTimeLine_range_contain-active":"_172X9","VideoTimeLine_buffer_contain":"_29Vqs"};

function _getClassModuleCss$2(className) {
  if (className === void 0) {
    className = '';
  }

  return getClassModuleCss({
    className: className,
    styles: stylesVideoTimeLine
  });
}

var VideoTimeLineDefaultProps = {
  range: react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$2('VideoTimeLine_range')
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$2('VideoTimeLine_range_contain')
  })),
  active_range: react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$2('VideoTimeLine_range') + " " + _getClassModuleCss$2('VideoTimeLine_range-active')
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$2('VideoTimeLine_range_contain') + " " + _getClassModuleCss$2('VideoTimeLine_range_contain-active')
  })),
  slider: react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$2('VideoTimeLine_slider')
  })
};

function VideoTimeLine(_ref) {
  var c_time = _ref.c_time,
      total_time = _ref.total_time,
      buffer_time = _ref.buffer_time,
      _ref$range = _ref.range,
      range = _ref$range === void 0 ? VideoTimeLineDefaultProps.range : _ref$range,
      _ref$active_range = _ref.active_range,
      active_range = _ref$active_range === void 0 ? VideoTimeLineDefaultProps.active_range : _ref$active_range,
      _ref$slider = _ref.slider,
      slider = _ref$slider === void 0 ? VideoTimeLineDefaultProps.slider : _ref$slider,
      ref_is_run = _ref.ref_is_run,
      handleChangeTime = _ref.handleChangeTime,
      handleStartMoveTime = _ref.handleStartMoveTime,
      handleEndMoveTime = _ref.handleEndMoveTime;

  function onChangeTime(time_percent) {
    if (time_percent === void 0) {
      time_percent = 0;
    }

    handleChangeTime({
      new_c_time: total_time * time_percent / 100
    });
  }

  return react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$2('VideoTimeLine') + " " + (ref_is_run !== null && ref_is_run !== void 0 && ref_is_run.current ? _getClassModuleCss$2('VideoTimeLine-run') : '')
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement(MyOneSlider, {
    range: react__WEBPACK_IMPORTED_MODULE_0__.createElement(react__WEBPACK_IMPORTED_MODULE_0__.Fragment, null, range, react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
      className: _getClassModuleCss$2('VideoTimeLine_buffer'),
      style: {
        width: buffer_time + "%"
      }
    }, react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
      className: _getClassModuleCss$2('VideoTimeLine_buffer_contain')
    }))),
    active_range: active_range,
    slider: slider,
    value: c_time,
    only_drag_slider: false,
    ref_is_run: ref_is_run,
    handleChange: onChangeTime,
    afterMouseDown: handleStartMoveTime,
    afterMouseUp: handleEndMoveTime
  }));
}

function IconSetting(_ref) {
  var _ref$class_icon = _ref.class_icon,
      class_icon = _ref$class_icon === void 0 ? '' : _ref$class_icon,
      _ref$fill = _ref.fill,
      fill = _ref$fill === void 0 ? 'none' : _ref$fill,
      _ref$size_icon = _ref.size_icon,
      size_icon = _ref$size_icon === void 0 ? '1rem' : _ref$size_icon,
      _ref$stroke = _ref.stroke,
      stroke = _ref$stroke === void 0 ? '' : _ref$stroke,
      _ref$stroke_width = _ref.stroke_width,
      stroke_width = _ref$stroke_width === void 0 ? 10 : _ref$stroke_width;
  return react__WEBPACK_IMPORTED_MODULE_0__.createElement("svg", {
    className: "IconSetting " + class_icon,
    width: size_icon,
    height: size_icon,
    viewBox: '0 0 200 200',
    fill: fill,
    strokeWidth: stroke_width,
    stroke: stroke,
    strokeLinecap: 'round',
    strokeLinejoin: 'round'
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement("path", {
    d: 'M113.576 20C113.576 20 114.534 35.5711 116.97 39.3189L132.821 45.7585L147.515 32.3839L166.92 52.1981L154.303 67.908L160.1 85.7408H180V113.622L160.1 116.736L154.303 132.941L166.92 148.297L147.03 168.111L132.821 154.737L115.771 160.469L113.576 180H86.4242L85.4545 160.469L69.4545 154.737L53.4545 168.111L33.9327 148.297L47.1515 132.941L40.3263 116.736L20 113.622V85.7408H40.3263L47.1515 67.908L33.9327 52.1981L53.4545 32.3839L69.4545 45.7585L85.4545 39.3189L86.4242 20H113.576Z'
  }), react__WEBPACK_IMPORTED_MODULE_0__.createElement("circle", {
    className: 'IconSetting_circle',
    cx: '100',
    cy: '100',
    r: '30',
    fill: 'none'
  }));
}

var stylesVideoSettings = {"VideoSettings_icon":"_2M3Uj","VideoSettings_icon_svg":"_3Q6k7","VideoSettings_icon_svg-open":"_3vkak"};

function _getClassModuleCss$3(className) {
  if (className === void 0) {
    className = '';
  }

  return getClassModuleCss({
    className: className,
    styles: stylesVideoSettings
  });
}

function VideoSettings(_ref) {
  var ref_btn_setting = _ref.ref_btn_setting,
      open_setting = _ref.open_setting,
      size_icon = _ref.size_icon,
      toggleOpenSetting = _ref.toggleOpenSetting;
  return react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$3('VideoSettings')
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    ref: ref_btn_setting,
    className: "" + _getClassModuleCss$3('VideoSettings_icon'),
    onClick: toggleOpenSetting
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement(IconSetting, {
    class_icon: _getClassModuleCss$3('VideoSettings_icon_svg') + " " + (open_setting ? _getClassModuleCss$3('VideoSettings_icon_svg-open') : ''),
    stroke: 'currentColor',
    size_icon: size_icon
  })));
}

var stylesIconZoomArrow = {"IconZoomArrow-out":"_2g7Ug","IconZoomArrow_top":"_39UP-","IconZoomArrow_bot":"_1eoEJ"};

function IconZoomArrow(_ref) {
  var _ref$class_icon = _ref.class_icon,
      class_icon = _ref$class_icon === void 0 ? '' : _ref$class_icon,
      _ref$x = _ref.x,
      x = _ref$x === void 0 ? 0 : _ref$x,
      _ref$y = _ref.y,
      y = _ref$y === void 0 ? 0 : _ref$y,
      _ref$size_icon = _ref.size_icon,
      size_icon = _ref$size_icon === void 0 ? '1rem' : _ref$size_icon,
      _ref$stroke = _ref.stroke,
      stroke = _ref$stroke === void 0 ? 'currentColor' : _ref$stroke,
      _ref$stroke_width = _ref.stroke_width,
      stroke_width = _ref$stroke_width === void 0 ? 5 : _ref$stroke_width,
      _ref$zoom_out = _ref.zoom_out,
      zoom_out = _ref$zoom_out === void 0 ? false : _ref$zoom_out;
  return react__WEBPACK_IMPORTED_MODULE_0__.createElement("svg", {
    className: "IconZoomArrow " + class_icon + " " + (zoom_out ? "IconZoomArrow-out " + stylesIconZoomArrow['IconZoomArrow-out'] : "IconZoomArrow-in " + stylesIconZoomArrow['IconZoomArrow-in']),
    viewBox: x + " " + y + " 200 200",
    width: size_icon,
    height: size_icon,
    stroke: stroke,
    fill: stroke,
    strokeWidth: stroke_width,
    strokeLinecap: 'round',
    strokeLinejoin: 'round'
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement("path", {
    className: "IconZoomArrow_top " + stylesIconZoomArrow['IconZoomArrow_top'],
    d: 'M117.705 82.2416H180.755L180.328 66.1516L152.096 60.5267L182.705 29.8436L170.355 17.2416L139.636 48.0355L133.172 18.5939L118.083 19.0373L117.705 82.2416Z'
  }), react__WEBPACK_IMPORTED_MODULE_0__.createElement("path", {
    className: "IconZoomArrow_bot " + stylesIconZoomArrow['IconZoomArrow_bot'],
    d: 'M80.1741 119.772H17.1241L17.5504 135.862L45.7822 141.487L15.1741 172.17L27.5241 184.772L58.2429 153.978L64.707 183.42L79.7953 182.976L80.1741 119.772Z'
  }));
}

function VideoZoomIcon(_ref) {
  var _ref$zoom_icon_name = _ref.zoom_icon_name,
      zoom_icon_name = _ref$zoom_icon_name === void 0 ? 'arrow' : _ref$zoom_icon_name,
      _ref$is_zoom_out = _ref.is_zoom_out,
      is_zoom_out = _ref$is_zoom_out === void 0 ? false : _ref$is_zoom_out,
      size_icon = _ref.size_icon;

  if (zoom_icon_name == 'arrow') {
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(IconZoomArrow, {
      zoom_out: !is_zoom_out,
      size_icon: size_icon
    });
  }

  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", null);
}

var stylesVideoZoom = {"VideoZoom":"_2_cmi"};

function _getClassModuleCss$4(className) {
  if (className === void 0) {
    className = '';
  }

  return getClassModuleCss({
    className: className,
    styles: stylesVideoZoom
  });
}

VideoZoom.propTypes = {};

function VideoZoom(_ref) {
  var _ref$zoom_icon_name = _ref.zoom_icon_name,
      zoom_icon_name = _ref$zoom_icon_name === void 0 ? 'arrow' : _ref$zoom_icon_name,
      size_icon = _ref.size_icon,
      is_zoom_out = _ref.is_zoom_out,
      toggleZoom = _ref.toggleZoom,
      _ref$IconComponent = _ref.IconComponent,
      IconComponent = _ref$IconComponent === void 0 ? VideoZoomIcon : _ref$IconComponent;
  return react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$4('VideoZoom'),
    onClick: toggleZoom
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement(IconComponent, {
    zoom_icon_name: zoom_icon_name,
    is_zoom_out: is_zoom_out,
    size_icon: size_icon
  }));
}

var styles = {"display-none":"_3auwb"};

function IconSound(_ref) {
  var _ref$class_icon = _ref.class_icon,
      class_icon = _ref$class_icon === void 0 ? '' : _ref$class_icon,
      _ref$x = _ref.x,
      x = _ref$x === void 0 ? 0 : _ref$x,
      _ref$y = _ref.y,
      y = _ref$y === void 0 ? 0 : _ref$y,
      _ref$size_icon = _ref.size_icon,
      size_icon = _ref$size_icon === void 0 ? '1rem' : _ref$size_icon,
      _ref$color = _ref.color,
      color = _ref$color === void 0 ? 'currentColor' : _ref$color,
      _ref$stroke_width = _ref.stroke_width,
      stroke_width = _ref$stroke_width === void 0 ? 10 : _ref$stroke_width,
      _ref$is_mute = _ref.is_mute,
      is_mute = _ref$is_mute === void 0 ? false : _ref$is_mute,
      _ref$is_too_low = _ref.is_too_low,
      is_too_low = _ref$is_too_low === void 0 ? false : _ref$is_too_low,
      _ref$is_high = _ref.is_high,
      is_high = _ref$is_high === void 0 ? false : _ref$is_high;
  return react__WEBPACK_IMPORTED_MODULE_0__.createElement("svg", {
    className: "IconSound " + class_icon,
    viewBox: x + " " + y + " 200 200",
    width: size_icon,
    height: size_icon,
    strokeWidth: stroke_width,
    strokeLinecap: 'round',
    strokeLinejoin: 'round'
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement("path", {
    fill: color,
    d: 'M27.0796 72.1164H50.2065L106.372 35.7526C109.634 34.3502 110.729 34.8539 111.563 37.7182V161.06C111.576 165.488 110.266 165.728 106.372 164.009L50.2065 126.662H27.0796C21.8852 126.852 20.3052 125.382 20 119.782V79.4874C20.5739 73.9936 22.1124 72.3657 27.0796 72.1164Z'
  }), react__WEBPACK_IMPORTED_MODULE_0__.createElement("g", {
    fill: 'none'
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement("path", {
    stroke: color,
    className: "" + (is_mute ? '' : styles['display-none']),
    d: 'M129.97 78.996L180 130.593M180 78.996L129.97 130.593'
  }), react__WEBPACK_IMPORTED_MODULE_0__.createElement("path", {
    stroke: color,
    className: "" + (is_mute || is_too_low ? styles['display-none'] : ''),
    d: 'M137.522 73.0992C156.078 88.8609 155.027 122.093 137.522 136.982',
    strokeLinecap: 'square'
  }), react__WEBPACK_IMPORTED_MODULE_0__.createElement("path", {
    stroke: color,
    className: "" + (is_mute || !is_high ? styles['display-none'] : ''),
    d: 'M154.513 58.3571C180 81.4531 181.416 127.645 154.513 151.724',
    strokeLinecap: 'square'
  })));
}

var stylesVideoSound = {"VideoSound":"_7mHcq","VideoSound-active":"_1Cegy","VideoSound_volume":"_Sk0aX","VideoSound_slider":"_1inAH","VideoSound_icon":"_4QmIq","VideoSound_volume_contain":"_38Plw","VideoSound_range":"_BpprM","VideoSound_range-active":"_3HzbK"};

function _getClassModuleCss$5(className) {
  if (className === void 0) {
    className = '';
  }

  return getClassModuleCss({
    className: className,
    styles: stylesVideoSound
  });
}

var VideoSoundDefaultProps = {
  range: react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$5('VideoSound_range')
  }),
  active_range: react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$5('VideoSound_range') + " " + _getClassModuleCss$5('VideoSound_range-active')
  }),
  slider: react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$5('VideoSound_slider')
  })
};

function VideoSound(_ref) {
  var volume = _ref.volume,
      is_mute = _ref.is_mute,
      size_icon = _ref.size_icon,
      _ref$range = _ref.range,
      range = _ref$range === void 0 ? VideoSoundDefaultProps.range : _ref$range,
      _ref$active_range = _ref.active_range,
      active_range = _ref$active_range === void 0 ? VideoSoundDefaultProps.active_range : _ref$active_range,
      _ref$slider = _ref.slider,
      slider = _ref$slider === void 0 ? VideoSoundDefaultProps.slider : _ref$slider,
      ref_is_run = _ref.ref_is_run,
      toggleMute = _ref.toggleMute,
      handleChangeVolume = _ref.handleChangeVolume;

  var _useState = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(false),
      is_mouse_down = _useState[0],
      setIsMouseDown = _useState[1];

  function getRangeAngel() {
    return -90;
  }

  function afterMouseDown() {
    setIsMouseDown(true);
  }

  function afterMouseUp() {
    setIsMouseDown(false);
  }

  function onVolumeValueChange(new_volume) {
    if (new_volume === void 0) {
      new_volume = 0;
    }

    handleChangeVolume({
      new_volume: new_volume / 100
    });
  }

  return react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$5('VideoSound') + " " + (is_mouse_down ? _getClassModuleCss$5('VideoSound-active') : '')
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$5('VideoSound_icon'),
    onClick: toggleMute
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement(IconSound, {
    is_mute: is_mute,
    is_too_low: volume <= 0.1,
    is_high: volume >= 0.6,
    color: 'white',
    size_icon: size_icon
  })), react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$5('VideoSound_volume')
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$5('VideoSound_volume_contain')
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement(MyOneSlider, {
    range: range,
    active_range: active_range,
    slider: slider,
    value: is_mute ? 0 : volume * 100,
    only_drag_slider: false,
    ref_is_run: ref_is_run,
    getRangeAngel: getRangeAngel,
    handleChange: onVolumeValueChange,
    afterMouseDown: afterMouseDown,
    afterMouseUp: afterMouseUp
  }))));
}

var VideoUtilsStyles = {"VideoUtils":"_3M8_9","VideoUtils_row":"_1kCDJ","VideoUtils_part":"_EqT8M","VideoUtils_timeline":"_1WcoX","VideoUtils-mobile":"_21MzP"};

function _getClassModuleCss$6(className) {
  if (className === void 0) {
    className = '';
  }

  return getClassModuleCss({
    className: className,
    styles: VideoUtilsStyles
  });
}

function VideoUtils(_ref) {
  var is_play = _ref.is_play,
      is_zoom_out = _ref.is_zoom_out,
      is_mute = _ref.is_mute,
      volume = _ref.volume,
      c_time = _ref.c_time,
      buffer_time = _ref.buffer_time,
      total_time = _ref.total_time,
      ref_btn_setting = _ref.ref_btn_setting,
      open_setting = _ref.open_setting,
      size_icon = _ref.size_icon,
      ref_timeline_run = _ref.ref_timeline_run,
      ref_sound_run = _ref.ref_sound_run,
      toggleOpenSetting = _ref.toggleOpenSetting,
      togglePlayPause = _ref.togglePlayPause,
      toggleZoom = _ref.toggleZoom,
      toggleMute = _ref.toggleMute,
      handleChangeVolume = _ref.handleChangeVolume,
      handleChangeTime = _ref.handleChangeTime,
      handleStartMoveTime = _ref.handleStartMoveTime,
      handleEndMoveTime = _ref.handleEndMoveTime;
  return react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$6('VideoUtils') + " " + (IS_MOBILE ? "" + _getClassModuleCss$6('VideoUtils-mobile') : '')
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$6('VideoUtils_row')
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$6('VideoUtils_part')
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement(VideoPause, {
    is_play: is_play,
    size_icon: size_icon,
    togglePlayPause: togglePlayPause
  })), IS_MOBILE ? null : react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$6('VideoUtils_part')
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement(VideoTimeTotalTime, {
    c_time: c_time,
    total_time: total_time
  })), react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$6('VideoUtils_timeline')
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement(VideoTimeLine, {
    c_time: c_time * 100 / total_time,
    buffer_time: buffer_time * 100 / total_time,
    total_time: total_time,
    ref_is_run: ref_timeline_run,
    handleChangeTime: handleChangeTime,
    handleStartMoveTime: handleStartMoveTime,
    handleEndMoveTime: handleEndMoveTime
  })), !IS_MOBILE ? null : react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$6('VideoUtils_part')
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement(VideoTimeTotalTime, {
    c_time: c_time,
    total_time: total_time
  })), IS_MOBILE ? null : react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$6('VideoUtils_part')
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement(VideoSettings, {
    ref_btn_setting: ref_btn_setting,
    open_setting: open_setting,
    size_icon: size_icon,
    toggleOpenSetting: toggleOpenSetting
  })), IS_MOBILE ? null : react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$6('VideoUtils_part')
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement(VideoZoom, {
    zoom_icon_name: 'arrow',
    size_icon: size_icon,
    is_zoom_out: is_zoom_out,
    toggleZoom: toggleZoom
  })), react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$6('VideoUtils_part')
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement(VideoSound, {
    volume: volume,
    is_mute: is_mute,
    size_icon: size_icon,
    ref_is_run: ref_sound_run,
    toggleMute: toggleMute,
    handleChangeVolume: handleChangeVolume
  }))));
}

var stylesVideoItem = {"VideoItem":"_2t6HJ","VideoItem_video":"_3rDTI"};

function _getClassModuleCss$7(className) {
  if (className === void 0) {
    className = '';
  }

  return getClassModuleCss({
    className: className,
    styles: stylesVideoItem
  });
}

function VideoItem(_ref) {
  var ref_video_elm = _ref.ref_video_elm,
      video = _ref.video,
      _ref$track_arr = _ref.track_arr,
      track_arr = _ref$track_arr === void 0 ? [] : _ref$track_arr;
  return react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$7('VideoItem')
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement("video", {
    ref: ref_video_elm,
    className: _getClassModuleCss$7('VideoItem_video'),
    controls: false,
    preload: 'metadata'
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement("source", {
    src: video
  }), track_arr.map(function (item, ix) {
    return react__WEBPACK_IMPORTED_MODULE_0__.createElement("track", {
      key: ix,
      label: item.label,
      kind: item.kind,
      srcLang: item.srcLang,
      src: item.src,
      "default": item["default"]
    });
  })));
}

var stylesVideoUtilsLayout = {"VideoUtilsLayout":"_mlaMQ","VideoUtilsLayout-out":"_13DoI"};

function _getClassModuleCss$8(className) {
  if (className === void 0) {
    className = '';
  }

  return getClassModuleCss({
    className: className,
    styles: stylesVideoUtilsLayout
  });
}

function VideoUtilsLayout(_ref) {
  var ref_main_video = _ref.ref_main_video,
      _ref$use_hide_utils = _ref.use_hide_utils,
      use_hide_utils = _ref$use_hide_utils === void 0 ? true : _ref$use_hide_utils,
      is_hide_cursor = _ref.is_hide_cursor,
      _ref$stop_hide_utils = _ref.stop_hide_utils,
      stop_hide_utils = _ref$stop_hide_utils === void 0 ? false : _ref$stop_hide_utils,
      children = _ref.children,
      open_setting = _ref.open_setting;

  var _useHold = (0,react_commons_ts__WEBPACK_IMPORTED_MODULE_1__.useHold)({
    time: 1500
  }),
      StartHold = _useHold.StartHold,
      StopHold = _useHold.StopHold;

  var _useBool = (0,react_commons_ts__WEBPACK_IMPORTED_MODULE_1__.useBool)(),
      is_true = _useBool.is_true,
      setIsTrue = _useBool.setIsTrue;

  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(function () {
    if (use_hide_utils && ref_main_video.current) {
      ref_main_video.current.addEventListener('mouseleave', handleMouseLeave);
      ref_main_video.current.addEventListener('mouseenter', handleMouseEnter);
      return function () {
        if (ref_main_video.current) {
          ref_main_video.current.removeEventListener('mouseleave', handleMouseLeave);
          ref_main_video.current.removeEventListener('mouseenter', handleMouseEnter);
        }
      };
    }
  }, []);

  function handleMouseLeave() {
    StartHold(function () {
      setIsTrue(true);
    });
  }

  function handleMouseEnter() {
    StopHold();
    setIsTrue(false);
  }

  return react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$8('VideoUtilsLayout') + " " + ((is_true || is_hide_cursor) && !stop_hide_utils && !open_setting ? _getClassModuleCss$8('VideoUtilsLayout-out') : '')
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$8('VideoUtilsLayout_contain')
  }, children));
}

var VideoElmStyles = {"VideoElm":"_UGXh9","VideoElm_face":"_153Mx","VideoElm_settings":"_Aavz4","VideoElm_settings-open":"_31h-n"};

function IconCheck(_ref) {
  var _ref$size_icon = _ref.size_icon,
      size_icon = _ref$size_icon === void 0 ? '1rem' : _ref$size_icon,
      _ref$x = _ref.x,
      x = _ref$x === void 0 ? 0 : _ref$x,
      _ref$y = _ref.y,
      y = _ref$y === void 0 ? 0 : _ref$y,
      _ref$stroke = _ref.stroke,
      stroke = _ref$stroke === void 0 ? 'currentColor' : _ref$stroke;
  return react__WEBPACK_IMPORTED_MODULE_0__.createElement("svg", {
    className: 'IconCheck',
    width: size_icon,
    height: size_icon,
    viewBox: x + " " + y + " 200 200",
    strokeLinecap: 'round'
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement("path", {
    d: 'M50,125 80,150 150,50',
    strokeWidth: '20',
    stroke: stroke,
    fill: 'none'
  }));
}

var VideoSetHomeItemLeftStyles = {"VideoSetHomeItemLeft":"_2-tit","VideoSetHomeItemLeft_title":"_qsMuA"};

function _getClassModuleCss$9(className) {
  if (className === void 0) {
    className = '';
  }

  return getClassModuleCss({
    className: className,
    styles: VideoSetHomeItemLeftStyles
  });
}

function VideoSetHomeItemLeft(_ref) {
  var icon = _ref.icon,
      title = _ref.title;
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: _getClassModuleCss$9('VideoSetHomeItemLeft')
  }, icon, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: _getClassModuleCss$9('VideoSetHomeItemLeft_title')
  }, title));
}

function IconAngle(_ref) {
  var _ref$class_icon = _ref.class_icon,
      class_icon = _ref$class_icon === void 0 ? '' : _ref$class_icon,
      _ref$size_icon = _ref.size_icon,
      size_icon = _ref$size_icon === void 0 ? '1rem' : _ref$size_icon,
      _ref$x = _ref.x,
      x = _ref$x === void 0 ? 0 : _ref$x,
      _ref$y = _ref.y,
      y = _ref$y === void 0 ? 0 : _ref$y,
      _ref$svg_props = _ref.svg_props,
      svg_props = _ref$svg_props === void 0 ? {} : _ref$svg_props;
  return react__WEBPACK_IMPORTED_MODULE_0__.createElement("svg", Object.assign({
    className: "IconAngle " + class_icon,
    viewBox: x + " " + y + " 200 200",
    width: size_icon,
    height: size_icon,
    fill: 'none',
    stroke: 'currentColor',
    strokeLinecap: 'round',
    strokeWidth: 25
  }, svg_props), react__WEBPACK_IMPORTED_MODULE_0__.createElement("path", {
    d: 'M75,20 125,100 75,180',
    className: 'IconAngle_next'
  }));
}

var VideoSetHomeItemRightStyles = {"VideoSetHomeItemRight":"_1MyH9","VideoSetHomeItemRight_title":"_3QR78"};

function _getClassModuleCss$a(className) {
  if (className === void 0) {
    className = '';
  }

  return getClassModuleCss({
    className: className,
    styles: VideoSetHomeItemRightStyles
  });
}

function VideoSetHomeItemRight(_ref) {
  var title = _ref.title;
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: _getClassModuleCss$a('VideoSetHomeItemRight')
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: _getClassModuleCss$a('VideoSetHomeItemRight_title')
  }, title), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(IconAngle, null));
}

var VideoSetHomeItemMoreStyles = {"VideoSetHomeItemMore":"_2FQaW"};

function _getClassModuleCss$b(className) {
  if (className === void 0) {
    className = '';
  }

  return getClassModuleCss({
    className: className,
    styles: VideoSetHomeItemMoreStyles
  });
}

function VideoSetHomeItemMore(_ref) {
  var title_left = _ref.title_left,
      title_right = _ref.title_right,
      icon_left = _ref.icon_left,
      setting_name = _ref.setting_name,
      handleSettingDetail = _ref.handleSettingDetail;

  function handleClick() {
    handleSettingDetail(setting_name);
  }

  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: _getClassModuleCss$b('VideoSetHomeItemMore'),
    onClick: handleClick
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(VideoSetHomeItemLeft, {
    icon: icon_left,
    title: title_left
  }), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(VideoSetHomeItemRight, {
    title: title_right
  }));
}

var VideoSetHomeStyles = {};

function _getClassModuleCss$c(className) {
  if (className === void 0) {
    className = '';
  }

  return getClassModuleCss({
    className: className,
    styles: VideoSetHomeStyles
  });
}

function VideoSetHome(_ref) {
  var speed = _ref.speed,
      handleSettingDetail = _ref.handleSettingDetail;
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: _getClassModuleCss$c('VideoSetHome')
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(VideoSetHomeItemMore, {
    icon_left: (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(IconCheck, null),
    title_left: 'Speed',
    title_right: ("" + speed).slice(0, 4),
    setting_name: 'speed',
    handleSettingDetail: handleSettingDetail
  }));
}

var VideoSettingsHeadLayoutBackStyles = {"VideoSettingsHeadLayoutBack":"_2vFYr","VideoSettingsHeadLayoutBack_icon":"_3TtMv"};

function _getClassModuleCss$d(className) {
  if (className === void 0) {
    className = '';
  }

  return getClassModuleCss({
    className: className,
    styles: VideoSettingsHeadLayoutBackStyles
  });
}

function VideoSettingsHeadLayoutBack(_ref) {
  var title = _ref.title,
      handleBack = _ref.handleBack;
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: _getClassModuleCss$d('VideoSettingsHeadLayoutBack'),
    onClick: handleBack
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: _getClassModuleCss$d('VideoSettingsHeadLayoutBack_icon')
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(IconAngle, {
    svg_props: {
      style: {
        transform: 'rotate(180deg)'
      }
    }
  })), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: _getClassModuleCss$d('VideoSettingsHeadLayoutBack_title')
  }, title));
}

var VideoSettingsHeadLayoutStyles = {"VideoSettingsHeadLayout":"_3YEJD"};

function _getClassModuleCss$e(className) {
  if (className === void 0) {
    className = '';
  }

  return getClassModuleCss({
    className: className,
    styles: VideoSettingsHeadLayoutStyles
  });
}

function VideoSettingsHeadLayout(_ref) {
  var title = _ref.title,
      right_elm = _ref.right_elm,
      handleBack = _ref.handleBack;
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: _getClassModuleCss$e('VideoSettingsHeadLayout')
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(VideoSettingsHeadLayoutBack, {
    title: title,
    handleBack: handleBack
  }), right_elm);
}

var VideoSpeedChoiceStyles = {"VideoSpeedChoice":"_1cQZi","VideoSpeedChoice_check":"_3Ux6m","VideoSpeedChoice_check-not":"_2IBfB"};

function _getClassModuleCss$f(className) {
  if (className === void 0) {
    className = '';
  }

  return getClassModuleCss({
    className: className,
    styles: VideoSpeedChoiceStyles
  });
}

function VideoSpeedChoice(_ref) {
  var speed = _ref.speed,
      title = _ref.title,
      is_chosen = _ref.is_chosen,
      handleChangeSpeed = _ref.handleChangeSpeed;

  function handleClick() {
    handleChangeSpeed(speed);
  }

  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: _getClassModuleCss$f('VideoSpeedChoice'),
    onClick: handleClick
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: _getClassModuleCss$f('VideoSpeedChoice_check') + " " + (is_chosen ? '' : _getClassModuleCss$f('VideoSpeedChoice_check-not'))
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(IconCheck, null)), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: _getClassModuleCss$f('VideoSpeedChoice_title')
  }, title));
}

var VideoSpeedChoicesStyles = {"VideoSpeedChoices_head_right":"_37L6V"};

function _getClassModuleCss$g(className) {
  if (className === void 0) {
    className = '';
  }

  return getClassModuleCss({
    className: className,
    styles: VideoSpeedChoicesStyles
  });
}

function VideoSpeedChoices(_ref) {
  var custom_speed = _ref.custom_speed,
      has_custom_speed = _ref.has_custom_speed,
      chosen_custom_speed = _ref.chosen_custom_speed,
      speed = _ref.speed,
      speed_arr = _ref.speed_arr,
      handleBack = _ref.handleBack,
      handleGotoCustom = _ref.handleGotoCustom,
      chooseCustomSpeed = _ref.chooseCustomSpeed,
      handleChangeSpeed = _ref.handleChangeSpeed;
  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: _getClassModuleCss$g('VideoSpeedChoices')
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: _getClassModuleCss$g('VideoSpeedChoices_head')
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(VideoSettingsHeadLayout, {
    title: 'Speed',
    handleBack: handleBack,
    right_elm: (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: _getClassModuleCss$g('VideoSpeedChoices_head_right'),
      onClick: handleGotoCustom
    }, "Custom")
  })), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: _getClassModuleCss$g('VideoSpeedChoices_list')
  }, has_custom_speed && (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(VideoSpeedChoice, {
    speed: custom_speed,
    title: "Custom (" + custom_speed.toString().slice(0, 4) + ")",
    is_chosen: chosen_custom_speed,
    handleChangeSpeed: chooseCustomSpeed
  }), speed_arr.map(function (item) {
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(VideoSpeedChoice, {
      key: item.title,
      speed: item.speed,
      title: item.title,
      is_chosen: !chosen_custom_speed && item.speed == speed,
      handleChangeSpeed: handleChangeSpeed
    });
  })));
}

var VideoSpeedsCustomStyles = {"VideoSpeedsCustom_slider":"_tMpvC","VideoSpeedsCustom_slider_contain":"_1zeKD","VideoSpeedsCustom_slider_speed":"_aDhmN","VideoSpeedsCustom_slider_range":"_3s4Gu","VideoSpeedsCustom_slider_slider":"_2Dra7"};

function _getClassModuleCss$h(className) {
  if (className === void 0) {
    className = '';
  }

  return getClassModuleCss({
    className: className,
    styles: VideoSpeedsCustomStyles
  });
}

function VideoSpeedsCustom(_ref) {
  var custom_speed = _ref.custom_speed,
      chosen_custom_speed = _ref.chosen_custom_speed,
      speed = _ref.speed,
      max_speed = _ref.max_speed,
      handleChangeCustomSpeed = _ref.handleChangeCustomSpeed,
      afterEndCustomSpeed = _ref.afterEndCustomSpeed,
      handleBack = _ref.handleBack;

  function handleChange(new_percent) {
    handleChangeCustomSpeed(new_percent * (max_speed - 0.25) / 100 + 0.25);
  }

  function onBack() {
    afterEndCustomSpeed();
    handleBack();
  }

  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: _getClassModuleCss$h('VideoSpeedsCustom')
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", null, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(VideoSettingsHeadLayout, {
    title: 'Custom',
    handleBack: onBack
  })), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: _getClassModuleCss$h('VideoSpeedsCustom_slider')
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: _getClassModuleCss$h('VideoSpeedsCustom_slider_contain')
  }, (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(react_range_slider_ts__WEBPACK_IMPORTED_MODULE_3__.OneSlider, {
    range: (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: _getClassModuleCss$h('VideoSpeedsCustom_slider_range')
    }),
    slider: (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
      className: _getClassModuleCss$h('VideoSpeedsCustom_slider_slider')
    }),
    value: (custom_speed - 0.25) * 100 / (max_speed - 0.25),
    handleChange: handleChange
  })), (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", {
    className: _getClassModuleCss$h('VideoSpeedsCustom_slider_speed')
  }, Math.round((chosen_custom_speed ? speed : custom_speed) * 100) / 100, "x")));
}

function VideoSpeeds(_ref) {
  var custom_speed = _ref.custom_speed,
      has_custom_speed = _ref.has_custom_speed,
      chosen_custom_speed = _ref.chosen_custom_speed,
      speed = _ref.speed,
      max_speed = _ref.max_speed,
      speed_arr = _ref.speed_arr,
      handleBack = _ref.handleBack,
      chooseCustomSpeed = _ref.chooseCustomSpeed,
      handleChangeSpeed = _ref.handleChangeSpeed,
      afterEndCustomSpeed = _ref.afterEndCustomSpeed,
      handleChangeCustomSpeed = _ref.handleChangeCustomSpeed;

  var _React$useState = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)('choice'),
      speed_page = _React$useState[0],
      setSpeedPage = _React$useState[1];

  function handleGotoCustom() {
    setSpeedPage('custom');
  }

  function handleBackToChoice() {
    setSpeedPage('choice');
  }

  if (speed_page == 'choice') {
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(VideoSpeedChoices, {
      custom_speed: custom_speed,
      has_custom_speed: has_custom_speed,
      chosen_custom_speed: chosen_custom_speed,
      speed: speed,
      speed_arr: speed_arr,
      handleBack: handleBack,
      handleGotoCustom: handleGotoCustom,
      chooseCustomSpeed: chooseCustomSpeed,
      handleChangeSpeed: handleChangeSpeed
    });
  }

  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(VideoSpeedsCustom, {
    custom_speed: custom_speed,
    chosen_custom_speed: chosen_custom_speed,
    speed: speed,
    max_speed: max_speed,
    handleChangeCustomSpeed: handleChangeCustomSpeed,
    afterEndCustomSpeed: afterEndCustomSpeed,
    handleBack: handleBackToChoice
  });
}

function VideoSettingsPage(_ref) {
  var setting_name = _ref.setting_name,
      custom_speed = _ref.custom_speed,
      has_custom_speed = _ref.has_custom_speed,
      chosen_custom_speed = _ref.chosen_custom_speed,
      speed = _ref.speed,
      max_speed = _ref.max_speed,
      speed_arr = _ref.speed_arr,
      handleSettingDetail = _ref.handleSettingDetail,
      chooseCustomSpeed = _ref.chooseCustomSpeed,
      handleChangeSpeed = _ref.handleChangeSpeed,
      afterEndCustomSpeed = _ref.afterEndCustomSpeed,
      handleChangeCustomSpeed = _ref.handleChangeCustomSpeed;

  function handleBackToHome() {
    handleSettingDetail('home');
  }

  if (setting_name == 'home') {
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(VideoSetHome, {
      speed: speed,
      handleSettingDetail: handleSettingDetail
    });
  }

  if (setting_name == 'speed') {
    return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)(VideoSpeeds, {
      custom_speed: custom_speed,
      has_custom_speed: has_custom_speed,
      chosen_custom_speed: chosen_custom_speed,
      speed: speed,
      max_speed: max_speed,
      speed_arr: speed_arr,
      handleBack: handleBackToHome,
      chooseCustomSpeed: chooseCustomSpeed,
      handleChangeSpeed: handleChangeSpeed,
      afterEndCustomSpeed: afterEndCustomSpeed,
      handleChangeCustomSpeed: handleChangeCustomSpeed
    });
  }

  return (0,react__WEBPACK_IMPORTED_MODULE_0__.createElement)("div", null);
}

function _getClassModuleCss$i(className) {
  if (className === void 0) {
    className = '';
  }

  return getClassModuleCss({
    className: className,
    styles: VideoElmStyles
  });
}

function VideoElm(_ref) {
  var ref_main_video = _ref.ref_main_video,
      ref_video_elm = _ref.ref_video_elm,
      face_video_elm = _ref.face_video_elm,
      video = _ref.video,
      track_arr = _ref.track_arr,
      _ref$size_icon_utils = _ref.size_icon_utils,
      size_icon_utils = _ref$size_icon_utils === void 0 ? IS_MOBILE ? '16px' : '20px' : _ref$size_icon_utils,
      is_play = _ref.is_play,
      is_mute = _ref.is_mute,
      volume = _ref.volume,
      is_zoom_out = _ref.is_zoom_out,
      is_hide_cursor = _ref.is_hide_cursor,
      c_time = _ref.c_time,
      buffer_time = _ref.buffer_time,
      total_time = _ref.total_time,
      setting_name = _ref.setting_name,
      custom_speed = _ref.custom_speed,
      has_custom_speed = _ref.has_custom_speed,
      chosen_custom_speed = _ref.chosen_custom_speed,
      speed = _ref.speed,
      max_speed = _ref.max_speed,
      speed_arr = _ref.speed_arr,
      open_setting = _ref.open_setting,
      toggleOpenSetting = _ref.toggleOpenSetting,
      handleSettingDetail = _ref.handleSettingDetail,
      chooseCustomSpeed = _ref.chooseCustomSpeed,
      handleChangeSpeed = _ref.handleChangeSpeed,
      afterEndCustomSpeed = _ref.afterEndCustomSpeed,
      handleChangeCustomSpeed = _ref.handleChangeCustomSpeed,
      togglePlayPause = _ref.togglePlayPause,
      toggleZoom = _ref.toggleZoom,
      toggleMute = _ref.toggleMute,
      handleChangeVolume = _ref.handleChangeVolume,
      handleChangeTime = _ref.handleChangeTime,
      handleStartMoveTime = _ref.handleStartMoveTime,
      handleEndMoveTime = _ref.handleEndMoveTime;
  var ref_timeline_run = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(false);
  var ref_sound_run = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(false);
  var ref_settings = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  var ref_btn_setting = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);

  function handleClickOutSide() {
    open_setting && toggleOpenSetting();
    handleSettingDetail('home');
  }

  return react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    ref: ref_main_video,
    className: "" + _getClassModuleCss$i('VideoElm')
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement(VideoItem, {
    ref_video_elm: ref_video_elm,
    video: video,
    track_arr: track_arr
  }), react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: "" + _getClassModuleCss$i('VideoElm_face')
  }, face_video_elm), open_setting && react__WEBPACK_IMPORTED_MODULE_0__.createElement(react_click_outside_ts__WEBPACK_IMPORTED_MODULE_2__.ClickingOutSideFull, {
    ref_child: ref_settings,
    refs_target: [ref_btn_setting],
    is_show: open_setting,
    handleClickOutSide: handleClickOutSide
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    ref: ref_settings,
    className: "" + _getClassModuleCss$i('VideoElm_settings')
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement(VideoSettingsPage, {
    setting_name: setting_name,
    custom_speed: custom_speed,
    has_custom_speed: has_custom_speed,
    chosen_custom_speed: chosen_custom_speed,
    speed: speed,
    max_speed: max_speed,
    speed_arr: speed_arr,
    handleSettingDetail: handleSettingDetail,
    chooseCustomSpeed: chooseCustomSpeed,
    handleChangeSpeed: handleChangeSpeed,
    afterEndCustomSpeed: afterEndCustomSpeed,
    handleChangeCustomSpeed: handleChangeCustomSpeed
  }))), react__WEBPACK_IMPORTED_MODULE_0__.createElement(VideoUtilsLayout, {
    ref_main_video: ref_main_video,
    is_hide_cursor: is_hide_cursor,
    stop_hide_utils: ref_sound_run.current || ref_timeline_run.current,
    open_setting: open_setting
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement(VideoUtils, {
    is_play: is_play,
    is_zoom_out: is_zoom_out,
    is_mute: is_mute,
    volume: volume,
    c_time: c_time,
    buffer_time: buffer_time,
    total_time: total_time,
    ref_btn_setting: ref_btn_setting,
    open_setting: open_setting,
    size_icon: size_icon_utils,
    ref_sound_run: ref_sound_run,
    ref_timeline_run: ref_timeline_run,
    toggleOpenSetting: toggleOpenSetting,
    togglePlayPause: togglePlayPause,
    toggleZoom: toggleZoom,
    toggleMute: toggleMute,
    handleChangeVolume: handleChangeVolume,
    handleChangeTime: handleChangeTime,
    handleStartMoveTime: handleStartMoveTime,
    handleEndMoveTime: handleEndMoveTime
  })));
}

var MAX_SPEED = 2;

function Video(_ref) {
  var video = _ref.video,
      track_arr = _ref.track_arr,
      total_view = _ref.total_view,
      initial_is_play = _ref.initial_is_play,
      initial_zoom_lv = _ref.initial_zoom_lv,
      _ref$max_zoom_lv = _ref.max_zoom_lv,
      max_zoom_lv = _ref$max_zoom_lv === void 0 ? 0 : _ref$max_zoom_lv,
      _ref$use_fullscreen = _ref.use_fullscreen,
      use_fullscreen = _ref$use_fullscreen === void 0 ? false : _ref$use_fullscreen,
      initial_volume = _ref.initial_volume,
      initial_is_mute = _ref.initial_is_mute,
      initial_c_time = _ref.initial_c_time,
      initial_total_time = _ref.initial_total_time,
      face_video_elm = _ref.face_video_elm,
      _ref$stop_when_over = _ref.stop_when_over,
      stop_when_over = _ref$stop_when_over === void 0 ? true : _ref$stop_when_over,
      _ref$max_speed = _ref.max_speed,
      max_speed = _ref$max_speed === void 0 ? MAX_SPEED : _ref$max_speed,
      _ref$initial_speed_ar = _ref.initial_speed_arr,
      initial_speed_arr = _ref$initial_speed_ar === void 0 ? INITIAL_SPEED_ARR() : _ref$initial_speed_ar,
      beforeTogglePlay = _ref.beforeTogglePlay,
      afterTogglePlay = _ref.afterTogglePlay,
      beforeChangeVolume = _ref.beforeChangeVolume,
      afterChangeVolume = _ref.afterChangeVolume,
      beforeToggleMute = _ref.beforeToggleMute,
      afterToggleMute = _ref.afterToggleMute,
      beforeChangeZoomLv = _ref.beforeChangeZoomLv,
      afterChangeZoomLv = _ref.afterChangeZoomLv,
      beforeChangeTime = _ref.beforeChangeTime,
      afterChangeTime = _ref.afterChangeTime;
  var ref_main_video = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  var ref_video_elm = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);

  var _useVideoUtils = useVideoUtils({
    ref_main_video: ref_main_video,
    ref_video_elm: ref_video_elm,
    is_live: false,
    stop_when_over: stop_when_over,
    use_fullscreen: use_fullscreen,
    initial_is_play: initial_is_play,
    initial_zoom_lv: initial_zoom_lv,
    max_zoom_lv: max_zoom_lv,
    initial_volume: initial_volume,
    initial_is_mute: initial_is_mute,
    initial_c_time: initial_c_time,
    initial_total_time: initial_total_time,
    beforeTogglePlay: beforeTogglePlay,
    afterTogglePlay: afterTogglePlay,
    beforeChangeVolume: beforeChangeVolume,
    afterChangeVolume: afterChangeVolume,
    beforeToggleMute: beforeToggleMute,
    afterToggleMute: afterToggleMute,
    beforeChangeZoomLv: beforeChangeZoomLv,
    afterChangeZoomLv: afterChangeZoomLv,
    beforeChangeTime: beforeChangeTime,
    afterChangeTime: afterChangeTime
  }),
      ref_is_play = _useVideoUtils.ref_is_play,
      ref_zoom_lv = _useVideoUtils.ref_zoom_lv,
      ref_volume = _useVideoUtils.ref_volume,
      ref_is_mute = _useVideoUtils.ref_is_mute,
      ref_c_time = _useVideoUtils.ref_c_time,
      ref_total_time = _useVideoUtils.ref_total_time,
      ref_buffer_time = _useVideoUtils.ref_buffer_time,
      ref_is_hide_cursor = _useVideoUtils.ref_is_hide_cursor,
      ref_setting_name = _useVideoUtils.ref_setting_name,
      ref_speed = _useVideoUtils.ref_speed,
      ref_custom_speed = _useVideoUtils.ref_custom_speed,
      ref_has_custom_speed = _useVideoUtils.ref_has_custom_speed,
      ref_chosen_custom_speed = _useVideoUtils.ref_chosen_custom_speed,
      ref_open_setting = _useVideoUtils.ref_open_setting,
      toggleOpenSetting = _useVideoUtils.toggleOpenSetting,
      handleSettingDetail = _useVideoUtils.handleSettingDetail,
      chooseCustomSpeed = _useVideoUtils.chooseCustomSpeed,
      handleChangeSpeed = _useVideoUtils.handleChangeSpeed,
      afterEndCustomSpeed = _useVideoUtils.afterEndCustomSpeed,
      handleChangeCustomSpeed = _useVideoUtils.handleChangeCustomSpeed,
      togglePlay = _useVideoUtils.togglePlay,
      changeVolume = _useVideoUtils.changeVolume,
      toggleMute = _useVideoUtils.toggleMute,
      changeZoomLv = _useVideoUtils.changeZoomLv,
      changeTime = _useVideoUtils.changeTime,
      startMoveTime = _useVideoUtils.startMoveTime,
      endMoveTime = _useVideoUtils.endMoveTime;

  return react__WEBPACK_IMPORTED_MODULE_0__.createElement(VideoElm, {
    ref_main_video: ref_main_video,
    ref_video_elm: ref_video_elm,
    video: video,
    track_arr: track_arr,
    total_view: total_view,
    is_play: ref_is_play.current,
    is_mute: ref_is_mute.current,
    volume: ref_volume.current,
    is_zoom_out: ref_zoom_lv.current == max_zoom_lv && max_zoom_lv > 0,
    is_hide_cursor: ref_is_hide_cursor.current,
    c_time: ref_c_time.current,
    buffer_time: ref_buffer_time.current,
    total_time: ref_total_time.current,
    face_video_elm: face_video_elm,
    open_setting: ref_open_setting.current,
    setting_name: ref_setting_name.current,
    custom_speed: ref_custom_speed.current,
    has_custom_speed: ref_has_custom_speed.current,
    chosen_custom_speed: ref_chosen_custom_speed.current,
    speed: ref_speed.current,
    max_speed: max_speed,
    speed_arr: initial_speed_arr,
    toggleOpenSetting: toggleOpenSetting,
    handleSettingDetail: handleSettingDetail,
    chooseCustomSpeed: chooseCustomSpeed,
    handleChangeSpeed: handleChangeSpeed,
    afterEndCustomSpeed: afterEndCustomSpeed,
    handleChangeCustomSpeed: handleChangeCustomSpeed,
    togglePlayPause: togglePlay,
    toggleZoom: changeZoomLv,
    toggleMute: toggleMute,
    handleChangeVolume: changeVolume,
    handleChangeTime: changeTime,
    handleStartMoveTime: startMoveTime,
    handleEndMoveTime: endMoveTime
  });
}

var VideoLiveTimeDelayStyles = {"VideoLiveTimeDelay":"_3NIih"};

function _getClassModuleCss$j(className) {
  if (className === void 0) {
    className = '';
  }

  return getClassModuleCss({
    className: className,
    styles: VideoLiveTimeDelayStyles
  });
}

function VideoLiveTimeDelay(_ref) {
  var c_time = _ref.c_time,
      total_time = _ref.total_time;

  if (c_time >= total_time) {
    return null;
  }

  return react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$j('VideoLiveTimeDelay')
  }, "-", getVideoTimeFromSeconds({
    second: total_time - c_time
  }));
}

var VideoLiveUtilsStyles = {"VideoLiveUtils":"_1-KVX","VideoLiveUtils-live":"_3BMDB","VideoLiveUtils_live_now":"_29fpJ","VideoTimeLine_range_contain-active":"_34akd","VideoLiveUtils_row":"_1oxBS","VideoLiveUtils_part":"_a7jbE","VideoLiveUtils_timeline":"_280C3","device-mobile":"_1SaML"};

function _getClassModuleCss$k(className) {
  if (className === void 0) {
    className = '';
  }

  return getClassModuleCss({
    className: className,
    styles: VideoLiveUtilsStyles
  });
}

function VideoLiveUtils(_ref) {
  var is_play = _ref.is_play,
      is_zoom_out = _ref.is_zoom_out,
      is_mute = _ref.is_mute,
      volume = _ref.volume,
      c_time = _ref.c_time,
      buffer_time = _ref.buffer_time,
      total_time = _ref.total_time,
      ref_btn_setting = _ref.ref_btn_setting,
      open_setting = _ref.open_setting,
      size_icon = _ref.size_icon,
      toggleOpenSetting = _ref.toggleOpenSetting,
      togglePlayPause = _ref.togglePlayPause,
      gotoLiveView = _ref.gotoLiveView,
      toggleZoom = _ref.toggleZoom,
      toggleMute = _ref.toggleMute,
      handleChangeVolume = _ref.handleChangeVolume,
      handleChangeTime = _ref.handleChangeTime,
      handleStartMoveTime = _ref.handleStartMoveTime,
      handleEndMoveTime = _ref.handleEndMoveTime;
  return react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$k('VideoLiveUtils') + " " + (c_time == total_time ? _getClassModuleCss$k('VideoLiveUtils-live') : '')
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$k('VideoLiveUtils_row')
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$k('VideoLiveUtils_part')
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement(VideoPause, {
    is_play: is_play,
    size_icon: size_icon,
    togglePlayPause: togglePlayPause
  })), c_time == total_time && IS_MOBILE ? null : react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$k('VideoLiveUtils_live_now'),
    onClick: gotoLiveView
  }, "Live"), IS_MOBILE ? null : react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$k('VideoLiveUtils_part')
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement(VideoLiveTimeDelay, {
    c_time: c_time,
    total_time: total_time
  })), react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$k('VideoLiveUtils_timeline')
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement(VideoTimeLine, {
    c_time: c_time * 100 / total_time,
    total_time: total_time,
    buffer_time: buffer_time * 100 / total_time,
    handleChangeTime: handleChangeTime,
    handleStartMoveTime: handleStartMoveTime,
    handleEndMoveTime: handleEndMoveTime
  })), IS_MOBILE ? null : react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$k('VideoLiveUtils_part')
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement(VideoSettings, {
    ref_btn_setting: ref_btn_setting,
    open_setting: open_setting,
    size_icon: size_icon,
    toggleOpenSetting: toggleOpenSetting
  })), IS_MOBILE ? null : react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$k('VideoLiveUtils_part')
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement(VideoZoom, {
    zoom_icon_name: 'arrow',
    size_icon: size_icon,
    is_zoom_out: is_zoom_out,
    toggleZoom: toggleZoom
  })), react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$k('VideoLiveUtils_part')
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement(VideoSound, {
    volume: volume,
    is_mute: is_mute,
    size_icon: size_icon,
    toggleMute: toggleMute,
    handleChangeVolume: handleChangeVolume
  }))));
}

function IconsEye(_ref) {
  var _ref$x = _ref.x,
      x = _ref$x === void 0 ? 200 : _ref$x,
      _ref$y = _ref.y,
      y = _ref$y === void 0 ? 200 : _ref$y,
      _ref$size_icon = _ref.size_icon,
      size_icon = _ref$size_icon === void 0 ? '1.5rem' : _ref$size_icon,
      _ref$close_eye = _ref.close_eye,
      close_eye = _ref$close_eye === void 0 ? true : _ref$close_eye;
  return react__WEBPACK_IMPORTED_MODULE_0__.createElement("svg", {
    className: 'IconsEye',
    height: size_icon,
    width: size_icon,
    viewBox: x + " " + y + " 200 200",
    stroke: 'currentColor',
    fill: 'currentColor',
    strokeLinecap: 'round'
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement("path", {
    d: 'M200,300 Q300,200 400,300 Q300,400 200,300',
    fill: 'none',
    strokeWidth: '10'
  }), react__WEBPACK_IMPORTED_MODULE_0__.createElement("circle", {
    className: "IconsEye_in " + (close_eye ? styles['display-none'] : ''),
    cx: '300',
    cy: '300',
    r: '30'
  }), react__WEBPACK_IMPORTED_MODULE_0__.createElement("g", {
    className: close_eye ? '' : styles['display-none']
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement("path", {
    d: 'M200,300 Q300,350 400,300',
    fill: 'none',
    strokeWidth: '20'
  }), react__WEBPACK_IMPORTED_MODULE_0__.createElement("line", {
    x1: '210',
    y1: '210',
    x2: '390',
    y2: '390',
    strokeWidth: '30'
  })));
}

var VideoLiveViewStyles = {"VideoLiveView":"_1_n5C","VideoLiveView_title":"_ko_Qh","VideoLiveView_title-live":"_39gHO","VideoLiveView_view":"_2Mtwc","VideoLiveView_count":"_3mB0i"};

function _getClassModuleCss$l(className) {
  if (className === void 0) {
    className = '';
  }

  return getClassModuleCss({
    className: className,
    styles: VideoLiveViewStyles
  });
}

function VideoLiveView(_ref) {
  var is_live_view = _ref.is_live_view,
      total_view = _ref.total_view;
  return react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$l('VideoLiveView')
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$l('VideoLiveView_title') + " " + (is_live_view ? _getClassModuleCss$l('VideoLiveView_title-live') : '')
  }, "LIVE"), react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$l('VideoLiveView_view')
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement(IconsEye, {
    size_icon: '13px',
    close_eye: false
  }), react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$l('VideoLiveView_count')
  }, total_view)));
}

var VideoLiveElmStyles = {"VideoLiveElm":"_1A6mG","IconsEye":"_3d0TT","IconsEye_in":"_31AXa","VideoLiveElm_face":"_Vtb9_","VideoLiveElm_view":"_11mcL","VideoLiveElm-mobile":"_7xCaU","VideoLiveElm-play":"_3Edu5","VideoLiveElm_utils":"_1sTiA"};

function _getClassModuleCss$m(className) {
  if (className === void 0) {
    className = '';
  }

  return getClassModuleCss({
    className: className,
    styles: VideoLiveElmStyles
  });
}

function VideoLiveElm(_ref) {
  var ref_main_video = _ref.ref_main_video,
      ref_video_elm = _ref.ref_video_elm,
      video = _ref.video,
      track_arr = _ref.track_arr,
      total_view = _ref.total_view,
      is_play = _ref.is_play,
      is_mute = _ref.is_mute,
      volume = _ref.volume,
      is_zoom_out = _ref.is_zoom_out,
      is_hide_cursor = _ref.is_hide_cursor,
      c_time = _ref.c_time,
      buffer_time = _ref.buffer_time,
      total_time = _ref.total_time,
      _ref$size_icon = _ref.size_icon,
      size_icon = _ref$size_icon === void 0 ? IS_MOBILE ? '16px' : '20px' : _ref$size_icon,
      face_video_elm = _ref.face_video_elm,
      open_setting = _ref.open_setting,
      toggleOpenSetting = _ref.toggleOpenSetting,
      togglePlayPause = _ref.togglePlayPause,
      gotoLiveView = _ref.gotoLiveView,
      toggleZoom = _ref.toggleZoom,
      toggleMute = _ref.toggleMute,
      handleChangeVolume = _ref.handleChangeVolume,
      handleChangeTime = _ref.handleChangeTime,
      handleStartMoveTime = _ref.handleStartMoveTime,
      handleEndMoveTime = _ref.handleEndMoveTime;
  var ref_btn_setting = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  return react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    ref: ref_main_video,
    className: _getClassModuleCss$m('VideoLiveElm') + " " + (IS_MOBILE ? _getClassModuleCss$m('VideoLiveElm-mobile') : '') + " " + (is_play ? _getClassModuleCss$m('VideoLiveElm-play') : '')
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement(VideoItem, {
    ref_video_elm: ref_video_elm,
    video: video,
    track_arr: track_arr
  }), react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$m('VideoLiveElm_face')
  }, face_video_elm), react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: _getClassModuleCss$m('VideoLiveElm_view')
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement(VideoLiveView, {
    is_live_view: c_time == total_time,
    total_view: total_view
  })), react__WEBPACK_IMPORTED_MODULE_0__.createElement(VideoUtilsLayout, {
    ref_main_video: ref_main_video,
    is_hide_cursor: is_hide_cursor,
    open_setting: open_setting
  }, react__WEBPACK_IMPORTED_MODULE_0__.createElement(VideoLiveUtils, {
    is_play: is_play,
    is_zoom_out: is_zoom_out,
    is_mute: is_mute,
    volume: volume,
    c_time: c_time,
    buffer_time: buffer_time,
    total_time: total_time,
    ref_btn_setting: ref_btn_setting,
    open_setting: open_setting,
    toggleOpenSetting: toggleOpenSetting,
    size_icon: size_icon,
    togglePlayPause: togglePlayPause,
    gotoLiveView: gotoLiveView,
    toggleZoom: toggleZoom,
    toggleMute: toggleMute,
    handleChangeVolume: handleChangeVolume,
    handleChangeTime: handleChangeTime,
    handleStartMoveTime: handleStartMoveTime,
    handleEndMoveTime: handleEndMoveTime
  })));
}

function VideoLive(_ref) {
  var video = _ref.video,
      track_arr = _ref.track_arr,
      total_view = _ref.total_view,
      face_video_elm = _ref.face_video_elm,
      stop_when_over = _ref.stop_when_over,
      initial_is_play = _ref.initial_is_play,
      initial_zoom_lv = _ref.initial_zoom_lv,
      _ref$use_fullscreen = _ref.use_fullscreen,
      use_fullscreen = _ref$use_fullscreen === void 0 ? false : _ref$use_fullscreen,
      _ref$max_zoom_lv = _ref.max_zoom_lv,
      max_zoom_lv = _ref$max_zoom_lv === void 0 ? 0 : _ref$max_zoom_lv,
      initial_volume = _ref.initial_volume,
      initial_is_mute = _ref.initial_is_mute,
      initial_c_time = _ref.initial_c_time,
      initial_total_time = _ref.initial_total_time,
      beforeTogglePlay = _ref.beforeTogglePlay,
      afterTogglePlay = _ref.afterTogglePlay,
      beforeChangeVolume = _ref.beforeChangeVolume,
      afterChangeVolume = _ref.afterChangeVolume,
      beforeToggleMute = _ref.beforeToggleMute,
      afterToggleMute = _ref.afterToggleMute,
      beforeChangeZoomLv = _ref.beforeChangeZoomLv,
      afterChangeZoomLv = _ref.afterChangeZoomLv,
      beforeChangeTime = _ref.beforeChangeTime,
      afterChangeTime = _ref.afterChangeTime;
  var ref_main_video = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  var ref_video_elm = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);

  var _useVideoUtils = useVideoUtils({
    ref_main_video: ref_main_video,
    ref_video_elm: ref_video_elm,
    is_live: true,
    stop_when_over: stop_when_over,
    use_fullscreen: use_fullscreen,
    initial_is_play: initial_is_play,
    initial_zoom_lv: initial_zoom_lv,
    initial_volume: initial_volume,
    max_zoom_lv: max_zoom_lv,
    initial_is_mute: initial_is_mute,
    initial_c_time: initial_c_time,
    initial_total_time: initial_total_time,
    beforeTogglePlay: beforeTogglePlay,
    afterTogglePlay: afterTogglePlay,
    beforeChangeVolume: beforeChangeVolume,
    afterChangeVolume: afterChangeVolume,
    beforeToggleMute: beforeToggleMute,
    afterToggleMute: afterToggleMute,
    beforeChangeZoomLv: beforeChangeZoomLv,
    afterChangeZoomLv: afterChangeZoomLv,
    beforeChangeTime: beforeChangeTime,
    afterChangeTime: afterChangeTime
  }),
      ref_is_play = _useVideoUtils.ref_is_play,
      ref_zoom_lv = _useVideoUtils.ref_zoom_lv,
      ref_volume = _useVideoUtils.ref_volume,
      ref_is_mute = _useVideoUtils.ref_is_mute,
      ref_c_time = _useVideoUtils.ref_c_time,
      ref_total_time = _useVideoUtils.ref_total_time,
      ref_buffer_time = _useVideoUtils.ref_buffer_time,
      ref_is_hide_cursor = _useVideoUtils.ref_is_hide_cursor,
      ref_open_setting = _useVideoUtils.ref_open_setting,
      toggleOpenSetting = _useVideoUtils.toggleOpenSetting,
      togglePlay = _useVideoUtils.togglePlay,
      changeVolume = _useVideoUtils.changeVolume,
      toggleMute = _useVideoUtils.toggleMute,
      changeZoomLv = _useVideoUtils.changeZoomLv,
      changeTime = _useVideoUtils.changeTime,
      startMoveTime = _useVideoUtils.startMoveTime,
      endMoveTime = _useVideoUtils.endMoveTime,
      gotoLiveView = _useVideoUtils.gotoLiveView;

  return react__WEBPACK_IMPORTED_MODULE_0__.createElement(VideoLiveElm, {
    ref_main_video: ref_main_video,
    ref_video_elm: ref_video_elm,
    video: video,
    track_arr: track_arr,
    total_view: total_view,
    is_play: ref_is_play.current,
    is_mute: ref_is_mute.current,
    volume: ref_volume.current,
    is_zoom_out: ref_zoom_lv.current == max_zoom_lv && max_zoom_lv > 0,
    is_hide_cursor: ref_is_hide_cursor.current,
    c_time: ref_c_time.current,
    buffer_time: ref_buffer_time.current,
    total_time: ref_total_time.current,
    face_video_elm: face_video_elm,
    open_setting: ref_open_setting.current,
    toggleOpenSetting: toggleOpenSetting,
    togglePlayPause: togglePlay,
    gotoLiveView: gotoLiveView,
    toggleZoom: changeZoomLv,
    toggleMute: toggleMute,
    handleChangeVolume: changeVolume,
    handleChangeTime: changeTime,
    handleStartMoveTime: startMoveTime,
    handleEndMoveTime: endMoveTime
  });
}


//# sourceMappingURL=index.modern.js.map


/***/ }),

/***/ "./node_modules/react-video-ts/node_modules/react-commons-ts/dist/index.modern.js":
/*!****************************************************************************************!*\
  !*** ./node_modules/react-video-ts/node_modules/react-commons-ts/dist/index.modern.js ***!
  \****************************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "common_constant": () => (/* binding */ constant),
/* harmony export */   "common_types": () => (/* binding */ _common),
/* harmony export */   "formatLocalDateString": () => (/* binding */ formatLocalDateString),
/* harmony export */   "formatLocalDateTimeString": () => (/* binding */ formatLocalDateTimeString),
/* harmony export */   "formatNum": () => (/* binding */ formatNum),
/* harmony export */   "getClassModuleCss": () => (/* binding */ getClassModuleCss),
/* harmony export */   "getTimeAndUnit": () => (/* binding */ getTimeAndUnit),
/* harmony export */   "toggleDataset": () => (/* binding */ toggleDataset),
/* harmony export */   "unitNumber": () => (/* binding */ unitNumber),
/* harmony export */   "unitTime": () => (/* binding */ unitTime),
/* harmony export */   "unitTimeJustNow": () => (/* binding */ unitTimeJustNow),
/* harmony export */   "useBool": () => (/* binding */ useBool),
/* harmony export */   "useDisplayBlockToNone": () => (/* binding */ useDisplayBlockToNone),
/* harmony export */   "useForceUpdate": () => (/* binding */ useForceUpdate),
/* harmony export */   "useHold": () => (/* binding */ useHold),
/* harmony export */   "useMounted": () => (/* binding */ useMounted),
/* harmony export */   "useMouseDownToWindowUp": () => (/* binding */ useMouseDownToWindowUp),
/* harmony export */   "useToggleDataset": () => (/* binding */ useToggleDataset),
/* harmony export */   "waitingToDoLast": () => (/* binding */ waitingToDoLast)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "./node_modules/react/index.js");


var IS_MOBILE = navigator.userAgent.toUpperCase().search('MOBILE') >= 0;

var constant = {
    __proto__: null,
    IS_MOBILE: IS_MOBILE
};



var _common = {
    __proto__: null
};

var useBool = function useBool(initial_bool) {
  if (initial_bool === void 0) {
    initial_bool = false;
  }

  var _useState = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(initial_bool),
      is_true = _useState[0],
      setIsTrue = _useState[1];

  function toggleBool() {
    setIsTrue(function (is_true) {
      return !is_true;
    });
  }

  return {
    is_true: is_true,
    setIsTrue: setIsTrue,
    toggleBool: toggleBool
  };
};

function useMounted() {
  var _useState = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(true),
      mounted = _useState[0],
      setMounted = _useState[1];

  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(function () {
    return function () {
      setMounted(false);
    };
  }, []);
  return mounted;
}

function useHold(_ref) {
  var _ref$time = _ref.time,
      time = _ref$time === void 0 ? 500 : _ref$time,
      _ref$use_holding = _ref.use_holding,
      use_holding = _ref$use_holding === void 0 ? false : _ref$use_holding,
      _ref$time_holding_sta = _ref.time_holding_start,
      time_holding_start = _ref$time_holding_sta === void 0 ? 100 : _ref$time_holding_sta;

  var _useState = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(false),
      holding = _useState[0],
      setHolding = _useState[1];

  var timeout = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  var timeout_holding = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  var mounted = useMounted();

  function StartHold(callback) {
    if (callback === void 0) {
      callback = function callback() {};
    }

    timeout_holding.current = setTimeout(function () {
      if (!mounted) {
        return;
      }

      use_holding && setHolding(true);
      handleHolding(callback);
    }, time_holding_start);
  }

  function handleHolding(callback) {
    if (callback === void 0) {
      callback = function callback() {};
    }

    timeout.current = setTimeout(function () {
      if (!mounted) {
        return;
      }

      use_holding && setHolding(false);
      timeout_holding.current && clearTimeout(timeout_holding.current);
      callback();
    }, time);
  }

  function StopHold() {
    use_holding && setHolding(false);
    timeout_holding.current && clearTimeout(timeout_holding.current);
    timeout.current && clearTimeout(timeout.current);
  }

  return {
    holding: holding,
    StartHold: StartHold,
    StopHold: StopHold
  };
}

function useForceUpdate() {
  var _useState = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(false),
      setForceUpdate = _useState[1];

  function forceUpdate() {
    setForceUpdate(function (force_update) {
      return !force_update;
    });
  }

  return forceUpdate;
}

function useDisplayBlockToNone(_ref) {
  var _ref$trans_time = _ref.trans_time,
      trans_time = _ref$trans_time === void 0 ? 250 : _ref$trans_time,
      _ref$initial_display_ = _ref.initial_display_none,
      initial_display_none = _ref$initial_display_ === void 0 ? true : _ref$initial_display_;

  var _useState = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(initial_display_none),
      opacity_0 = _useState[0],
      setOpacity0 = _useState[1];

  var _useState2 = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(initial_display_none),
      display_none = _useState2[0],
      setDisplayNone = _useState2[1];

  var ref_interval_to_none = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  var ref_interval_to_block = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);

  function changeBlockToNone() {
    setOpacity0(true);
    ref_interval_to_none.current = setTimeout(function () {
      setDisplayNone(true);
    }, trans_time);
  }

  function changeNoneToBlock() {
    setDisplayNone(false);
    ref_interval_to_block.current = setTimeout(function () {
      setOpacity0(false);
    }, 0);
  }

  function toggleBlockNone() {
    if (ref_interval_to_none.current) {
      clearTimeout(ref_interval_to_none.current);
      ref_interval_to_none.current = null;
      changeNoneToBlock();
      return;
    }

    if (ref_interval_to_block.current) {
      clearTimeout(ref_interval_to_block.current);
      ref_interval_to_block.current = null;
      changeBlockToNone();
      return;
    }

    if (opacity_0) {
      changeNoneToBlock();
    } else {
      changeBlockToNone();
    }
  }

  return {
    opacity_0: opacity_0,
    display_none: display_none,
    changeNoneToBlock: changeNoneToBlock,
    changeBlockToNone: changeBlockToNone,
    toggleBlockNone: toggleBlockNone
  };
}

function toggleDataset(_ref) {
  var elm = _ref.elm,
      attr_str = _ref.attr_str,
      is_remove = _ref.is_remove;
  var value = parseInt(elm.getAttribute(attr_str) || '0');

  if (!is_remove) {
    elm.setAttribute(attr_str, (value + 1).toString());
  } else {
    if (value <= 1) {
      elm.removeAttribute(attr_str);
    } else {
      elm.setAttribute(attr_str, (value - 1).toString());
    }
  }
}

function useToggleDataset(_ref) {
  var elm = _ref.elm,
      attr_str = _ref.attr_str,
      is_remove = _ref.is_remove;
  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(function () {
    toggleDataset({
      elm: elm,
      attr_str: attr_str,
      is_remove: is_remove
    });
    return function () {
      toggleDataset({
        elm: elm,
        attr_str: attr_str,
        is_remove: true
      });
    };
  }, [is_remove]);
}

function detectNoTouch(e) {
  return e.touches.length == 0;
}

function detectHasTouch(e) {
  return e.touches.length > 0;
}

function useMouseDownToWindowUp(_ref) {
  var _ref$handleDown = _ref.handleDown,
      handleDown = _ref$handleDown === void 0 ? function () {} : _ref$handleDown,
      _ref$handleMove = _ref.handleMove,
      handleMove = _ref$handleMove === void 0 ? function () {} : _ref$handleMove,
      _ref$handleEnd = _ref.handleEnd,
      handleEnd = _ref$handleEnd === void 0 ? function () {} : _ref$handleEnd,
      _ref$detectEndTouch = _ref.detectEndTouch,
      detectEndTouch = _ref$detectEndTouch === void 0 ? detectNoTouch : _ref$detectEndTouch,
      _ref$detectStartTouch = _ref.detectStartTouch,
      detectStartTouch = _ref$detectStartTouch === void 0 ? detectHasTouch : _ref$detectStartTouch;
  var ref_func_move = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  var ref_func_up = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);

  function onDown(e) {
    handleDown(e);

    if (!ref_func_move.current) {
      ref_func_move.current = onMove;
    }

    if (!ref_func_up.current) {
      ref_func_up.current = onUp;
    }

    if (e instanceof TouchEvent) {
      if (detectStartTouch(e)) {
        window.addEventListener('touchmove', ref_func_move.current);
        window.addEventListener('touchend', ref_func_up.current);
      }
    } else {
      window.addEventListener('mousemove', ref_func_move.current);
      window.addEventListener('mouseup', ref_func_up.current);
    }
  }

  function onMove(e) {
    handleMove(e);
  }

  function onUp(e) {
    handleEnd(e);

    if (e instanceof TouchEvent) {
      if (detectEndTouch(e)) {
        ref_func_move.current && window.removeEventListener('touchmove', ref_func_move.current);
        ref_func_up.current && window.removeEventListener('touchend', ref_func_up.current);
      }
    } else {
      ref_func_move.current && window.removeEventListener('mousemove', ref_func_move.current);
      ref_func_up.current && window.removeEventListener('mouseup', ref_func_up.current);
    }

    ref_func_move.current = null;
    ref_func_up.current = null;
  }

  return {
    onDown: onDown
  };
}

function formatLocalDateString(_ref) {
  var _ref$date = _ref.date,
      date = _ref$date === void 0 ? new Date() : _ref$date,
      _ref$locales = _ref.locales,
      locales = _ref$locales === void 0 ? 'vi-VN' : _ref$locales;
  return new Intl.DateTimeFormat(locales, {
    day: '2-digit',
    month: '2-digit',
    year: 'numeric'
  }).format(new Date(date));
}
function formatLocalDateTimeString(_ref2) {
  var _ref2$date = _ref2.date,
      date = _ref2$date === void 0 ? new Date() : _ref2$date,
      _ref2$locales = _ref2.locales,
      locales = _ref2$locales === void 0 ? 'vi-VN' : _ref2$locales;
  return new Intl.DateTimeFormat(locales, {
    hour: '2-digit',
    minute: '2-digit',
    second: '2-digit',
    day: '2-digit',
    month: '2-digit',
    year: 'numeric'
  }).format(new Date(date));
}

var DATA_UNIT_LONG_ARR = [' year', ' month', ' day', ' hour', ' minute', ' second'];
function getTimeAndUnit(_ref) {
  var _ref$time_change = _ref.time_change,
      time_change = _ref$time_change === void 0 ? 0 : _ref$time_change,
      _ref$unit_arr = _ref.unit_arr,
      unit_arr = _ref$unit_arr === void 0 ? DATA_UNIT_LONG_ARR : _ref$unit_arr;

  if (time_change >= 31536000000) {
    return {
      time: Math.floor(time_change / 31536000000),
      unit: unit_arr[0]
    };
  }

  if (time_change >= 2592000000) {
    return {
      time: Math.floor(time_change / 2592000000),
      unit: unit_arr[1]
    };
  }

  if (time_change >= 86400000) {
    return {
      time: Math.floor(time_change / 86400000),
      unit: unit_arr[2]
    };
  }

  if (time_change >= 3600000) {
    return {
      time: Math.floor(time_change / 3600000),
      unit: unit_arr[3]
    };
  }

  if (time_change >= 60000) {
    return {
      time: Math.floor(time_change / 3600000),
      unit: unit_arr[4]
    };
  }

  return {
    time: Math.floor(time_change / 1000),
    unit: unit_arr[5]
  };
}
function unitTime(_ref2) {
  var _ref2$time_change = _ref2.time_change,
      time_change = _ref2$time_change === void 0 ? 0 : _ref2$time_change,
      _ref2$unit_arr = _ref2.unit_arr,
      unit_arr = _ref2$unit_arr === void 0 ? DATA_UNIT_LONG_ARR : _ref2$unit_arr;
  var unit_and_time = getTimeAndUnit({
    time_change: time_change,
    unit_arr: unit_arr
  });
  return "" + unit_and_time.time + unit_and_time.unit;
}
function unitTimeHasSpecial(_ref3) {
  var _ref3$time_change = _ref3.time_change,
      time_change = _ref3$time_change === void 0 ? 0 : _ref3$time_change,
      _ref3$unit_arr = _ref3.unit_arr,
      unit_arr = _ref3$unit_arr === void 0 ? DATA_UNIT_LONG_ARR : _ref3$unit_arr,
      _ref3$detectIsSpecial = _ref3.detectIsSpecial,
      detectIsSpecial = _ref3$detectIsSpecial === void 0 ? function () {
    return false;
  } : _ref3$detectIsSpecial,
      _ref3$getUnitTimeSpec = _ref3.getUnitTimeSpecial,
      getUnitTimeSpecial = _ref3$getUnitTimeSpec === void 0 ? function () {
    return '';
  } : _ref3$getUnitTimeSpec;

  if (detectIsSpecial()) {
    return getUnitTimeSpecial();
  }

  return unitTime({
    time_change: time_change,
    unit_arr: unit_arr
  });
}
function unitTimeJustNow(_ref4) {
  var _ref4$time_change = _ref4.time_change,
      time_change = _ref4$time_change === void 0 ? 0 : _ref4$time_change,
      _ref4$unit_arr = _ref4.unit_arr,
      unit_arr = _ref4$unit_arr === void 0 ? DATA_UNIT_LONG_ARR : _ref4$unit_arr;
  unitTimeHasSpecial({
    time_change: time_change,
    unit_arr: unit_arr,
    detectIsSpecial: function detectIsSpecial() {
      return time_change <= 60;
    },
    getUnitTimeSpecial: function getUnitTimeSpecial() {
      return 'Just now';
    }
  });
}

function formatNum(num) {
  if (num === void 0) {
    num = 0;
  }

  return new Intl.NumberFormat('en').format(num);
}

function unitNumber(_ref) {
  var _ref$num = _ref.num,
      num = _ref$num === void 0 ? 0 : _ref$num,
      _ref$unit_arr = _ref.unit_arr,
      unit_arr = _ref$unit_arr === void 0 ? ['K', 'M', 'B'] : _ref$unit_arr;

  if (num >= 1000000000) {
    return "" + Math.floor(num / 100000000) / 10 + unit_arr[2];
  }

  if (num >= 1000000) {
    return "" + Math.floor(num / 100000) / 10 + unit_arr[1];
  }

  if (num >= 1000) {
    return "" + Math.floor(num / 100) / 10 + unit_arr[0];
  }

  return num;
}

function waitingToDoLast(_ref) {
  var ref_interval = _ref.ref_interval,
      _ref$time = _ref.time,
      time = _ref$time === void 0 ? 200 : _ref$time,
      _ref$callback = _ref.callback,
      callback = _ref$callback === void 0 ? function () {} : _ref$callback;
  ref_interval.current && clearTimeout(ref_interval.current);
  ref_interval.current = setTimeout(function () {
    ref_interval.current && clearTimeout(ref_interval.current);
    callback();
  }, time);
}

function getClassModuleCss(_ref) {
  var _ref$className = _ref.className,
      className = _ref$className === void 0 ? '' : _ref$className,
      styles = _ref.styles;
  return className + " " + (styles[className] || '');
}


//# sourceMappingURL=index.modern.js.map


/***/ }),

/***/ "./node_modules/redux-saga/dist/redux-saga-core-npm-proxy.esm.js":
/*!***********************************************************************!*\
  !*** ./node_modules/redux-saga/dist/redux-saga-core-npm-proxy.esm.js ***!
  \***********************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "CANCEL": () => (/* reexport safe */ _redux_saga_core__WEBPACK_IMPORTED_MODULE_0__.CANCEL),
/* harmony export */   "END": () => (/* reexport safe */ _redux_saga_core__WEBPACK_IMPORTED_MODULE_0__.END),
/* harmony export */   "SAGA_LOCATION": () => (/* reexport safe */ _redux_saga_core__WEBPACK_IMPORTED_MODULE_0__.SAGA_LOCATION),
/* harmony export */   "buffers": () => (/* reexport safe */ _redux_saga_core__WEBPACK_IMPORTED_MODULE_0__.buffers),
/* harmony export */   "channel": () => (/* reexport safe */ _redux_saga_core__WEBPACK_IMPORTED_MODULE_0__.channel),
/* harmony export */   "detach": () => (/* reexport safe */ _redux_saga_core__WEBPACK_IMPORTED_MODULE_0__.detach),
/* harmony export */   "eventChannel": () => (/* reexport safe */ _redux_saga_core__WEBPACK_IMPORTED_MODULE_0__.eventChannel),
/* harmony export */   "isEnd": () => (/* reexport safe */ _redux_saga_core__WEBPACK_IMPORTED_MODULE_0__.isEnd),
/* harmony export */   "multicastChannel": () => (/* reexport safe */ _redux_saga_core__WEBPACK_IMPORTED_MODULE_0__.multicastChannel),
/* harmony export */   "runSaga": () => (/* reexport safe */ _redux_saga_core__WEBPACK_IMPORTED_MODULE_0__.runSaga),
/* harmony export */   "stdChannel": () => (/* reexport safe */ _redux_saga_core__WEBPACK_IMPORTED_MODULE_0__.stdChannel),
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _redux_saga_core__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @redux-saga/core */ "./node_modules/redux-saga/node_modules/@redux-saga/core/dist/redux-saga-core.esm.js");





/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (_redux_saga_core__WEBPACK_IMPORTED_MODULE_0__.default);


/***/ }),

/***/ "./node_modules/redux-saga/dist/redux-saga-effects-npm-proxy.esm.js":
/*!**************************************************************************!*\
  !*** ./node_modules/redux-saga/dist/redux-saga-effects-npm-proxy.esm.js ***!
  \**************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "actionChannel": () => (/* reexport safe */ _redux_saga_core_effects__WEBPACK_IMPORTED_MODULE_0__.actionChannel),
/* harmony export */   "all": () => (/* reexport safe */ _redux_saga_core_effects__WEBPACK_IMPORTED_MODULE_0__.all),
/* harmony export */   "apply": () => (/* reexport safe */ _redux_saga_core_effects__WEBPACK_IMPORTED_MODULE_0__.apply),
/* harmony export */   "call": () => (/* reexport safe */ _redux_saga_core_effects__WEBPACK_IMPORTED_MODULE_0__.call),
/* harmony export */   "cancel": () => (/* reexport safe */ _redux_saga_core_effects__WEBPACK_IMPORTED_MODULE_0__.cancel),
/* harmony export */   "cancelled": () => (/* reexport safe */ _redux_saga_core_effects__WEBPACK_IMPORTED_MODULE_0__.cancelled),
/* harmony export */   "cps": () => (/* reexport safe */ _redux_saga_core_effects__WEBPACK_IMPORTED_MODULE_0__.cps),
/* harmony export */   "debounce": () => (/* reexport safe */ _redux_saga_core_effects__WEBPACK_IMPORTED_MODULE_0__.debounce),
/* harmony export */   "delay": () => (/* reexport safe */ _redux_saga_core_effects__WEBPACK_IMPORTED_MODULE_0__.delay),
/* harmony export */   "effectTypes": () => (/* reexport safe */ _redux_saga_core_effects__WEBPACK_IMPORTED_MODULE_0__.effectTypes),
/* harmony export */   "flush": () => (/* reexport safe */ _redux_saga_core_effects__WEBPACK_IMPORTED_MODULE_0__.flush),
/* harmony export */   "fork": () => (/* reexport safe */ _redux_saga_core_effects__WEBPACK_IMPORTED_MODULE_0__.fork),
/* harmony export */   "getContext": () => (/* reexport safe */ _redux_saga_core_effects__WEBPACK_IMPORTED_MODULE_0__.getContext),
/* harmony export */   "join": () => (/* reexport safe */ _redux_saga_core_effects__WEBPACK_IMPORTED_MODULE_0__.join),
/* harmony export */   "put": () => (/* reexport safe */ _redux_saga_core_effects__WEBPACK_IMPORTED_MODULE_0__.put),
/* harmony export */   "putResolve": () => (/* reexport safe */ _redux_saga_core_effects__WEBPACK_IMPORTED_MODULE_0__.putResolve),
/* harmony export */   "race": () => (/* reexport safe */ _redux_saga_core_effects__WEBPACK_IMPORTED_MODULE_0__.race),
/* harmony export */   "retry": () => (/* reexport safe */ _redux_saga_core_effects__WEBPACK_IMPORTED_MODULE_0__.retry),
/* harmony export */   "select": () => (/* reexport safe */ _redux_saga_core_effects__WEBPACK_IMPORTED_MODULE_0__.select),
/* harmony export */   "setContext": () => (/* reexport safe */ _redux_saga_core_effects__WEBPACK_IMPORTED_MODULE_0__.setContext),
/* harmony export */   "spawn": () => (/* reexport safe */ _redux_saga_core_effects__WEBPACK_IMPORTED_MODULE_0__.spawn),
/* harmony export */   "take": () => (/* reexport safe */ _redux_saga_core_effects__WEBPACK_IMPORTED_MODULE_0__.take),
/* harmony export */   "takeEvery": () => (/* reexport safe */ _redux_saga_core_effects__WEBPACK_IMPORTED_MODULE_0__.takeEvery),
/* harmony export */   "takeLatest": () => (/* reexport safe */ _redux_saga_core_effects__WEBPACK_IMPORTED_MODULE_0__.takeLatest),
/* harmony export */   "takeLeading": () => (/* reexport safe */ _redux_saga_core_effects__WEBPACK_IMPORTED_MODULE_0__.takeLeading),
/* harmony export */   "takeMaybe": () => (/* reexport safe */ _redux_saga_core_effects__WEBPACK_IMPORTED_MODULE_0__.takeMaybe),
/* harmony export */   "throttle": () => (/* reexport safe */ _redux_saga_core_effects__WEBPACK_IMPORTED_MODULE_0__.throttle)
/* harmony export */ });
/* harmony import */ var _redux_saga_core_effects__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @redux-saga/core/effects */ "./node_modules/redux-saga/node_modules/@redux-saga/core/dist/redux-saga-effects.esm.js");



/***/ }),

/***/ "./node_modules/redux-saga/node_modules/@babel/runtime/helpers/esm/extends.js":
/*!************************************************************************************!*\
  !*** ./node_modules/redux-saga/node_modules/@babel/runtime/helpers/esm/extends.js ***!
  \************************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* binding */ _extends)
/* harmony export */ });
function _extends() {
  _extends = Object.assign || function (target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];

      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }

    return target;
  };

  return _extends.apply(this, arguments);
}

/***/ }),

/***/ "./node_modules/redux-saga/node_modules/@babel/runtime/helpers/esm/objectWithoutPropertiesLoose.js":
/*!*********************************************************************************************************!*\
  !*** ./node_modules/redux-saga/node_modules/@babel/runtime/helpers/esm/objectWithoutPropertiesLoose.js ***!
  \*********************************************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* binding */ _objectWithoutPropertiesLoose)
/* harmony export */ });
function _objectWithoutPropertiesLoose(source, excluded) {
  if (source == null) return {};
  var target = {};
  var sourceKeys = Object.keys(source);
  var key, i;

  for (i = 0; i < sourceKeys.length; i++) {
    key = sourceKeys[i];
    if (excluded.indexOf(key) >= 0) continue;
    target[key] = source[key];
  }

  return target;
}

/***/ }),

/***/ "./node_modules/redux-saga/node_modules/@redux-saga/core/dist/io-6de156f3.js":
/*!***********************************************************************************!*\
  !*** ./node_modules/redux-saga/node_modules/@redux-saga/core/dist/io-6de156f3.js ***!
  \***********************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "$": () => (/* binding */ apply),
/* harmony export */   "A": () => (/* binding */ ALL),
/* harmony export */   "B": () => (/* binding */ logError),
/* harmony export */   "C": () => (/* binding */ CALL),
/* harmony export */   "D": () => (/* binding */ wrapSagaDispatch),
/* harmony export */   "E": () => (/* binding */ identity),
/* harmony export */   "F": () => (/* binding */ FORK),
/* harmony export */   "G": () => (/* binding */ GET_CONTEXT),
/* harmony export */   "H": () => (/* binding */ buffers),
/* harmony export */   "I": () => (/* binding */ detach),
/* harmony export */   "J": () => (/* binding */ JOIN),
/* harmony export */   "K": () => (/* binding */ take),
/* harmony export */   "L": () => (/* binding */ fork),
/* harmony export */   "M": () => (/* binding */ cancel),
/* harmony export */   "N": () => (/* binding */ call),
/* harmony export */   "O": () => (/* binding */ actionChannel),
/* harmony export */   "P": () => (/* binding */ PUT),
/* harmony export */   "Q": () => (/* binding */ sliding),
/* harmony export */   "R": () => (/* binding */ RACE),
/* harmony export */   "S": () => (/* binding */ SELECT),
/* harmony export */   "T": () => (/* binding */ TAKE),
/* harmony export */   "U": () => (/* binding */ delay),
/* harmony export */   "V": () => (/* binding */ race),
/* harmony export */   "W": () => (/* binding */ effectTypes),
/* harmony export */   "X": () => (/* binding */ takeMaybe),
/* harmony export */   "Y": () => (/* binding */ put),
/* harmony export */   "Z": () => (/* binding */ putResolve),
/* harmony export */   "_": () => (/* binding */ all),
/* harmony export */   "a": () => (/* binding */ CPS),
/* harmony export */   "a0": () => (/* binding */ cps),
/* harmony export */   "a1": () => (/* binding */ spawn),
/* harmony export */   "a2": () => (/* binding */ join),
/* harmony export */   "a3": () => (/* binding */ select),
/* harmony export */   "a4": () => (/* binding */ cancelled),
/* harmony export */   "a5": () => (/* binding */ flush),
/* harmony export */   "a6": () => (/* binding */ getContext),
/* harmony export */   "a7": () => (/* binding */ setContext),
/* harmony export */   "b": () => (/* binding */ CANCEL),
/* harmony export */   "c": () => (/* binding */ check),
/* harmony export */   "d": () => (/* binding */ ACTION_CHANNEL),
/* harmony export */   "e": () => (/* binding */ expanding),
/* harmony export */   "f": () => (/* binding */ CANCELLED),
/* harmony export */   "g": () => (/* binding */ FLUSH),
/* harmony export */   "h": () => (/* binding */ SET_CONTEXT),
/* harmony export */   "i": () => (/* binding */ internalErr),
/* harmony export */   "j": () => (/* binding */ getMetaInfo),
/* harmony export */   "k": () => (/* binding */ kTrue),
/* harmony export */   "l": () => (/* binding */ createAllStyleChildCallbacks),
/* harmony export */   "m": () => (/* binding */ createEmptyArray),
/* harmony export */   "n": () => (/* binding */ none),
/* harmony export */   "o": () => (/* binding */ once),
/* harmony export */   "p": () => (/* binding */ assignWithSymbols),
/* harmony export */   "q": () => (/* binding */ makeIterator),
/* harmony export */   "r": () => (/* binding */ remove),
/* harmony export */   "s": () => (/* binding */ shouldComplete),
/* harmony export */   "t": () => (/* binding */ noop),
/* harmony export */   "u": () => (/* binding */ flatMap),
/* harmony export */   "v": () => (/* binding */ getLocation),
/* harmony export */   "w": () => (/* binding */ createSetContextWarning),
/* harmony export */   "x": () => (/* binding */ asyncIteratorSymbol),
/* harmony export */   "y": () => (/* binding */ shouldCancel),
/* harmony export */   "z": () => (/* binding */ shouldTerminate)
/* harmony export */ });
/* harmony import */ var _redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @redux-saga/symbols */ "./node_modules/redux-saga/node_modules/@redux-saga/symbols/dist/redux-saga-symbols.esm.js");
/* harmony import */ var _babel_runtime_helpers_esm_extends__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @babel/runtime/helpers/esm/extends */ "./node_modules/redux-saga/node_modules/@babel/runtime/helpers/esm/extends.js");
/* harmony import */ var _redux_saga_is__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! @redux-saga/is */ "./node_modules/redux-saga/node_modules/@redux-saga/is/dist/redux-saga-is.esm.js");
/* harmony import */ var _redux_saga_delay_p__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! @redux-saga/delay-p */ "./node_modules/redux-saga/node_modules/@redux-saga/delay-p/dist/redux-saga-delay-p.esm.js");





var konst = function konst(v) {
  return function () {
    return v;
  };
};
var kTrue =
/*#__PURE__*/
konst(true);

var noop = function noop() {};

if ( true && typeof Proxy !== 'undefined') {
  noop =
  /*#__PURE__*/
  new Proxy(noop, {
    set: function set() {
      throw internalErr('There was an attempt to assign a property to internal `noop` function.');
    }
  });
}
var identity = function identity(v) {
  return v;
};
var hasSymbol = typeof Symbol === 'function';
var asyncIteratorSymbol = hasSymbol && Symbol.asyncIterator ? Symbol.asyncIterator : '@@asyncIterator';
function check(value, predicate, error) {
  if (!predicate(value)) {
    throw new Error(error);
  }
}
var assignWithSymbols = function assignWithSymbols(target, source) {
  (0,_babel_runtime_helpers_esm_extends__WEBPACK_IMPORTED_MODULE_1__.default)(target, source);

  if (Object.getOwnPropertySymbols) {
    Object.getOwnPropertySymbols(source).forEach(function (s) {
      target[s] = source[s];
    });
  }
};
var flatMap = function flatMap(mapper, arr) {
  var _ref;

  return (_ref = []).concat.apply(_ref, arr.map(mapper));
};
function remove(array, item) {
  var index = array.indexOf(item);

  if (index >= 0) {
    array.splice(index, 1);
  }
}
function once(fn) {
  var called = false;
  return function () {
    if (called) {
      return;
    }

    called = true;
    fn();
  };
}

var kThrow = function kThrow(err) {
  throw err;
};

var kReturn = function kReturn(value) {
  return {
    value: value,
    done: true
  };
};

function makeIterator(next, thro, name) {
  if (thro === void 0) {
    thro = kThrow;
  }

  if (name === void 0) {
    name = 'iterator';
  }

  var iterator = {
    meta: {
      name: name
    },
    next: next,
    throw: thro,
    return: kReturn,
    isSagaIterator: true
  };

  if (typeof Symbol !== 'undefined') {
    iterator[Symbol.iterator] = function () {
      return iterator;
    };
  }

  return iterator;
}
function logError(error, _ref2) {
  var sagaStack = _ref2.sagaStack;

  /*eslint-disable no-console*/
  console.error(error);
  console.error(sagaStack);
}
var internalErr = function internalErr(err) {
  return new Error("\n  redux-saga: Error checking hooks detected an inconsistent state. This is likely a bug\n  in redux-saga code and not yours. Thanks for reporting this in the project's github repo.\n  Error: " + err + "\n");
};
var createSetContextWarning = function createSetContextWarning(ctx, props) {
  return (ctx ? ctx + '.' : '') + "setContext(props): argument " + props + " is not a plain object";
};
var FROZEN_ACTION_ERROR = "You can't put (a.k.a. dispatch from saga) frozen actions.\nWe have to define a special non-enumerable property on those actions for scheduling purposes.\nOtherwise you wouldn't be able to communicate properly between sagas & other subscribers (action ordering would become far less predictable).\nIf you are using redux and you care about this behaviour (frozen actions),\nthen you might want to switch to freezing actions in a middleware rather than in action creator.\nExample implementation:\n\nconst freezeActions = store => next => action => next(Object.freeze(action))\n"; // creates empty, but not-holey array

var createEmptyArray = function createEmptyArray(n) {
  return Array.apply(null, new Array(n));
};
var wrapSagaDispatch = function wrapSagaDispatch(dispatch) {
  return function (action) {
    if (true) {
      check(action, function (ac) {
        return !Object.isFrozen(ac);
      }, FROZEN_ACTION_ERROR);
    }

    return dispatch(Object.defineProperty(action, _redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__.SAGA_ACTION, {
      value: true
    }));
  };
};
var shouldTerminate = function shouldTerminate(res) {
  return res === _redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__.TERMINATE;
};
var shouldCancel = function shouldCancel(res) {
  return res === _redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__.TASK_CANCEL;
};
var shouldComplete = function shouldComplete(res) {
  return shouldTerminate(res) || shouldCancel(res);
};
function createAllStyleChildCallbacks(shape, parentCallback) {
  var keys = Object.keys(shape);
  var totalCount = keys.length;

  if (true) {
    check(totalCount, function (c) {
      return c > 0;
    }, 'createAllStyleChildCallbacks: get an empty array or object');
  }

  var completedCount = 0;
  var completed;
  var results = (0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.array)(shape) ? createEmptyArray(totalCount) : {};
  var childCallbacks = {};

  function checkEnd() {
    if (completedCount === totalCount) {
      completed = true;
      parentCallback(results);
    }
  }

  keys.forEach(function (key) {
    var chCbAtKey = function chCbAtKey(res, isErr) {
      if (completed) {
        return;
      }

      if (isErr || shouldComplete(res)) {
        parentCallback.cancel();
        parentCallback(res, isErr);
      } else {
        results[key] = res;
        completedCount++;
        checkEnd();
      }
    };

    chCbAtKey.cancel = noop;
    childCallbacks[key] = chCbAtKey;
  });

  parentCallback.cancel = function () {
    if (!completed) {
      completed = true;
      keys.forEach(function (key) {
        return childCallbacks[key].cancel();
      });
    }
  };

  return childCallbacks;
}
function getMetaInfo(fn) {
  return {
    name: fn.name || 'anonymous',
    location: getLocation(fn)
  };
}
function getLocation(instrumented) {
  return instrumented[_redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__.SAGA_LOCATION];
}

var BUFFER_OVERFLOW = "Channel's Buffer overflow!";
var ON_OVERFLOW_THROW = 1;
var ON_OVERFLOW_DROP = 2;
var ON_OVERFLOW_SLIDE = 3;
var ON_OVERFLOW_EXPAND = 4;
var zeroBuffer = {
  isEmpty: kTrue,
  put: noop,
  take: noop
};

function ringBuffer(limit, overflowAction) {
  if (limit === void 0) {
    limit = 10;
  }

  var arr = new Array(limit);
  var length = 0;
  var pushIndex = 0;
  var popIndex = 0;

  var push = function push(it) {
    arr[pushIndex] = it;
    pushIndex = (pushIndex + 1) % limit;
    length++;
  };

  var take = function take() {
    if (length != 0) {
      var it = arr[popIndex];
      arr[popIndex] = null;
      length--;
      popIndex = (popIndex + 1) % limit;
      return it;
    }
  };

  var flush = function flush() {
    var items = [];

    while (length) {
      items.push(take());
    }

    return items;
  };

  return {
    isEmpty: function isEmpty() {
      return length == 0;
    },
    put: function put(it) {
      if (length < limit) {
        push(it);
      } else {
        var doubledLimit;

        switch (overflowAction) {
          case ON_OVERFLOW_THROW:
            throw new Error(BUFFER_OVERFLOW);

          case ON_OVERFLOW_SLIDE:
            arr[pushIndex] = it;
            pushIndex = (pushIndex + 1) % limit;
            popIndex = pushIndex;
            break;

          case ON_OVERFLOW_EXPAND:
            doubledLimit = 2 * limit;
            arr = flush();
            length = arr.length;
            pushIndex = arr.length;
            popIndex = 0;
            arr.length = doubledLimit;
            limit = doubledLimit;
            push(it);
            break;

          default: // DROP

        }
      }
    },
    take: take,
    flush: flush
  };
}

var none = function none() {
  return zeroBuffer;
};
var fixed = function fixed(limit) {
  return ringBuffer(limit, ON_OVERFLOW_THROW);
};
var dropping = function dropping(limit) {
  return ringBuffer(limit, ON_OVERFLOW_DROP);
};
var sliding = function sliding(limit) {
  return ringBuffer(limit, ON_OVERFLOW_SLIDE);
};
var expanding = function expanding(initialSize) {
  return ringBuffer(initialSize, ON_OVERFLOW_EXPAND);
};

var buffers = /*#__PURE__*/Object.freeze({
  __proto__: null,
  none: none,
  fixed: fixed,
  dropping: dropping,
  sliding: sliding,
  expanding: expanding
});

var TAKE = 'TAKE';
var PUT = 'PUT';
var ALL = 'ALL';
var RACE = 'RACE';
var CALL = 'CALL';
var CPS = 'CPS';
var FORK = 'FORK';
var JOIN = 'JOIN';
var CANCEL = 'CANCEL';
var SELECT = 'SELECT';
var ACTION_CHANNEL = 'ACTION_CHANNEL';
var CANCELLED = 'CANCELLED';
var FLUSH = 'FLUSH';
var GET_CONTEXT = 'GET_CONTEXT';
var SET_CONTEXT = 'SET_CONTEXT';

var effectTypes = /*#__PURE__*/Object.freeze({
  __proto__: null,
  TAKE: TAKE,
  PUT: PUT,
  ALL: ALL,
  RACE: RACE,
  CALL: CALL,
  CPS: CPS,
  FORK: FORK,
  JOIN: JOIN,
  CANCEL: CANCEL,
  SELECT: SELECT,
  ACTION_CHANNEL: ACTION_CHANNEL,
  CANCELLED: CANCELLED,
  FLUSH: FLUSH,
  GET_CONTEXT: GET_CONTEXT,
  SET_CONTEXT: SET_CONTEXT
});

var TEST_HINT = '\n(HINT: if you are getting these errors in tests, consider using createMockTask from @redux-saga/testing-utils)';

var makeEffect = function makeEffect(type, payload) {
  var _ref;

  return _ref = {}, _ref[_redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__.IO] = true, _ref.combinator = false, _ref.type = type, _ref.payload = payload, _ref;
};

var isForkEffect = function isForkEffect(eff) {
  return (0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.effect)(eff) && eff.type === FORK;
};

var detach = function detach(eff) {
  if (true) {
    check(eff, isForkEffect, 'detach(eff): argument must be a fork effect');
  }

  return makeEffect(FORK, (0,_babel_runtime_helpers_esm_extends__WEBPACK_IMPORTED_MODULE_1__.default)({}, eff.payload, {
    detached: true
  }));
};
function take(patternOrChannel, multicastPattern) {
  if (patternOrChannel === void 0) {
    patternOrChannel = '*';
  }

  if ( true && arguments.length) {
    check(arguments[0], _redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.notUndef, 'take(patternOrChannel): patternOrChannel is undefined');
  }

  if ((0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.pattern)(patternOrChannel)) {
    return makeEffect(TAKE, {
      pattern: patternOrChannel
    });
  }

  if ((0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.multicast)(patternOrChannel) && (0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.notUndef)(multicastPattern) && (0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.pattern)(multicastPattern)) {
    return makeEffect(TAKE, {
      channel: patternOrChannel,
      pattern: multicastPattern
    });
  }

  if ((0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.channel)(patternOrChannel)) {
    return makeEffect(TAKE, {
      channel: patternOrChannel
    });
  }

  if (true) {
    throw new Error("take(patternOrChannel): argument " + patternOrChannel + " is not valid channel or a valid pattern");
  }
}
var takeMaybe = function takeMaybe() {
  var eff = take.apply(void 0, arguments);
  eff.payload.maybe = true;
  return eff;
};
function put(channel$1, action) {
  if (true) {
    if (arguments.length > 1) {
      check(channel$1, _redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.notUndef, 'put(channel, action): argument channel is undefined');
      check(channel$1, _redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.channel, "put(channel, action): argument " + channel$1 + " is not a valid channel");
      check(action, _redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.notUndef, 'put(channel, action): argument action is undefined');
    } else {
      check(channel$1, _redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.notUndef, 'put(action): argument action is undefined');
    }
  }

  if ((0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.undef)(action)) {
    action = channel$1; // `undefined` instead of `null` to make default parameter work

    channel$1 = undefined;
  }

  return makeEffect(PUT, {
    channel: channel$1,
    action: action
  });
}
var putResolve = function putResolve() {
  var eff = put.apply(void 0, arguments);
  eff.payload.resolve = true;
  return eff;
};
function all(effects) {
  var eff = makeEffect(ALL, effects);
  eff.combinator = true;
  return eff;
}
function race(effects) {
  var eff = makeEffect(RACE, effects);
  eff.combinator = true;
  return eff;
} // this match getFnCallDescriptor logic

var validateFnDescriptor = function validateFnDescriptor(effectName, fnDescriptor) {
  check(fnDescriptor, _redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.notUndef, effectName + ": argument fn is undefined or null");

  if ((0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.func)(fnDescriptor)) {
    return;
  }

  var context = null;
  var fn;

  if ((0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.array)(fnDescriptor)) {
    context = fnDescriptor[0];
    fn = fnDescriptor[1];
    check(fn, _redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.notUndef, effectName + ": argument of type [context, fn] has undefined or null `fn`");
  } else if ((0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.object)(fnDescriptor)) {
    context = fnDescriptor.context;
    fn = fnDescriptor.fn;
    check(fn, _redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.notUndef, effectName + ": argument of type {context, fn} has undefined or null `fn`");
  } else {
    check(fnDescriptor, _redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.func, effectName + ": argument fn is not function");
    return;
  }

  if (context && (0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.string)(fn)) {
    check(context[fn], _redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.func, effectName + ": context arguments has no such method - \"" + fn + "\"");
    return;
  }

  check(fn, _redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.func, effectName + ": unpacked fn argument (from [context, fn] or {context, fn}) is not a function");
};

function getFnCallDescriptor(fnDescriptor, args) {
  var context = null;
  var fn;

  if ((0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.func)(fnDescriptor)) {
    fn = fnDescriptor;
  } else {
    if ((0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.array)(fnDescriptor)) {
      context = fnDescriptor[0];
      fn = fnDescriptor[1];
    } else {
      context = fnDescriptor.context;
      fn = fnDescriptor.fn;
    }

    if (context && (0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.string)(fn) && (0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.func)(context[fn])) {
      fn = context[fn];
    }
  }

  return {
    context: context,
    fn: fn,
    args: args
  };
}

var isNotDelayEffect = function isNotDelayEffect(fn) {
  return fn !== delay;
};

function call(fnDescriptor) {
  for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    args[_key - 1] = arguments[_key];
  }

  if (true) {
    var arg0 = typeof args[0] === 'number' ? args[0] : 'ms';
    check(fnDescriptor, isNotDelayEffect, "instead of writing `yield call(delay, " + arg0 + ")` where delay is an effect from `redux-saga/effects` you should write `yield delay(" + arg0 + ")`");
    validateFnDescriptor('call', fnDescriptor);
  }

  return makeEffect(CALL, getFnCallDescriptor(fnDescriptor, args));
}
function apply(context, fn, args) {
  if (args === void 0) {
    args = [];
  }

  var fnDescriptor = [context, fn];

  if (true) {
    validateFnDescriptor('apply', fnDescriptor);
  }

  return makeEffect(CALL, getFnCallDescriptor([context, fn], args));
}
function cps(fnDescriptor) {
  if (true) {
    validateFnDescriptor('cps', fnDescriptor);
  }

  for (var _len2 = arguments.length, args = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++) {
    args[_key2 - 1] = arguments[_key2];
  }

  return makeEffect(CPS, getFnCallDescriptor(fnDescriptor, args));
}
function fork(fnDescriptor) {
  if (true) {
    validateFnDescriptor('fork', fnDescriptor);
    check(fnDescriptor, function (arg) {
      return !(0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.effect)(arg);
    }, 'fork: argument must not be an effect');
  }

  for (var _len3 = arguments.length, args = new Array(_len3 > 1 ? _len3 - 1 : 0), _key3 = 1; _key3 < _len3; _key3++) {
    args[_key3 - 1] = arguments[_key3];
  }

  return makeEffect(FORK, getFnCallDescriptor(fnDescriptor, args));
}
function spawn(fnDescriptor) {
  if (true) {
    validateFnDescriptor('spawn', fnDescriptor);
  }

  for (var _len4 = arguments.length, args = new Array(_len4 > 1 ? _len4 - 1 : 0), _key4 = 1; _key4 < _len4; _key4++) {
    args[_key4 - 1] = arguments[_key4];
  }

  return detach(fork.apply(void 0, [fnDescriptor].concat(args)));
}
function join(taskOrTasks) {
  if (true) {
    if (arguments.length > 1) {
      throw new Error('join(...tasks) is not supported any more. Please use join([...tasks]) to join multiple tasks.');
    }

    if ((0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.array)(taskOrTasks)) {
      taskOrTasks.forEach(function (t) {
        check(t, _redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.task, "join([...tasks]): argument " + t + " is not a valid Task object " + TEST_HINT);
      });
    } else {
      check(taskOrTasks, _redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.task, "join(task): argument " + taskOrTasks + " is not a valid Task object " + TEST_HINT);
    }
  }

  return makeEffect(JOIN, taskOrTasks);
}
function cancel(taskOrTasks) {
  if (taskOrTasks === void 0) {
    taskOrTasks = _redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__.SELF_CANCELLATION;
  }

  if (true) {
    if (arguments.length > 1) {
      throw new Error('cancel(...tasks) is not supported any more. Please use cancel([...tasks]) to cancel multiple tasks.');
    }

    if ((0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.array)(taskOrTasks)) {
      taskOrTasks.forEach(function (t) {
        check(t, _redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.task, "cancel([...tasks]): argument " + t + " is not a valid Task object " + TEST_HINT);
      });
    } else if (taskOrTasks !== _redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__.SELF_CANCELLATION && (0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.notUndef)(taskOrTasks)) {
      check(taskOrTasks, _redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.task, "cancel(task): argument " + taskOrTasks + " is not a valid Task object " + TEST_HINT);
    }
  }

  return makeEffect(CANCEL, taskOrTasks);
}
function select(selector) {
  if (selector === void 0) {
    selector = identity;
  }

  for (var _len5 = arguments.length, args = new Array(_len5 > 1 ? _len5 - 1 : 0), _key5 = 1; _key5 < _len5; _key5++) {
    args[_key5 - 1] = arguments[_key5];
  }

  if ( true && arguments.length) {
    check(arguments[0], _redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.notUndef, 'select(selector, [...]): argument selector is undefined');
    check(selector, _redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.func, "select(selector, [...]): argument " + selector + " is not a function");
  }

  return makeEffect(SELECT, {
    selector: selector,
    args: args
  });
}
/**
  channel(pattern, [buffer])    => creates a proxy channel for store actions
**/

function actionChannel(pattern$1, buffer$1) {
  if (true) {
    check(pattern$1, _redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.pattern, 'actionChannel(pattern,...): argument pattern is not valid');

    if (arguments.length > 1) {
      check(buffer$1, _redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.notUndef, 'actionChannel(pattern, buffer): argument buffer is undefined');
      check(buffer$1, _redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.buffer, "actionChannel(pattern, buffer): argument " + buffer$1 + " is not a valid buffer");
    }
  }

  return makeEffect(ACTION_CHANNEL, {
    pattern: pattern$1,
    buffer: buffer$1
  });
}
function cancelled() {
  return makeEffect(CANCELLED, {});
}
function flush(channel$1) {
  if (true) {
    check(channel$1, _redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.channel, "flush(channel): argument " + channel$1 + " is not valid channel");
  }

  return makeEffect(FLUSH, channel$1);
}
function getContext(prop) {
  if (true) {
    check(prop, _redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.string, "getContext(prop): argument " + prop + " is not a string");
  }

  return makeEffect(GET_CONTEXT, prop);
}
function setContext(props) {
  if (true) {
    check(props, _redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.object, createSetContextWarning(null, props));
  }

  return makeEffect(SET_CONTEXT, props);
}
var delay =
/*#__PURE__*/
call.bind(null, _redux_saga_delay_p__WEBPACK_IMPORTED_MODULE_3__.default);




/***/ }),

/***/ "./node_modules/redux-saga/node_modules/@redux-saga/core/dist/redux-saga-core.esm.js":
/*!*******************************************************************************************!*\
  !*** ./node_modules/redux-saga/node_modules/@redux-saga/core/dist/redux-saga-core.esm.js ***!
  \*******************************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "CANCEL": () => (/* reexport safe */ _redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__.CANCEL),
/* harmony export */   "SAGA_LOCATION": () => (/* reexport safe */ _redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__.SAGA_LOCATION),
/* harmony export */   "buffers": () => (/* reexport safe */ _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.H),
/* harmony export */   "detach": () => (/* reexport safe */ _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.I),
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__),
/* harmony export */   "END": () => (/* binding */ END),
/* harmony export */   "channel": () => (/* binding */ channel),
/* harmony export */   "eventChannel": () => (/* binding */ eventChannel),
/* harmony export */   "isEnd": () => (/* binding */ isEnd),
/* harmony export */   "multicastChannel": () => (/* binding */ multicastChannel),
/* harmony export */   "runSaga": () => (/* binding */ runSaga),
/* harmony export */   "stdChannel": () => (/* binding */ stdChannel)
/* harmony export */ });
/* harmony import */ var _redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @redux-saga/symbols */ "./node_modules/redux-saga/node_modules/@redux-saga/symbols/dist/redux-saga-symbols.esm.js");
/* harmony import */ var _babel_runtime_helpers_esm_extends__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @babel/runtime/helpers/esm/extends */ "./node_modules/redux-saga/node_modules/@babel/runtime/helpers/esm/extends.js");
/* harmony import */ var _babel_runtime_helpers_esm_objectWithoutPropertiesLoose__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! @babel/runtime/helpers/esm/objectWithoutPropertiesLoose */ "./node_modules/redux-saga/node_modules/@babel/runtime/helpers/esm/objectWithoutPropertiesLoose.js");
/* harmony import */ var _redux_saga_is__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! @redux-saga/is */ "./node_modules/redux-saga/node_modules/@redux-saga/is/dist/redux-saga-is.esm.js");
/* harmony import */ var _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./io-6de156f3.js */ "./node_modules/redux-saga/node_modules/@redux-saga/core/dist/io-6de156f3.js");
/* harmony import */ var redux__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! redux */ "./node_modules/redux/es/redux.js");
/* harmony import */ var _redux_saga_deferred__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! @redux-saga/deferred */ "./node_modules/redux-saga/node_modules/@redux-saga/deferred/dist/redux-saga-deferred.esm.js");
/* harmony import */ var _redux_saga_delay_p__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! @redux-saga/delay-p */ "./node_modules/redux-saga/node_modules/@redux-saga/delay-p/dist/redux-saga-delay-p.esm.js");











var queue = [];
/**
  Variable to hold a counting semaphore
  - Incrementing adds a lock and puts the scheduler in a `suspended` state (if it's not
    already suspended)
  - Decrementing releases a lock. Zero locks puts the scheduler in a `released` state. This
    triggers flushing the queued tasks.
**/

var semaphore = 0;
/**
  Executes a task 'atomically'. Tasks scheduled during this execution will be queued
  and flushed after this task has finished (assuming the scheduler endup in a released
  state).
**/

function exec(task) {
  try {
    suspend();
    task();
  } finally {
    release();
  }
}
/**
  Executes or queues a task depending on the state of the scheduler (`suspended` or `released`)
**/


function asap(task) {
  queue.push(task);

  if (!semaphore) {
    suspend();
    flush();
  }
}
/**
 * Puts the scheduler in a `suspended` state and executes a task immediately.
 */

function immediately(task) {
  try {
    suspend();
    return task();
  } finally {
    flush();
  }
}
/**
  Puts the scheduler in a `suspended` state. Scheduled tasks will be queued until the
  scheduler is released.
**/

function suspend() {
  semaphore++;
}
/**
  Puts the scheduler in a `released` state.
**/


function release() {
  semaphore--;
}
/**
  Releases the current lock. Executes all queued tasks if the scheduler is in the released state.
**/


function flush() {
  release();
  var task;

  while (!semaphore && (task = queue.shift()) !== undefined) {
    exec(task);
  }
}

var array = function array(patterns) {
  return function (input) {
    return patterns.some(function (p) {
      return matcher(p)(input);
    });
  };
};
var predicate = function predicate(_predicate) {
  return function (input) {
    return _predicate(input);
  };
};
var string = function string(pattern) {
  return function (input) {
    return input.type === String(pattern);
  };
};
var symbol = function symbol(pattern) {
  return function (input) {
    return input.type === pattern;
  };
};
var wildcard = function wildcard() {
  return _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.k;
};
function matcher(pattern) {
  // prettier-ignore
  var matcherCreator = pattern === '*' ? wildcard : (0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.string)(pattern) ? string : (0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.array)(pattern) ? array : (0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.stringableFunc)(pattern) ? string : (0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.func)(pattern) ? predicate : (0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.symbol)(pattern) ? symbol : null;

  if (matcherCreator === null) {
    throw new Error("invalid pattern: " + pattern);
  }

  return matcherCreator(pattern);
}

var END = {
  type: _redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__.CHANNEL_END_TYPE
};
var isEnd = function isEnd(a) {
  return a && a.type === _redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__.CHANNEL_END_TYPE;
};
var CLOSED_CHANNEL_WITH_TAKERS = 'Cannot have a closed channel with pending takers';
var INVALID_BUFFER = 'invalid buffer passed to channel factory function';
var UNDEFINED_INPUT_ERROR = "Saga or channel was provided with an undefined action\nHints:\n  - check that your Action Creator returns a non-undefined value\n  - if the Saga was started using runSaga, check that your subscribe source provides the action to its listeners";
function channel(buffer$1) {
  if (buffer$1 === void 0) {
    buffer$1 = (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.e)();
  }

  var closed = false;
  var takers = [];

  if (true) {
    (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.c)(buffer$1, _redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.buffer, INVALID_BUFFER);
  }

  function checkForbiddenStates() {
    if (closed && takers.length) {
      throw (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.i)(CLOSED_CHANNEL_WITH_TAKERS);
    }

    if (takers.length && !buffer$1.isEmpty()) {
      throw (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.i)('Cannot have pending takers with non empty buffer');
    }
  }

  function put(input) {
    if (true) {
      checkForbiddenStates();
      (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.c)(input, _redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.notUndef, UNDEFINED_INPUT_ERROR);
    }

    if (closed) {
      return;
    }

    if (takers.length === 0) {
      return buffer$1.put(input);
    }

    var cb = takers.shift();
    cb(input);
  }

  function take(cb) {
    if (true) {
      checkForbiddenStates();
      (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.c)(cb, _redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.func, "channel.take's callback must be a function");
    }

    if (closed && buffer$1.isEmpty()) {
      cb(END);
    } else if (!buffer$1.isEmpty()) {
      cb(buffer$1.take());
    } else {
      takers.push(cb);

      cb.cancel = function () {
        (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.r)(takers, cb);
      };
    }
  }

  function flush(cb) {
    if (true) {
      checkForbiddenStates();
      (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.c)(cb, _redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.func, "channel.flush' callback must be a function");
    }

    if (closed && buffer$1.isEmpty()) {
      cb(END);
      return;
    }

    cb(buffer$1.flush());
  }

  function close() {
    if (true) {
      checkForbiddenStates();
    }

    if (closed) {
      return;
    }

    closed = true;
    var arr = takers;
    takers = [];

    for (var i = 0, len = arr.length; i < len; i++) {
      var taker = arr[i];
      taker(END);
    }
  }

  return {
    take: take,
    put: put,
    flush: flush,
    close: close
  };
}
function eventChannel(subscribe, buffer) {
  if (buffer === void 0) {
    buffer = (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.n)();
  }

  var closed = false;
  var unsubscribe;
  var chan = channel(buffer);

  var close = function close() {
    if (closed) {
      return;
    }

    closed = true;

    if ((0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.func)(unsubscribe)) {
      unsubscribe();
    }

    chan.close();
  };

  unsubscribe = subscribe(function (input) {
    if (isEnd(input)) {
      close();
      return;
    }

    chan.put(input);
  });

  if (true) {
    (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.c)(unsubscribe, _redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.func, 'in eventChannel: subscribe should return a function to unsubscribe');
  }

  unsubscribe = (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.o)(unsubscribe);

  if (closed) {
    unsubscribe();
  }

  return {
    take: chan.take,
    flush: chan.flush,
    close: close
  };
}
function multicastChannel() {
  var _ref;

  var closed = false;
  var currentTakers = [];
  var nextTakers = currentTakers;

  function checkForbiddenStates() {
    if (closed && nextTakers.length) {
      throw (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.i)(CLOSED_CHANNEL_WITH_TAKERS);
    }
  }

  var ensureCanMutateNextTakers = function ensureCanMutateNextTakers() {
    if (nextTakers !== currentTakers) {
      return;
    }

    nextTakers = currentTakers.slice();
  };

  var close = function close() {
    if (true) {
      checkForbiddenStates();
    }

    closed = true;
    var takers = currentTakers = nextTakers;
    nextTakers = [];
    takers.forEach(function (taker) {
      taker(END);
    });
  };

  return _ref = {}, _ref[_redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__.MULTICAST] = true, _ref.put = function put(input) {
    if (true) {
      checkForbiddenStates();
      (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.c)(input, _redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.notUndef, UNDEFINED_INPUT_ERROR);
    }

    if (closed) {
      return;
    }

    if (isEnd(input)) {
      close();
      return;
    }

    var takers = currentTakers = nextTakers;

    for (var i = 0, len = takers.length; i < len; i++) {
      var taker = takers[i];

      if (taker[_redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__.MATCH](input)) {
        taker.cancel();
        taker(input);
      }
    }
  }, _ref.take = function take(cb, matcher) {
    if (matcher === void 0) {
      matcher = wildcard;
    }

    if (true) {
      checkForbiddenStates();
    }

    if (closed) {
      cb(END);
      return;
    }

    cb[_redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__.MATCH] = matcher;
    ensureCanMutateNextTakers();
    nextTakers.push(cb);
    cb.cancel = (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.o)(function () {
      ensureCanMutateNextTakers();
      (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.r)(nextTakers, cb);
    });
  }, _ref.close = close, _ref;
}
function stdChannel() {
  var chan = multicastChannel();
  var put = chan.put;

  chan.put = function (input) {
    if (input[_redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__.SAGA_ACTION]) {
      put(input);
      return;
    }

    asap(function () {
      put(input);
    });
  };

  return chan;
}

var RUNNING = 0;
var CANCELLED = 1;
var ABORTED = 2;
var DONE = 3;

function resolvePromise(promise, cb) {
  var cancelPromise = promise[_redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__.CANCEL];

  if ((0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.func)(cancelPromise)) {
    cb.cancel = cancelPromise;
  }

  promise.then(cb, function (error) {
    cb(error, true);
  });
}

var current = 0;
var nextSagaId = (function () {
  return ++current;
});

var _effectRunnerMap;

function getIteratorMetaInfo(iterator, fn) {
  if (iterator.isSagaIterator) {
    return {
      name: iterator.meta.name
    };
  }

  return (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.j)(fn);
}

function createTaskIterator(_ref) {
  var context = _ref.context,
      fn = _ref.fn,
      args = _ref.args;

  // catch synchronous failures; see #152 and #441
  try {
    var result = fn.apply(context, args); // i.e. a generator function returns an iterator

    if ((0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.iterator)(result)) {
      return result;
    }

    var resolved = false;

    var next = function next(arg) {
      if (!resolved) {
        resolved = true; // Only promises returned from fork will be interpreted. See #1573

        return {
          value: result,
          done: !(0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.promise)(result)
        };
      } else {
        return {
          value: arg,
          done: true
        };
      }
    };

    return (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.q)(next);
  } catch (err) {
    // do not bubble up synchronous failures for detached forks
    // instead create a failed task. See #152 and #441
    return (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.q)(function () {
      throw err;
    });
  }
}

function runPutEffect(env, _ref2, cb) {
  var channel = _ref2.channel,
      action = _ref2.action,
      resolve = _ref2.resolve;

  /**
   Schedule the put in case another saga is holding a lock.
   The put will be executed atomically. ie nested puts will execute after
   this put has terminated.
   **/
  asap(function () {
    var result;

    try {
      result = (channel ? channel.put : env.dispatch)(action);
    } catch (error) {
      cb(error, true);
      return;
    }

    if (resolve && (0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.promise)(result)) {
      resolvePromise(result, cb);
    } else {
      cb(result);
    }
  }); // Put effects are non cancellables
}

function runTakeEffect(env, _ref3, cb) {
  var _ref3$channel = _ref3.channel,
      channel = _ref3$channel === void 0 ? env.channel : _ref3$channel,
      pattern = _ref3.pattern,
      maybe = _ref3.maybe;

  var takeCb = function takeCb(input) {
    if (input instanceof Error) {
      cb(input, true);
      return;
    }

    if (isEnd(input) && !maybe) {
      cb(_redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__.TERMINATE);
      return;
    }

    cb(input);
  };

  try {
    channel.take(takeCb, (0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.notUndef)(pattern) ? matcher(pattern) : null);
  } catch (err) {
    cb(err, true);
    return;
  }

  cb.cancel = takeCb.cancel;
}

function runCallEffect(env, _ref4, cb, _ref5) {
  var context = _ref4.context,
      fn = _ref4.fn,
      args = _ref4.args;
  var task = _ref5.task;

  // catch synchronous failures; see #152
  try {
    var result = fn.apply(context, args);

    if ((0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.promise)(result)) {
      resolvePromise(result, cb);
      return;
    }

    if ((0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.iterator)(result)) {
      // resolve iterator
      proc(env, result, task.context, current, (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.j)(fn),
      /* isRoot */
      false, cb);
      return;
    }

    cb(result);
  } catch (error) {
    cb(error, true);
  }
}

function runCPSEffect(env, _ref6, cb) {
  var context = _ref6.context,
      fn = _ref6.fn,
      args = _ref6.args;

  // CPS (ie node style functions) can define their own cancellation logic
  // by setting cancel field on the cb
  // catch synchronous failures; see #152
  try {
    var cpsCb = function cpsCb(err, res) {
      if ((0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.undef)(err)) {
        cb(res);
      } else {
        cb(err, true);
      }
    };

    fn.apply(context, args.concat(cpsCb));

    if (cpsCb.cancel) {
      cb.cancel = cpsCb.cancel;
    }
  } catch (error) {
    cb(error, true);
  }
}

function runForkEffect(env, _ref7, cb, _ref8) {
  var context = _ref7.context,
      fn = _ref7.fn,
      args = _ref7.args,
      detached = _ref7.detached;
  var parent = _ref8.task;
  var taskIterator = createTaskIterator({
    context: context,
    fn: fn,
    args: args
  });
  var meta = getIteratorMetaInfo(taskIterator, fn);
  immediately(function () {
    var child = proc(env, taskIterator, parent.context, current, meta, detached, undefined);

    if (detached) {
      cb(child);
    } else {
      if (child.isRunning()) {
        parent.queue.addTask(child);
        cb(child);
      } else if (child.isAborted()) {
        parent.queue.abort(child.error());
      } else {
        cb(child);
      }
    }
  }); // Fork effects are non cancellables
}

function runJoinEffect(env, taskOrTasks, cb, _ref9) {
  var task = _ref9.task;

  var joinSingleTask = function joinSingleTask(taskToJoin, cb) {
    if (taskToJoin.isRunning()) {
      var joiner = {
        task: task,
        cb: cb
      };

      cb.cancel = function () {
        if (taskToJoin.isRunning()) (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.r)(taskToJoin.joiners, joiner);
      };

      taskToJoin.joiners.push(joiner);
    } else {
      if (taskToJoin.isAborted()) {
        cb(taskToJoin.error(), true);
      } else {
        cb(taskToJoin.result());
      }
    }
  };

  if ((0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.array)(taskOrTasks)) {
    if (taskOrTasks.length === 0) {
      cb([]);
      return;
    }

    var childCallbacks = (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.l)(taskOrTasks, cb);
    taskOrTasks.forEach(function (t, i) {
      joinSingleTask(t, childCallbacks[i]);
    });
  } else {
    joinSingleTask(taskOrTasks, cb);
  }
}

function cancelSingleTask(taskToCancel) {
  if (taskToCancel.isRunning()) {
    taskToCancel.cancel();
  }
}

function runCancelEffect(env, taskOrTasks, cb, _ref10) {
  var task = _ref10.task;

  if (taskOrTasks === _redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__.SELF_CANCELLATION) {
    cancelSingleTask(task);
  } else if ((0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.array)(taskOrTasks)) {
    taskOrTasks.forEach(cancelSingleTask);
  } else {
    cancelSingleTask(taskOrTasks);
  }

  cb(); // cancel effects are non cancellables
}

function runAllEffect(env, effects, cb, _ref11) {
  var digestEffect = _ref11.digestEffect;
  var effectId = current;
  var keys = Object.keys(effects);

  if (keys.length === 0) {
    cb((0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.array)(effects) ? [] : {});
    return;
  }

  var childCallbacks = (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.l)(effects, cb);
  keys.forEach(function (key) {
    digestEffect(effects[key], effectId, childCallbacks[key], key);
  });
}

function runRaceEffect(env, effects, cb, _ref12) {
  var digestEffect = _ref12.digestEffect;
  var effectId = current;
  var keys = Object.keys(effects);
  var response = (0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.array)(effects) ? (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.m)(keys.length) : {};
  var childCbs = {};
  var completed = false;
  keys.forEach(function (key) {
    var chCbAtKey = function chCbAtKey(res, isErr) {
      if (completed) {
        return;
      }

      if (isErr || (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.s)(res)) {
        // Race Auto cancellation
        cb.cancel();
        cb(res, isErr);
      } else {
        cb.cancel();
        completed = true;
        response[key] = res;
        cb(response);
      }
    };

    chCbAtKey.cancel = _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.t;
    childCbs[key] = chCbAtKey;
  });

  cb.cancel = function () {
    // prevents unnecessary cancellation
    if (!completed) {
      completed = true;
      keys.forEach(function (key) {
        return childCbs[key].cancel();
      });
    }
  };

  keys.forEach(function (key) {
    if (completed) {
      return;
    }

    digestEffect(effects[key], effectId, childCbs[key], key);
  });
}

function runSelectEffect(env, _ref13, cb) {
  var selector = _ref13.selector,
      args = _ref13.args;

  try {
    var state = selector.apply(void 0, [env.getState()].concat(args));
    cb(state);
  } catch (error) {
    cb(error, true);
  }
}

function runChannelEffect(env, _ref14, cb) {
  var pattern = _ref14.pattern,
      buffer = _ref14.buffer;
  var chan = channel(buffer);
  var match = matcher(pattern);

  var taker = function taker(action) {
    if (!isEnd(action)) {
      env.channel.take(taker, match);
    }

    chan.put(action);
  };

  var close = chan.close;

  chan.close = function () {
    taker.cancel();
    close();
  };

  env.channel.take(taker, match);
  cb(chan);
}

function runCancelledEffect(env, data, cb, _ref15) {
  var task = _ref15.task;
  cb(task.isCancelled());
}

function runFlushEffect(env, channel, cb) {
  channel.flush(cb);
}

function runGetContextEffect(env, prop, cb, _ref16) {
  var task = _ref16.task;
  cb(task.context[prop]);
}

function runSetContextEffect(env, props, cb, _ref17) {
  var task = _ref17.task;
  (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.p)(task.context, props);
  cb();
}

var effectRunnerMap = (_effectRunnerMap = {}, _effectRunnerMap[_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.T] = runTakeEffect, _effectRunnerMap[_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.P] = runPutEffect, _effectRunnerMap[_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.A] = runAllEffect, _effectRunnerMap[_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.R] = runRaceEffect, _effectRunnerMap[_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.C] = runCallEffect, _effectRunnerMap[_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.a] = runCPSEffect, _effectRunnerMap[_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.F] = runForkEffect, _effectRunnerMap[_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.J] = runJoinEffect, _effectRunnerMap[_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.b] = runCancelEffect, _effectRunnerMap[_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.S] = runSelectEffect, _effectRunnerMap[_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.d] = runChannelEffect, _effectRunnerMap[_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.f] = runCancelledEffect, _effectRunnerMap[_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.g] = runFlushEffect, _effectRunnerMap[_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.G] = runGetContextEffect, _effectRunnerMap[_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.h] = runSetContextEffect, _effectRunnerMap);

/**
 Used to track a parent task and its forks
 In the fork model, forked tasks are attached by default to their parent
 We model this using the concept of Parent task && main Task
 main task is the main flow of the current Generator, the parent tasks is the
 aggregation of the main tasks + all its forked tasks.
 Thus the whole model represents an execution tree with multiple branches (vs the
 linear execution tree in sequential (non parallel) programming)

 A parent tasks has the following semantics
 - It completes if all its forks either complete or all cancelled
 - If it's cancelled, all forks are cancelled as well
 - It aborts if any uncaught error bubbles up from forks
 - If it completes, the return value is the one returned by the main task
 **/

function forkQueue(mainTask, onAbort, cont) {
  var tasks = [];
  var result;
  var completed = false;
  addTask(mainTask);

  var getTasks = function getTasks() {
    return tasks;
  };

  function abort(err) {
    onAbort();
    cancelAll();
    cont(err, true);
  }

  function addTask(task) {
    tasks.push(task);

    task.cont = function (res, isErr) {
      if (completed) {
        return;
      }

      (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.r)(tasks, task);
      task.cont = _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.t;

      if (isErr) {
        abort(res);
      } else {
        if (task === mainTask) {
          result = res;
        }

        if (!tasks.length) {
          completed = true;
          cont(result);
        }
      }
    };
  }

  function cancelAll() {
    if (completed) {
      return;
    }

    completed = true;
    tasks.forEach(function (t) {
      t.cont = _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.t;
      t.cancel();
    });
    tasks = [];
  }

  return {
    addTask: addTask,
    cancelAll: cancelAll,
    abort: abort,
    getTasks: getTasks
  };
}

// there can be only a single saga error created at any given moment

function formatLocation(fileName, lineNumber) {
  return fileName + "?" + lineNumber;
}

function effectLocationAsString(effect) {
  var location = (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.v)(effect);

  if (location) {
    var code = location.code,
        fileName = location.fileName,
        lineNumber = location.lineNumber;
    var source = code + "  " + formatLocation(fileName, lineNumber);
    return source;
  }

  return '';
}

function sagaLocationAsString(sagaMeta) {
  var name = sagaMeta.name,
      location = sagaMeta.location;

  if (location) {
    return name + "  " + formatLocation(location.fileName, location.lineNumber);
  }

  return name;
}

function cancelledTasksAsString(sagaStack) {
  var cancelledTasks = (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.u)(function (i) {
    return i.cancelledTasks;
  }, sagaStack);

  if (!cancelledTasks.length) {
    return '';
  }

  return ['Tasks cancelled due to error:'].concat(cancelledTasks).join('\n');
}

var crashedEffect = null;
var sagaStack = [];
var addSagaFrame = function addSagaFrame(frame) {
  frame.crashedEffect = crashedEffect;
  sagaStack.push(frame);
};
var clear = function clear() {
  crashedEffect = null;
  sagaStack.length = 0;
}; // this sets crashed effect for the soon-to-be-reported saga frame
// this slightly streatches the singleton nature of this module into wrong direction
// as it's even less obvious what's the data flow here, but it is what it is for now

var setCrashedEffect = function setCrashedEffect(effect) {
  crashedEffect = effect;
};
/**
  @returns {string}

  @example
  The above error occurred in task errorInPutSaga {pathToFile}
  when executing effect put({type: 'REDUCER_ACTION_ERROR_IN_PUT'}) {pathToFile}
      created by fetchSaga {pathToFile}
      created by rootSaga {pathToFile}
*/

var toString = function toString() {
  var firstSaga = sagaStack[0],
      otherSagas = sagaStack.slice(1);
  var crashedEffectLocation = firstSaga.crashedEffect ? effectLocationAsString(firstSaga.crashedEffect) : null;
  var errorMessage = "The above error occurred in task " + sagaLocationAsString(firstSaga.meta) + (crashedEffectLocation ? " \n when executing effect " + crashedEffectLocation : '');
  return [errorMessage].concat(otherSagas.map(function (s) {
    return "    created by " + sagaLocationAsString(s.meta);
  }), [cancelledTasksAsString(sagaStack)]).join('\n');
};

function newTask(env, mainTask, parentContext, parentEffectId, meta, isRoot, cont) {
  var _task;

  if (cont === void 0) {
    cont = _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.t;
  }

  var status = RUNNING;
  var taskResult;
  var taskError;
  var deferredEnd = null;
  var cancelledDueToErrorTasks = [];
  var context = Object.create(parentContext);
  var queue = forkQueue(mainTask, function onAbort() {
    cancelledDueToErrorTasks.push.apply(cancelledDueToErrorTasks, queue.getTasks().map(function (t) {
      return t.meta.name;
    }));
  }, end);
  /**
   This may be called by a parent generator to trigger/propagate cancellation
   cancel all pending tasks (including the main task), then end the current task.
    Cancellation propagates down to the whole execution tree held by this Parent task
   It's also propagated to all joiners of this task and their execution tree/joiners
    Cancellation is noop for terminated/Cancelled tasks tasks
   **/

  function cancel() {
    if (status === RUNNING) {
      // Setting status to CANCELLED does not necessarily mean that the task/iterators are stopped
      // effects in the iterator's finally block will still be executed
      status = CANCELLED;
      queue.cancelAll(); // Ending with a TASK_CANCEL will propagate the Cancellation to all joiners

      end(_redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__.TASK_CANCEL, false);
    }
  }

  function end(result, isErr) {
    if (!isErr) {
      // The status here may be RUNNING or CANCELLED
      // If the status is CANCELLED, then we do not need to change it here
      if (result === _redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__.TASK_CANCEL) {
        status = CANCELLED;
      } else if (status !== CANCELLED) {
        status = DONE;
      }

      taskResult = result;
      deferredEnd && deferredEnd.resolve(result);
    } else {
      status = ABORTED;
      addSagaFrame({
        meta: meta,
        cancelledTasks: cancelledDueToErrorTasks
      });

      if (task.isRoot) {
        var sagaStack = toString(); // we've dumped the saga stack to string and are passing it to user's code
        // we know that it won't be needed anymore and we need to clear it

        clear();
        env.onError(result, {
          sagaStack: sagaStack
        });
      }

      taskError = result;
      deferredEnd && deferredEnd.reject(result);
    }

    task.cont(result, isErr);
    task.joiners.forEach(function (joiner) {
      joiner.cb(result, isErr);
    });
    task.joiners = null;
  }

  function setContext(props) {
    if (true) {
      (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.c)(props, _redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.object, (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.w)('task', props));
    }

    (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.p)(context, props);
  }

  function toPromise() {
    if (deferredEnd) {
      return deferredEnd.promise;
    }

    deferredEnd = (0,_redux_saga_deferred__WEBPACK_IMPORTED_MODULE_5__.default)();

    if (status === ABORTED) {
      deferredEnd.reject(taskError);
    } else if (status !== RUNNING) {
      deferredEnd.resolve(taskResult);
    }

    return deferredEnd.promise;
  }

  var task = (_task = {}, _task[_redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__.TASK] = true, _task.id = parentEffectId, _task.meta = meta, _task.isRoot = isRoot, _task.context = context, _task.joiners = [], _task.queue = queue, _task.cancel = cancel, _task.cont = cont, _task.end = end, _task.setContext = setContext, _task.toPromise = toPromise, _task.isRunning = function isRunning() {
    return status === RUNNING;
  }, _task.isCancelled = function isCancelled() {
    return status === CANCELLED || status === RUNNING && mainTask.status === CANCELLED;
  }, _task.isAborted = function isAborted() {
    return status === ABORTED;
  }, _task.result = function result() {
    return taskResult;
  }, _task.error = function error() {
    return taskError;
  }, _task);
  return task;
}

function proc(env, iterator$1, parentContext, parentEffectId, meta, isRoot, cont) {
  if ( true && iterator$1[_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.x]) {
    throw new Error("redux-saga doesn't support async generators, please use only regular ones");
  }

  var finalRunEffect = env.finalizeRunEffect(runEffect);
  /**
    Tracks the current effect cancellation
    Each time the generator progresses. calling runEffect will set a new value
    on it. It allows propagating cancellation to child effects
  **/

  next.cancel = _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.t;
  /** Creates a main task to track the main flow */

  var mainTask = {
    meta: meta,
    cancel: cancelMain,
    status: RUNNING
  };
  /**
   Creates a new task descriptor for this generator.
   A task is the aggregation of it's mainTask and all it's forked tasks.
   **/

  var task = newTask(env, mainTask, parentContext, parentEffectId, meta, isRoot, cont);
  var executingContext = {
    task: task,
    digestEffect: digestEffect
  };
  /**
    cancellation of the main task. We'll simply resume the Generator with a TASK_CANCEL
  **/

  function cancelMain() {
    if (mainTask.status === RUNNING) {
      mainTask.status = CANCELLED;
      next(_redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__.TASK_CANCEL);
    }
  }
  /**
    attaches cancellation logic to this task's continuation
    this will permit cancellation to propagate down the call chain
  **/


  if (cont) {
    cont.cancel = task.cancel;
  } // kicks up the generator


  next(); // then return the task descriptor to the caller

  return task;
  /**
   * This is the generator driver
   * It's a recursive async/continuation function which calls itself
   * until the generator terminates or throws
   * @param {internal commands(TASK_CANCEL | TERMINATE) | any} arg - value, generator will be resumed with.
   * @param {boolean} isErr - the flag shows if effect finished with an error
   *
   * receives either (command | effect result, false) or (any thrown thing, true)
   */

  function next(arg, isErr) {
    try {
      var result;

      if (isErr) {
        result = iterator$1.throw(arg); // user handled the error, we can clear bookkept values

        clear();
      } else if ((0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.y)(arg)) {
        /**
          getting TASK_CANCEL automatically cancels the main task
          We can get this value here
           - By cancelling the parent task manually
          - By joining a Cancelled task
        **/
        mainTask.status = CANCELLED;
        /**
          Cancels the current effect; this will propagate the cancellation down to any called tasks
        **/

        next.cancel();
        /**
          If this Generator has a `return` method then invokes it
          This will jump to the finally block
        **/

        result = (0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.func)(iterator$1.return) ? iterator$1.return(_redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__.TASK_CANCEL) : {
          done: true,
          value: _redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__.TASK_CANCEL
        };
      } else if ((0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.z)(arg)) {
        // We get TERMINATE flag, i.e. by taking from a channel that ended using `take` (and not `takem` used to trap End of channels)
        result = (0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.func)(iterator$1.return) ? iterator$1.return() : {
          done: true
        };
      } else {
        result = iterator$1.next(arg);
      }

      if (!result.done) {
        digestEffect(result.value, parentEffectId, next);
      } else {
        /**
          This Generator has ended, terminate the main task and notify the fork queue
        **/
        if (mainTask.status !== CANCELLED) {
          mainTask.status = DONE;
        }

        mainTask.cont(result.value);
      }
    } catch (error) {
      if (mainTask.status === CANCELLED) {
        throw error;
      }

      mainTask.status = ABORTED;
      mainTask.cont(error, true);
    }
  }

  function runEffect(effect, effectId, currCb) {
    /**
      each effect runner must attach its own logic of cancellation to the provided callback
      it allows this generator to propagate cancellation downward.
       ATTENTION! effect runners must setup the cancel logic by setting cb.cancel = [cancelMethod]
      And the setup must occur before calling the callback
       This is a sort of inversion of control: called async functions are responsible
      of completing the flow by calling the provided continuation; while caller functions
      are responsible for aborting the current flow by calling the attached cancel function
       Library users can attach their own cancellation logic to promises by defining a
      promise[CANCEL] method in their returned promises
      ATTENTION! calling cancel must have no effect on an already completed or cancelled effect
    **/
    if ((0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.promise)(effect)) {
      resolvePromise(effect, currCb);
    } else if ((0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.iterator)(effect)) {
      // resolve iterator
      proc(env, effect, task.context, effectId, meta,
      /* isRoot */
      false, currCb);
    } else if (effect && effect[_redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__.IO]) {
      var effectRunner = effectRunnerMap[effect.type];
      effectRunner(env, effect.payload, currCb, executingContext);
    } else {
      // anything else returned as is
      currCb(effect);
    }
  }

  function digestEffect(effect, parentEffectId, cb, label) {
    if (label === void 0) {
      label = '';
    }

    var effectId = nextSagaId();
    env.sagaMonitor && env.sagaMonitor.effectTriggered({
      effectId: effectId,
      parentEffectId: parentEffectId,
      label: label,
      effect: effect
    });
    /**
      completion callback and cancel callback are mutually exclusive
      We can't cancel an already completed effect
      And We can't complete an already cancelled effectId
    **/

    var effectSettled; // Completion callback passed to the appropriate effect runner

    function currCb(res, isErr) {
      if (effectSettled) {
        return;
      }

      effectSettled = true;
      cb.cancel = _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.t; // defensive measure

      if (env.sagaMonitor) {
        if (isErr) {
          env.sagaMonitor.effectRejected(effectId, res);
        } else {
          env.sagaMonitor.effectResolved(effectId, res);
        }
      }

      if (isErr) {
        setCrashedEffect(effect);
      }

      cb(res, isErr);
    } // tracks down the current cancel


    currCb.cancel = _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.t; // setup cancellation logic on the parent cb

    cb.cancel = function () {
      // prevents cancelling an already completed effect
      if (effectSettled) {
        return;
      }

      effectSettled = true;
      currCb.cancel(); // propagates cancel downward

      currCb.cancel = _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.t; // defensive measure

      env.sagaMonitor && env.sagaMonitor.effectCancelled(effectId);
    };

    finalRunEffect(effect, effectId, currCb);
  }
}

var RUN_SAGA_SIGNATURE = 'runSaga(options, saga, ...args)';
var NON_GENERATOR_ERR = RUN_SAGA_SIGNATURE + ": saga argument must be a Generator function!";
function runSaga(_ref, saga) {
  var _ref$channel = _ref.channel,
      channel = _ref$channel === void 0 ? stdChannel() : _ref$channel,
      dispatch = _ref.dispatch,
      getState = _ref.getState,
      _ref$context = _ref.context,
      context = _ref$context === void 0 ? {} : _ref$context,
      sagaMonitor = _ref.sagaMonitor,
      effectMiddlewares = _ref.effectMiddlewares,
      _ref$onError = _ref.onError,
      onError = _ref$onError === void 0 ? _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.B : _ref$onError;

  if (true) {
    (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.c)(saga, _redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.func, NON_GENERATOR_ERR);
  }

  for (var _len = arguments.length, args = new Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++) {
    args[_key - 2] = arguments[_key];
  }

  var iterator$1 = saga.apply(void 0, args);

  if (true) {
    (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.c)(iterator$1, _redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.iterator, NON_GENERATOR_ERR);
  }

  var effectId = nextSagaId();

  if (sagaMonitor) {
    // monitors are expected to have a certain interface, let's fill-in any missing ones
    sagaMonitor.rootSagaStarted = sagaMonitor.rootSagaStarted || _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.t;
    sagaMonitor.effectTriggered = sagaMonitor.effectTriggered || _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.t;
    sagaMonitor.effectResolved = sagaMonitor.effectResolved || _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.t;
    sagaMonitor.effectRejected = sagaMonitor.effectRejected || _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.t;
    sagaMonitor.effectCancelled = sagaMonitor.effectCancelled || _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.t;
    sagaMonitor.actionDispatched = sagaMonitor.actionDispatched || _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.t;
    sagaMonitor.rootSagaStarted({
      effectId: effectId,
      saga: saga,
      args: args
    });
  }

  if (true) {
    if ((0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.notUndef)(dispatch)) {
      (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.c)(dispatch, _redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.func, 'dispatch must be a function');
    }

    if ((0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.notUndef)(getState)) {
      (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.c)(getState, _redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.func, 'getState must be a function');
    }

    if ((0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.notUndef)(effectMiddlewares)) {
      var MIDDLEWARE_TYPE_ERROR = 'effectMiddlewares must be an array of functions';
      (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.c)(effectMiddlewares, _redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.array, MIDDLEWARE_TYPE_ERROR);
      effectMiddlewares.forEach(function (effectMiddleware) {
        return (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.c)(effectMiddleware, _redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.func, MIDDLEWARE_TYPE_ERROR);
      });
    }

    (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.c)(onError, _redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.func, 'onError passed to the redux-saga is not a function!');
  }

  var finalizeRunEffect;

  if (effectMiddlewares) {
    var middleware = redux__WEBPACK_IMPORTED_MODULE_7__.compose.apply(void 0, effectMiddlewares);

    finalizeRunEffect = function finalizeRunEffect(runEffect) {
      return function (effect, effectId, currCb) {
        var plainRunEffect = function plainRunEffect(eff) {
          return runEffect(eff, effectId, currCb);
        };

        return middleware(plainRunEffect)(effect);
      };
    };
  } else {
    finalizeRunEffect = _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.E;
  }

  var env = {
    channel: channel,
    dispatch: (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.D)(dispatch),
    getState: getState,
    sagaMonitor: sagaMonitor,
    onError: onError,
    finalizeRunEffect: finalizeRunEffect
  };
  return immediately(function () {
    var task = proc(env, iterator$1, context, effectId, (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.j)(saga),
    /* isRoot */
    true, undefined);

    if (sagaMonitor) {
      sagaMonitor.effectResolved(effectId, task);
    }

    return task;
  });
}

function sagaMiddlewareFactory(_temp) {
  var _ref = _temp === void 0 ? {} : _temp,
      _ref$context = _ref.context,
      context = _ref$context === void 0 ? {} : _ref$context,
      _ref$channel = _ref.channel,
      channel = _ref$channel === void 0 ? stdChannel() : _ref$channel,
      sagaMonitor = _ref.sagaMonitor,
      options = (0,_babel_runtime_helpers_esm_objectWithoutPropertiesLoose__WEBPACK_IMPORTED_MODULE_2__.default)(_ref, ["context", "channel", "sagaMonitor"]);

  var boundRunSaga;

  if (true) {
    (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.c)(channel, _redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.channel, 'options.channel passed to the Saga middleware is not a channel');
  }

  function sagaMiddleware(_ref2) {
    var getState = _ref2.getState,
        dispatch = _ref2.dispatch;
    boundRunSaga = runSaga.bind(null, (0,_babel_runtime_helpers_esm_extends__WEBPACK_IMPORTED_MODULE_1__.default)({}, options, {
      context: context,
      channel: channel,
      dispatch: dispatch,
      getState: getState,
      sagaMonitor: sagaMonitor
    }));
    return function (next) {
      return function (action) {
        if (sagaMonitor && sagaMonitor.actionDispatched) {
          sagaMonitor.actionDispatched(action);
        }

        var result = next(action); // hit reducers

        channel.put(action);
        return result;
      };
    };
  }

  sagaMiddleware.run = function () {
    if ( true && !boundRunSaga) {
      throw new Error('Before running a Saga, you must mount the Saga middleware on the Store using applyMiddleware');
    }

    return boundRunSaga.apply(void 0, arguments);
  };

  sagaMiddleware.setContext = function (props) {
    if (true) {
      (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.c)(props, _redux_saga_is__WEBPACK_IMPORTED_MODULE_3__.object, (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.w)('sagaMiddleware', props));
    }

    (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_4__.p)(context, props);
  };

  return sagaMiddleware;
}

/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (sagaMiddlewareFactory);



/***/ }),

/***/ "./node_modules/redux-saga/node_modules/@redux-saga/core/dist/redux-saga-effects.esm.js":
/*!**********************************************************************************************!*\
  !*** ./node_modules/redux-saga/node_modules/@redux-saga/core/dist/redux-saga-effects.esm.js ***!
  \**********************************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "actionChannel": () => (/* reexport safe */ _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.O),
/* harmony export */   "all": () => (/* reexport safe */ _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__._),
/* harmony export */   "apply": () => (/* reexport safe */ _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.$),
/* harmony export */   "call": () => (/* reexport safe */ _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.N),
/* harmony export */   "cancel": () => (/* reexport safe */ _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.M),
/* harmony export */   "cancelled": () => (/* reexport safe */ _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.a4),
/* harmony export */   "cps": () => (/* reexport safe */ _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.a0),
/* harmony export */   "delay": () => (/* reexport safe */ _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.U),
/* harmony export */   "effectTypes": () => (/* reexport safe */ _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.W),
/* harmony export */   "flush": () => (/* reexport safe */ _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.a5),
/* harmony export */   "fork": () => (/* reexport safe */ _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.L),
/* harmony export */   "getContext": () => (/* reexport safe */ _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.a6),
/* harmony export */   "join": () => (/* reexport safe */ _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.a2),
/* harmony export */   "put": () => (/* reexport safe */ _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.Y),
/* harmony export */   "putResolve": () => (/* reexport safe */ _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.Z),
/* harmony export */   "race": () => (/* reexport safe */ _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.V),
/* harmony export */   "select": () => (/* reexport safe */ _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.a3),
/* harmony export */   "setContext": () => (/* reexport safe */ _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.a7),
/* harmony export */   "spawn": () => (/* reexport safe */ _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.a1),
/* harmony export */   "take": () => (/* reexport safe */ _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.K),
/* harmony export */   "takeMaybe": () => (/* reexport safe */ _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.X),
/* harmony export */   "debounce": () => (/* binding */ debounce),
/* harmony export */   "retry": () => (/* binding */ retry$1),
/* harmony export */   "takeEvery": () => (/* binding */ takeEvery$1),
/* harmony export */   "takeLatest": () => (/* binding */ takeLatest$1),
/* harmony export */   "takeLeading": () => (/* binding */ takeLeading$1),
/* harmony export */   "throttle": () => (/* binding */ throttle$1)
/* harmony export */ });
/* harmony import */ var _redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @redux-saga/symbols */ "./node_modules/redux-saga/node_modules/@redux-saga/symbols/dist/redux-saga-symbols.esm.js");
/* harmony import */ var _babel_runtime_helpers_esm_extends__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @babel/runtime/helpers/esm/extends */ "./node_modules/redux-saga/node_modules/@babel/runtime/helpers/esm/extends.js");
/* harmony import */ var _redux_saga_is__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! @redux-saga/is */ "./node_modules/redux-saga/node_modules/@redux-saga/is/dist/redux-saga-is.esm.js");
/* harmony import */ var _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./io-6de156f3.js */ "./node_modules/redux-saga/node_modules/@redux-saga/core/dist/io-6de156f3.js");
/* harmony import */ var _redux_saga_delay_p__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! @redux-saga/delay-p */ "./node_modules/redux-saga/node_modules/@redux-saga/delay-p/dist/redux-saga-delay-p.esm.js");







var done = function done(value) {
  return {
    done: true,
    value: value
  };
};

var qEnd = {};
function safeName(patternOrChannel) {
  if ((0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.channel)(patternOrChannel)) {
    return 'channel';
  }

  if ((0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.stringableFunc)(patternOrChannel)) {
    return String(patternOrChannel);
  }

  if ((0,_redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.func)(patternOrChannel)) {
    return patternOrChannel.name;
  }

  return String(patternOrChannel);
}
function fsmIterator(fsm, startState, name) {
  var stateUpdater,
      errorState,
      effect,
      nextState = startState;

  function next(arg, error) {
    if (nextState === qEnd) {
      return done(arg);
    }

    if (error && !errorState) {
      nextState = qEnd;
      throw error;
    } else {
      stateUpdater && stateUpdater(arg);
      var currentState = error ? fsm[errorState](error) : fsm[nextState]();
      nextState = currentState.nextState;
      effect = currentState.effect;
      stateUpdater = currentState.stateUpdater;
      errorState = currentState.errorState;
      return nextState === qEnd ? done(arg) : effect;
    }
  }

  return (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.q)(next, function (error) {
    return next(null, error);
  }, name);
}

function takeEvery(patternOrChannel, worker) {
  for (var _len = arguments.length, args = new Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++) {
    args[_key - 2] = arguments[_key];
  }

  var yTake = {
    done: false,
    value: (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.K)(patternOrChannel)
  };

  var yFork = function yFork(ac) {
    return {
      done: false,
      value: _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.L.apply(void 0, [worker].concat(args, [ac]))
    };
  };

  var action,
      setAction = function setAction(ac) {
    return action = ac;
  };

  return fsmIterator({
    q1: function q1() {
      return {
        nextState: 'q2',
        effect: yTake,
        stateUpdater: setAction
      };
    },
    q2: function q2() {
      return {
        nextState: 'q1',
        effect: yFork(action)
      };
    }
  }, 'q1', "takeEvery(" + safeName(patternOrChannel) + ", " + worker.name + ")");
}

function takeLatest(patternOrChannel, worker) {
  for (var _len = arguments.length, args = new Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++) {
    args[_key - 2] = arguments[_key];
  }

  var yTake = {
    done: false,
    value: (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.K)(patternOrChannel)
  };

  var yFork = function yFork(ac) {
    return {
      done: false,
      value: _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.L.apply(void 0, [worker].concat(args, [ac]))
    };
  };

  var yCancel = function yCancel(task) {
    return {
      done: false,
      value: (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.M)(task)
    };
  };

  var task, action;

  var setTask = function setTask(t) {
    return task = t;
  };

  var setAction = function setAction(ac) {
    return action = ac;
  };

  return fsmIterator({
    q1: function q1() {
      return {
        nextState: 'q2',
        effect: yTake,
        stateUpdater: setAction
      };
    },
    q2: function q2() {
      return task ? {
        nextState: 'q3',
        effect: yCancel(task)
      } : {
        nextState: 'q1',
        effect: yFork(action),
        stateUpdater: setTask
      };
    },
    q3: function q3() {
      return {
        nextState: 'q1',
        effect: yFork(action),
        stateUpdater: setTask
      };
    }
  }, 'q1', "takeLatest(" + safeName(patternOrChannel) + ", " + worker.name + ")");
}

function takeLeading(patternOrChannel, worker) {
  for (var _len = arguments.length, args = new Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++) {
    args[_key - 2] = arguments[_key];
  }

  var yTake = {
    done: false,
    value: (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.K)(patternOrChannel)
  };

  var yCall = function yCall(ac) {
    return {
      done: false,
      value: _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.N.apply(void 0, [worker].concat(args, [ac]))
    };
  };

  var action;

  var setAction = function setAction(ac) {
    return action = ac;
  };

  return fsmIterator({
    q1: function q1() {
      return {
        nextState: 'q2',
        effect: yTake,
        stateUpdater: setAction
      };
    },
    q2: function q2() {
      return {
        nextState: 'q1',
        effect: yCall(action)
      };
    }
  }, 'q1', "takeLeading(" + safeName(patternOrChannel) + ", " + worker.name + ")");
}

function throttle(delayLength, pattern, worker) {
  for (var _len = arguments.length, args = new Array(_len > 3 ? _len - 3 : 0), _key = 3; _key < _len; _key++) {
    args[_key - 3] = arguments[_key];
  }

  var action, channel;
  var yActionChannel = {
    done: false,
    value: (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.O)(pattern, (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.Q)(1))
  };

  var yTake = function yTake() {
    return {
      done: false,
      value: (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.K)(channel)
    };
  };

  var yFork = function yFork(ac) {
    return {
      done: false,
      value: _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.L.apply(void 0, [worker].concat(args, [ac]))
    };
  };

  var yDelay = {
    done: false,
    value: (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.U)(delayLength)
  };

  var setAction = function setAction(ac) {
    return action = ac;
  };

  var setChannel = function setChannel(ch) {
    return channel = ch;
  };

  return fsmIterator({
    q1: function q1() {
      return {
        nextState: 'q2',
        effect: yActionChannel,
        stateUpdater: setChannel
      };
    },
    q2: function q2() {
      return {
        nextState: 'q3',
        effect: yTake(),
        stateUpdater: setAction
      };
    },
    q3: function q3() {
      return {
        nextState: 'q4',
        effect: yFork(action)
      };
    },
    q4: function q4() {
      return {
        nextState: 'q2',
        effect: yDelay
      };
    }
  }, 'q1', "throttle(" + safeName(pattern) + ", " + worker.name + ")");
}

function retry(maxTries, delayLength, fn) {
  var counter = maxTries;

  for (var _len = arguments.length, args = new Array(_len > 3 ? _len - 3 : 0), _key = 3; _key < _len; _key++) {
    args[_key - 3] = arguments[_key];
  }

  var yCall = {
    done: false,
    value: _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.N.apply(void 0, [fn].concat(args))
  };
  var yDelay = {
    done: false,
    value: (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.U)(delayLength)
  };
  return fsmIterator({
    q1: function q1() {
      return {
        nextState: 'q2',
        effect: yCall,
        errorState: 'q10'
      };
    },
    q2: function q2() {
      return {
        nextState: qEnd
      };
    },
    q10: function q10(error) {
      counter -= 1;

      if (counter <= 0) {
        throw error;
      }

      return {
        nextState: 'q1',
        effect: yDelay
      };
    }
  }, 'q1', "retry(" + fn.name + ")");
}

function debounceHelper(delayLength, patternOrChannel, worker) {
  for (var _len = arguments.length, args = new Array(_len > 3 ? _len - 3 : 0), _key = 3; _key < _len; _key++) {
    args[_key - 3] = arguments[_key];
  }

  var action, raceOutput;
  var yTake = {
    done: false,
    value: (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.K)(patternOrChannel)
  };
  var yRace = {
    done: false,
    value: (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.V)({
      action: (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.K)(patternOrChannel),
      debounce: (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.U)(delayLength)
    })
  };

  var yFork = function yFork(ac) {
    return {
      done: false,
      value: _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.L.apply(void 0, [worker].concat(args, [ac]))
    };
  };

  var yNoop = function yNoop(value) {
    return {
      done: false,
      value: value
    };
  };

  var setAction = function setAction(ac) {
    return action = ac;
  };

  var setRaceOutput = function setRaceOutput(ro) {
    return raceOutput = ro;
  };

  return fsmIterator({
    q1: function q1() {
      return {
        nextState: 'q2',
        effect: yTake,
        stateUpdater: setAction
      };
    },
    q2: function q2() {
      return {
        nextState: 'q3',
        effect: yRace,
        stateUpdater: setRaceOutput
      };
    },
    q3: function q3() {
      return raceOutput.debounce ? {
        nextState: 'q1',
        effect: yFork(action)
      } : {
        nextState: 'q2',
        effect: yNoop(raceOutput.action),
        stateUpdater: setAction
      };
    }
  }, 'q1', "debounce(" + safeName(patternOrChannel) + ", " + worker.name + ")");
}

var validateTakeEffect = function validateTakeEffect(fn, patternOrChannel, worker) {
  (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.c)(patternOrChannel, _redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.notUndef, fn.name + " requires a pattern or channel");
  (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.c)(worker, _redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.notUndef, fn.name + " requires a saga parameter");
};

function takeEvery$1(patternOrChannel, worker) {
  if (true) {
    validateTakeEffect(takeEvery$1, patternOrChannel, worker);
  }

  for (var _len = arguments.length, args = new Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++) {
    args[_key - 2] = arguments[_key];
  }

  return _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.L.apply(void 0, [takeEvery, patternOrChannel, worker].concat(args));
}
function takeLatest$1(patternOrChannel, worker) {
  if (true) {
    validateTakeEffect(takeLatest$1, patternOrChannel, worker);
  }

  for (var _len2 = arguments.length, args = new Array(_len2 > 2 ? _len2 - 2 : 0), _key2 = 2; _key2 < _len2; _key2++) {
    args[_key2 - 2] = arguments[_key2];
  }

  return _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.L.apply(void 0, [takeLatest, patternOrChannel, worker].concat(args));
}
function takeLeading$1(patternOrChannel, worker) {
  if (true) {
    validateTakeEffect(takeLeading$1, patternOrChannel, worker);
  }

  for (var _len3 = arguments.length, args = new Array(_len3 > 2 ? _len3 - 2 : 0), _key3 = 2; _key3 < _len3; _key3++) {
    args[_key3 - 2] = arguments[_key3];
  }

  return _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.L.apply(void 0, [takeLeading, patternOrChannel, worker].concat(args));
}
function throttle$1(ms, pattern, worker) {
  if (true) {
    (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.c)(pattern, _redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.notUndef, 'throttle requires a pattern');
    (0,_io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.c)(worker, _redux_saga_is__WEBPACK_IMPORTED_MODULE_2__.notUndef, 'throttle requires a saga parameter');
  }

  for (var _len4 = arguments.length, args = new Array(_len4 > 3 ? _len4 - 3 : 0), _key4 = 3; _key4 < _len4; _key4++) {
    args[_key4 - 3] = arguments[_key4];
  }

  return _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.L.apply(void 0, [throttle, ms, pattern, worker].concat(args));
}
function retry$1(maxTries, delayLength, worker) {
  for (var _len5 = arguments.length, args = new Array(_len5 > 3 ? _len5 - 3 : 0), _key5 = 3; _key5 < _len5; _key5++) {
    args[_key5 - 3] = arguments[_key5];
  }

  return _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.N.apply(void 0, [retry, maxTries, delayLength, worker].concat(args));
}
function debounce(delayLength, pattern, worker) {
  for (var _len6 = arguments.length, args = new Array(_len6 > 3 ? _len6 - 3 : 0), _key6 = 3; _key6 < _len6; _key6++) {
    args[_key6 - 3] = arguments[_key6];
  }

  return _io_6de156f3_js__WEBPACK_IMPORTED_MODULE_3__.L.apply(void 0, [debounceHelper, delayLength, pattern, worker].concat(args));
}




/***/ }),

/***/ "./node_modules/redux-saga/node_modules/@redux-saga/deferred/dist/redux-saga-deferred.esm.js":
/*!***************************************************************************************************!*\
  !*** ./node_modules/redux-saga/node_modules/@redux-saga/deferred/dist/redux-saga-deferred.esm.js ***!
  \***************************************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__),
/* harmony export */   "arrayOfDeferred": () => (/* binding */ arrayOfDeferred)
/* harmony export */ });
function deferred() {
  var def = {};
  def.promise = new Promise(function (resolve, reject) {
    def.resolve = resolve;
    def.reject = reject;
  });
  return def;
}
function arrayOfDeferred(length) {
  var arr = [];

  for (var i = 0; i < length; i++) {
    arr.push(deferred());
  }

  return arr;
}

/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (deferred);



/***/ }),

/***/ "./node_modules/redux-saga/node_modules/@redux-saga/delay-p/dist/redux-saga-delay-p.esm.js":
/*!*************************************************************************************************!*\
  !*** ./node_modules/redux-saga/node_modules/@redux-saga/delay-p/dist/redux-saga-delay-p.esm.js ***!
  \*************************************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @redux-saga/symbols */ "./node_modules/redux-saga/node_modules/@redux-saga/symbols/dist/redux-saga-symbols.esm.js");


function delayP(ms, val) {
  if (val === void 0) {
    val = true;
  }

  var timeoutId;
  var promise = new Promise(function (resolve) {
    timeoutId = setTimeout(resolve, ms, val);
  });

  promise[_redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__.CANCEL] = function () {
    clearTimeout(timeoutId);
  };

  return promise;
}

/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (delayP);


/***/ }),

/***/ "./node_modules/redux-saga/node_modules/@redux-saga/is/dist/redux-saga-is.esm.js":
/*!***************************************************************************************!*\
  !*** ./node_modules/redux-saga/node_modules/@redux-saga/is/dist/redux-saga-is.esm.js ***!
  \***************************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "array": () => (/* binding */ array),
/* harmony export */   "buffer": () => (/* binding */ buffer),
/* harmony export */   "channel": () => (/* binding */ channel),
/* harmony export */   "effect": () => (/* binding */ effect),
/* harmony export */   "func": () => (/* binding */ func),
/* harmony export */   "iterable": () => (/* binding */ iterable),
/* harmony export */   "iterator": () => (/* binding */ iterator),
/* harmony export */   "multicast": () => (/* binding */ multicast),
/* harmony export */   "notUndef": () => (/* binding */ notUndef),
/* harmony export */   "number": () => (/* binding */ number),
/* harmony export */   "object": () => (/* binding */ object),
/* harmony export */   "observable": () => (/* binding */ observable),
/* harmony export */   "pattern": () => (/* binding */ pattern),
/* harmony export */   "promise": () => (/* binding */ promise),
/* harmony export */   "sagaAction": () => (/* binding */ sagaAction),
/* harmony export */   "string": () => (/* binding */ string),
/* harmony export */   "stringableFunc": () => (/* binding */ stringableFunc),
/* harmony export */   "symbol": () => (/* binding */ symbol),
/* harmony export */   "task": () => (/* binding */ task),
/* harmony export */   "undef": () => (/* binding */ undef)
/* harmony export */ });
/* harmony import */ var _redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @redux-saga/symbols */ "./node_modules/redux-saga/node_modules/@redux-saga/symbols/dist/redux-saga-symbols.esm.js");


var undef = function undef(v) {
  return v === null || v === undefined;
};
var notUndef = function notUndef(v) {
  return v !== null && v !== undefined;
};
var func = function func(f) {
  return typeof f === 'function';
};
var number = function number(n) {
  return typeof n === 'number';
};
var string = function string(s) {
  return typeof s === 'string';
};
var array = Array.isArray;
var object = function object(obj) {
  return obj && !array(obj) && typeof obj === 'object';
};
var promise = function promise(p) {
  return p && func(p.then);
};
var iterator = function iterator(it) {
  return it && func(it.next) && func(it.throw);
};
var iterable = function iterable(it) {
  return it && func(Symbol) ? func(it[Symbol.iterator]) : array(it);
};
var task = function task(t) {
  return t && t[_redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__.TASK];
};
var sagaAction = function sagaAction(a) {
  return Boolean(a && a[_redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__.SAGA_ACTION]);
};
var observable = function observable(ob) {
  return ob && func(ob.subscribe);
};
var buffer = function buffer(buf) {
  return buf && func(buf.isEmpty) && func(buf.take) && func(buf.put);
};
var pattern = function pattern(pat) {
  return pat && (string(pat) || symbol(pat) || func(pat) || array(pat) && pat.every(pattern));
};
var channel = function channel(ch) {
  return ch && func(ch.take) && func(ch.close);
};
var stringableFunc = function stringableFunc(f) {
  return func(f) && f.hasOwnProperty('toString');
};
var symbol = function symbol(sym) {
  return Boolean(sym) && typeof Symbol === 'function' && sym.constructor === Symbol && sym !== Symbol.prototype;
};
var multicast = function multicast(ch) {
  return channel(ch) && ch[_redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__.MULTICAST];
};
var effect = function effect(eff) {
  return eff && eff[_redux_saga_symbols__WEBPACK_IMPORTED_MODULE_0__.IO];
};




/***/ }),

/***/ "./node_modules/redux-saga/node_modules/@redux-saga/symbols/dist/redux-saga-symbols.esm.js":
/*!*************************************************************************************************!*\
  !*** ./node_modules/redux-saga/node_modules/@redux-saga/symbols/dist/redux-saga-symbols.esm.js ***!
  \*************************************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "CANCEL": () => (/* binding */ CANCEL),
/* harmony export */   "CHANNEL_END_TYPE": () => (/* binding */ CHANNEL_END_TYPE),
/* harmony export */   "IO": () => (/* binding */ IO),
/* harmony export */   "MATCH": () => (/* binding */ MATCH),
/* harmony export */   "MULTICAST": () => (/* binding */ MULTICAST),
/* harmony export */   "SAGA_ACTION": () => (/* binding */ SAGA_ACTION),
/* harmony export */   "SAGA_LOCATION": () => (/* binding */ SAGA_LOCATION),
/* harmony export */   "SELF_CANCELLATION": () => (/* binding */ SELF_CANCELLATION),
/* harmony export */   "TASK": () => (/* binding */ TASK),
/* harmony export */   "TASK_CANCEL": () => (/* binding */ TASK_CANCEL),
/* harmony export */   "TERMINATE": () => (/* binding */ TERMINATE)
/* harmony export */ });
var createSymbol = function createSymbol(name) {
  return "@@redux-saga/" + name;
};

var CANCEL =
/*#__PURE__*/
createSymbol('CANCEL_PROMISE');
var CHANNEL_END_TYPE =
/*#__PURE__*/
createSymbol('CHANNEL_END');
var IO =
/*#__PURE__*/
createSymbol('IO');
var MATCH =
/*#__PURE__*/
createSymbol('MATCH');
var MULTICAST =
/*#__PURE__*/
createSymbol('MULTICAST');
var SAGA_ACTION =
/*#__PURE__*/
createSymbol('SAGA_ACTION');
var SELF_CANCELLATION =
/*#__PURE__*/
createSymbol('SELF_CANCELLATION');
var TASK =
/*#__PURE__*/
createSymbol('TASK');
var TASK_CANCEL =
/*#__PURE__*/
createSymbol('TASK_CANCEL');
var TERMINATE =
/*#__PURE__*/
createSymbol('TERMINATE');
var SAGA_LOCATION =
/*#__PURE__*/
createSymbol('LOCATION');




/***/ }),

/***/ "./node_modules/redux-thunk/es/index.js":
/*!**********************************************!*\
  !*** ./node_modules/redux-thunk/es/index.js ***!
  \**********************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
function createThunkMiddleware(extraArgument) {
  return function (_ref) {
    var dispatch = _ref.dispatch,
        getState = _ref.getState;
    return function (next) {
      return function (action) {
        if (typeof action === 'function') {
          return action(dispatch, getState, extraArgument);
        }

        return next(action);
      };
    };
  };
}

var thunk = createThunkMiddleware();
thunk.withExtraArgument = createThunkMiddleware;

/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (thunk);

/***/ }),

/***/ "./node_modules/regenerator-runtime/runtime.js":
/*!*****************************************************!*\
  !*** ./node_modules/regenerator-runtime/runtime.js ***!
  \*****************************************************/
/***/ ((module) => {

/**
 * Copyright (c) 2014-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */

var runtime = (function (exports) {
  "use strict";

  var Op = Object.prototype;
  var hasOwn = Op.hasOwnProperty;
  var undefined; // More compressible than void 0.
  var $Symbol = typeof Symbol === "function" ? Symbol : {};
  var iteratorSymbol = $Symbol.iterator || "@@iterator";
  var asyncIteratorSymbol = $Symbol.asyncIterator || "@@asyncIterator";
  var toStringTagSymbol = $Symbol.toStringTag || "@@toStringTag";

  function define(obj, key, value) {
    Object.defineProperty(obj, key, {
      value: value,
      enumerable: true,
      configurable: true,
      writable: true
    });
    return obj[key];
  }
  try {
    // IE 8 has a broken Object.defineProperty that only works on DOM objects.
    define({}, "");
  } catch (err) {
    define = function(obj, key, value) {
      return obj[key] = value;
    };
  }

  function wrap(innerFn, outerFn, self, tryLocsList) {
    // If outerFn provided and outerFn.prototype is a Generator, then outerFn.prototype instanceof Generator.
    var protoGenerator = outerFn && outerFn.prototype instanceof Generator ? outerFn : Generator;
    var generator = Object.create(protoGenerator.prototype);
    var context = new Context(tryLocsList || []);

    // The ._invoke method unifies the implementations of the .next,
    // .throw, and .return methods.
    generator._invoke = makeInvokeMethod(innerFn, self, context);

    return generator;
  }
  exports.wrap = wrap;

  // Try/catch helper to minimize deoptimizations. Returns a completion
  // record like context.tryEntries[i].completion. This interface could
  // have been (and was previously) designed to take a closure to be
  // invoked without arguments, but in all the cases we care about we
  // already have an existing method we want to call, so there's no need
  // to create a new function object. We can even get away with assuming
  // the method takes exactly one argument, since that happens to be true
  // in every case, so we don't have to touch the arguments object. The
  // only additional allocation required is the completion record, which
  // has a stable shape and so hopefully should be cheap to allocate.
  function tryCatch(fn, obj, arg) {
    try {
      return { type: "normal", arg: fn.call(obj, arg) };
    } catch (err) {
      return { type: "throw", arg: err };
    }
  }

  var GenStateSuspendedStart = "suspendedStart";
  var GenStateSuspendedYield = "suspendedYield";
  var GenStateExecuting = "executing";
  var GenStateCompleted = "completed";

  // Returning this object from the innerFn has the same effect as
  // breaking out of the dispatch switch statement.
  var ContinueSentinel = {};

  // Dummy constructor functions that we use as the .constructor and
  // .constructor.prototype properties for functions that return Generator
  // objects. For full spec compliance, you may wish to configure your
  // minifier not to mangle the names of these two functions.
  function Generator() {}
  function GeneratorFunction() {}
  function GeneratorFunctionPrototype() {}

  // This is a polyfill for %IteratorPrototype% for environments that
  // don't natively support it.
  var IteratorPrototype = {};
  IteratorPrototype[iteratorSymbol] = function () {
    return this;
  };

  var getProto = Object.getPrototypeOf;
  var NativeIteratorPrototype = getProto && getProto(getProto(values([])));
  if (NativeIteratorPrototype &&
      NativeIteratorPrototype !== Op &&
      hasOwn.call(NativeIteratorPrototype, iteratorSymbol)) {
    // This environment has a native %IteratorPrototype%; use it instead
    // of the polyfill.
    IteratorPrototype = NativeIteratorPrototype;
  }

  var Gp = GeneratorFunctionPrototype.prototype =
    Generator.prototype = Object.create(IteratorPrototype);
  GeneratorFunction.prototype = Gp.constructor = GeneratorFunctionPrototype;
  GeneratorFunctionPrototype.constructor = GeneratorFunction;
  GeneratorFunction.displayName = define(
    GeneratorFunctionPrototype,
    toStringTagSymbol,
    "GeneratorFunction"
  );

  // Helper for defining the .next, .throw, and .return methods of the
  // Iterator interface in terms of a single ._invoke method.
  function defineIteratorMethods(prototype) {
    ["next", "throw", "return"].forEach(function(method) {
      define(prototype, method, function(arg) {
        return this._invoke(method, arg);
      });
    });
  }

  exports.isGeneratorFunction = function(genFun) {
    var ctor = typeof genFun === "function" && genFun.constructor;
    return ctor
      ? ctor === GeneratorFunction ||
        // For the native GeneratorFunction constructor, the best we can
        // do is to check its .name property.
        (ctor.displayName || ctor.name) === "GeneratorFunction"
      : false;
  };

  exports.mark = function(genFun) {
    if (Object.setPrototypeOf) {
      Object.setPrototypeOf(genFun, GeneratorFunctionPrototype);
    } else {
      genFun.__proto__ = GeneratorFunctionPrototype;
      define(genFun, toStringTagSymbol, "GeneratorFunction");
    }
    genFun.prototype = Object.create(Gp);
    return genFun;
  };

  // Within the body of any async function, `await x` is transformed to
  // `yield regeneratorRuntime.awrap(x)`, so that the runtime can test
  // `hasOwn.call(value, "__await")` to determine if the yielded value is
  // meant to be awaited.
  exports.awrap = function(arg) {
    return { __await: arg };
  };

  function AsyncIterator(generator, PromiseImpl) {
    function invoke(method, arg, resolve, reject) {
      var record = tryCatch(generator[method], generator, arg);
      if (record.type === "throw") {
        reject(record.arg);
      } else {
        var result = record.arg;
        var value = result.value;
        if (value &&
            typeof value === "object" &&
            hasOwn.call(value, "__await")) {
          return PromiseImpl.resolve(value.__await).then(function(value) {
            invoke("next", value, resolve, reject);
          }, function(err) {
            invoke("throw", err, resolve, reject);
          });
        }

        return PromiseImpl.resolve(value).then(function(unwrapped) {
          // When a yielded Promise is resolved, its final value becomes
          // the .value of the Promise<{value,done}> result for the
          // current iteration.
          result.value = unwrapped;
          resolve(result);
        }, function(error) {
          // If a rejected Promise was yielded, throw the rejection back
          // into the async generator function so it can be handled there.
          return invoke("throw", error, resolve, reject);
        });
      }
    }

    var previousPromise;

    function enqueue(method, arg) {
      function callInvokeWithMethodAndArg() {
        return new PromiseImpl(function(resolve, reject) {
          invoke(method, arg, resolve, reject);
        });
      }

      return previousPromise =
        // If enqueue has been called before, then we want to wait until
        // all previous Promises have been resolved before calling invoke,
        // so that results are always delivered in the correct order. If
        // enqueue has not been called before, then it is important to
        // call invoke immediately, without waiting on a callback to fire,
        // so that the async generator function has the opportunity to do
        // any necessary setup in a predictable way. This predictability
        // is why the Promise constructor synchronously invokes its
        // executor callback, and why async functions synchronously
        // execute code before the first await. Since we implement simple
        // async functions in terms of async generators, it is especially
        // important to get this right, even though it requires care.
        previousPromise ? previousPromise.then(
          callInvokeWithMethodAndArg,
          // Avoid propagating failures to Promises returned by later
          // invocations of the iterator.
          callInvokeWithMethodAndArg
        ) : callInvokeWithMethodAndArg();
    }

    // Define the unified helper method that is used to implement .next,
    // .throw, and .return (see defineIteratorMethods).
    this._invoke = enqueue;
  }

  defineIteratorMethods(AsyncIterator.prototype);
  AsyncIterator.prototype[asyncIteratorSymbol] = function () {
    return this;
  };
  exports.AsyncIterator = AsyncIterator;

  // Note that simple async functions are implemented on top of
  // AsyncIterator objects; they just return a Promise for the value of
  // the final result produced by the iterator.
  exports.async = function(innerFn, outerFn, self, tryLocsList, PromiseImpl) {
    if (PromiseImpl === void 0) PromiseImpl = Promise;

    var iter = new AsyncIterator(
      wrap(innerFn, outerFn, self, tryLocsList),
      PromiseImpl
    );

    return exports.isGeneratorFunction(outerFn)
      ? iter // If outerFn is a generator, return the full iterator.
      : iter.next().then(function(result) {
          return result.done ? result.value : iter.next();
        });
  };

  function makeInvokeMethod(innerFn, self, context) {
    var state = GenStateSuspendedStart;

    return function invoke(method, arg) {
      if (state === GenStateExecuting) {
        throw new Error("Generator is already running");
      }

      if (state === GenStateCompleted) {
        if (method === "throw") {
          throw arg;
        }

        // Be forgiving, per 25.3.3.3.3 of the spec:
        // https://people.mozilla.org/~jorendorff/es6-draft.html#sec-generatorresume
        return doneResult();
      }

      context.method = method;
      context.arg = arg;

      while (true) {
        var delegate = context.delegate;
        if (delegate) {
          var delegateResult = maybeInvokeDelegate(delegate, context);
          if (delegateResult) {
            if (delegateResult === ContinueSentinel) continue;
            return delegateResult;
          }
        }

        if (context.method === "next") {
          // Setting context._sent for legacy support of Babel's
          // function.sent implementation.
          context.sent = context._sent = context.arg;

        } else if (context.method === "throw") {
          if (state === GenStateSuspendedStart) {
            state = GenStateCompleted;
            throw context.arg;
          }

          context.dispatchException(context.arg);

        } else if (context.method === "return") {
          context.abrupt("return", context.arg);
        }

        state = GenStateExecuting;

        var record = tryCatch(innerFn, self, context);
        if (record.type === "normal") {
          // If an exception is thrown from innerFn, we leave state ===
          // GenStateExecuting and loop back for another invocation.
          state = context.done
            ? GenStateCompleted
            : GenStateSuspendedYield;

          if (record.arg === ContinueSentinel) {
            continue;
          }

          return {
            value: record.arg,
            done: context.done
          };

        } else if (record.type === "throw") {
          state = GenStateCompleted;
          // Dispatch the exception by looping back around to the
          // context.dispatchException(context.arg) call above.
          context.method = "throw";
          context.arg = record.arg;
        }
      }
    };
  }

  // Call delegate.iterator[context.method](context.arg) and handle the
  // result, either by returning a { value, done } result from the
  // delegate iterator, or by modifying context.method and context.arg,
  // setting context.delegate to null, and returning the ContinueSentinel.
  function maybeInvokeDelegate(delegate, context) {
    var method = delegate.iterator[context.method];
    if (method === undefined) {
      // A .throw or .return when the delegate iterator has no .throw
      // method always terminates the yield* loop.
      context.delegate = null;

      if (context.method === "throw") {
        // Note: ["return"] must be used for ES3 parsing compatibility.
        if (delegate.iterator["return"]) {
          // If the delegate iterator has a return method, give it a
          // chance to clean up.
          context.method = "return";
          context.arg = undefined;
          maybeInvokeDelegate(delegate, context);

          if (context.method === "throw") {
            // If maybeInvokeDelegate(context) changed context.method from
            // "return" to "throw", let that override the TypeError below.
            return ContinueSentinel;
          }
        }

        context.method = "throw";
        context.arg = new TypeError(
          "The iterator does not provide a 'throw' method");
      }

      return ContinueSentinel;
    }

    var record = tryCatch(method, delegate.iterator, context.arg);

    if (record.type === "throw") {
      context.method = "throw";
      context.arg = record.arg;
      context.delegate = null;
      return ContinueSentinel;
    }

    var info = record.arg;

    if (! info) {
      context.method = "throw";
      context.arg = new TypeError("iterator result is not an object");
      context.delegate = null;
      return ContinueSentinel;
    }

    if (info.done) {
      // Assign the result of the finished delegate to the temporary
      // variable specified by delegate.resultName (see delegateYield).
      context[delegate.resultName] = info.value;

      // Resume execution at the desired location (see delegateYield).
      context.next = delegate.nextLoc;

      // If context.method was "throw" but the delegate handled the
      // exception, let the outer generator proceed normally. If
      // context.method was "next", forget context.arg since it has been
      // "consumed" by the delegate iterator. If context.method was
      // "return", allow the original .return call to continue in the
      // outer generator.
      if (context.method !== "return") {
        context.method = "next";
        context.arg = undefined;
      }

    } else {
      // Re-yield the result returned by the delegate method.
      return info;
    }

    // The delegate iterator is finished, so forget it and continue with
    // the outer generator.
    context.delegate = null;
    return ContinueSentinel;
  }

  // Define Generator.prototype.{next,throw,return} in terms of the
  // unified ._invoke helper method.
  defineIteratorMethods(Gp);

  define(Gp, toStringTagSymbol, "Generator");

  // A Generator should always return itself as the iterator object when the
  // @@iterator function is called on it. Some browsers' implementations of the
  // iterator prototype chain incorrectly implement this, causing the Generator
  // object to not be returned from this call. This ensures that doesn't happen.
  // See https://github.com/facebook/regenerator/issues/274 for more details.
  Gp[iteratorSymbol] = function() {
    return this;
  };

  Gp.toString = function() {
    return "[object Generator]";
  };

  function pushTryEntry(locs) {
    var entry = { tryLoc: locs[0] };

    if (1 in locs) {
      entry.catchLoc = locs[1];
    }

    if (2 in locs) {
      entry.finallyLoc = locs[2];
      entry.afterLoc = locs[3];
    }

    this.tryEntries.push(entry);
  }

  function resetTryEntry(entry) {
    var record = entry.completion || {};
    record.type = "normal";
    delete record.arg;
    entry.completion = record;
  }

  function Context(tryLocsList) {
    // The root entry object (effectively a try statement without a catch
    // or a finally block) gives us a place to store values thrown from
    // locations where there is no enclosing try statement.
    this.tryEntries = [{ tryLoc: "root" }];
    tryLocsList.forEach(pushTryEntry, this);
    this.reset(true);
  }

  exports.keys = function(object) {
    var keys = [];
    for (var key in object) {
      keys.push(key);
    }
    keys.reverse();

    // Rather than returning an object with a next method, we keep
    // things simple and return the next function itself.
    return function next() {
      while (keys.length) {
        var key = keys.pop();
        if (key in object) {
          next.value = key;
          next.done = false;
          return next;
        }
      }

      // To avoid creating an additional object, we just hang the .value
      // and .done properties off the next function object itself. This
      // also ensures that the minifier will not anonymize the function.
      next.done = true;
      return next;
    };
  };

  function values(iterable) {
    if (iterable) {
      var iteratorMethod = iterable[iteratorSymbol];
      if (iteratorMethod) {
        return iteratorMethod.call(iterable);
      }

      if (typeof iterable.next === "function") {
        return iterable;
      }

      if (!isNaN(iterable.length)) {
        var i = -1, next = function next() {
          while (++i < iterable.length) {
            if (hasOwn.call(iterable, i)) {
              next.value = iterable[i];
              next.done = false;
              return next;
            }
          }

          next.value = undefined;
          next.done = true;

          return next;
        };

        return next.next = next;
      }
    }

    // Return an iterator with no values.
    return { next: doneResult };
  }
  exports.values = values;

  function doneResult() {
    return { value: undefined, done: true };
  }

  Context.prototype = {
    constructor: Context,

    reset: function(skipTempReset) {
      this.prev = 0;
      this.next = 0;
      // Resetting context._sent for legacy support of Babel's
      // function.sent implementation.
      this.sent = this._sent = undefined;
      this.done = false;
      this.delegate = null;

      this.method = "next";
      this.arg = undefined;

      this.tryEntries.forEach(resetTryEntry);

      if (!skipTempReset) {
        for (var name in this) {
          // Not sure about the optimal order of these conditions:
          if (name.charAt(0) === "t" &&
              hasOwn.call(this, name) &&
              !isNaN(+name.slice(1))) {
            this[name] = undefined;
          }
        }
      }
    },

    stop: function() {
      this.done = true;

      var rootEntry = this.tryEntries[0];
      var rootRecord = rootEntry.completion;
      if (rootRecord.type === "throw") {
        throw rootRecord.arg;
      }

      return this.rval;
    },

    dispatchException: function(exception) {
      if (this.done) {
        throw exception;
      }

      var context = this;
      function handle(loc, caught) {
        record.type = "throw";
        record.arg = exception;
        context.next = loc;

        if (caught) {
          // If the dispatched exception was caught by a catch block,
          // then let that catch block handle the exception normally.
          context.method = "next";
          context.arg = undefined;
        }

        return !! caught;
      }

      for (var i = this.tryEntries.length - 1; i >= 0; --i) {
        var entry = this.tryEntries[i];
        var record = entry.completion;

        if (entry.tryLoc === "root") {
          // Exception thrown outside of any try block that could handle
          // it, so set the completion value of the entire function to
          // throw the exception.
          return handle("end");
        }

        if (entry.tryLoc <= this.prev) {
          var hasCatch = hasOwn.call(entry, "catchLoc");
          var hasFinally = hasOwn.call(entry, "finallyLoc");

          if (hasCatch && hasFinally) {
            if (this.prev < entry.catchLoc) {
              return handle(entry.catchLoc, true);
            } else if (this.prev < entry.finallyLoc) {
              return handle(entry.finallyLoc);
            }

          } else if (hasCatch) {
            if (this.prev < entry.catchLoc) {
              return handle(entry.catchLoc, true);
            }

          } else if (hasFinally) {
            if (this.prev < entry.finallyLoc) {
              return handle(entry.finallyLoc);
            }

          } else {
            throw new Error("try statement without catch or finally");
          }
        }
      }
    },

    abrupt: function(type, arg) {
      for (var i = this.tryEntries.length - 1; i >= 0; --i) {
        var entry = this.tryEntries[i];
        if (entry.tryLoc <= this.prev &&
            hasOwn.call(entry, "finallyLoc") &&
            this.prev < entry.finallyLoc) {
          var finallyEntry = entry;
          break;
        }
      }

      if (finallyEntry &&
          (type === "break" ||
           type === "continue") &&
          finallyEntry.tryLoc <= arg &&
          arg <= finallyEntry.finallyLoc) {
        // Ignore the finally entry if control is not jumping to a
        // location outside the try/catch block.
        finallyEntry = null;
      }

      var record = finallyEntry ? finallyEntry.completion : {};
      record.type = type;
      record.arg = arg;

      if (finallyEntry) {
        this.method = "next";
        this.next = finallyEntry.finallyLoc;
        return ContinueSentinel;
      }

      return this.complete(record);
    },

    complete: function(record, afterLoc) {
      if (record.type === "throw") {
        throw record.arg;
      }

      if (record.type === "break" ||
          record.type === "continue") {
        this.next = record.arg;
      } else if (record.type === "return") {
        this.rval = this.arg = record.arg;
        this.method = "return";
        this.next = "end";
      } else if (record.type === "normal" && afterLoc) {
        this.next = afterLoc;
      }

      return ContinueSentinel;
    },

    finish: function(finallyLoc) {
      for (var i = this.tryEntries.length - 1; i >= 0; --i) {
        var entry = this.tryEntries[i];
        if (entry.finallyLoc === finallyLoc) {
          this.complete(entry.completion, entry.afterLoc);
          resetTryEntry(entry);
          return ContinueSentinel;
        }
      }
    },

    "catch": function(tryLoc) {
      for (var i = this.tryEntries.length - 1; i >= 0; --i) {
        var entry = this.tryEntries[i];
        if (entry.tryLoc === tryLoc) {
          var record = entry.completion;
          if (record.type === "throw") {
            var thrown = record.arg;
            resetTryEntry(entry);
          }
          return thrown;
        }
      }

      // The context.catch method must only be called with a location
      // argument that corresponds to a known catch block.
      throw new Error("illegal catch attempt");
    },

    delegateYield: function(iterable, resultName, nextLoc) {
      this.delegate = {
        iterator: values(iterable),
        resultName: resultName,
        nextLoc: nextLoc
      };

      if (this.method === "next") {
        // Deliberately forget the last sent value so that we don't
        // accidentally pass it on to the delegate.
        this.arg = undefined;
      }

      return ContinueSentinel;
    }
  };

  // Regardless of whether this script is executing as a CommonJS module
  // or not, return the runtime object so that we can declare the variable
  // regeneratorRuntime in the outer scope, which allows this module to be
  // injected easily by `bin/regenerator --include-runtime script.js`.
  return exports;

}(
  // If this script is executing as a CommonJS module, use module.exports
  // as the regeneratorRuntime namespace. Otherwise create a new empty
  // object. Either way, the resulting object will be used to initialize
  // the regeneratorRuntime variable at the top of this file.
   true ? module.exports : 0
));

try {
  regeneratorRuntime = runtime;
} catch (accidentalStrictMode) {
  // This module should not be running in strict mode, so the above
  // assignment should always work unless something is misconfigured. Just
  // in case runtime.js accidentally runs in strict mode, we can escape
  // strict mode using a global Function call. This could conceivably fail
  // if a Content Security Policy forbids using Function, but in that case
  // the proper solution is to fix the accidental strict mode problem. If
  // you've misconfigured your bundler to force strict mode and applied a
  // CSP to forbid Function, and you're not willing to fix either of those
  // problems, please detail your unique predicament in a GitHub issue.
  Function("r", "regeneratorRuntime = r")(runtime);
}


/***/ }),

/***/ "./node_modules/reselect/es/index.js":
/*!*******************************************!*\
  !*** ./node_modules/reselect/es/index.js ***!
  \*******************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "defaultMemoize": () => (/* binding */ defaultMemoize),
/* harmony export */   "createSelectorCreator": () => (/* binding */ createSelectorCreator),
/* harmony export */   "createSelector": () => (/* binding */ createSelector),
/* harmony export */   "createStructuredSelector": () => (/* binding */ createStructuredSelector)
/* harmony export */ });
function defaultEqualityCheck(a, b) {
  return a === b;
}

function areArgumentsShallowlyEqual(equalityCheck, prev, next) {
  if (prev === null || next === null || prev.length !== next.length) {
    return false;
  }

  // Do this in a for loop (and not a `forEach` or an `every`) so we can determine equality as fast as possible.
  var length = prev.length;
  for (var i = 0; i < length; i++) {
    if (!equalityCheck(prev[i], next[i])) {
      return false;
    }
  }

  return true;
}

function defaultMemoize(func) {
  var equalityCheck = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : defaultEqualityCheck;

  var lastArgs = null;
  var lastResult = null;
  // we reference arguments instead of spreading them for performance reasons
  return function () {
    if (!areArgumentsShallowlyEqual(equalityCheck, lastArgs, arguments)) {
      // apply arguments instead of spreading for performance.
      lastResult = func.apply(null, arguments);
    }

    lastArgs = arguments;
    return lastResult;
  };
}

function getDependencies(funcs) {
  var dependencies = Array.isArray(funcs[0]) ? funcs[0] : funcs;

  if (!dependencies.every(function (dep) {
    return typeof dep === 'function';
  })) {
    var dependencyTypes = dependencies.map(function (dep) {
      return typeof dep;
    }).join(', ');
    throw new Error('Selector creators expect all input-selectors to be functions, ' + ('instead received the following types: [' + dependencyTypes + ']'));
  }

  return dependencies;
}

function createSelectorCreator(memoize) {
  for (var _len = arguments.length, memoizeOptions = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    memoizeOptions[_key - 1] = arguments[_key];
  }

  return function () {
    for (var _len2 = arguments.length, funcs = Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
      funcs[_key2] = arguments[_key2];
    }

    var recomputations = 0;
    var resultFunc = funcs.pop();
    var dependencies = getDependencies(funcs);

    var memoizedResultFunc = memoize.apply(undefined, [function () {
      recomputations++;
      // apply arguments instead of spreading for performance.
      return resultFunc.apply(null, arguments);
    }].concat(memoizeOptions));

    // If a selector is called with the exact same arguments we don't need to traverse our dependencies again.
    var selector = memoize(function () {
      var params = [];
      var length = dependencies.length;

      for (var i = 0; i < length; i++) {
        // apply arguments instead of spreading and mutate a local list of params for performance.
        params.push(dependencies[i].apply(null, arguments));
      }

      // apply arguments instead of spreading for performance.
      return memoizedResultFunc.apply(null, params);
    });

    selector.resultFunc = resultFunc;
    selector.dependencies = dependencies;
    selector.recomputations = function () {
      return recomputations;
    };
    selector.resetRecomputations = function () {
      return recomputations = 0;
    };
    return selector;
  };
}

var createSelector = createSelectorCreator(defaultMemoize);

function createStructuredSelector(selectors) {
  var selectorCreator = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : createSelector;

  if (typeof selectors !== 'object') {
    throw new Error('createStructuredSelector expects first argument to be an object ' + ('where each property is a selector, instead received a ' + typeof selectors));
  }
  var objectKeys = Object.keys(selectors);
  return selectorCreator(objectKeys.map(function (key) {
    return selectors[key];
  }), function () {
    for (var _len3 = arguments.length, values = Array(_len3), _key3 = 0; _key3 < _len3; _key3++) {
      values[_key3] = arguments[_key3];
    }

    return values.reduce(function (composition, value, index) {
      composition[objectKeys[index]] = value;
      return composition;
    }, {});
  });
}

/***/ }),

/***/ "./node_modules/react-range-slider-ts/dist/index.css":
/*!***********************************************************!*\
  !*** ./node_modules/react-range-slider-ts/dist/index.css ***!
  \***********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _style_loader_dist_runtime_injectStylesIntoStyleTag_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! !../../style-loader/dist/runtime/injectStylesIntoStyleTag.js */ "./node_modules/style-loader/dist/runtime/injectStylesIntoStyleTag.js");
/* harmony import */ var _style_loader_dist_runtime_injectStylesIntoStyleTag_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_style_loader_dist_runtime_injectStylesIntoStyleTag_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _css_loader_dist_cjs_js_ruleSet_1_rules_1_use_1_sass_loader_dist_cjs_js_index_css__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! !!../../css-loader/dist/cjs.js??ruleSet[1].rules[1].use[1]!../../sass-loader/dist/cjs.js!./index.css */ "./node_modules/css-loader/dist/cjs.js??ruleSet[1].rules[1].use[1]!./node_modules/sass-loader/dist/cjs.js!./node_modules/react-range-slider-ts/dist/index.css");

            

var options = {};

options.insert = "head";
options.singleton = false;

var update = _style_loader_dist_runtime_injectStylesIntoStyleTag_js__WEBPACK_IMPORTED_MODULE_0___default()(_css_loader_dist_cjs_js_ruleSet_1_rules_1_use_1_sass_loader_dist_cjs_js_index_css__WEBPACK_IMPORTED_MODULE_1__.default, options);



/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (_css_loader_dist_cjs_js_ruleSet_1_rules_1_use_1_sass_loader_dist_cjs_js_index_css__WEBPACK_IMPORTED_MODULE_1__.default.locals || {});

/***/ }),

/***/ "./node_modules/react-video-ts/dist/index.css":
/*!****************************************************!*\
  !*** ./node_modules/react-video-ts/dist/index.css ***!
  \****************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _style_loader_dist_runtime_injectStylesIntoStyleTag_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! !../../style-loader/dist/runtime/injectStylesIntoStyleTag.js */ "./node_modules/style-loader/dist/runtime/injectStylesIntoStyleTag.js");
/* harmony import */ var _style_loader_dist_runtime_injectStylesIntoStyleTag_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_style_loader_dist_runtime_injectStylesIntoStyleTag_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _css_loader_dist_cjs_js_ruleSet_1_rules_1_use_1_sass_loader_dist_cjs_js_index_css__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! !!../../css-loader/dist/cjs.js??ruleSet[1].rules[1].use[1]!../../sass-loader/dist/cjs.js!./index.css */ "./node_modules/css-loader/dist/cjs.js??ruleSet[1].rules[1].use[1]!./node_modules/sass-loader/dist/cjs.js!./node_modules/react-video-ts/dist/index.css");

            

var options = {};

options.insert = "head";
options.singleton = false;

var update = _style_loader_dist_runtime_injectStylesIntoStyleTag_js__WEBPACK_IMPORTED_MODULE_0___default()(_css_loader_dist_cjs_js_ruleSet_1_rules_1_use_1_sass_loader_dist_cjs_js_index_css__WEBPACK_IMPORTED_MODULE_1__.default, options);



/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (_css_loader_dist_cjs_js_ruleSet_1_rules_1_use_1_sass_loader_dist_cjs_js_index_css__WEBPACK_IMPORTED_MODULE_1__.default.locals || {});

/***/ }),

/***/ "./node_modules/style-loader/dist/runtime/injectStylesIntoStyleTag.js":
/*!****************************************************************************!*\
  !*** ./node_modules/style-loader/dist/runtime/injectStylesIntoStyleTag.js ***!
  \****************************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";


var isOldIE = function isOldIE() {
  var memo;
  return function memorize() {
    if (typeof memo === 'undefined') {
      // Test for IE <= 9 as proposed by Browserhacks
      // @see http://browserhacks.com/#hack-e71d8692f65334173fee715c222cb805
      // Tests for existence of standard globals is to allow style-loader
      // to operate correctly into non-standard environments
      // @see https://github.com/webpack-contrib/style-loader/issues/177
      memo = Boolean(window && document && document.all && !window.atob);
    }

    return memo;
  };
}();

var getTarget = function getTarget() {
  var memo = {};
  return function memorize(target) {
    if (typeof memo[target] === 'undefined') {
      var styleTarget = document.querySelector(target); // Special case to return head of iframe instead of iframe itself

      if (window.HTMLIFrameElement && styleTarget instanceof window.HTMLIFrameElement) {
        try {
          // This will throw an exception if access to iframe is blocked
          // due to cross-origin restrictions
          styleTarget = styleTarget.contentDocument.head;
        } catch (e) {
          // istanbul ignore next
          styleTarget = null;
        }
      }

      memo[target] = styleTarget;
    }

    return memo[target];
  };
}();

var stylesInDom = [];

function getIndexByIdentifier(identifier) {
  var result = -1;

  for (var i = 0; i < stylesInDom.length; i++) {
    if (stylesInDom[i].identifier === identifier) {
      result = i;
      break;
    }
  }

  return result;
}

function modulesToDom(list, options) {
  var idCountMap = {};
  var identifiers = [];

  for (var i = 0; i < list.length; i++) {
    var item = list[i];
    var id = options.base ? item[0] + options.base : item[0];
    var count = idCountMap[id] || 0;
    var identifier = "".concat(id, " ").concat(count);
    idCountMap[id] = count + 1;
    var index = getIndexByIdentifier(identifier);
    var obj = {
      css: item[1],
      media: item[2],
      sourceMap: item[3]
    };

    if (index !== -1) {
      stylesInDom[index].references++;
      stylesInDom[index].updater(obj);
    } else {
      stylesInDom.push({
        identifier: identifier,
        updater: addStyle(obj, options),
        references: 1
      });
    }

    identifiers.push(identifier);
  }

  return identifiers;
}

function insertStyleElement(options) {
  var style = document.createElement('style');
  var attributes = options.attributes || {};

  if (typeof attributes.nonce === 'undefined') {
    var nonce =  true ? __webpack_require__.nc : 0;

    if (nonce) {
      attributes.nonce = nonce;
    }
  }

  Object.keys(attributes).forEach(function (key) {
    style.setAttribute(key, attributes[key]);
  });

  if (typeof options.insert === 'function') {
    options.insert(style);
  } else {
    var target = getTarget(options.insert || 'head');

    if (!target) {
      throw new Error("Couldn't find a style target. This probably means that the value for the 'insert' parameter is invalid.");
    }

    target.appendChild(style);
  }

  return style;
}

function removeStyleElement(style) {
  // istanbul ignore if
  if (style.parentNode === null) {
    return false;
  }

  style.parentNode.removeChild(style);
}
/* istanbul ignore next  */


var replaceText = function replaceText() {
  var textStore = [];
  return function replace(index, replacement) {
    textStore[index] = replacement;
    return textStore.filter(Boolean).join('\n');
  };
}();

function applyToSingletonTag(style, index, remove, obj) {
  var css = remove ? '' : obj.media ? "@media ".concat(obj.media, " {").concat(obj.css, "}") : obj.css; // For old IE

  /* istanbul ignore if  */

  if (style.styleSheet) {
    style.styleSheet.cssText = replaceText(index, css);
  } else {
    var cssNode = document.createTextNode(css);
    var childNodes = style.childNodes;

    if (childNodes[index]) {
      style.removeChild(childNodes[index]);
    }

    if (childNodes.length) {
      style.insertBefore(cssNode, childNodes[index]);
    } else {
      style.appendChild(cssNode);
    }
  }
}

function applyToTag(style, options, obj) {
  var css = obj.css;
  var media = obj.media;
  var sourceMap = obj.sourceMap;

  if (media) {
    style.setAttribute('media', media);
  } else {
    style.removeAttribute('media');
  }

  if (sourceMap && typeof btoa !== 'undefined') {
    css += "\n/*# sourceMappingURL=data:application/json;base64,".concat(btoa(unescape(encodeURIComponent(JSON.stringify(sourceMap)))), " */");
  } // For old IE

  /* istanbul ignore if  */


  if (style.styleSheet) {
    style.styleSheet.cssText = css;
  } else {
    while (style.firstChild) {
      style.removeChild(style.firstChild);
    }

    style.appendChild(document.createTextNode(css));
  }
}

var singleton = null;
var singletonCounter = 0;

function addStyle(obj, options) {
  var style;
  var update;
  var remove;

  if (options.singleton) {
    var styleIndex = singletonCounter++;
    style = singleton || (singleton = insertStyleElement(options));
    update = applyToSingletonTag.bind(null, style, styleIndex, false);
    remove = applyToSingletonTag.bind(null, style, styleIndex, true);
  } else {
    style = insertStyleElement(options);
    update = applyToTag.bind(null, style, options);

    remove = function remove() {
      removeStyleElement(style);
    };
  }

  update(obj);
  return function updateStyle(newObj) {
    if (newObj) {
      if (newObj.css === obj.css && newObj.media === obj.media && newObj.sourceMap === obj.sourceMap) {
        return;
      }

      update(obj = newObj);
    } else {
      remove();
    }
  };
}

module.exports = function (list, options) {
  options = options || {}; // Force single-tag solution on IE6-9, which has a hard limit on the # of <style>
  // tags it will allow on a page

  if (!options.singleton && typeof options.singleton !== 'boolean') {
    options.singleton = isOldIE();
  }

  list = list || [];
  var lastIdentifiers = modulesToDom(list, options);
  return function update(newList) {
    newList = newList || [];

    if (Object.prototype.toString.call(newList) !== '[object Array]') {
      return;
    }

    for (var i = 0; i < lastIdentifiers.length; i++) {
      var identifier = lastIdentifiers[i];
      var index = getIndexByIdentifier(identifier);
      stylesInDom[index].references--;
    }

    var newLastIdentifiers = modulesToDom(newList, options);

    for (var _i = 0; _i < lastIdentifiers.length; _i++) {
      var _identifier = lastIdentifiers[_i];

      var _index = getIndexByIdentifier(_identifier);

      if (stylesInDom[_index].references === 0) {
        stylesInDom[_index].updater();

        stylesInDom.splice(_index, 1);
      }
    }

    lastIdentifiers = newLastIdentifiers;
  };
};

/***/ })

}]);
//# sourceMappingURL=vendors-node_modules_reduxjs_toolkit_dist_redux-toolkit_esm_js-node_modules_query-string_inde-923475.js.map